# CHANGELOG

版本纪律：语义化版本；平台迁移段 V1–V999，实施段 V10000+；升级 = 停服→备份→换产物→自动前滚→回归抽查（多医院部署操作指南 §三）。

## 1.2.2（2026-08-18）

时区免疫（v1.2.1 的 CI 在 UTC 22:40=北京 06:40 打红后修复）——跨时区窗口首次真实现形。

- **根因两层**：①1.1.9 把业务"今天"统一为北京时区，但测试与 E2E 侧的
  `LocalDate.now()`/`date.today()` 仍取 runner 本地（UTC）——单号日戳断言 `CF<UTC日期>`
  对不上后端 `CF<北京日期>`；②更深一层：**PGJDBC 把 JVM 时区设为会话时区**，
  UTC JVM 下 `?::date`/`current_date` 的日窗口整体偏移 8 小时，与北京"今天"错位——
  这正是部署手册"show timezone 必须 Asia/Shanghai"验收项防的事故，CI 环境没守这个不变量
- **修法（免疫而非掩盖）**：连接池 `connection-init-sql: set time zone 'Asia/Shanghai'`
  钉死会话时区，应用对宿主/容器时区漂移免疫；测试侧 15 个类换 `BusinessDates.today()`；
  E2E 侧 e2elib 新增 `today_bj()`（UTC+8 显式计算，与 runner 时区无关）替换 16 个脚本；
  **CI 保持 UTC 不配时区 env——让它成为常驻的时区回归金丝雀**
- **验证按失败现场做**：`mvn test -Duser.timezone=UTC` 141 全绿；
  UTC JVM 起服务端跑 20 套 E2E 全过（正是 CI 当时的失败组合）

本地北京时区永远测不出这一类——这是"验证要对真实现场"的又一实例：现场包括**时间**。

## 1.2.1（2026-08-18）

第四轮清单"留按需"三项清零——至此四轮审阅全部条目无遗留。

- **单测字典自给（TestSeeds）**：9 个通用流程测试类不再按中文名硬取 B 类种子药品/项目，
  查无则建（`TST-` 前缀，随测试事务回滚零残留）。**失败路径实证**：把 hip_test 的
  布洛芬改名后 14 个已转换测试照常全绿——实施替换种子不再打崩 `mvn test`。
  规则耦合的 4 个测试类（CDSS/合理用药/医保对照）无法解耦，维持清单「测试契约」标注
- **前端核心域类型沉淀**（`src/types/domain.ts`）：Patient/Registration/OutpOrder/Charge/
  Admission/Schedule 六接口 + snake/camel 命名口径规约；RegisterView/AdmissionView 示范迁移。
  迁移当场即有回报：编译器抓出两个未声明的聚合展示字段（patientName）
- **建档表单 `:rules`**：证件号 18 位、手机 11 位就地校验；**含 `*` 的脱敏值拒绝提交**
  （1.0.9 A-2 脱敏回写防线的前端半边）。浏览器实测：三条错误就地全亮、合法值保存成功

测试 141；前端 vue-tsc + vite 构建通过；建档表单浏览器实测。

## 1.2.0（2026-08-17）

第四轮审阅 P2 清尾：留后续小版本的 ☐ 项全部闭环或裁决。

- **模块开关挡横向调用**：关闭 insurance 后，门诊结算/住院出院传 `payMethod=YB` 被业务码
  5010/9027 拦截——此前只挡菜单与路由，未采购医保的医院仍会写 yb_* 表，对账时无从解释。
  冲销侧有意不设开关（既存 YB 结算必须始终可纠正）
- **调度锁键改强哈希**：`String.hashCode` 32 位可碰撞（"Aa"/"BB"），碰撞即任务互判
  "其他实例执行中"静默跳过；改 SHA-256 前 8 字节
- **Portal 解除跨模块 Repository 直引**（ADR-0001 边界）：归属校验改 SQL
- **日结 CSV 口径可复算**：收款/退款分行、退款金额取负——金额列求和 = 当日净额，
  按口径分组 = 日结两侧合计，历史日期重导结果稳定（原先"当日新建且当日退费"一行无符号，
  求和两头都不等，重导历史快照"事后变脸"）
- **交款核查按 user id 归并**：重名收费员曾被并成一行，与"账各归各"目的相悖
- **日期参数预校验**：`date=2026-13-01` 曾 500 + ERROR 堆栈污染告警，现 4000
- **敏感读审计记 query string**：`/api/patients?keyword=张` 此前只剩路径，"查了谁"丢失；
  `/api/finance/charge-search` 补入前缀表；匿名 401 按 IP 10 秒节流（窗口可配，测试置 0——
  token 过期后的轮询与扫描器曾以请求速率写审计表）
- **E2E 消灭 patientId 1/2 硬编码**（种子契约）：outpatient/inpatient/phase912 三脚本
  全部改为自建患者——此前只在 CI 处女库成立，实施替换种子/本地复跑即 404 且报错指向错误方向
- **团检建档裁决 ✋**：保持单事务（成员无证件号、建档不幂等，分批部分提交会让中断重跑产生
  重复档案；全回滚可安全重试）；加 2000 人上限拦截误传超大名单
- lis_allow_substitute 校验核实 1.1.6 已修（清单旧账勾销）

测试 141（新增 Phase120Cleanup×5）；20 套 E2E 全通过。

## 1.1.9（2026-08-17）

第四轮审阅方向 D：工程与实施纪律。

- **impl 与平台升级共存**（B-13）：`spring.flyway.out-of-order: true`——医院库到 V10000+ 后，
  平台下一版新迁移（<10000）曾被判乱序**升级拒启**。**实做演练**：V1–56+V10000 装机 →
  换含 V57/58 的产物 → 两迁移以 `[out of order]` 前滚成功；记入彩排记录 §五，ADR-0003 补注。
  演练过程再证一条验收纪律：升级验收必须确认响应来自新产物（本次曾被未退出的旧进程假 UP 骗过）
- **release 打包清单与 CHANGELOG 联动**（B-14）：`check_manifest()` 在打包前校验 CHANGELOG
  提到的工具都在清单里——1.1.5 的存量文书导入工具曾被漏打；先补入 `import-documents.py`
- **错误码分段登记**（docs/错误码分段.md）：按模块分段 + 历史撞码档案 + 现状查询命令——
  已撞码 4 起证明口口相传失效，新增码先登记再写代码
- **`HipBizException` 统一基类**：三套同构异常家族改继承之，GlobalExceptionHandler 收敛为
  基类一条，不再随模块数量增长 import；新模块有了明确范式
- **`BusinessDates.today()`**：19 处裸 `LocalDate.now()` 全量替换——三套"今天"口径
  （JVM 时区/业务时区/DB current_date）曾互相漂移且只在跨 00:00–08:00 班次暴露
- **`Masking.*` 下沉**：脱敏规则三处手写归一（等保口径将来调整只改一处）
- **build-info**：产物内置构建版本与时间，`/actuator/info` 可自证"这台医院跑的是哪版"
- **接手者文档**：README 补 hip_test 准备三行命令；种子数据分层清单标注"测试契约"节
  （替换 B 类种子的失败形态与排查方向）；ConcurrencyFixTest 补"名不副实"警示注释
  （@Transactional 单线程测不了并发，真范式指向 StockRestore/Phase116ChannelOrder）
- ☐ 留后续：E2E patientId 1/2 硬编码消除、测试自建字典辅助、前端类型沉淀、el-form 体系化

## 1.1.8（2026-08-17）

第四轮审阅方向 C：前端体验。

- **患者门户接入统一客户端**（B-10）：`createHipClient` 工厂让院内端与门户共用
  在途去重/15s 超时/错误提示三道防线——门户此前自建裸 axios，手机双击「预约」=两条挂号、
  双击「我已支付」=两次回调，弱网失败零提示。**浏览器实测：双击预约第二次被拦、第一次成功**；
  登录页网络错误补 toast、院名加载失败不再是未处理 rejection
- **Dashboard 生命周期清理**（B-11）：resize 监听具名化并 onBeforeUnmount 移除、
  实例 dispose、表格/图表切换同步清实例——登录默认页此前每次进入都累积泄漏，老电脑下午卡顿
- **体征录入数值校验**（B-12，临床数据）：中文句号归一化、非数字/超量程（体温 30–45 等）拦截——
  "36。5" 曾变 null 静默丢失但成功提示照弹。**浏览器实测三路径：abc 拦、50 超范围拦、36.8 过**
- **在途去重 key 纳入 params**：叫号台给两个不同患者连续「过号」曾被误拦，
  而真正的同参数重复提交反而漏防
- **杂项**：`/m` 提为顶级路由（PDA 上曾套桌面侧边栏占半屏，浏览器实测护士单列正常）；
  CSV 导出遇业务错误不再下载错误 JSON 文件；`ElMessageBox.confirm` 取消不再是未处理 rejection；
  跨午夜"今天"改每次取值（夜班 0 点后曾查昨天队列）；手术排台时区令牌统一 `Z`；
  用户表单密码位数前端先拦；死路由 `/403` 清理
- **实测回归修复：结算单号方案迁移撞号**——V54 前旧号含 admissionId、纯数字后缀，
  同日升级后新结算 id 追上旧行 admissionId 值即撞唯一约束（6 套 E2E 复现）。
  新号加方案区分符 `S`（旧号永远纯数字，永不相等）。**这正是第四轮教训三
  "实施机制的组合场景没演练过"的又一实例：同日升级 + 同日结算**
- CDR 列表投影化的两处 E2E 契约同步（content 改经明细端点取）

测试 136；20 套 E2E 全通过；关键路径浏览器实测（护士 /m、体征三路径、门户双击、Dashboard 三图）。

## 1.1.7（2026-08-17）

第四轮审阅方向 B：性能收口（V58）。

- **CDR 同步批量化**（B-7，存量切换前必须）：子表按 id 集合批取（原每行 3-4 条子查询）、
  upsert 改单条 `on conflict`（原 JPA 版每次 find 触发 auto-flush + 对已累积托管实体的
  O(N²) 脏检查）、每 500 条一个事务（原全量同步单事务 50 万+ SQL，随时 OOM 且失败全部重来）
- **序列取号免全会话 flush**（B-8）：三处 `nextval` 原生查询声明空 query space——
  原先医生每开一行医嘱，取号都会 flush 整个会话并脏检查此前全部托管实体（开单热路径）
- **对账报文预取**（P2）：一次对账涉及的全部单号报文一次取回建字典——
  原每单 2 次点查，日结算 3000 笔 = 6000+ 条 SQL（同步接口用户点一下等数秒）
- **索引**（V58）：lis_sample/ris_exam 未终态部分索引（检验/检查工作站每次刷新曾全表扫）
- **查询收口**：`/stats/daily` 子查询补日期下界（要 7 天数据曾聚合全史）；
  DRG `regroup/{id}` 只重算本例（曾误触全量重算）；工资导入工号解析并入 upsert（2N→N 次往返）；
  医保目录限量 500 + 关键字检索 + 总数（真实目录数万条曾全量拉取）；
  CDR 患者文档列表投影化不含全文、明细点开懒加载（慢病患者曾一次响应数 MB）
- ✋ 不修：DRG regroup-all 保持单事务——罕见运维动作，重建期间分析页一致性优先于速度

测试 136 全绿；前端 vue-tsc 通过。

## 1.1.6（2026-08-17）

第四轮审阅方向 A：安全与账务正确性（V57）。

- **INTERFACE 全域隔离**（A-1，P0）：安全链上 `/api/integration/**` 之外对 INTERFACE 一律拒绝——
  不再依赖每个控制器记得写注解。此前 LIS 接口机凭据可直调无注解的入院/出院/**结算冲销**并发出
  真实医保冲正报文（两轮修复在同一盲区互相放大）。InpatientController/RiskAssessController
  补类级注解，冲销限 ADMIN/CASHIER
- **配置校验对齐真实键集**（B-1）：原规则校验的 `yb_annual_limit` 是幽灵键，真实封顶线键
  `yb_cap_*` 零校验——B-6 声称堵死的"填 abc 全院结算 500"换一半键原样复发。补齐 `yb_cap_/
  yb_audit_*/lis_allow_substitute(双语义)/empi_idcard_checksum`；分割引擎与 DRG 费率改走
  ConfigReader（缓存终于接到热点），坏值 error 告警而非 500 或静默全自费
- **冲销再出院不再双入组**（B-2）：DRG/护理病历/CDR 摘要三处 join 补 `status='PAID'`
  （1.1.4 只修了 StatsController）；`drg_case.admission_id` 先清重再建唯一索引；
  删除隐患接口 `findByAdmissionId`
- **渠道调用一律是事务内最后一步**（B-3）：冲销/退费的 reverse、出院的床位释放全部挪到
  医保上传**之前**——本地失败则报文根本不发，渠道失败则本地随事务回滚。
  新增真提交模式失败路径测试（可编程假适配器定向让冲正失败→断言全量回滚+重试安全）
- **对账补退费侧窗口**（B-4）：异日退费/冲销按退费日复检冲正报文——"本地已退、医保未冲"
  此前永远漏网；PAID 单存在冲正报文也标异常（反向悬账信号）
- **MLLP**（B-5/B-6）：CIDR 前缀越界显式拒绝（`/33` 曾因 Java 移位 mod 64 静默放行全网）；
  线程池改 SynchronousQueue + 连接数上限 `mllp-max-conn`（原队列型池让第 5–36 个长连接
  "已握手永不被读"，LIS 等 ACK 超时反复重发）
- **杂项**：legacy_key 校验上限对齐列宽（B-15）；9013 撞码拆分（医保上传失败→9026）；
  `yb_settle_split/yb_audit_log` 建 created_at 索引（B-9，1.1.3 的半开区间就此真正生效）

测试 136（新增 Phase116Security×5 / Phase116ChannelOrder×1）。
测试方法论新证：`@Transactional` 测试里 ①assertThrows 接住异常后回滚不会发生——
回滚语义必须真提交模式验证；②事务内直写的配置被 ConfigReader 缓存后回滚，缓存带毒
污染后续测试——直写配置的测试类须 @AfterEach evictAll。

## 1.1.5（2026-08-17）

试点支撑批（V56）：存量切换工具链收口 + 执行计划实证 + 巡检残项。

- **老 HIS 历史文书入 CDR**：`tools/import-documents.py` + `POST /api/cdr/legacy-documents`——
  切换后医生在患者 360 视图回看老系统病史，全文检索同步命中（此前迁移只覆盖患者索引
  与在院患者，历史病历在切换瞬间"消失"）。幂等键=类别+老单号（cdr_legacy_ref 把老单号
  映射成平台内稳定 ref_id），断点续跑/修正重导均为覆盖更新。实测：模板 CSV 首跑新建 3、
  复跑覆盖 3、零失败；360 文档与 ILIKE 检索均命中
- **B-4 修复的执行计划实证**：EXPLAIN 确认半开区间走 `idx_outp_charge_created`
  Bitmap Index Scan，旧 `::date` 写法为 Seq Scan——"哪条查询因此改变了行为"有了计划层证据
- **磁盘告警路径可配**（上轮 P2 残项）：`hip.ops.disk-paths` 支持逗号分隔多卷，
  试点应配数据库卷与备份卷；路径配错本身也开单告警（原先只查应用工作目录，
  数据库卷满了它毫无察觉）

测试 130（新增 Phase115LegacyImport×3）。

## 1.1.4（2026-08-17）

第三轮审阅方向 C：工程与观测（V55）。

**语义与安全**
- 404/405 不再被兜底吞成 500 + ERROR 堆栈（拼错的 URL、扫描器探测都曾污染错误告警）
- 报表引擎用户 SQL 改回 PreparedStatement（1.0.6 曾退回 Statement，防线只剩 `contains(";")`）；
  禁用词补 `commit|rollback|begin|discard|reset`（一旦执行会脱掉 `set local role` 沙箱）
- 新增 INTERFACE 接口机角色：HL7 进站不再要求 ADMIN 凭据（LIS 中间件的凭据曾是全院最高权限）
- 分享链接有效期上限 30 天 + 吊销端点（曾可造百年外链且泄漏后无任何回收手段）
- 敏感读留痕：患者检索/CDR/病历/打印/工资的 GET 进审计——"谁查过谁的病历"此前查不出来
- `StatsController.days` 上限 366（`generate_series` 无界时一个参数可打出上亿行）

**正确性与性能**
- 金额统计 `Double` → `BigDecimal`（`sum(numeric)` 走 Double 会出 `12345.670000000001`
  且与日结对不上）；统计口径排除已冲销住院结算单
- 报文/文档列 `varchar(8000)` → `text`（截断的留痕不可举证、CDR 文档残缺），截断逻辑随之移除
- 对账：`int_message_log(ref_no)` 建索引（原每单 1–3 次 8000 字符列 LIKE 全表扫）；
  反向核对改一条反连接（原 N+1）
- 门户四端点单条 SQL 化（患者端并发高峰即门诊高峰，原 50 挂号×医嘱×结果逐次查询）
- 慢接口 path 参数归一化（`/api/patients/1、/2…` 各成一行使聚合毫无价值）
- ConfigReader 30 秒缓存 + 更新即失效（医保比例每次结算都读，原每读一趟 DB）
- MLLP 字符集按 MSH-18 解码（国产 LIS 发 GBK 曾中文乱码入库），ACK 同字符集回包
- 导诊关键词 LIKE 转义；门户令牌主体显式校验；`.claude/worktrees` 残留副本清理入 gitignore

测试 127；20 套 E2E 全通过（phase2931 增分享上限与吊销断言）；前端 vue-tsc + vite 构建通过。

## 1.1.3（2026-08-17）

第三轮审阅方向 B：账务与口径闭环（V54）——这批决定试点能不能对账。

- **退费口径落地**（B-1，`refunded_at` 此前有列有索引**零查询引用**）：日结、交款核查、
  日结 CSV、ODR 今日收入全部改为「收款按收款日、退款按退费日」双侧归集——
  D1 收费 D2 退费不再让 D1 历史报表事后变脸，D2 的实际出账可见
- **退费操作员**（B-2）：`outp_charge.refund_by` 新列，退费时写入；交款核查按操作员双向拆分——
  甲窗口收的钱由乙窗口退，此前账挂在甲头上，稽核要查的正是这类差异却查不出来
- **对账比金额**（B-3）：`consistent` 纳入金额比对（通道留痕 `"amount"` vs 业务单金额），
  差异明细区分「缺报文」与「金额不符」——金额差异是最常见的对账形态，此前 100% 判一致；
  反向核对与住院侧同步覆盖，已冲销单要求成对 settle+refund 报文
- **住院结算冲销（出院召回）**（B-5）：住院侧此前无任何退费/冲正路径，结算错误只能改库，
  且住院医保年度起付线/统筹额度永不回退（`reverse()` 全仓仅门诊一个调用点）。
  新端点 `POST /admissions/{id}/cancel-settlement`：抢占冲销 → 占床 → 恢复在院 →
  **最后**才医保冲正（渠道冲正成功后不允许再有可失败的本地步骤——C-2 悬空教训复用）；
  结算单唯一约束改「非作废单唯一」部分索引，单号改先存后编号（原含 admissionId，
  当日冲销重结必撞唯一约束）
- **27 处 `::date` 谓词改半开区间**（B-4）：1.1.1 建的索引遇 `created_at::date = ?` 全部
  不可 sarg 仍走全表扫——审阅当时写明「改 range 零成本立刻生效」，实际只建了索引没改查询
- **sys_config 按键校验**（B-6）：比例 0–1、开关 0/1、单号前缀字母数字——
  `yb_ratio_staff` 填 1.5 医院倒贴、填 abc 全院医保结算 500，而改配置是管理员日常操作
- **归档时区**（B-7）：小时判断改与 cron 同源（Asia/Shanghai）——原 `LocalTime.now()` 用
  JVM 默认时区，容器 TZ 未生效时与 cron 永不相交，V53 的归档一次都不会执行

测试 120（新增 Phase113Finance×5）。

## 1.1.2（2026-08-17）

第三轮审阅（docs/代码审阅-20260817.md）方向 A：试点阻塞项四项。

- **调度选主锁重写**（A-1，实测复现）：`pg_try_advisory_lock` 是会话级锁，而 JdbcTemplate
  每次调用各借一条连接——加锁在 A、解锁打在 C 上必然失败且返回值被丢弃，锁随 A 归还连接池后
  永不释放，下一次同名任务被误判为"其他实例正在执行"而静默跳过（**单实例部署亦然**）。
  受害者是巡检/CDR 同步/医保对账三个夜间任务，而巡检是"备份 24h 未成功"的唯一告警源。
  改为整个加锁-执行-解锁包在一个 ConnectionCallback 里钉住同一连接，解锁返回值必须核对。
  回归测试复现失败路径：任务体内占用连接后断言锁归零、同名任务可再跑、异常路径同样释放
- **MLLP 加固**（A-2/A-3）：该端口写入检验结果并触发危急值流程，而 MLLP 协议无鉴权，
  同功能 HTTP 端点却要 ADMIN。补：来源 IP 白名单（支持 CIDR，回环恒放行）+ 可配绑定地址 +
  **pilot/prod 未配白名单拒绝启动监听**（fail closed，与 Mock 门禁同理，有自动化覆盖）；
  帧长上限 1MB（原 readFrame 无限缓冲，一个不发结束符的连接即可持续吃堆）；
  固定线程池 + 有界队列替换 cachedThreadPool；业务异常不再逃逸（不回 ACK 会让 LIS 反复重发）
- **401/403 落审计**（A-4）：1.1.1 的过滤器排序只排了三个自定义过滤器彼此的顺序，而安全链
  注册序 -100 在审计过滤器 30 **之前**——被拦下的请求根本走不到审计。改在
  entryPoint/accessDeniedHandler 内留痕（过滤器级与方法级拒绝的必经点），GET 也记
  （被拒绝的读尝试正是"谁试图翻谁的病历"）；审计过滤器对在途安全异常跳过记录避免重复行，
  也修掉了它原本为方法级 403 记下的误导性 200 状态行
- CI：JWT 闸门断言收紧为完整文案（裸 `grep 拒绝启动` 会被 MLLP 白名单闸门的同前缀日志误命中）

测试 115（新增 Phase112PilotBlocker×7，全部验证失败路径）。

## 1.1.1（2026-08-16）

工程基线（第二轮审阅方向 C / P2，V53）：

- **全局异常处理器**：此前全仓无 `@RestControllerAdvice`，BizException/InpException 只在各 Controller
  逐个 try/catch，漏掉的路径与 DataIntegrityViolation/NumberFormat/非法日期一律 500 + Spring 默认错误体，
  前端只能显示"网络请求失败"——这是历次审阅中多条问题严重度的共同放大器。
  安全异常原样重抛给 accessDeniedHandler（否则 403 会被兜底吃成 500，语义又丢）
- **状态列长度与 CHECK**：9 张表的状态列 varchar(8)→16（`'REPORTED'` 正好 8 字符，
  再加 `'CANCELLED'` 就复现历史事故）；关键枚举加 CHECK——
  `insurance_type` 无白名单曾让未知值使分割比例取 0，**患者被静默全额自费且无告警**
- **索引批 14 个**：`outp_order(order_type,status)`（20 处 WHERE）、`inp_admission` 状态与四个连接键、
  `outp_charge(created_at)` 与 `(status,created_at)`（日结/对账/打印六处此前全表扫）等
- **观测表归档**：`hip_purge_observability()`（慢接口/集成报文 90 天、审计 180 天），
  每日 03 点随巡检执行——三张只增不减的表此前无任何清理，`ops_slow_api` 还会自我放大
- **过滤器顺序显式化**：Audit(30) → SlowApi(40) → ModuleGate(50)，此前三者同为 LOWEST_PRECEDENCE
  靠 Bean 名排序，ModuleGate 若排前面，停用模块的写请求就不入审计
- **ModuleGate 缓存与失败放行**：每请求查一次 sys_config；DB 抖动时异常直落 Tomcat 错误页，
  连报告 dbUp=false 的运维健康页自己都打不开。改 30 秒缓存 + 查询失败沿用上次结果，配置变更即时失效
- **CI**：新增 `pilot-guard` 作业冒烟两道生产闸门（缺密钥必须拒启、带 Mock 必须拒启）——
  这正是 1.0.9 暴露的"闸门从未被自动化执行过"的补课；E2E 加 30 分钟超时、
  ai-service 就绪失败即报错（原真因被后续断言掩盖）、失败时输出 ai.log
- CSV 导出转义与公式注入防护（姓名含逗号串列、以 `=+-@` 开头被 Excel 当公式执行）

测试 114；20 套 E2E 全通过。

## 1.1.0（2026-08-16）

第二轮审阅方向 B：迁移安全、数据完整性、集成正确性、前端质量（V52）。

**迁移安全（发版阻塞项）**
- V50 的四个唯一索引修的正是「1.0.6 及以前并发会产生重复数据」的缺陷，**升级存量库时
  建索引必然失败 → 迁移中断 → 应用起不来**。V52 先清洗（重复挂号置退号并回补号源、
  重复待支付单作废、非法押金留痕订正）再补建索引，并补 `alter default privileges`
  （否则新建表一出现，自定义报表即 permission denied）

**数据完整性与集成**
- 同一患者只能有一条在院记录（两轮审阅都提过，此前可重复入院占两张床）；E2E 入院前自净
- ORU 多 OBR 分组：一条报文含多组时，B 申请单的结果曾被写进 A 单（静默的检验结果串号）
- 调度线程池 size=4 + advisory lock 选主：三个定时任务此前共用单线程，任一卡住其余永久不跑
  （而巡检正是"备份 24h 未成功"的唯一告警源）；多实例部署会重复开工单、重复对账批次
- 集成留痕字段按列宽截断且失败不外抛（超长曾一路逃到 MllpServer 致连接被关、消息静默丢失）
- `forward-headers-strategy`：审计 client_ip 此前恒为网关容器地址

**401/403 语义（1.0.9 权限批的副作用，实测发现）**
- 安全层只配了 `authenticationEntryPoint`，方法级权限拒绝也返回 401，前端据此判定
  "登录过期"→清 token 跳登录页。**护士登录后立刻被踢出，完全无法使用系统**——
  20 套 E2E 全绿是因为都用 admin。补 `accessDeniedHandler` 并改直接写响应
  （`sendError` 会转发 /error，而 /error 同样被安全链拦成 401）
- 驾驶舱是所有角色的登录首页，放宽为 `isAuthenticated()`
- 新增 `Phase110RoleUsabilityTest`：七个内置角色逐一验证「能登录、能开首页」

**前端质量**
- 请求在途去重：双击「挂号/补交押金/开医嘱/出码」这类纯新增接口时后端条件更新救不了
- UTC 日期 11 处改本地日期（北京时间 08:00 前挂号页、药房、日结全部显示昨天）
- 存储型 XSS 清零：四处 `dangerouslyUseHTMLString` 改纯文本渲染
  （项目名来自 CSV 导入的自由文本，拼进 HTML 即可盗 token）
- 路由角色守卫：以 `/auth/me` 返回的菜单为准（后端已按角色过滤，前端跟随零维护），
  无权访问跳回首页并提示；client.ts 403 独立提示

测试 114（新增 Phase110DataIntegrity×4 / RoleUsability×3）；20 套 E2E 全通过；浏览器实测护士角色可用。

## 1.0.9（2026-08-16）

第二轮审阅（docs/代码审阅-20260816.md）方向 A + A′：部署真实性、权限清点、上轮修复的回归（V51）。

**A-1 部署路径真实性（最重要）**
- `docker-compose.full.yml` 补 `SPRING_PROFILES_ACTIVE: pilot` 与 `HIP_JWT_SECRET/DB/REDIS` 的 `:?` 强制注入
  ——1.0.6 的两道安全闸门（JWT 密钥校验、Mock 适配器门禁）触发条件是 pilot/prod profile，
  而编排从不设 profile，**按文档部署时两道闸门全部跳过**：JWT 回落仓库公开占位串、Mock 医保照常工作
- 抽 `HipProfiles.isProduction()`：profile 判断从三处散落收敛为一处
- 新增 `Phase109DeploymentGuardTest`——闸门自实现起从未被任何自动化路径执行过，这是那条缺失的覆盖
- 容器时区 `TZ=Asia/Shanghai` + `@Scheduled(zone=...)`；Dockerfile 非 root、HEALTHCHECK、MaxRAMPercentage
- DB/Redis 只绑回环、Redis 加密码、Orthanc 改口令、MLLP 端口 publish、healthcheck/资源限制/restart

**A-2～A-6 隐私与权限**
- 脱敏串禁止回写（2004）：前端把 `138****1111` 原样保存曾**永久覆盖真实手机号**
- create/update 返回值按角色脱敏：非 ADMIN 发一个空 PUT 曾能读到完整明文证件号
- **权限清点**（不再逐个打补丁）：34 个无注解控制器按角色矩阵补齐，含工资批量覆写、
  打印与日结 CSV、CDR 病历调阅——此前对任意在职账号开放
- `ModuleGate` 前缀加路径段边界：关闭人事模块曾连带 404 掉整片 HRP

**A-8 / 前端**
- `client.ts` blob 响应跳过信封校验（日结 CSV 导出曾 100% 失效）、403 独立提示

**A′ C 组：上轮修复自身的缺陷**
- **C-1 预约取号**：重试循环写在事务内，PG 唯一冲突后事务 aborted，第二轮必 500——
  1.0.8 的修复从未生效且把「两人同号」换成了「500」。改单语句原子取号 + 唯一索引裁决
- **C-2 支付金额不符**：原抛异常整单回滚 → 支付单退回 PENDING 而渠道钱已扣，提示会让患者付第二次。
  改为落 `MISMATCH` 终态 + 人工差错单（`PayMismatchService` 走 REQUIRES_NEW 确保不被回滚）
- **C-3 出院释放床位**：用抢占前的陈旧快照且丢弃返回值 → 并发转床后新床永久 OCCUPIED
- **C-4 发药凭条**：整表回查会把上午已发的药列进下午的凭条
- **C-5 `refunded_at` 只写不读**：日结口径改为「当日收费的 PAID ∪ 当日退费的 REFUNDED」
- **C-6 患者端锁定**：锁定键改「患者号@来源IP」（原可遍历号段锁死全院患者）、
  锁定窗口过期后重新计数（原锁一次即永久）、patientNo 格式校验（超长曾让未认证接口 500）
- C-7～C-10：陈旧 status 决错误码、同方重复药改按「药+用法+频次+剂量」判重（放行负荷量+维持量
  与双途径医嘱）、删除抢占后的死代码 update、9911 撞码改 9912
- 顺带：退药纳入抢占家族（并发退药曾重复回补库存）、6003 撞码、CSV 转义与公式注入防护

测试 107（新增 Phase109DeploymentGuard×5 / Privacy×5 / Regression×5）；20 套 E2E 全通过。

## 1.0.8（2026-08-08）

实施工具批（审阅 T 组）+ 皮试匹配修正：

**备份与恢复（最高危：备份假成功）**
- `db-backup.ps1`：拷贝失败不再无条件删源 dump 且不再谎报 SUCCESS——改用 `wslpath` 换算路径、
  逐步校验退出码/文件存在/非空，任一失败即保留 WSL 侧 dump 并中止；新增 30 天保留策略
- `db-restore.ps1`：`--exit-on-error` + 退出码检查（原先部分表恢复失败仍判"演练通过"）
- `schedule-backup.ps1`：`-LogonType S4U` 注册（原交互令牌在无人登录的凌晨不触发）

**迁移工具幂等性**
- `init-hospital.py`：建床只补差额（原重跑一次就多出 count 张可占用的幽灵床）
- `import-patients.py`：无证件号行按姓名+手机号客户端查重（后端仅按证件号幂等）；补退出码
- `import-inpatients.py`：无证件号且同名多条时报错而非取第一条（原会把入院押金挂到同名他人）；
  入院成功但备注失败改为单独提示不计整行失败；新增 `--orders-for-existing` 补开模式；
  押金传字符串保精度；assert 全部改显式异常（`python -O` 会剥离 assert）
- `release.py`：补发 `import-inpatients.py`；多个 jar 时报错而非取第一个（防打包旧 jar 发新版本号）

**P1 补做（1.0.7 未覆盖的七项）**
- 检验条码/病理号取序列；预约顺序号加唯一约束 + 冲突重试（保持时段内连号语义）
- 医保分割年度累计改为「仅当分割行确实新插入时才累加」（原重试/补跑会重复吃起付线，而冲销只退一次）
- LIS 发布先抢占终态再发事件（并发发布双写结果行 + 双发危急值告警）
- 叫号 call-next 条件更新（两台叫号器并发会双写 CALLED 与两条呼叫日志）
- DRG 新增单例重算 `/drg/regroup/{admissionId}`（补录出院诊断后原先只能全量重算）
- 医保对账补反向核对：通道有报文而业务库无单据（疑似医保侧多扣）原先永远发现不了

**皮试匹配修正（1.0.7 改动的回归）**
- 1.0.7 按单一药名匹配过严——阿莫西林属青霉素类、临床做的就是青霉素皮试。
  改为**按皮试类别匹配**（青霉素类含"青霉素/西林"、头孢类含"头孢"）：同类覆盖、跨类不覆盖
- `/inpatient/surgeries` 列表补 `admission_id`/`op_icd` 字段

92 测试 + 20 套 E2E 全通过。

## 1.0.7（2026-08-08）

并发与数据正确性批：审阅 P1（docs/代码审阅-20260808.md），V50。
两个 bug 家族的系统性收口——1.0.2 只修了门诊三处，本版覆盖其余漏网点。

**自造单号 → 数据库序列**（内存计数重启回绕即撞号）
- 住院医嘱 YZ 组号、入库单号、支付单号（另加日期段）；`inp_order.group_no` 补索引

**读-判-写 → 条件更新 + 判定受影响行数**
- 门诊结算/退费/发药：三者互相竞态可致双倍扣款、"钱已退药也发了"、库存双扣
- 住院医嘱执行（双护士各扣一次库存）、审方结论（双药师覆盖、"已收费却作废"）
- 出院结算改为先抢占 DISCHARGED 再汇总（窗口内并发缴的押金原本既不进结算也不退还）
- 床位释放校验占用者（并发转床致床位泄漏；陈旧事务释放他人床位致两人同床）
- 库存入库改原子加、盘点改条件更新（原整行写覆盖并发发药的扣减）
- 医保冲销幂等（`yb_settle_split.reversed`，并发退费原会重复退还年度统筹额度）
- 登录失败计数改原子累加（并发猜测下计数恒停在 1，5 次锁定形同虚设）
- 重复挂号、待支付单唯一性由部分唯一索引兜底

**功能缺失与口径**
- 新增住院医嘱作废端点（原 9012 提示"执行或作废"但作废 API 不存在，误开医嘱只能执行掉→多计费）
- 押金金额须为正（DB check + 服务校验）；停用账号登录返回 1004 而非 500
- 皮试拦截按药名匹配（原做过头孢皮试即放行青霉素输液）
- 同一处方内重复开药拦截 4013（原只查已持久化订单）
- DRG 手术组只认已实施手术（原含未做的手术申请→入高权重组）；单病种改取出院诊断
- 退费记 `refunded_at`，日结按退费日归集（原改写历史日报表口径）
- CDR 触发器补 `outp_order→outp_registration` 上推，且全部子表支持 DELETE 上推

测试 92（新增 Phase107ConcurrencyTest×9）；20 套 E2E 全通过。

## 1.0.6（2026-08-08）

安全批：全库代码审阅（docs/代码审阅-20260808.md）P0 八项 + 工具脚本（V49）。

- **JWT 密钥启动校验**：pilot/prod profile 下检出开发占位值或长度<32 直接阻断启动
  （原有默认值兜底，漏配 `HIP_JWT_SECRET` 时任何人可用仓库公开串签发 admin 令牌）
- **Mock 适配器生产门禁**：`MockAdapterGuard` 在 pilot/prod 检出任一 Mock SPI 即阻断启动
  （Mock 医保无条件返回成功并自写留痕，而对账正以该留痕为准——漏切实现时结算是假的、对账恒匹配）
- **患者端支付确认**：pilot/prod 拒绝患者自调 confirm（原可不付款直接结算，白拿诊疗）
- **患者端登录防爆破**：`portal_login_attempt` 原子累加失败计数，5 次锁定 15 分钟
  （患者号是顺序号，仅凭手机号可枚举接管账号）
- **资金/库存端点方法级权限**：收费退费=CASHIER、发药退药与库存=PHARMACIST、审方=PHARMACIST、
  医技执行=TECHNICIAN/NURSE（原任何在职账号直连 API 均可调，菜单只挡 UI）
- **病历全文检索限角色**：`/cdr/search` 限 ADMIN/DOCTOR_OUTP/QUALITY（原任意账号可翻全院病历）
- **报表引擎只读角色**：改以 `hip_report_reader`（仅业务表 SELECT）执行；角色缺失时拒绝执行
  （fail closed）；执行异常转业务码 4637。正则黑名单挡不住 `setval()`/`query_to_xml()`，
  数据库授权才是边界。**DBA 需按部署手册创建角色并 `grant hip_report_reader to <应用账号>`**
- **患者更新改为部分字段语义**：仅覆盖请求中出现的字段（原全字段覆盖，前端少传即静默清空
  过敏史/血型/医保类型——过敏史是合理用药拦截的数据源）

测试 83（新增 Phase106SecurityTest×5）；20 套 E2E 全通过。

## 1.0.5（2026-08-08）

工程与运维批（规划 v18 方向 C+D 收口）：

- **自动巡检（V48）**：健康告警由拉式改定时——每小时评估备份时效/慢接口/磁盘，
  异常自动开故障工单（同题未处理不重复开单；`ops_auto_health_enabled` 可关）；
  短信/IM 外发依赖网关资质，告警落点即接入点
- **CI E2E 补齐 20 套**：新增 phase3/48/912/1316/1821/emr-stats 六套（CI 内拉起 ai-service）
- **echarts/core 按需**：驾驶舱 chunk 1126KB→517KB（gzip 378KB→177KB），浏览器实测三图正常
- **文档口径一致性**：Java 21/Boot 3.5 回写总体规划；模块数统一 13+server；ADR-0001 按落地
  修订（单 schema+表前缀）；ADR-0003 编号冲突解决（案件查办让号为 ADR-0004）；
  信创口径更正（金仓 PG 内核/达梦自研需移植）；sys_config 33 键

测试 78（新增 OpsHealthSchedulerTest×2）。

## 1.0.4（2026-08-07）

CDR 数据正确性专项（规划 v18 方向 B，缺陷源自教材第 11/12 章代码抽查）：

- **增量水位重构（V47）**：三张同步根表补 `updated_at`（触发器维护），子表变更触发器上推根表
  （病历/诊断→就诊、检验结果→医嘱、住院文书/结算/诊断→住院）——原按 `created_at` 过滤，
  「创建在前、变更在后」的记录（检验发布/出院/退号）增量抓不到；水位改取 `max(updated_at)`
  并带 5 分钟重叠窗口读取（长事务时间戳早于水位会永久漏失；upsert 幂等重抽无副作用）
- **退号纳入同步**：去掉 `status<>'CANCELLED'` 过滤——原退号后 CDR 文档状态永久停留在退号前
- **同步任务化**：每日 02:10 自动增量（`cdr_auto_sync_enabled`，与医保自动对账同为夜间任务）
- **病案编码基础**：`inp_admission` 补出院诊断（ICD-10）、`inp_surgery` 补手术操作编码
  （ICD-9-CM-3）；补录 API `PUT /inpatient/admissions/{id}/discharge-diag`；DRG 入组改取
  `coalesce(出院诊断, 入院诊断)`；病案统计新增真编码率 `dischargeCodedRate`（原 codedRate
  实为入院诊断填充率，保留兼容）
- **住院病历 CA 签名**：`inp_medical_record` 补签名列，签名走 SignatureAdapter（与门诊同语义，
  重签拦截 9103）；住院医生站病历时间轴加签名按钮/已签名标识；出院结算页加出院诊断录入

**已知边界**：住院检验危急值告警落点依赖「住院医嘱进标本流转链路」（LIS 现仅门诊单），
先建表无消费路径——随住院检验链路一并做，不在本版。

测试 76（新增 Phase104CdrTest×4）；e2e-1821 增量断言适配重叠窗口语义。

## 1.0.3（2026-08-07）

安全加固批（规划 v18 方向 A，缺陷源自教材撰写抽查）：

- **CDA 端点加固**：`/cdr/documents/{id}/cda` 限 ADMIN/QUALITY 角色；证件号非 ADMIN 按 EMPI
  同口径脱敏（前4后3）；全部字段 XML 转义 + CDATA 防 `]]>` 提前闭合（此前明文证件号
  对任意登录用户可见、内容可注入 XML）
- **报表引擎加固**（原 ADMIN 可查 sys_user 口令散列）：禁用词改词边界匹配（`updated_at` 不再误伤）；
  业务表**前缀白名单**（45 前缀 + sys_dept 精确放行）；`sys_*`/`pg_*`/information_schema/迁移史
  在 SQL 任意位置出现即拒（堵别名与逗号连接绕过）；报表查询 5 秒超时（独立 JdbcTemplate）
- **EMPI 身份证校验位**：建档/修改按 GB 11643 加权模 11 验真（2003），
  `empi_idcard_checksum=0` 可关（存量迁移含历史假号时用，V46 种子默认开）
- 部署手册环境变量统一为 `HIP_JWT_SECRET`（原第 10 行 `HIP_SECURITY_JWT_SECRET` 靠
  relaxed binding 偶然生效）

测试 72（新增 Phase103SecurityTest×5）；演示/E2E/迁移模板中的示例身份证号全部换为校验位合法号。

## 1.0.2（2026-08-07）

安全与并发修复版（两条独立修复分支合并，均为教材撰写抽查发现的缺陷）：

**安全（platform/core）**
- 停用账号的未过期令牌即时失效：JwtAuthenticationFilter 校验 `isEnabled()`，停用即 401
- `GET /api/config/public` 由返回 sys_config 全表改为白名单四键（院名/简称/票据抬头/咨询电话），
  医保比例、审核阈值、模块开关等敏感配置不再出库

**并发（modules/outpatient，V45）**
- 处方/申请单组号弃用内存 AtomicLong（重启种子回绕可撞号），改数据库序列 `outp_order_group_seq`，
  跨实例、跨重启唯一；前缀仍走 `billno_prefix_rx/req` 配置（与 1.0.1 合并）
- 退药库存回补由读-改-写改为原子 `update stock = stock + ?`，并发不丢更新
- 急诊留观占床加部分唯一索引（status='IN' 的 triage_id/bed_no），占床冲突映射 4561/4562

测试 67（新增 DisabledAccountToken/PublicConfigWhitelist/ConcurrencyFix×6/StockRestoreConcurrency）；
e2e-1821 消除套件间数据顺序依赖（挑带结构化结果的检验文档）。

## 1.0.1（2026-08-07）

快赢包：部分响应 152 条中筛出的 8 条一句话增强（docs/验收/部分响应筛选.md），升为正偏离：

- **2067** 住院每日费用清单：`GET /inpatient/admissions/{id}/daily-fees?date=` 按执行日期聚合，出院结算页内嵌页签
- **1227** 结账明细按项目类型检索：`/finance/charge-search` 加 `orderType` 即切明细行模式
- **893** 排班上一周/下一周快捷切换（前端）
- **505** 药师单次待审列表上限：`sys_config.review_pending_limit`（0=不限）
- **1749** 单据编号前缀配置化：`billno_prefix_charge/admission/settle/rx/req` 五键，默认与历史一致（SJ/ZY/CY/CF/SQ）
- **1814** 输血不良反应处置方案字典：`blood_reaction_plan` 表 + 输血记录留痕（`blood_apply.reaction_plan`）
- **1028** 死亡登记卡：`mr_death_card`（死因链 a/b/c/d + ICD）、病案统计页登记页签、打印数据集 `/print/death-card/{id}`
- **1938** 体检套餐"不进入总检结果"项目配置：`pe_exam_package.hidden_items`

新增 `platform/core ConfigReader`（sys_config 轻量读取）；数据库 V44；测试 58（新增 QuickWin101Test×3）；
20 套 E2E（新增 e2e-101 进 CI）。1938 为接口层达成，套餐维护 UI 随体检深化。

## 1.0.0（2026-08-07）

首个定版。范围即采购需求 107 模块的软件可推进全集 + 产品化能力：

- **业务全集**：门诊/住院/医技/护理/院感/运营/数据中心/集成平台八域 55 功能页 + 患者端 H5（功能清单.md）
- **医保域**：目录对照（CSV 导入+对照率）、行级费用分割（门诊+住院）、年度起付线/封顶线待遇模型、
  智能审核雏形、结算/冲正通道（失败回滚）、每日自动对账（差异开工单）、DRG 支付模拟
- **产品化**：机构参数化、模块级功能开关（6 域双路生效）、init-hospital 一键装机、
  impl 实施定制层规约与模板（V10000 段）、五类 SPI 适配点（含 Mock）
- **安全**：JWT 密钥环境变量注入、密码策略/防爆破/90 天提醒、装机强改默认口令、写操作全量审计、敏感操作过滤
- **质量**：55 单元/集成测试、19 套 E2E 回归（CI 三任务）、装机演练与全量彩排通过
- **数据库**：Flyway V1–V43
- **已知边界**：外部条件项（省医保 SDK/CA/微信/设备直连/官方目录）接入点就绪待资质；配套产品项见技术偏离表
