# 全库代码审阅清单（2026-08-18，v1.2.2 基线 · 第五轮 · 七版新代码回查）

> 方法：对 v1.1.5..v1.2.2 十二个提交（116 文件、+2051/-431）做对抗性回查——
> 全部是最近七个版本的修复与重构，"修复者的盲区是最高收益矿脉"的又一次应用。
> 单代理逐文件读现版全文 + 主审对每条 P1 实测/复核（标 **实测**）。
>
> 状态列：☐ 待修 ｜ ✅ 已修（注版本）｜ ✋ 不修（附理由）

## 总体判断

七版修复的主体（渠道时序、MLLP 语义、批量化结构、统一客户端、锁与哈希）经复核**成立**；
缺陷集中在"修复只落到一侧"的老形态：门诊补了 orphanRefund 住院没补、公式守卫打了字符串
连带打了数字、水位推进的取值时刻没随批量化重想、锁定键信任了可伪造的头。

---

## P1 应修

| # | 条目 | 位置 | 失败场景 | 状态 |
|---|---|---|---|---|
| A-1 | **住院侧对账缺 orphanRefund 标记（门诊有、住院漏）** **实测（造数复现）** | `InsuranceReconService` INP 分支 | `cancelSettlement` 渠道冲正成功后本地提交阶段失败：refund 报文留痕存活（REQUIRES_NEW）、结算仍 PAID → 对账判"一致"，医保基金悬空永不现形——正是该字段被引入要抓的 B-3 残留面，1.2.0 只补了门诊 | ✅ 1.2.3 |
| A-2 | **CDR 增量水位跑完取表 max(updated_at)：同步时长超过重叠窗即永久漏数** **实测（触发器绕过后复现旧行为特征）** | `CdrSyncService.syncIncremental` | 存量首次增量（EPOCH 起全量，轻松超 5 分钟）：第 2 分钟被改的行，其时间戳被第 10 分钟写入的水位盖过 → 永不重抽。改为**抽取开始时刻**的 DB now() 作水位——期间一切变更留在下次窗口 | ✅ 1.2.3 |
| A-3 | **CSV 公式守卫把负数金额变文本，净额口径被自己击穿** **实测** | `PrintReportController.csv()` | 1.2.0 退款行取负后，`-15.00` 被加 `'` 前缀 → Excel 文本，SUM 跳过——"金额列求和=当日净额"的口径承诺失效。数值类型跳过守卫（数字无公式注入风险） | ✅ 1.2.3 |
| A-4 | **门户防爆破锁定键信任可伪造的 XFF 首段；且键列宽 32 容不下 IPv6** **实测（列宽 grep 确认）** | `PortalController.clientIp`、V49 `patient_no varchar(32)` | 每次请求换随机 XFF → 细锁被无限拆分绕过，手机号可无限猜；IPv6 来源的计数行插入一直失败（4091），防爆破对其完全失效。修：V59 扩列 128；clientIp 改 framework 解析后的 getRemoteAddr；**新增粗锁**（纯患者号跨 IP 聚合，60 分钟 50 次）——绕过成本抬高 10 倍，恶意锁人代价同步抬高 | ✅ 1.2.3 |

## P2

| # | 条目 | 处置 |
|---|---|---|
| B-1 | 自动对账差异工单 title 超 varchar(255) 抛约束错，告警丢失 | ✅ 1.2.3 超长时 title 只放计数，明细留对账批次 |
| B-2 | 审计 query string 记录 15/18 位证件号明文（审计表读权限面大于患者查询面） | ✅ 1.2.3 长数字串中段打码后入库 |
| B-3 | 门户登录页仍裸 axios（快速双击两次失败计数）；且手动 code 检查与拦截器双弹 | ✅ 1.2.3 接入 portalClient、信任拦截器 |
| B-4 | MLLP acceptLoop 对失效 socket 会忙等刷日志 | ✅ 1.2.3 异常路径 200ms 退避 |
| B-5 | discharge 渠道成功后仍有回填 ybSettleNo 的 save（提交点残留） | ✋ 不修：提交失败概率级残留，异步回填的复杂度不成比例；对账 orphan 标记（A-1 同款形态检测）已兜底 |
| B-6 | SecurityAuditWriter 节流 Map 清空竞态（最多多记一轮） | ✋ 已接受，注释已声明 |
| B-7 | connection-init-sql 对 Flyway 生效性 | 已核对：Flyway 复用主 DataSource（未配独立 url），init-sql 覆盖迁移连接；边界=不经应用连接池的直连（运维 psql/BI）不受保护，部署手册验收项兜底 |
| B-8 | 测试环境缺 `forward-headers-strategy`（与生产不一致，XFF 相关行为在测试失真） | ✅ 1.2.3 test yml 对齐（主审补充发现） |

## 已核对无洞（简记）

- cancelSettlement/refund 渠道时序（本地写全部在前）、PayService 抢占+MISMATCH 终态、JobLock 同连接语义、
  MLLP 拒绝路径语义（池满裸断连为有意设计）、去重 key 含 params 且 GET 不去重、
  SysConfigController validate 键集与实际使用全量对照无幽灵键、`reconRows` 预取字典与反向核对自洽、
  release.py check_manifest 无误报面（e2e 前缀已排除）、TestSeeds 回滚语义
