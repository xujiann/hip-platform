# 错误码分段登记（1.1.9 起为强制规约）

> 历史上已撞码 4 起（6003 两义、9911、9950 三义、9013 双义），每次都只改码没沉淀分配表——
> 多人接手后第五次撞码只是时间问题，且撞码返工会牵连 E2E 契约。
> **纪律：新增错误码先查本表登记，再写代码**（与配置手册"新键必须补一行"同一纪律）。
> impl 实施定制层一律用 20000+（ADR-0003）。

## 通用保留段（GlobalExceptionHandler / 安全层占用，业务代码禁用）

| 码 | 含义 | 出处 |
|---|---|---|
| 1000 | 未登录或登录已过期 | SecurityConfig entryPoint |
| 1005 | 无该功能权限 | SecurityConfig accessDeniedHandler |
| 4000 | 请求参数不正确 | 兜底（参数/格式类异常） |
| 4040/4041 | 记录不存在 / 接口不存在 | 兜底 |
| 4050 | 请求方法不支持 | 兜底 |
| 4090/4091 | 唯一约束冲突 / 数据完整性约束 | 兜底 |
| 5000 | 系统内部错误 | 兜底 |

## 业务分段（现状反推，2026-08-17 全量 grep 为准）

| 段 | 域 | 主要出处 |
|---|---|---|
| 1001–1499 | 认证/账号/角色/科室/配置（1006 原密码不正确、1007 新密码强度不足、1008 新旧密码相同、1009 请先修改初始密码——v27-A 自助改密；1003 历史空置勿复用；1401 配置不存在、1402 配置校验） | platform-core |
| 2001–2099 | EMPI 患者（2003 证件校验、2004 脱敏回写拒绝、2005 身份修改需管理员） | platform-empi |
| 3001–3399 | 门诊挂号/排班/退号（**3110–3120 分时段预约——v37：3110 时段不存在/停用、3111 时段/排班号源已满、3112 同排班重复预约、3113 预约不存在、3114 状态不允许签到/取消、3120 建时段参数/容量合计非法**） | outpatient |
| 4001–4099 | 门诊医生站/病历（4008 签名冻结、4012 过敏、4013 重复用药、4014 抗菌药分级、4015/4017 CDSS；**4016 补正病历不存在、4018 未签名无需补正、4019 补正内容/原因空——1.2.13 阻塞4 病历合规补正**） | outpatient |
| 4101–4199 | 审方 | outpatient |
| 4500–4799 | 医技/护理/专科/质控/数据治理长尾（按十位分小段：4630 datagov、4660 分享、4680 CDR-legacy、4770 替检……新增按空十位申请；**4565–4568 急诊抢救记录——车道B 收尾③，紧邻急诊留观 4560–4564：4565 分诊/留观不存在、4566 抢救记录不存在或已结束、4567 GCS 越界、4568 转归非法**） | 各长尾模块 |
| 5001–5199 | 收费/退费/支付（5101 支付、5106 金额不符；5011–5015 大额退费审批链——v30（5015 禁自申自批）；**5020–5024 收费员班结缴款单——v41：5020 当日班结已提交不得重复、5021 实点金额非法（空/非数字/为负）、5022 班结单不存在、5023 班结非待确认状态（并发重复确认）、5024 无法识别当前登录收费员**） | outpatient / server FinanceController |
| 6001–6099 | 发药/退药 | outpatient |
| 7001–7199 | ~~库存~~（实为门诊医嘱执行/检验告警：7001–7003 ExecStation、7101 LabResult 告警不存在——历史误占，masterdata 库存实际在 8000 段，勿再往 7xxx 加库存码；**7102 危急值处置必填/已确认、7103 仅开单医师可确认、7104 参考区间项目代码必填、7105 参考区间不存在——v33 危急值闭环+参考区间主数据；**7106 微生物标本不存在/未核收、7107 菌种名必填、7108 药敏行非法、7109 质控必填项缺失、7110 SD 必须>0——v36 LIS 质控轮**） | outpatient/medtech |
| 8001–8099 | 药库进销存 InventoryService（8001 入库量≤0、8002 药品不存在、8003 库存不能为负、8004 盘点期间账面已变；**8005 盘点单不存在、8006 盘点单状态不允许此操作、8007 实盘数为负、8008 盘点确认时账面漂移、8009 盘点单无有效行、8010 入库单不存在、8011 入库单非待验收、8012 拒收原因必填——1.2.13 车道A 药事全链**） | masterdata |
| 9001–9099 | 住院（9021–9026 冲销/召回段；**9030–9032 住院中间结算——车道B 收尾①：9030 已出院不能中间结算、9031 无新增已发生费用可结算、9032 中间结算不走医保通道；9033/9034 中间结算冲销——v31 第七轮审阅 P3-B；9018 诊断补录对象级归属校验——v32：非跨域角色只能补录本人主管病案，紧邻既有 9017 押金金额校验；**v41 住院欠费挂账台账 9035–9040：9035 欠费记录不存在、9036 补缴金额须>0、9037 补缴超过剩余欠费、9038 已结清/已核销不可再操作、9039 催缴方式非法、9040 核销原因必填**——紧邻中间结算段 9030–9034，空置的 9019/9028/9029 不复用以免与历史语义混淆） | inpatient |
| 9101–9199 | 住院病历/签名（9101–9104 记录/签名；9105/9106 出院诊断补录；9107 补正记录不存在、9108 未签名无需补正、9109 补正内容/原因空——1.2.13 阻塞4 病历合规补正；**v34 EMR 合规文书轮先定死码表避撞号：知情同意 9110 类型非法/9111 内容空/9112 同意书不存在/9113 委托信息缺失/9114 状态不允许签名/9115 CA 签名失败/9116 无有效手术同意书/9117 无有效输血同意书/9118 无有效自费同意书；三级查房 9119 查房级别非法/9120 查房意见空；v35 出院完整性 gate 9124 病历不完整不能出院结算（block 模式）；**v39 长期医嘱 9125 长期医嘱须按执行行执行、9126 执行行不存在/已执行、9127 已停嘱不能执行/重复停嘱、9128 停嘱仅限长期医嘱**） | inpatient |
| 9501–9599 | 患者门户（9501 患者号/手机号不正确、9502 挂号或支付单非本人、9503 登录尝试过频锁定、9504 生产环境禁手动确认支付；**v40 患者端：9505 住院记录不存在或非本人、9506 预约不存在或非本人**——患者端分时段预约复用院内挂号段 3110–3114，不另立码） | portal |
| 9601–9899 | 集成/运维/病案（9801 病案；9810–9814 病历复印——车道B 收尾②：9810 患者/病案不存在、9811 复印申请不存在、9812 状态不允许该操作、9813 申请人/范围/用途空、9814 份数<1；**9820 病历不完整不能归档——v35 归档完整性 gate（block 模式）**） | integration/server |
| 9901–9999 | HRP/体检/团检/杂项 + medtech LIS/RIS（9950 故障工单幂等、9951 死亡登记、9952 医技无结果——三者曾同码；**medtech LIS/RIS 占 9940–9949：9940 采样、9941 核收、9942 发布、9943 结果空、9944 RIS 报告、9945 RIS 须先报告、9946 手术、9947 RIS 审核人≠报告人、9948 影像危急描述必填、9949 影像已标危急——v33 RIS 双签+影像危急值**） | hrp/qualitycare/medtech |
| 4800–4999 | **护理文书与病案终末质控（v42 新开段）** —— 4500–4799 长尾段十位实测已全部占满（4500-4590、4700-4792 密集占用），`新增按空十位申请`在该段已无法满足；全量 grep 实证 4800–4999 当时 100% 空置，整段登记以免第五次撞码。子段：**4800–4819 护理记录单/日常巡视**（v42 车道2 **实测用掉 14 个**：4800 护理记录不存在、4801 记录类型非法（OBSERVE/ROUNDS/MEASURE）、4802 病情观察与护理措施同时为空、4803 已签名不可修改、4804 非本人不可签名、4805 住院记录不存在或已出院、4806 护理级别非法、4807 护理级别变更原因必填、4808 已签名不可重复签名、4809 护理记录 CA 签名失败、4810 交接班记录不存在或已签收、4811 日期时间格式非法（护理时间/班次日期）、4812 交班人不能签收自己的班、4813 无法识别当前登录用户不能签收；**4814–4819 空置**。注意 **4806 只用于新增的 `PUT /care-level/change`（带变更原因）端点**——既有 `PUT /care-level` 的非法级别仍返 **9801 原样不动**，E2E e2e-phase912 直接 `PUT ?level=一级` 无 reason，把 4807 必填倒灌进既有端点会打断 CI）；**4820–4839 体温单/三测单**（v42 车道1 **实测只用掉 2 个**：4820 住院记录不存在或缺入院时间、4821 住院周次越界；4822 测量部位非法、4823 未测原因必填**预留未启用**——体温单本版只加读侧打印端点，`POST /vitals` 写路径一行未动，取值校验统一推 v43「写入校验收口版」，届时再启用这两码）；**4840–4859 病案终末质控**（v42 实测用掉 4840–4846：4840 评分单不存在（未预填建单即加项/提交/查详情）、4841 已提交不可再评（预填/加项/删项/再提交四条路径同码）、4842 扣分项不存在或已停用（含字典必填项为空）、4843 扣分分值越界（须 0＜分值≤100，含创建时漏填）、4844 病案未出院不可终末评分（含住院记录不存在）、4845 评级阈值配置非法（`mr.qc.grade_a_min`/`grade_b_min` 非数字或不满足 0≤乙级线＜甲级线≤100）、**4846 扣分项编码重复**；4847–4859 空置）；**4860–4879 费用类别字典**（v42 车道4 实测已用 4860–4864，其余空置：4860 类别码重复、4861 类别不存在或已停用（含挂类时引用未知/停用类别）、4862 类别码/名称空、4863 停用/删除前仍有项目挂靠、**4864 挂类目标的收费项目或药品不存在**——MasterDataController 挂类端点新增） | qualitycare / inpatient / masterdata |
| 4020–4029 | **门诊病历签名（v43 车道A）** —— 4001–4019 已满，紧邻续段。**实测只用掉 2 个：4022 病历内容为空不可签名、4023 仅病历书写医师本人可签名**（DoctorStationService.signEmr）。原预分配的 **4020/4021/4024 作废不启用**——签名端点自 1.0 起就在，「病历不存在」「已签名」「CA 签名失败」三条既有语义早已分别占用 **4009/4010/4011** 且被前端与 E2E（e2e-phase48、e2e-emr-closure）消费，v43 只补入口与缺失校验，不改既有契约，换码只会平白造出一组同义双码。**4020/4021/4024/4025–4029 留空，后续勿按上述作废语义复用。** | outpatient DoctorStation |
| 8013–8019 | **药品启用停用（v43 车道C，已交付）** —— 紧邻 8001–8012 库存段，**实测用掉 4 个**：8013 药品不存在（停用/启用两个端点同码）、8014 药品已停用/已启用无需重复操作（走**条件更新+受影响行数**判定，非读-判-写）、8015 停用原因必填（null body / null 字段 / 纯空白三种写法都拦）、8016 已停用药品不可开单（**门诊 DoctorStationService.createOrders 与住院 InpatientService.createOrders 两处只读预检共用同一码**，位置在 nextGroupSeq/save 之前，失败零副作用）；**8017–8019 空置**。注意 8016 虽由 outpatient/inpatient 两个模块抛出，但语义单一（同一条主数据规则的两个执行点），属同义复用、不算撞码 | masterdata（8013–8015）/ outpatient+inpatient（8016） |
| 4880–4919 | **住院检索与单据打印（v43，4800 段续用）** —— **原登记把「单据打印」的 4882/4883 与「住院检索」的 4880/4881 挤在同一个十位里，两条车道并行开发必撞码；本次按十位拆开登记。** **4880–4889 住院患者与医嘱多维检索（v43 车道D，实测只用掉 3 个）**：**4880 检索条件非法**（科室/病区/主管医生 id 非正、关键词超 64 字、`mine=true` 但识别不出当前登录用户、`mine` 与 `doctorId` 互相矛盾、医嘱检索一个条件都不给（等于全表扫描）、医嘱状态不在 CREATED/EXECUTED/CANCELLED 白名单——六条路径同码，纸面上都是"这个检索条件不成立"）、**4881 护理级别取值非法**（特级/一级/二级/三级，与 4806 同语义但分属检索与写入两条路径，刻意不合并）、**4884 主管医生必填/不存在或已停用**（`PUT /admissions/{id}/attending-doctor`，2013★ 唯一写路径）；4882/4883 与 4885–4889 空置。注意该写端点的「住院记录不存在」**沿用既有 9003 不新造码**（与 `printDailyFee` 同口径），检索类端点一律不因"查不到"报错，空结果就是空列表。**4890–4899 门诊五种日常单据打印（v43 车道B，实测只用掉 2 个）**：**4892 单据类型不支持**（docType 不在 `prescription`/`lab-request`/`exam-request`/`treat-sheet`/`guide-sheet` 白名单内，唯一收口点 `PrintReportController.clinicalDoc`）、**4893 单据数据不存在**（挂号不存在／本次就诊无该类医嘱行／指定的处方号·申请单号不存在，三条路径同码——纸面上都是"这张单打不出来"）；4890/4891/4894–4899 空置。注意导诊单**待办为空不算 4893**（只挂号未开单的患者照样要拿单子找诊室） | inpatient / server |
| 4030–4039 | **门诊诊断域完整化（v44）** —— 4030 诊断不存在或非本次就诊、4031 ICD 编码不存在、4032 诊断名称必填、4033 确诊/疑诊标记取值非法、4034 中医诊断编码非法、4035 医保特殊病种备案信息不全 | outpatient DoctorStation |
| 4060–4079 | **门诊开单与处方模板（v44）** —— **跳过 4040–4059：4040/4041/4050 是 GlobalExceptionHandler 通用保留段，业务代码禁用**。4060 处方模板不存在或无权使用、4061 模板名称必填、4062 模板明细为空、4063 模板作用范围非法、4064 协定处方不可修改明细、4065 医嘱备注超长、4066 加急标志取值非法、4067 标本类型/采样部位必填、4068 处方作废需双签 | outpatient |
| 20000+ | **impl 实施定制专段** | impl/<hospital> |

## 已知历史撞码（已拆分，勿再复用）

- 9911：HRP 出入库数量 ↔（原）体检套餐——1.0.7 拆分
- 9950：故障工单幂等 / 死亡登记必填 /（原）医技无结果——1.1.5 拆出 9952
- 9013：已出院不能转科 /（原）医保上传失败——1.1.6 拆出 9026

## 查询现状的命令

```bash
grep -rhn "R.fail(\|new BizException(\|new InpException(\|new LegacyException(" --include=*.java modules platform server/src/main datacenter | grep -oE "(fail|Exception)\(([0-9]+)" | grep -o "[0-9]*" | sort -n | uniq -c | sort -rn | head
```

计数 >1 且跨文件的码需人工确认是否同义复用。
