# v65 交付后复核（2026-09-18，main=bbf8012 / tag v1.6.5） —— 结论：**3 条中 0 条上调**（上调：无；维持「部分响应」：[2530, 2563, 2576]）

方法与前七轮相同：只传序号、各自读 `技术偏离表-v3.csv` 回填参数原文、三个独立镜头、**零推翻才上调**。12 条 `param_verbatim` 逐条比对 CSV 第 4 列，全部逐字一致。证据落 `audit_r2_wave18.json`，本轮不出改写。

本轮 **9 个镜头 9 个推翻**。**总模式：打回的几乎全是本轮没碰过的旧文案——而本轮新建的守卫对「没动过的假话」结构性看不见。**

## 病史

| 轮次 | 形态 |
|---|---|
| v61 | 每条修复只做一半 |
| v62 | 只修被点名的那个入口 |
| v63 | 后端改完前端没接 |
| v64 | 修复自身带进新的假话 |
| **v65** | **旧假话原样留着，新守卫看不见它**——diff 驱动棘轮只收本轮 + 行，文案一字未动就永远不进登记簿；且本轮的修复把相邻旧假话衬得更显眼（新标题说「不是某一天」，压在它上面的旧 caveat 说「当日」） |

## 结论表

| 序号 | 审计者 | 子要求拆解 | 演示可行性 | 数据真实性与口径 | 结论 |
|---|---|---|---|---|---|
| 2530 | 维持 | 推翻 | 推翻 | 推翻 | 维持部分响应 |
| 2563 | 维持 | 推翻 | 推翻 | 推翻 | 维持部分响应 |
| 2576 | 维持 | 推翻 | 推翻 | 推翻 | 维持部分响应 |

## 主控自行抽验（不是只信 agent）

| 指控 | 实测 |
|---|---|
| 「被取代版本」判定两处自 v63 一字未动 | `git blame` `GrossingPanel.vue:976` 与 `TrailPanel.vue:399-401` 都停在 `bf2efbd`（v63）；v64 复核审计者主证据点名过，v64 漏派、v65 任务书未收——**第三轮漏** |
| `resampleLabel` 只读 `block_code`（coalesce 短路第四个消费方） | `GrossingPanel.vue:577` 逐字如此。**我把它写进车道 B 的任务书、又因文件归 A 让 B「只回报进 blocked_notes 不要动」，B 照办了，我在合并时读到了那条 blocked_notes 却没办**——缝是我自己划的 |
| REGISTER/REPORT caveat 仍说「当日」，压在新标题「不是某一天的数」之上 | `PathQcController.java:486/488/570` 逐字在；v65 只改了标题一句，caveat 正文未动，相邻两行互斥口径 |
| CSV 页脚描述文件里没有的合计格 | `:1880` 传给 CSV 的是 `shown`（只有 rows）+ `summaryVsDailyNote` + caveat |
| FACT 档 caveat 里「各列一律以「当日」起头」无人求值 | `:519` 在；按日表实际列 `stat_day→日期`、`dehydrate_batches→脱水篮批次数` 不以「当日」起头——**簿首 limits (a) 一槽多句，当场兑现** |
| `:277`「医嘱进度随之变为『已补取材待切片』」无条件 | 逐字在；医嘱已挂片时进度是「切片中」，这句为假 |
| summaryTitle 在一根柱库态仍说「不是某一天的数」 | **只对了一半**：`:863/:875` 明写只在 `days > 1` 时说；但 30 天窗口一根柱时确会说，而同屏正下方的现算说明已写「相同是本次库态使然」——记为措辞有歧义、同屏已自释，不计硬缺口 |

## 逐条：参数原文、审计者的主张、反驳者的硬缺口（原话，未转述）

### 2530

**参数原文（CSV 逐字）**：支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。 +---------------------------------------------------------------------------------------------------------------------------------------------------------+

**CSV 响应结论列**：部分响应

**CSV 说明列**：已提供：病理标本描述字段的存储与保存接口。尚未提供：①取材登记界面的取材过程描述录入入口（当前描述内容为固定预设值）；②标本描述在流转列表中的调阅展示。上述缺项列入后续开发计划。

**审计者**：维持。

子要求核对：

- [✓] 「支持文字化记录…取材过程信息」——取材登记界面有真实的文字录入入口（逐字段 + 自定义字段 + 自由描述），后端不预置任何值 —— frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:304-305 `<el-form-item v-for="f in grossFields" :key="f" :label="f">` / `<el-input v-model="form.gross[f]" maxlength="300" show-word-limit />`；:316 `<el-form-item label="添加字段">`；:326-327 `<el-form-item label="自由描述">` / `<el-input v-model="form.grossText" type="textarea" :rows="2"`。后端全取入参：modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:2338 `private static String assembleGross(Map<String, String> gross, String freeText) {`、:2347-2348 `String label = trim(e.getKey());` / `String value = trim(e.getValue());`。CSV 说明列缺项①在当前主树已不存在。
- [✓] 「完整的」——首写 / 补取材追加 / 取材修订三条写路径都落字段行并留修订痕 —— PathologyProcessController.java:544 `revisionSeq = insertGrossRevision(jdbc, req.specimenId(), existingGross == null ? null : existingRaw,`、:546 `grossFieldCount = storeGrossFields(req.specimenId(), revisionSeq, req.gross(), uid);`（取材端点，首写与追加同一处）；:771-772 `int revisionSeq = insertGrossRevision(jdbc, specimenId, existingRaw, assembled, "GROSSING_EDIT", uid, templateCode);` / `int grossFieldCount = storeGrossFields(specimenId, revisionSeq, req.gross(), uid);`（取材修订）；诊断端覆盖也走同一条留痕 SQL：modules/medtech/src/main/java/cn/hip/medtech/web/PathologyController.java:161 `PathologyProcessController.insertGrossRevision(jdbc, ((Number) row.get("id")).longValue(),`。
- [✓] 「确保描述内容的结构化存储」——逐字段落库、随修订版本化、各版都在且只增不改 —— PathologyProcessController.java:1664-1665 `Integer fieldsRevisionSeq = jdbc.queryForObject(` / `"select max(revision_seq) from path_gross_field where specimen_id = ?", Integer.class, specimenId);`；:1723-1728 `var allFieldRows = jdbc.queryForList("""` / `select f.revision_seq, f.seq, f.label, f.value` / `from path_gross_field f` …；:1856 `body.put("fieldsByRevision", fieldsByRevision);   // v60：全部版本的字段行…`。全仓对 path_gross_field 只有 insert（:2490 `insert into path_gross_field(specimen_id, revision_seq, seq, label, value, operator_id)`），server/src/test/java/cn/hip/server/V62GrossReviseGuardTest.java:790 `void fieldRowsAreNeverUpdatedOrDeletedAnywhere()` 扫全仓钉着。
- [✗] 「调阅」之一：「查看大体所见」的版本标签对第 1 版所述必须与同一次返回体里的库内事实一致（不得把内容仍逐字在正文里、字段行仍是该标签唯一活记录的那一版叫「被取代…仅供调阅」） —— v64 复核主证据这一处 v65 一字未动（`git show --stat 61ec8f1` 车道 A 只改 GrossingPanel.vue / PathologyProcessController.java / V62GrossReviseGuardTest.java / 登记簿；`git diff v1.6.4..v1.6.5` 下该函数无 hunk）。GrossingPanel.vue:976 `const superseded = earlier && !cumulative`、:978-979 `if (superseded) {` / `return { type: 'info', text: \`被取代版本（第 ${seq} 版，${n} 项）：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅\` }`。取材修订后 cumulative 恒 false：PathologyProcessController.java:1716 `lastIsAppend = "GROSSING".equals(r.get("source")) && r.get("old_text") != null;`，而修订行 source 是 :771 的 `"GROSSING_EDIT"`。库态由仓库自己的测试钉死：V62GrossReviseGuardTest.java:149-216（①）走「取材填 2 项→补取材填 2 项→照预填原样修订」后 :210-212 `assertEquals(4, ((Number) v2.get("textFieldForms")).intValue(),` … `assertEquals(2, ((Number) v2.get("textFieldFormsBacked")).intValue(), "其中只有 2 个有结构化字段行");`——第 1 版那 2 项此刻仍逐字在正文里、字段行仍在 rev1，而屏上切到第 1 版打「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」；正下方同一返回体的 fieldsNote（:1845-1846 `cn.append("；另有 ").append(formsInEarlier)` / `.append(" 个的字段行落在更早的版本里，按版本调阅得到，…")`）说的是相反的话。守卫只钉了一半：server/src/test/java/cn/hip/server/V63FrontendLabelGuardTest.java:468-470 `String gate = lineContaining(panel, "const superseded = ");` / `assertTrue(gate.contains("!cumulative"),`。
- [✗] 「调阅」之二：同一事实的第二个读出口（⑥ 流转与异常 → 查看轨迹 → 大体所见修订逐版展开）与之一致 —— frontend/shell/src/views/medtech/pathology/TrailPanel.vue:396-401 `function olderVersionNote(seq: unknown): string {` / `const latestSeq = gross.value.fieldsRevisionSeq` / `if (latestSeq == null || seq == null || !(Number(seq) < Number(latestSeq))) return ''` / `return gross.value.textIsCumulative === true` / `? '较早字段版：…未被取代'` / `: \`被取代版本：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅\``，渲染点 :223 `<span v-if="olderVersionNote(row.seq)" class="muted" style="margin-left: 6px">{{ olderVersionNote(row.seq) }}</span>`。TrailPanel.vue 在 v65 规划节车道 A 的文件独占清单里（docs/下一步开发规划.md:78），但 `git diff v1.6.4..v1.6.5 --stat -- frontend/shell/src/views/medtech/pathology/` 不含它。
- [✗] 「调阅/结构化存储」之三：「取材完成」弹窗「已落库的字段级记录」下打出的项数必须是本次这一版的行 —— GrossingPanel.vue:386-388 `<b>已落库的字段级记录</b>` / `<template v-if="resultView.fieldsAvailable === true">` / `<el-tag size="small" type="success" style="margin-left: 6px">{{ resultFields.length }} 项，按填写顺序</el-tag>`，数据源 :1033 `resultView.value = (await client.get(\`/pathology/process/grossing/${specimenId}\`)).data.data as typeof resultView.value`——GET 回的是「最新有字段行的那一版」（PathologyProcessController.java:1664-1665 max(revision_seq)），不是本次；本次行数后端明明回了 :602 `body.put("grossFieldCount", grossFieldCount);   // v58：本次落 path_gross_field 的字段行数（纯自由文本为 0）`，全文件 grep `grossFieldCount` 只在 :863 的修订成功提示里被消费，结果弹窗不用。补取材只写自由描述（V62GrossReviseGuardTest.java:709-710 那一步）后，该弹窗打「2 项，按填写顺序」而本次落 0 行。v63/v64 两轮点名，v65 未动、交接单未记。这是四处里最弱的一处（标题字面无「本次」，语境由 :398-400 的 v-else 分支钉成「本次」）。
- [✗] 「调阅」之四：读端点 fieldsNote 上屏的「字段级查询」是平台真有的调阅手段 —— PathologyProcessController.java:1846 `.append(" 个的字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中");`、:1850 `.append(" 个在任何一版里都没有对应的字段行，只以文本形式存在，字段级查询与统计取不到");`，经 :1854 `body.put("fieldsNote", …)` → format.ts:240-243 grossFieldsNote → GrossingPanel.vue:139-140 `<el-alert v-if="viewFieldsNote" …` / `:title="viewFieldsNote" />` 与 TrailPanel.vue:208-210 上屏。而全仓对 path_gross_field 的 SQL 只有按 specimen_id 取本标本（:709、:711、:1665、:1668、:1725）与 :2490 insert，没有任何按 label/value 检索或聚合的端点。v65 只把写端点两条告警里的「取不到」删了（:749 注释、V62GrossReviseGuardTest.java:997-1001 `assertNoDeadFieldClaim` 只喂写端点 warnings；tools/e2e-v59-audit.py 改后探针也只断言 `rv['warnings']`），CHANGELOG 1.6.5「『取不到』是假话…改成实话」只覆盖了写侧，读侧这两句原样在屏上。
- [✗] v65 新生成的文案（第五形态检查）：写端点告警在每个可达库态下都为真 —— PathologyProcessController.java:708-709 `Integer priorSeq = jdbc.queryForObject(` / `"select max(revision_seq) from path_gross_field where specimen_id = ?", Integer.class, specimenId);`（取的是**有字段行的最后一版**，注释 :704-705 自己写「最新一版取的就是『最后一个有字段行的版本』」），而上屏句把它叫「修订前最新一版」：:739 `warnings.add("本次修订不再写入任何结构化字段（修订前最新一版是第 " + priorSeq + " 版、" + priorLatest`、:732 `return R.fail(5277, "本次修订不含任何结构化字段，而修订前最新一版（第 " + priorSeq + " 版）有 "`。反例库态就是车道 A 自己在 ②g 建的那一格（V62GrossReviseGuardTest.java:696-710：取材填字段→补取材填字段→补取材只写描述）：textRevisionSeq=3、priorSeq=2；打开「修订取材描述」，标题旁 GrossingPanel.vue:233 打 `当前第 ${fmt(existingTextSeq)} 版` = 「当前第 3 版」，此时字段栏为空（②g :717 `assertTrue(split.gross().isEmpty(),`），照默认 warn 档提交（只在自由描述末尾加一句）→ :866 `for (const w of ((r.warnings ?? []) as unknown[])) ElMessage.warning(String(w))` 弹出「…修订前最新一版是第 2 版、2 项，本次 0 项…」——同屏刚说「当前第 3 版」。同句尾 :742 `+ "在「查看大体所见」里按版本调阅得到，只是不再对应当前描述"` 也在这一格为假：UNPARSED 预填把全文原样放进自由描述，新文本仍逐字含「补取块数：2 块；最大径：0.8cm」，提交后同一读端点按 :2446-2452 扣字段算出 backed=2，并在 fieldsNote 里说这 2 个「在最新字段版（第 2 版）里有对应的结构化字段行」——平台自己下一屏就否定了「不再对应当前描述」。登记簿 FACT 行 4d40bb885c74 的对拍用例 ② 只在 priorSeq==textRevisionSeq 的库态求值（V62GrossReviseGuardTest.java:295-296 `assertTrue(ws.get(0).contains("修订前最新一版是第 3 版、1 项，本次 0 项"),`），两个数相等时这句话看不出错。
- [✓] v65 声称修好的三条（v64 三个反驳镜头）确已修好 —— ①「已预填」不再写死：GrossingPanel.vue:767-776 `const reviseSubmitNote = computed(() => {` / `const k = Object.keys(reviseFormFields.value).length` …；:793 `return \`当前这段描述里有 ${n} 处「标签：值」，其中 ${backed} 处在最新字段版里有结构化字段行。\``；提交同源 :852 `const gross = reviseFormFields.value`。全文件 grep「已预填」只剩注释 :758、:761。②备注印模板中文名：PathologyProcessController.java:428 `templateName = String.valueOf(tpl.get("name"));`、:583 `+ (templateName == null ? "" : "（模板 " + templateName + "）")`，V62GrossReviseGuardTest.java:752 `void theGrossingNodeRemarkNamesTheTemplateInChinese()` 真库态实查 path_process.remark。③写侧「取不到」删除、基准统一：:708-713 只剩 priorSeq/priorLabels 一个基准，:753-755 `+ " 项（少 " + (priorLatest - plannedFieldCount) + " 项）：不再写入的是" + nameList(dropped)` / `+ "，这几项不进入本次这一版的字段级记录；它们在第 " + priorSeq + " 版的字段行仍在库里，"`。这三条我核为已修，但见上一条：③的重写自带新的假话。
- [✓] CSV 说明列缺项②「标本描述在流转列表中的调阅展示」 —— frontend/shell/src/views/medtech/pathology/RegistryPanel.vue:147-148 `<el-table-column label="标本描述" min-width="150" show-overflow-tooltip>` / `<template #default="{ row }">{{ fmt(row.specimen_desc) }}</template>`（登记列表）、:352-353 同形（标本检索）；GrossingPanel.vue:56-57 同形（取材工作列表）；查看弹窗 :100 `<el-descriptions-item label="标本描述（登记时录入）" :span="3">…`；轨迹抽屉头 TrailPanel.vue:166 `<el-descriptions-item label="标本描述" :span="4">{{ fmt(tSpecimen.specimen_desc ?? gSpecimen.specimen_desc) }}</el-descriptions-item>`。保留：屏上唯一自称「流转」的工位六（PathologyWorkbenchView.vue:29 `⑥ 流转与异常`）两张表（TrailPanel.vue:40-84、:110-145）的列清单里仍无 specimen_desc，判 met 是我按「按最近登记倒序」那张表取的，见保留项。

**审计者主证据**：【结论一句话】v65 只修了 v64 三个反驳者的三个镜头（已预填 / 模板码备注 / 写侧「取不到」与双基准），v64 审计者自己列的三处「调阅」假话（查看弹窗第 1 版标签、轨迹抽屉同句、取材完成弹窗把上一版当本次）一字未动、未登记结转；而 v65 为修「取不到」重写的那段告警，在车道 A 自己建的库态里又新造了一句假话——生成器把「有字段行的最后一版」叫成「修订前最新一版」。「调阅」这一维在当前主树仍对不上参数要求。

【1】v64 主证据那一处原样在（gap：走完平台默认修订流程后，「查看大体所见」切到第 1 版，标签说它「已被第 3 版字段取代，仅供调阅」，而它那 2 项正逐字在同屏正文里、字段行仍在 rev1，正下方黄条说的是相反的话）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:974-979
```
  const earlier = latestSeq != null && Number(v.revisionSeq) < Number(latestSeq)
  // 真被取代 = 存在一个更晚的、有字段行的版本（latestSeq 就是它；全无字段行时它为 null），
  // **且当前正文不是累积全文**——累积全文场景里这一版写下的内容仍原样留在当前 gross_finding 里，没被谁取代
  const superseded = earlier && !cumulative
  if (n === 0) return { type: 'info', text: `第 ${seq} 版：${viewNoFieldsTag.value}` }
  if (superseded) {
    return { type: 'info', text: `被取代版本（第 ${seq} 版，${n} 项）：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅` }
```
渲染点 :123 `<el-tag size="small" style="margin-left: 6px" :type="viewVersionTag.type">{{ viewVersionTag.text }}</el-tag>`。
取材修订后 cumulative 恒 false：modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:1716
```
            lastIsAppend = "GROSSING".equals(r.get("source")) && r.get("old_text") != null;
```
修订端点落的 source 是 :771 `int revisionSeq = insertGrossRevision(jdbc, specimenId, existingRaw, assembled, "GROSSING_EDIT", uid, templateCode);`。
库态由仓库自己的测试钉死：server/src/test/java/cn/hip/server/V62GrossReviseGuardTest.java:197-212（①：取材 2 项→补取材 2 项→照预填原样修订）
```
        var r = ok(process.reviseGrossFields(specimenId,
                new GrossReviseReq(null, new LinkedHashMap<>(fixed.gross()), fixed.free()), doc));
        assertEquals(2, ((Number) r.get("grossFieldCount")).intValue(), "预填原样提交 → 结构化字段不丢");
        ...
        assertEquals(Boolean.FALSE, v2.get("textIsCumulative"), "新版文本是一次拼装的，不再带「。补取材：」");
        ...
        assertEquals(4, ((Number) v2.get("textFieldForms")).intValue(),
                "当前文本里 4 个「标签：值」形态：" + v2.get("grossFinding"));
        assertEquals(2, ((Number) v2.get("textFieldFormsBacked")).intValue(), "其中只有 2 个有结构化字段行");
```
代进模板：latestSeq=3、seq=1、n=2 → 「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」。同屏正下方 :139-140 渲染的 fieldsNote 来自 PathologyProcessController.java:1845-1846
```
                cn.append("；另有 ").append(formsInEarlier)
                  .append(" 个的字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中");
```
v65 车道 A 没碰它：`git show --stat 61ec8f1` 列出的四个文件里 GrossingPanel.vue 的 hunk 只在 :738-797 与 :848-853（`git show b21e6c5 -- …GrossingPanel.vue`），viewVersionTag 无 hunk。docs/下一步开发规划.md:78 车道 A 任务只有 ①②③ 三项，docs/技术债交接单.md 全文 grep「被取代 / resultView / superseded / 取材完成」零命中。

【2】第二个入口同句（gap：⑥ 查看轨迹 → 大体所见修订表展开第 1 版打同一句假话）
frontend/shell/src/views/medtech/pathology/TrailPanel.vue:396-402
```
function olderVersionNote(seq: unknown): string {
  const latestSeq = gross.value.fieldsRevisionSeq
  if (latestSeq == null || seq == null || !(Number(seq) < Number(latestSeq))) return ''
  return gross.value.textIsCumulative === true
    ? '较早字段版：当前文本是累积全文，本版字段写下的内容仍在正文里，未被取代'
    : `被取代版本：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅`
}
```
渲染点 :223。TrailPanel.vue 在车道 A 独占清单里（docs/下一步开发规划.md:78 `PathologyProcessController`、`GrossingPanel.vue`、`TrailPanel.vue`），但 `git diff v1.6.4..v1.6.5 --stat -- frontend/shell/src/views/medtech/pathology/` 只列 GrossingPanel.vue / PathQcView.vue / ProcessingPanel.vue。

【3】v65 新造的那句（第五形态：生成器的标签与它查的那张表不是一回事——gap：修订弹窗头上写「当前第 3 版」，提交后弹出的告警写「修订前最新一版是第 2 版」）
PathologyProcessController.java:704-711
```
        // hadFields 的判据不因此变宽变窄：最新一版取的就是「最后一个有字段行的版本」，
        // 它有行 ⟺ 该标本有行，两个数同为 0、同为正。
        Integer priorSeq = jdbc.queryForObject(
                "select max(revision_seq) from path_gross_field where specimen_id = ?", Integer.class, specimenId);
        List<String> priorLabels = priorSeq == null ? List.of() : jdbc.queryForList(
                "select label from path_gross_field where specimen_id = ? and revision_seq = ? order by seq asc",
```
:739-743
```
            warnings.add("本次修订不再写入任何结构化字段（修订前最新一版是第 " + priorSeq + " 版、" + priorLatest
                    + " 项，本次 0 项）：这一版的大体所见只剩一段扁平文本；"
                    + "第 " + priorSeq + " 版那 " + priorLatest + " 项字段行仍在库里，"
                    + "在「查看大体所见」里按版本调阅得到，只是不再对应当前描述"
```
:732-734（block 档同型）
```
            return R.fail(5277, "本次修订不含任何结构化字段，而修订前最新一版（第 " + priorSeq + " 版）有 "
                    + priorLatest + " 项字段级记录：提交后这一版的大体所见只剩一段扁平文本，"
                    + "结构化记录停在第 " + priorSeq + " 版——那几行不会被删、按版本仍调阅得到，只是不再对应当前描述"
```
反例库态是车道 A 自己建的：V62GrossReviseGuardTest.java:696-710（②g，取材填 2 项→补取材填 2 项→补取材只写描述），:717 `assertTrue(split.gross().isEmpty(),` 坐实预填 0 项。此时修订表 seq 有 1/2/3 三行、path_gross_field 只有 rev1/rev2 → textRevisionSeq=3、priorSeq=2。修订弹窗头 GrossingPanel.vue:233
```
        {{ mode === 'REVISE' ? `当前第 ${fmt(existingTextSeq)} 版` : '只读；不覆盖，勾选「补取材」后可作为新版本追加' }}</el-tag>
```
（existingTextSeq 由 :841 `existingTextSeq.value = d.textRevisionSeq ?? null` 取）打「当前第 3 版」；出厂 warn 档（V62GrossReviseGuardTest.java:118 `gatePinned = setGate("warn");` 注释「档位先钉到出厂值」）下只在自由描述末尾加一句提交，:866 `for (const w of ((r.warnings ?? []) as unknown[])) ElMessage.warning(String(w))` 弹出「修订前最新一版是第 2 版、2 项，本次 0 项」。同句尾「只是不再对应当前描述」：UNPARSED 预填把全文原样放进自由描述（GrossingPanel.vue:677 `return { gross: {}, free: text, mode: 'UNPARSED' }`），新文本仍含「补取块数：2 块；最大径：0.8cm」，提交后同一读端点 :2446-2452 `for (String[] kv : latestForms) {` … `backed++;` 算出 backed=2，fieldsNote 打「其中 2 个在最新字段版（第 2 版）里有对应的结构化字段行」——平台下一屏否定了自己上一屏。登记簿 FACT 行 docs/屏上断言登记簿.tsv 4d40bb885c74 的对拍用例只求值 priorSeq==textRevisionSeq 那一格：V62GrossReviseGuardTest.java:295-296
```
        assertTrue(ws.get(0).contains("修订前最新一版是第 3 版、1 项，本次 0 项"),
                "**修复前的反向事实**：这里打的是全部版本累计行数（此刻 3 行），不是最新一版的 1 项：" + ws.get(0));
```

【4】读侧 fieldsNote 仍许诺不存在的「字段级查询」（gap：查看弹窗与轨迹抽屉的黄条说「字段级查询按它所属的那一版命中」「字段级查询与统计取不到」，而全仓没有任何按字段检索/统计的端点）
PathologyProcessController.java:1846、:1850 见上；上屏链 :1854 `body.put("fieldsNote", versionNote == null ? coverNote` → frontend/shell/src/views/medtech/pathology/format.ts:242-243 `const fromServer = mdText(v.fieldsNote)` / `if (fromServer !== '') return fromServer` → GrossingPanel.vue:139-140、TrailPanel.vue:208-210。`grep -rn path_gross_field modules/*/src/main server/src/main` 的 SQL 命中只有 :709、:711、:1665、:1668、:1725（全部 `where specimen_id = ?`）与 :2490 insert。v65 的禁令只喂写端点：V62GrossReviseGuardTest.java:997-999
```
    private static void assertNoDeadFieldClaim(String warning) {
        assertFalse(warning.contains("字段级查询与统计取不到"),
```
tools/e2e-v59-audit.py（05b7c11 改后）`assert '字段级查询与统计取不到' not in rv['warnings'][0]` 同样只看 warnings。

【5】取材完成弹窗（gap：补取材只写自由描述后，「已落库的字段级记录」打「2 项，按填写顺序」，那 2 项是上一版的）
GrossingPanel.vue:386-388 / :1033 见子要求六；后端本次行数 :602 `body.put("grossFieldCount", grossFieldCount);` 弹窗不消费（全文件 grep 只在 :863 命中）。

【6】三件被要求专门验的事
(a) 尺子能被绕过——本轮改动里就有一条落在它的盲区：5277 那句整段重写（:732-736），但 `git diff v1.6.4..v1.6.5 -- …PathologyProcessController.java` 里这三行 + 行只含「：」「，」「——」，句末「。」在未改动的上下文行 `+ "（gate " + GROSS_FIELD_GATE_KEY + "=block）。"` 上；tools/claim-ratchet.py:52 `SENTENCE = re.compile('[。；！？]')` 与 :229-234 `flush` 只对「连续 + 行」拼出的字面量判句读，于是这句改了正文的上屏文案不进棘轮。车道 A 在登记簿 186336d59420 行末自己承认「它的句读落在未改动的上下文行上、棘轮抽不到它」。这是文件头四条之外的第五种逃逸形态：改句中、留句末。另：:229-234 把一段连续 + 行的全部字面量并成一个指纹，所以 :739-745 整段告警只登记一行（4d40bb885c74），其中「修订前最新一版」「只是不再对应当前描述」两个从句从未在为假的库态被求值——正是文件头 limits (a)「一槽多句」在本轮车道 A 上的实例。
(b) 登记簿对拍用例：2530 的五行 FACT 里，e0195cb365f2（②c/②d）、56d91898facf（①）、186336d59420（②/②b）确在真库态求值后端返回体；4d40bb885c74（②）真库态但只有 priorSeq==textRevisionSeq 一格（见【3】）；f1879db993e6（②g）真库态，但被求值的句子是 Java 同构副本 V62GrossReviseGuardTest.java:1077-1085 `private static String submitNote(int formFieldCount) {`，屏上那句只由 ②f 源码扫描钉住（:681 `assertTrue(panel.contains(SUBMIT_NOTE_ZERO_HEAD), …)`），真正的生成器 `reviseFormFields`（GrossingPanel.vue:748-752）没有被任何用例执行——「真库态 + 副本」不等于「真库态 + 真前端」。
(c) 「它在什么状态下是假的」没想全的一句：4d40bb885c74 那行答的是「基准两套」与「旧版仍可调阅」，没答「修订前最新一版」这个称谓在**最后一版没有字段行**时是不是真的；反例已在【3】造出，且用的是同一车道 ②g 自己搭的库态再多提交一次。

**演示路径（审计者）**：前提：全新零种子库（Flyway 到 V168）、单个 ADMIN 账号一次登录、只读主树 main=bbf8012 / tag v1.6.5、不跑任何铺数脚本。

【演示陷阱 0：别跑 tools/demo-pathology.py】它的取材只传 grossText、不传结构化字段——tools/demo-pathology.py:172-173 `do('取材', 'POST', '/pathology/process/grossing',` / `{'specimenId': sid, 'grossText': text, 'blocks': [{'tissueDesc': d} for d in descs]})`，补取材 :217-219 同样只有 grossText。v65 车道 B 给它加了 157 行（`git diff v1.6.4..v1.6.5 --stat`），仍无一处传 gross 字段。它铺出来的每个标本打开「查看大体所见」都是 GrossingPanel.vue:157 `'历史标本或纯自由文本，无字段级记录（不从文本反解析）'`，把本条要演的结构化记录演示没了。字段必须现场手工录。

(1) 菜单：「门诊业务 → 病理工作台」。server/src/main/resources/db/migration/V144__pathology_base.sql:198 `(168, 7, '病理工作台', 'MENU', '/pathology/workbench', 'path:work', 'Files', 30),`；ADMIN 授权 V156__v52_menus.sql:25 `select r.id, 168 from sys_role r where r.code in ('ADMIN', 'TECHNICIAN', 'DOCTOR_OUTP')`。

(2) 造待登记申请：门诊挂号 → 门诊医生站开一条项目名含「病理」或「活检」的检查项 → 收费。判据 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyRegistryController.java:169-170 `where o.status = 'CHARGED'` / `and (o.item_name like '%病理%' or o.item_name like '%活检%')`。走不通门诊链时可用「① 登记」页的「手工登记」（RegistryPanel.vue:71），但它直接置为已核收、跳过接收核对（:186 那条 alert 原话），且仍要一个院内医嘱 id。

(3) ① 登记 → 待登记申请 → 「登记」→ 【陷阱 1】务必手工填「标本描述」：RegistryPanel.vue:206 `<el-form-item label="标本描述">` 无 required，后端只校长度 PathologyRegistryController.java:781 `if (trim(req.specimenDesc()).length() > 255) return "标本描述过长（最多 255 字）";`。不填，则缺项②那几处展示全是「—」。再到「标本检索」页 → 「接收核对」（:170）→「核收」（:293）。

(4) ② 取材：范围默认「待取材」→ 选中 →「取材登记」→ 填两项字段（标本大小=5×4×3cm、切面=灰白）→ 自由描述「首次」→ 加 1 块 →「登记取材」。
【陷阱 2】关掉「取材完成」后必须把「范围」切到「全部」：GrossingPanel.vue:449 `const query = reactive({ scope: 'pending', specimenType: '', urgentOnly: false, keyword: '' })`，而 pending 的判据是 PathologyProcessController.java:307-308 `sql.append(" and s.diagnosed_at is null ")` / `.append(" and not exists (select 1 from path_block b where b.specimen_id = s.id) ");`——刚取完材的标本立刻从默认列表消失。
【陷阱 3】切范围时别照下拉括注讲：GrossingPanel.vue:7 `<el-option value="all" label="全部（已核收未拒收，含存量无蜡块标本）" />` 没写「含已取材的」，正下方 :28 渲染的后端 note（:333 `+ "「全部」列出已核收未拒收的标本，含已取材的，…"`）写了。v64 登记为结转 v65，v65 仍未动。

(5) 切「全部」→ 同一标本再「取材登记」→ 勾「补取材」→ 再填两项字段（补取块数=2 块、最大径=0.8cm）→ 自由描述「补取材X」→ 加 1 块 →「登记取材」。

(6) 【可正面演的部分】点「修订取材描述」→ 什么都不改直接「提交修订」：预填走 CUMULATIVE 档，覆盖条前半句「4 处…其中 2 处…差的 2 处是「标本大小」「切面」」与后半句「下面的字段栏此刻填了 2 项…」都是真的（V62GrossReviseGuardTest.java ① / ②g 钉着），这是 v65 真修好的部分。
【陷阱 4】这一屏标题行仍打库列名：GrossingPanel.vue:231 `<b>{{ mode === 'REVISE' ? '当前大体所见（修订前，将记为 old_text）' : '该标本已录入的大体所见' }}</b>`。

(7) 【第一处翻车】点「查看大体所见」→ 版本下拉切到「第 1 版」：标签打「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」（GrossingPanel.vue:979），正下方黄条（:139-140 渲染 :1845-1846）写「另有 2 个的字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中」，再往上 :107 的 `<pre>` 里「标本大小：5×4×3cm；切面：灰白」逐字躺着。三处互相打脸，且黄条许诺的「字段级查询」全仓不存在。

(8) 【第二处】「⑥ 流转与异常 → 轨迹查询」→ 关键词搜该标本 →「查看轨迹」→ 大体所见修订表展开第 1 版：同一句「被取代版本：已被第 3 版字段取代，仅供调阅」（TrailPanel.vue:401，渲染 :223）。
【陷阱 5】工位六两张表没有「标本描述」列（TrailPanel.vue:40-84、:110-145），只有抽屉头 :166 有；别在这里演缺项②。

(9) 【第三处 + v65 新造的第四处】回「② 取材」→ 同一标本再「取材登记」→ 勾「补取材」→ 字段全留空、只写自由描述「再补一次」→ 加 1 块 →「登记取材」：
  (9a)「取材完成」弹窗「已落库的字段级记录」打「2 项，按填写顺序」（GrossingPanel.vue:388）而本次落 0 行；
  (9b) 关掉后点「修订取材描述」：弹窗头打「当前第 3 版」（:233）、上面黄条说字段留空、覆盖条尾句说「下面的字段栏此刻一项都没有填…结构化记录停在修订前那一版」——这一屏本身是真话（v65 修好的）；现在什么字段都不填、只在自由描述末尾加一句「复核补一句」→「提交修订」：出厂 warn 档下右上角弹出「本次修订不再写入任何结构化字段（修订前最新一版是第 2 版、2 项，本次 0 项）：…第 2 版那 2 项字段行仍在库里…只是不再对应当前描述（「提示」档放行）」（PathologyProcessController.java:739-743，经 GrossingPanel.vue:866 上屏）——刚才屏上写的是「当前第 3 版」；再点「查看大体所见」，黄条又说「其中 2 个在最新字段版（第 2 版）里有对应的结构化字段行」，即那 2 项仍对应当前描述。
【陷阱 6】(9b) 的「第 2 版 vs 第 3 版」只在「最后一版没落字段行」的库态出现；若 (5) 之后直接在 (6) 那一步提交 0 字段，priorSeq 与 textRevisionSeq 相等（都是 2），这句话看不出错——这正是仓库对拍用例站的那一格。

【安全演法】在 (6) 提交修订时把自由描述里第 1 版那两句手工搬进字段输入框（4 项全成字段）：N==M、coverNote 为空、第 1 版内容被第 3 版字段真正吸收后「被取代」不再是假话；并且此后不要再做 (9)。但这要求演示者事先知道要这么做，参数要求的是平台本身别说假话。

**审计者自列的保留项**：1.【只读审计】没跑 mvn、没起服务、没连库、没改文件。(7)(8)(9) 的屏上文字是按当前主树代码逐行推演的，不是实机截图；库态用仓库自己的 V62GrossReviseGuardTest ①（:149-216）与 ②g（:696-750）钉住，(9b) 那一格是在 ②g 的库态上多提交一次修订推出来的，这一步没有任何仓库测试或 E2E 走过——若实机结果与我不同，以实机为准。

2.【没核到的】(a) CHANGELOG 自称的 1076 测试 / 46 次 E2E 是否全绿；(b) /actuator/info 与产物一致性；(c) frontend/shell/dist 是否与 src 同版（判断对象一律是 src）；(d) 没实际执行 tools/claim-ratchet.py 与 contract-wired-check.py，只读了源码判逻辑；(e) 门诊挂号→开单→收费那段前置演示步骤我只核了病理侧判据（PathologyRegistryController.java:169-170）与菜单迁移，门诊侧菜单名没逐一回查。

3.【本结论的边界】录入入口、三条写路径留痕、逐字段版本化落库、CSV 缺项①，我逐行核为 met；v65 声称修的三条（已预填 / 模板中文名 / 写侧取不到与双基准）我核为确已修。卡住的全部在「调阅」这一维的口径真实性：两处是 v63 起点名、v64/v65 两轮都没派修的（查看弹窗与轨迹抽屉同一判定），一处是 v63 主证据点名后四轮未动的（取材完成弹窗），一处是读侧 fieldsNote 许诺不存在的「字段级查询」，一处是 v65 本轮重写告警时新造的（「修订前最新一版」= 有字段行的最后一版）。离上调差的是前端三处判定改读后端已算好的 textFieldFormsInEarlierVersions / textFieldFormsUnbacked / 本次 grossFieldCount、读侧删掉「字段级查询」四个字、写侧告警把称谓改成「修订前最后一个有字段行的版本（第 N 版）」——不是缺一块功能。

4.【存疑、我给了 met 但不踏实的】CSV 缺项②「流转列表」我按「按最近登记倒序」那张表判 met（RegistryPanel.vue:352-353），但屏上唯一自称「流转」的是工位六，其两张表无 specimen_desc 列（TrailPanel.vue:40-84、:110-145）。签认人若按屏上标签读，这一项会再掉一条。与 v64 复核同一取舍。

5.【判断上的主观点，明说】(a)「只是不再对应当前描述」（PathologyProcessController.java:734、:742）我判假，依据是平台下一屏自己算出的 backed=2；若把「对应」读成「隶属于当前这一版」而非「内容对应」，这半句可以不算，但「修订前最新一版是第 2 版」不受此读法影响。(b) 取材完成弹窗那处是四处里最弱的（标题字面没写「本次」），若按字面读可以不算；前面任何一处单独成立都足以维持。(c)「字段级查询」那两句，textOnly 那半句「取不到」在「那几处确无字段行」的意义上为真，我判的是它许诺了一个不存在的检索手段；这是否够得上「屏上说假话」由签认人取舍，我按本项目「屏上说假话即维持」的口径计入。

6.【闸门自身的洞（prompt 要我验的第三件事）】(a) claim-ratchet.py 的 diff 抽取要求「连续 + 行」内含 `[。；！？]`（:52、:229-234），改句中留句末即逃逸——本轮 5277 消息（:732-736）就是实例，车道 A 在登记簿里自己承认；(b) 一段连续 + 行并成一个指纹（:236 `out.setdefault(fingerprint(joined), …)`），整段告警只登记一行，从句没有各自的求值义务——:739-745 的「修订前最新一版」「不再对应当前描述」由此漏过；(c) `strip_comments` 逐行调用（:230 `strip_comments(ln)`），跨行块注释的中间行剥不掉，含 ASCII 引号的 javadoc 行会并进相邻代码句的指纹（本轮没撞上，是潜在假红/假绿源）；(d) `touched_claims` 只 diff `frontend/shell/src` 与 `modules`（:213），server 模块控制器里的上屏串不在棘轮内；(e) 登记簿 f1879db993e6 的对拍是 Java 副本，真生成器 reviseFormFields 无用例执行——「同构副本 + 源码扫描钉措辞」是把 v63 那种源码扫描换了个壳，比纯扫描强，但不是 FACT 档承诺的「在真实库态下逐档求值这句话」。

7.【已知结转项复核】(a) docs/下一步开发规划.md:86-87 与 :88 的两条（「全部」下拉括注、format.ts:245 兜底）在当前主树仍原样（GrossingPanel.vue:7；format.ts:245-248），前者我列为演示陷阱 3，后者我按当前后端推为不可达（fieldsNote 在 fieldsCurrent=false 各档都非空），未据此判 false；(b) 「开发者视角内容上屏」残留：GrossingPanel.vue:231 的 old_text、:1093 `补取材医嘱挂接被拒（5275）：` 裸码，仍在，不单独计入结论。

8.【方法交代】参数原文与两列由我自己用 python csv（utf-8-sig、newline=''）按序号列定位读出，并用「重建字符串（前缀 34 字符含末尾半角空格 + 153 个连字符 + '+'）== CSV 原值」断言校验通过。docs/验收/v64复核-偏离表三条.md 只用来知道上一轮打回了什么，其中任何转述都没当证据，全部指控回当前主树按行号复核；CHANGELOG、规划节、登记簿备注全程只当被审对象的自述。本条为 ★ 实质性条目的单 agent 只读审计，不等于已改表。

**反驳者·子要求拆解**（推翻）：

「调阅」这一子要求仍被平台自己说假：走平台默认流程（取材登记填 2 项字段 → 补取材再填 2 项 → 「修订取材描述」照预填原样提交）后，「查看大体所见」弹窗把版本下拉切到第 1 版，标签打「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」，而同屏正下方渲染后端 fieldsNote 的黄条写着「另有 2 个的字段行落在更早的版本里，按版本调阅得到」、再往上的大体所见正文里第 1 版的「标本大小 / 切面」逐字躺着、库里 revision_seq=1 的两行也仍是这两个标签唯一的字段行；同一句假话在第二个入口（⑥ 轨迹查询 → 查看轨迹 → 大体所见修订表展开第 1 版，TrailPanel.olderVersionNote）再打一次。判定处 GrossingPanel.vue:976 的 `superseded = earlier && !cumulative` 与 TrailPanel.vue:399-401 自 v63（bf2efbd）起一字未动，v65 的 GrossingPanel 改动只碰了 reviseCoverNote/reviseSubmitNote，TrailPanel.vue 最后一次提交仍是 v64 的 4f9f0f1；后端早已算出的 textFieldFormsInEarlierVersions / textFieldFormsUnbacked（含 earlierRevisionSeq）只被修订弹窗一个消费方接走，真正需要它的两处「被取代」判定一个键都没取——这正是 v64 复核审计者主证据点名、v65 任务书与结转清单都没收进去的那一条。

【证据】【1】说假话的判定与渲染（当前主树，main=bbf8012）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:971-979
971	  const latestSeq = view.value.fieldsRevisionSeq
972	  const cumulative = view.value.textIsCumulative === true
973	  const earlier = latestSeq != null && Number(v.revisionSeq) < Number(latestSeq)
974	  // 真被取代 = 存在一个更晚的、有字段行的版本（latestSeq 就是它；全无字段行时它为 null），
975	  // **且当前正文不是累积全文**——累积全文场景里这一版写下的内容仍原样留在当前 gross_finding 里，没被谁取代
976	  const superseded = earlier && !cumulative
977	  if (n === 0) return { type: 'info', text: `第 ${seq} 版：${viewNoFieldsTag.value}` }
978	  if (superseded) {
979	    return { type: 'info', text: `被取代版本（第 ${seq} 版，${n} 项）：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅` }
渲染点 GrossingPanel.vue:123
123	          <el-tag size="small" style="margin-left: 6px" :type="viewVersionTag.type">{{ viewVersionTag.text }}</el-tag>
同屏正下方那条说相反话的黄条 GrossingPanel.vue:139-140
139	          <el-alert v-if="viewFieldsNote" type="warning" show-icon :closable="false" style="margin-top: 4px"
140	                    :title="viewFieldsNote" />
（viewFieldsNote → format.ts:240-243 `const fromServer = mdText(v.fieldsNote)` / `if (fromServer !== '') return fromServer`，即后端 fieldsNote 原文）

【2】修订后 cumulative 恒为 false，第 1 版必落「被取代」档
modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:771
771	        int revisionSeq = insertGrossRevision(jdbc, specimenId, existingRaw, assembled, "GROSSING_EDIT", uid, templateCode);
同文件 :1716
1716	            lastIsAppend = "GROSSING".equals(r.get("source")) && r.get("old_text") != null;
同文件 :1824
1824	        body.put("textIsCumulative", lastIsAppend);   // v61：当前文本是累积全文（补取材追加），不是某一版字段的拼装结果

【3】同一次返回体里后端说的是相反的话（这句正是【1】黄条渲染的内容）
PathologyProcessController.java:1844-1846
1844	            if (formsInEarlier > 0) {
1845	                cn.append("；另有 ").append(formsInEarlier)
1846	                  .append(" 个的字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中");

【4】第二个入口同一句假话
frontend/shell/src/views/medtech/pathology/TrailPanel.vue:396-402
396	function olderVersionNote(seq: unknown): string {
397	  const latestSeq = gross.value.fieldsRevisionSeq
398	  if (latestSeq == null || seq == null || !(Number(seq) < Number(latestSeq))) return ''
399	  return gross.value.textIsCumulative === true
400	    ? '较早字段版：当前文本是累积全文，本版字段写下的内容仍在正文里，未被取代'
401	    : `被取代版本：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅`
402	}
渲染点 TrailPanel.vue:223
223	                  <span v-if="olderVersionNote(row.seq)" class="muted" style="margin-left: 6px">{{ olderVersionNote(row.seq) }}</span>

【5】触发所需库态由仓库自己的测试逐行钉死（当前主树行号）
server/src/test/java/cn/hip/server/V62GrossReviseGuardTest.java:482-483
482	    void unbackedFormsAreSplitByWhetherAnEarlierVersionStillHoldsTheirFieldRows() {
483	        // 复核者站的那一格：取材登记（标本大小 / 切面）→ 补取材（补取块数 / 最大径）→ 照预填原样修订
同文件 :500-502
500	        assertEquals(4, ((Number) v.get("textFieldForms")).intValue(),
501	                "当前这段描述里 4 个「标签：值」：" + v.get("grossFinding"));
502	        assertEquals(2, ((Number) v.get("textFieldFormsBacked")).intValue(), "最新字段版只覆盖其中 2 个");
同文件 :507-508、:512-517、:520-523
507	        assertEquals(2, ((Number) v.get("textFieldFormsInEarlierVersions")).intValue(),
512	        assertEquals(List.of("标本大小", "切面"),
517	            assertEquals(1, ((Number) u.get("earlierRevisionSeq")).intValue(), "它们是第 1 版录入的：" + u);
520	        assertEquals(2L, (long) jdbc.queryForObject("""
521	                select count(*) from path_gross_field
522	                where specimen_id = ? and revision_seq = 1 and label in ('标本大小', '切面')
同文件 :537-539（版号相等且 fieldsVersionCurrent=true ⇒ 后端 :1780 `versionCurrent = sameSeq && !lastIsAppend` ⇒ lastIsAppend=false ⇒ textIsCumulative=false）
537	        assertEquals(v.get("fieldsRevisionSeq"), v.get("textRevisionSeq"),
539	        assertEquals(Boolean.TRUE, v.get("fieldsVersionCurrent"), "版号这一维为真");
代入【1】：latestSeq=3、seq=1、n=2（viewFields 取 fieldsByRevision 第 1 版两行，GrossingPanel.vue:912-927）→「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」。

【6】v65 没碰这两处，也没登记
`git diff v1.6.4..v1.6.5 -- GrossingPanel.vue` 只有三个 hunk：`@@ -738,6 +738,42 @@`（新增 reviseFormFields / reviseSubmitNote）、`@@ -752,11 +788,13 @@`（reviseCoverNote 措辞）、`@@ -810,8 +848,8 @@`（submitRevise 改用 reviseFormFields）；`git log --oneline -S"const superseded = earlier && !cumulative" -- GrossingPanel.vue` 唯一命中 `bf2efbd v63 车道B`；`git log --oneline -3 -- TrailPanel.vue` 最新为 `4f9f0f1 v64 车道A`（v65 零提交）。`git diff v1.6.4..v1.6.5 -- PathologyProcessController.java` 未触及 :1716/:1824/:1844 任何一行。
docs/下一步开发规划.md:24（v65 结转清单原文）
24	**结转 v66**：`AnesQcView` 合计块无标题（同型缺陷跨域第三次被点名，交接单已记处置方向）；`ANOMALY_NOTE` 那行 FACT 档仍无机器对拍用例；兄弟指标 caveat 的「当日」措辞。
docs/屏上断言登记簿.tsv 全文无「被取代」「viewVersionTag」「olderVersionNote」任何一行。

【7】唯一盯这行的守卫钉的是那个近似值本身
server/src/test/java/cn/hip/server/V63FrontendLabelGuardTest.java:468-470
468	        String gate = lineContaining(panel, "const superseded = ");
469	        assertTrue(gate.contains("!cumulative"),
470	                "superseded 必须排除累积全文场景（更早各版的内容仍在正文里，没被谁取代）：" + gate);

【8】（次要，同型未清）「取材完成」弹窗仍把上一版的行当本次的
GrossingPanel.vue:386-388
386	      <b>已落库的字段级记录</b>
387	      <template v-if="resultView.fieldsAvailable === true">
388	        <el-tag size="small" type="success" style="margin-left: 6px">{{ resultFields.length }} 项，按填写顺序</el-tag>
数据源 :1045 `resultView.value = (await client.get(`/pathology/process/grossing/${specimenId}`)).data.data`；后端 :1771-1773
1771	        body.put("fieldsAvailable", !fields.isEmpty());
1773	        body.put("fieldsRevisionSeq", fields.isEmpty() ? null : fieldsRevisionSeq);   // v59：fields 属于哪一版
（fields = max(revision_seq) 那一版；补取材只写自由描述、本次落 0 行时仍打绿标「2 项」。v65 diff 未触及。）

【反向核对】先怕自己冤枉它，做了六件反向核对：(1) 确认「被取代」档不会被更上游分档吃掉：GrossingPanel.vue:977 的 n===0 要求该版零字段行，第 1 版有 2 行（fieldsByRevision 逐版回出，:891-895 排序、:912-927 取该版 fields）；:978 superseded 分支排在 :981 earlier 分支之前；标签外层 `<template v-if="viewFieldVersions.length">`（:114）在 fieldsByRevision 有 3 版时为真，下拉（:117-120）逐版列出含第 1 版。(2) 确认 cumulative 确为 false 不是我推的：修订端点落 source=GROSSING_EDIT（:771），:1716 只对 GROSSING 且 old_text 非空判真；V62GrossReviseGuardTest:539 在同一格里断言 fieldsVersionCurrent=TRUE，而 :1780 `versionCurrent = sameSeq && !lastIsAppend`，故 lastIsAppend 必为 false。(3) 确认没有别处兜底盖掉这句：全仓消费 textIsCumulative 的只有 GrossingPanel.viewVersionTag、TrailPanel.olderVersionNote 与 format.ts:246 的兜底（后者仅在后端 fieldsNote 为空时走，本格 fieldsNote 非空）；后端算好的 textFieldFormsUnbacked/earlierRevisionSeq 在 frontend 只被 reviseCoverNote 消费（grep 命中 GrossingPanel.vue:762-800 一处），两处「被取代」判定零引用。(4) 确认不是读法之争：无论「被取代」指文本内容还是字段行，第 1 版两项都不成立——正文含 4 个形态（测试 :500-501）、rev 1 两行仍在（:520-523）、后端自己那句「按版本调阅得到」（:1846）同屏渲染。(5) 确认 v65 声称修的三条我核为确已修好、没拿它们凑数：reviseCoverNote 不再含「已预填」（diff hunk @@ -752）、备注印中文模板名（PathologyProcessController.java:417-428、:583）、退化告警不再说「取不到」且只留一个基准（:698-760）。(6) 确认 v65 自己的闸门结构上抓不到这一条：tools/claim-ratchet.py:37-38 自列的 (b)「文案一字未动、而它底下的代码被改判——diff 驱动看不见」正是本条形态（这句自 bf2efbd 起未动，registry 无此行），且这一轮登记簿的 FACT 档全部落在修订弹窗/告警，查看弹窗与轨迹抽屉的版本标签一行都没登。最刁钻的反例我也试了：若第 (6) 步修订时把第 1 版两项手工搬回字段栏（4 项全填），则 fieldsCoverText=true、第 1 版内容真被第 3 版字段吸收，这句标签才不假——但那要求演示者事先知道要这么做，参数要求的是平台本身别说假话，且仓库自己的 ②d 用例钉的默认路径正是「照预填原样提交」。

**反驳者·演示可行性**（推翻）：

评委只按平台自己的默认流程走（② 取材 → 取材登记填 2 项字段 → 切「全部」→ 补取材再填 2 项 → 「修订取材描述」照预填原样提交），再点「查看大体所见」把版本下拉切到「第 1 版」，屏上标签打出「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」——而第 3 版字段是「补取块数／最大径」、根本没取代第 1 版的「标本大小／切面」：这两项此刻正逐字躺在同屏上方的大体所见正文里，其字段行仍是这两个标签唯一的结构化记录，同屏正下方那条后端黄条还写着它们「字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中」；⑥ 流转与异常 → 查看轨迹 → 逐版展开第 1 版打的是同一句「被取代版本：已被第 3 版字段取代，仅供调阅」。参数要求的「调阅」在平台自己的调阅屏上被说成「已被取代、仅供调阅」，同屏三处互相打脸。v65 对 GrossingPanel.vue:976-979 与 TrailPanel.vue:396-402 一字未动（两处 blame 均停在 v63 的 bf2efbd），登记簿没有这两行，v65 规划节车道 A 的三条不含它，交接单也没记——这是 v63 复核点名、v64 漏派、v65 再漏的第三轮；而本轮新立的 diff 驱动棘轮只收 v1.6.4 之后的 + 行，对这句「文案没动、一直是假」的话结构上看不见，它的全绿不是这句话为真的证据。

【证据】【1】说假话的那一句（当前主树，v65 未动；git blame 972-976 = bf2efbd v63 车道 B，977-979 = f79f0a5 v62）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:970-980
  const n = viewFields.value.length
  const latestSeq = view.value.fieldsRevisionSeq
  const cumulative = view.value.textIsCumulative === true
  const earlier = latestSeq != null && Number(v.revisionSeq) < Number(latestSeq)
  // 真被取代 = 存在一个更晚的、有字段行的版本（latestSeq 就是它；全无字段行时它为 null），
  // **且当前正文不是累积全文**——累积全文场景里这一版写下的内容仍原样留在当前 gross_finding 里，没被谁取代
  const superseded = earlier && !cumulative
  if (n === 0) return { type: 'info', text: `第 ${seq} 版：${viewNoFieldsTag.value}` }
  if (superseded) {
    return { type: 'info', text: `被取代版本（第 ${seq} 版，${n} 项）：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅` }
  }
渲染点 GrossingPanel.vue:123
  <el-tag size="small" style="margin-left: 6px" :type="viewVersionTag.type">{{ viewVersionTag.text }}</el-tag>
同屏正上方的正文 GrossingPanel.vue:107
  <pre v-if="view.grossFindingPresent">{{ view.grossFinding }}</pre>
同屏正下方说相反话的黄条 GrossingPanel.vue:139-140
  <el-alert v-if="viewFieldsNote" type="warning" show-icon :closable="false" style="margin-top: 4px"
            :title="viewFieldsNote" />
黄条文案来源 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:1840-1846
  String coverNote = null;
  if (!fields.isEmpty() && !fieldsCoverText) {
      var cn = new StringBuilder("当前大体所见文本里有 " + textFieldForms + " 个「标签：值」形态，其中 "
              + textFieldFormsBacked + " 个在最新字段版（第 " + fieldsRevisionSeq + " 版）里有对应的结构化字段行");
      if (formsInEarlier > 0) {
          cn.append("；另有 ").append(formsInEarlier)
            .append(" 个的字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中");

【2】为什么这条路上 cumulative 恒为 false、第 1 版必落「被取代」档
PathologyProcessController.java:771（取材修订落的 source）
  int revisionSeq = insertGrossRevision(jdbc, specimenId, existingRaw, assembled, "GROSSING_EDIT", uid, templateCode);
PathologyProcessController.java:1716
  lastIsAppend = "GROSSING".equals(r.get("source")) && r.get("old_text") != null;
PathologyProcessController.java:1780 / :1824
  boolean versionCurrent = sameSeq && !lastIsAppend;   // v59 / v61 的口径：**只比版号**
  body.put("textIsCumulative", lastIsAppend);   // v61：当前文本是累积全文（补取材追加），不是某一版字段的拼装结果

【3】库态由仓库自己的测试钉死（不是我推演的）
server/src/test/java/cn/hip/server/V62GrossReviseGuardTest.java:482-483
  void unbackedFormsAreSplitByWhetherAnEarlierVersionStillHoldsTheirFieldRows() {
      // 复核者站的那一格：取材登记（标本大小 / 切面）→ 补取材（补取块数 / 最大径）→ 照预填原样修订
同文件 :512-517
  assertEquals(List.of("标本大小", "切面"),
          unbacked.stream().map(u -> String.valueOf(u.get("label"))).toList(),
          "逐条给出差在哪几项——屏上要指名道姓，这是修订弹窗那条提示的数据源");
  for (var u : unbacked) {
      assertEquals(Boolean.TRUE, u.get("inEarlierVersion"), u.toString());
      assertEquals(1, ((Number) u.get("earlierRevisionSeq")).intValue(), "它们是第 1 版录入的：" + u);
同文件 :520-523（库内对照组：第 1 版那 2 行真的还在）
  assertEquals(2L, (long) jdbc.queryForObject("""
          select count(*) from path_gross_field
          where specimen_id = ? and revision_seq = 1 and label in ('标本大小', '切面')
          """, Long.class, specimenId));
同文件 :537-539
  assertEquals(v.get("fieldsRevisionSeq"), v.get("textRevisionSeq"),
          "字段版号与文本版号是同一个数——把它说成「文本已是第 N 版」就是同一个版号说两件事");
  assertEquals(Boolean.TRUE, v.get("fieldsVersionCurrent"), "版号这一维为真");
同文件 :438（同一三步场景直接断言）
  assertEquals(Boolean.FALSE, v2.get("textIsCumulative"), "v62 条件二：文本不是累积拼装形态");
代入【1】：latestSeq=3、v.revisionSeq=1 → earlier=true；cumulative=false → superseded=true；n=2 → 「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」。

【4】同一事实的第二个读出口（⑥ 流转与异常 → 查看轨迹 → 逐版展开），同样未动（blame 396-402 = bf2efbd）
frontend/shell/src/views/medtech/pathology/TrailPanel.vue:396-402
  function olderVersionNote(seq: unknown): string {
    const latestSeq = gross.value.fieldsRevisionSeq
    if (latestSeq == null || seq == null || !(Number(seq) < Number(latestSeq))) return ''
    return gross.value.textIsCumulative === true
      ? '较早字段版：当前文本是累积全文，本版字段写下的内容仍在正文里，未被取代'
      : `被取代版本：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅`
  }
渲染点 TrailPanel.vue:223
  <span v-if="olderVersionNote(row.seq)" class="muted" style="margin-left: 6px">{{ olderVersionNote(row.seq) }}</span>

【5】v65 确实没碰它
`git diff --stat v1.6.4..v1.6.5`：TrailPanel.vue 不在改动清单里；GrossingPanel.vue 的三个 hunk 是 `@@ -738,6 +738,42 @@`、`@@ -752,11 +788,13 @@`、`@@ -810,8 +848,8 @@`（reviseFormFields / reviseSubmitNote / reviseCoverNote / submitRevise），不含 970-984。
docs/屏上断言登记簿.tsv:35-37 只登了 GrossingPanel.vue:762 / :793 / :796（修订弹窗三句），没有 :979，也没有 TrailPanel.vue 任何一行。
docs/下一步开发规划.md:73 v65 车道 A 行：`| **A** | 2530 三条 | ① **v64 新造**：\`reviseCoverNote\` 把「已预填在下面的字段栏里」写死…；② 英文模板码上屏的**第四个**入口…；③ 部分退化告警说「字段级查询与统计取不到」…`——三条都不是这一处。`grep 被取代 docs/技术债交接单.md` 零命中。
docs/验收/v64复核-偏离表三条.md:141 已写明「这是 v63 复核已经点名、v64 没修也没登记的一条」。

【6】本轮新守卫结构上看不见它
tools/claim-ratchet.py:196-205
  def touched_claims(base, head='HEAD'):
      """<base>..HEAD 新增行里的带句读文案。diff 只看 + 行——改过的那一句才进棘轮。"""
      ...
      diff = subprocess.run(['git', '-C', str(ROOT), 'diff', f'{base}..{head}', '--', *paths],
文件头自述 limits (b)：`b. **文案一字未动、而它底下的代码被改判**……diff 驱动看不见这种。`——本句连代码都没改，更在其外。

【7】演示路径（当前主树，全新零种子库、单 ADMIN）
菜单：server/src/main/resources/db/migration/V144__pathology_base.sql:198 `(168, 7, '病理工作台', 'MENU', '/pathology/workbench', 'path:work', 'Files', 30),`；六工位 PathologyWorkbenchView.vue:14-31（① 登记 … ⑥ 流转与异常）。
② 取材默认范围 GrossingPanel.vue:449 `const query = reactive({ scope: 'pending', specimenType: '', urgentOnly: false, keyword: '' })`，pending 判据 PathologyProcessController.java:307-308 `sql.append(" and s.diagnosed_at is null ")` / `.append(" and not exists (select 1 from path_block b where b.specimen_id = s.id) ");`——首次取材后该标本从默认列表消失，须手动切「全部」才能再点「取材登记」「修订取材描述」「查看大体所见」（:76-79 三个按钮都在行上）。
修订弹窗上 v65 新生成的那句（GrossingPanel.vue:788-795 reviseCoverNote：`${inEarlier} 处的字段行留在更早的版本里（在「查看大体所见」里按版本可以调阅到），当前这一版没有`）恰把评委指到「查看大体所见」去按版本调阅——到了那里，第 1 版的标签就是【1】那句。

【反向核对】(a) 先确认没读错「别处已有兜底」：viewVersionTag 的 n===0 档（:977）要求该版零字段行，而第 1 版有 2 行（测试 :520-523 count=2）；`if (earlier)` 那档「较早字段版…未被取代」（:981-983）排在 superseded 之后，永远轮不到；查看弹窗里与「字段 vs 文本」有关的上屏文字只有 :123 的 tag 与 :139-140 的 alert，后者渲染的正是相反的话。第 1 版在下拉里可达：viewFieldVersions（:892-903）取自后端 fieldsByRevision（PathologyProcessController.java:1719-1763 全部版本归组，测试 :926 连 0 行的版都列），versionLabel 打「第 1 版 · 取材首写（2 项）」。
(b) 确认 cumulative=false 不是我的推论：测试 :539 断言 fieldsVersionCurrent=TRUE，而后端 :1780 `versionCurrent = sameSeq && !lastIsAppend` ⇒ lastIsAppend=false ⇒ :1824 textIsCumulative=false；同场景 :438 还直接断言了 FALSE。
(c) 确认 v65 车道 A 自报的三条确已修好、我没有重复起诉：① reviseSubmitNote（:768-778）由 reviseFormFields 生成、submitRevise（:852-853）用同一份；我试图打破它——k 只数 trim 后非空的值、与提交过滤同一表达式；后端 effectiveGrossFields 另 trim label，但 UI 路径下 label 只来自模板字段、库里字段行、addCustomField（maxlength 32），造不出前后端计数不一致；它只拼在 coverNote 非空时（fieldsAvailable=true 且 fieldsCoverText=false），所以 k=0 那句「结构化记录停在修订前那一版」总有那一版可指；「落库后…」是条件句，block 档不落库时不为假。没找到 UI 可达的反例。② 备注印模板中文名：PathologyProcessController.java:417-428 取 templateCatalog().get(code).get("name")，:583 `(templateName == null ? "" : "（模板 " + templateName + "）")`。③ 两条告警统一基准 priorSeq/priorLabels（:707-722）并 nameList 点名（:2497-2508），「取不到」半句已删。这三条是好的。
(d) 确认棘轮全绿不能替这句话背书：`--since v1.6.4` 只收 git diff 的 + 行（claim-ratchet.py:196-205），而 :972-976 最后一次改动是 bf2efbd（v1.6.3 已含），结构上不在扫描域；文件头 limits (b) 自己承认这一类。
(e) 我核过但不计入缺口的演示陷阱：tools/demo-pathology.py:170-173 与 :217-220 的取材仍只传 grossText、不传 gross/templateCode，脚本铺出的标本在查看弹窗一律是 GrossingPanel.vue:157「历史标本或纯自由文本，无字段级记录（不从文本反解析）」——评委须现场手填字段；首次取材后须切「全部」才能继续。两者都走得通，故不当硬缺口。取材完成弹窗 :386-388「N 项，按填写顺序」仍取读端点最新字段版而非本次（补取材只写描述时仍打上一版的 2 项）也未动，但标题字面没写「本次」，我不靠它。
(f) 反向找证据推翻自己：把「被取代」读成「字段行被取代」——第 1 版两行仍在 revision_seq=1（测试 :520-523），后端同屏说「字段级查询按它所属的那一版命中」；读成「文本内容被取代」——同屏 <pre>（:107）里正逐字躺着「标本大小：5×4×3cm；切面：灰白」（测试 :500-501 钉 textFieldForms=4）。两种读法都不成立。

**反驳者·数据真实性与口径**（推翻）：

走平台自己的默认流程（② 取材登记填「标本大小/切面」→ 补取材再填「补取块数/最大径」→「修订取材描述」照预填原样提交）之后，打开「查看大体所见」把版本下拉切到第 1 版，屏上那只标签打「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」；而库里第 3 版的字段行是「补取块数/最大径」，没有任何一行取代第 1 版的「标本大小/切面」——这两项的字段行只存在于第 1 版（仓库自己的测试钉着 count=2），其正文此刻逐字躺在同屏上方 gross_finding 的 <pre> 里，同屏正下方那条由同一份返回体生成的后端黄条说的是相反的话「另有 2 个的字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中」。判定依据是 textIsCumulative（最新修订行是不是 GROSSING 追加），它与标签断言的事实（第 3 版字段是否取代了第 1 版）根本不同源；取材修订落 source=GROSSING_EDIT 后 textIsCumulative 恒为 false，于是每个走过修订的标本、每个更早字段版都被打成「被取代」。⑥ 流转与异常 → 查看轨迹 → 大体所见修订表逐版展开处（TrailPanel.olderVersionNote）同一句假话。这是 v64 复核审计者主证据【1】-【4】点名的那处，v65 对 GrossingPanel.vue 只动了 :738 与 :810 两个 hunk、TrailPanel.vue 一字未动，登记簿没有这两行，交接单也没记——参数要求的「调阅」在这两个入口上仍由平台自己说成「已被取代、仅供调阅」，同一事实同屏两套口径。

【证据】【1】说假话的判定与渲染点（当前主树）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:972-981
```
  const cumulative = view.value.textIsCumulative === true
  const earlier = latestSeq != null && Number(v.revisionSeq) < Number(latestSeq)
  // 真被取代 = 存在一个更晚的、有字段行的版本（latestSeq 就是它；全无字段行时它为 null），
  // **且当前正文不是累积全文**——累积全文场景里这一版写下的内容仍原样留在当前 gross_finding 里，没被谁取代
  const superseded = earlier && !cumulative
  if (n === 0) return { type: 'info', text: `第 ${seq} 版：${viewNoFieldsTag.value}` }
  if (superseded) {
    return { type: 'info', text: `被取代版本（第 ${seq} 版，${n} 项）：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅` }
  }
```
渲染点 GrossingPanel.vue:123（无条件绑定）
```
          <el-tag size="small" style="margin-left: 6px" :type="viewVersionTag.type">{{ viewVersionTag.text }}</el-tag>
```
同屏正文 :107 `<pre v-if="view.grossFindingPresent">{{ view.grossFinding }}</pre>`；同屏黄条 :139-140
```
          <el-alert v-if="viewFieldsNote" type="warning" show-icon :closable="false" style="margin-top: 4px"
                    :title="viewFieldsNote" />
```
（:942 `const viewFieldsNote = computed(() => grossFieldsNote(view.value as Row))`，format.ts:242 `const fromServer = mdText(v.fieldsNote)`——即后端 fieldsNote，不看当前选的是哪一版）

【2】第二个入口同一句
frontend/shell/src/views/medtech/pathology/TrailPanel.vue:396-402
```
function olderVersionNote(seq: unknown): string {
  const latestSeq = gross.value.fieldsRevisionSeq
  if (latestSeq == null || seq == null || !(Number(seq) < Number(latestSeq))) return ''
  return gross.value.textIsCumulative === true
    ? '较早字段版：当前文本是累积全文，本版字段写下的内容仍在正文里，未被取代'
    : `被取代版本：已被第 ${fmt(latestSeq)} 版字段取代，仅供调阅`
}
```
渲染点 TrailPanel.vue:223 `<span v-if="olderVersionNote(row.seq)" class="muted" style="margin-left: 6px">{{ olderVersionNote(row.seq) }}</span>`

【3】取材修订后 textIsCumulative 恒 false（判定依据与断言事实不同源）
modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:1714-1716
```
            // v61（2530 复核）：最新一版是不是「补取材追加」——source=GROSSING 且 old_text 非空即是。
            // 追加时文本是**累积全文**（旧文本 + 「。补取材：」+ 本次），而字段行只落本次那几项。
            lastIsAppend = "GROSSING".equals(r.get("source")) && r.get("old_text") != null;
```
:1824 `body.put("textIsCumulative", lastIsAppend);`；修订端点落的来源 :771
```
        int revisionSeq = insertGrossRevision(jdbc, specimenId, existingRaw, assembled, "GROSSING_EDIT", uid, templateCode);
```

【4】同一份返回体里说相反话的那条黄条（后端）
PathologyProcessController.java:1840-1847
```
        String coverNote = null;
        if (!fields.isEmpty() && !fieldsCoverText) {
            var cn = new StringBuilder("当前大体所见文本里有 " + textFieldForms + " 个「标签：值」形态，其中 "
                    + textFieldFormsBacked + " 个在最新字段版（第 " + fieldsRevisionSeq + " 版）里有对应的结构化字段行");
            if (formsInEarlier > 0) {
                cn.append("；另有 ").append(formsInEarlier)
                  .append(" 个的字段行落在更早的版本里，按版本调阅得到，字段级查询按它所属的那一版命中");
            }
```
:1854-1855 `body.put("fieldsNote", versionNote == null ? coverNote : coverNote == null ? versionNote : versionNote + "。" + coverNote);`（本库态 versionNote 为 null：:1779-1780 `sameSeq`=true、`versionCurrent = sameSeq && !lastIsAppend`=true → :2558 `if (noFields || current || …) return null;`）

【5】库态由仓库自己的测试钉死（复核者站的那一格，不是我推演）
server/src/test/java/cn/hip/server/V62GrossReviseGuardTest.java:482-483
```
    void unbackedFormsAreSplitByWhetherAnEarlierVersionStillHoldsTheirFieldRows() {
        // 复核者站的那一格：取材登记（标本大小 / 切面）→ 补取材（补取块数 / 最大径）→ 照预填原样修订
```
:508-517
```
        var unbacked = rows(v, "textFieldFormsUnbacked");
        assertEquals(List.of("标本大小", "切面"),
                unbacked.stream().map(u -> String.valueOf(u.get("label"))).toList(),
                "逐条给出差在哪几项——屏上要指名道姓，这是修订弹窗那条提示的数据源");
        for (var u : unbacked) {
            assertEquals(Boolean.TRUE, u.get("inEarlierVersion"), u.toString());
            assertEquals(1, ((Number) u.get("earlierRevisionSeq")).intValue(), "它们是第 1 版录入的：" + u);
        }
        // 库内对照组：这两条字段行真的还在（断言钉的是库内事实，不是「后端换了个说法」）
        assertEquals(2L, (long) jdbc.queryForObject("""
                select count(*) from path_gross_field
                where specimen_id = ? and revision_seq = 1 and label in ('标本大小', '切面')
                """, Long.class, specimenId));
```
:531-533
```
        assertEquals(v.get("fieldsRevisionSeq"), v.get("textRevisionSeq"),
                "字段版号与文本版号是同一个数——把它说成「文本已是第 N 版」就是同一个版号说两件事");
        assertEquals(Boolean.TRUE, v.get("fieldsVersionCurrent"), "版号这一维为真");
```
代入【1】模板：seq=1、n=2、latestSeq=3、cumulative=false → 「被取代版本（第 1 版，2 项）：已被第 3 版字段取代，仅供调阅」。

【6】守卫把这条假判定锁成契约
server/src/test/java/cn/hip/server/V63FrontendLabelGuardTest.java:468-470
```
        String gate = lineContaining(panel, "const superseded = ");
        assertTrue(gate.contains("!cumulative"),
                "superseded 必须排除累积全文场景（更早各版的内容仍在正文里，没被谁取代）：" + gate);
```

【7】v65 没碰它、也没登记
`git log --oneline -S"const superseded = earlier && !cumulative" -- frontend/shell/src/views/medtech/pathology/GrossingPanel.vue` → 仅 `bf2efbd v63 车道B`；`git show b21e6c5 -- GrossingPanel.vue` 只有 `@@ -738,6 +738,42 @@` 与 `@@ -810,8 +848,8 @@` 两个 hunk；`git diff --stat v1.6.4..v1.6.5 -- TrailPanel.vue` 为空。docs/屏上断言登记簿.tsv 对 GrossingPanel.vue 只有 :762 / :793 / :796 三行，无 :979、无 TrailPanel.vue:401（棘轮 tools/claim-ratchet.py:33 自述「只问这句是不是本轮动过的」，未动的句子不进扫描域）。docs/技术债交接单.md 中 grep「被取代|superseded|olderVersionNote」零命中。

【反向核对】怕自己读错，做了这些反向核对：
(1) 确认没有别的分支或兜底盖住这句：GrossingPanel.vue:123 的 el-tag 在 `<template v-if="viewFieldVersions.length">` 内无条件绑定；:977 的 `n === 0` 档抓不到第 1 版（该版 2 行，viewFields 走 :915-925 的 fieldsByRevision 分支取到 2 行）；:139 的黄条 v-if 只看 viewFieldsNote 有没有值、与所选版本无关，所以标签与黄条**必然同屏**。
(2) 确认「被取代」不是读法之争：即使把它读成「第 1 版不再是最新字段版」，句尾「仅供调阅」仍与同屏黄条「字段级查询按它所属的那一版命中」互斥；而库内事实由 V62GrossReviseGuardTest:519-523 的 count=2 与 :514 的 earlierRevisionSeq=1 钉死，第 3 版字段行的 label 是「补取块数/最大径」（:490-492），没有一行同名取代第 1 版。
(3) 确认可达且是默认路径：修订弹窗预填 splitGross（GrossingPanel.vue:662-678）对「A；B。首次。补取材：C；D。补取材 x」落 CUMULATIVE 档、照原样提交即得此库态，测试 :495-497 走的正是这条 `prefill(...)` → `reviseGrossFields`。
(4) 反过来找 v65 新造的「生成器说假话」（第五种形态），没找到硬的：reviseSubmitNote 的 k（:749-753 只数 trim 后非空值的键）与后端落库行数同源——assembleGross :2338-2372 对空名/重名/超长一律抛 IllegalArgumentException 转 5222、不静默丢，addCustomField :501-510 先拦空名/重名/>20，所以 k 要么等于落库行数、要么整次不落库；losesSome 告警的 nameList(dropped) 不会为空（:669 的 assembleGross 已拒重名，:715 之后 plannedFieldCount<priorLatest 必有 label 差集）；templateName 不会是 "null"（templateCatalog :2318 / :2327 两支都填 name）。这些我判为好的。
(5) 对拍用例的成色：登记簿 f1879db993e6 那行的 ②g（V62GrossReviseGuardTest:696-750）库态是真的，但被求值的句子是 Java 同构版 `submitNote()`（:974-982）与 `prefill()`（:948-970），Vue 里那句只靠 ②f :679-683 的源码 contains 钉字面——「真库态 + 源码扫描」的混合，不是对 Vue computed 本身求值；这不构成本条缺口，但登记簿「在真实库态下逐档求值、不是源码扫描」的自述只对了一半。
(6) 两处次级未清（不作主证据，供签认人取舍）：「取材完成」弹窗 :385-388 的「N 项，按填写顺序」仍取 :1041-1049 回读的「最新有字段的那一版」，本次落 0 行时打的是上一版的行（后端 :602 明明回了 grossFieldCount，弹窗不用）；读侧 coverNote :1846/:1850 仍承诺「字段级查询」，而 `grep path_gross_field modules/ server/src/main` 的读者只有 :709/:711/:1665/:1725 四处按 specimen/version 取行的 SQL，全仓没有任何按 label/value 检索或统计的端点。

---

### 2563

**参数原文（CSV 逐字）**：支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。 +--------------------------------------------------------------------------------------------------------------------------------------------

**CSV 响应结论列**：部分响应

**CSV 说明列**：平台当前提供病理标本基础信息列表（标本编号、检查项目、患者、标本状态）；本条所述特检医嘱的集中化管理与状态追踪，含技术分类、具体检测项目、关联蜡块编号及执行进度的完整明细功能尚未提供，列入后续开发计划。

**审计者**：维持。

子要求核对：

- [✓] ①「集中化管理」：存在不分标本的全院特检医嘱清单，菜单可达、可筛选、可在清单上直接完成/取消，单 ADMIN 账号走得通 —— frontend/shell/src/views/medtech/pathology/PathologyWorkbenchView.vue:26 `<el-tab-pane name="tech" label="⑤ 特检工作台" lazy>`；TechOrderPanel.vue:272-275 `const d = (await client.get('/pathology/report/tech-orders', {` / `params: {` / `// 刻意不传 specimenId：这是全院清单分支` / `status: query.status,`；modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:1227-1228 `} else if (specimenId == null) {` / `st = "ORDERED";   // 全院清单默认只看待执行`；server/src/main/resources/db/migration/V144__pathology_base.sql:198 `(168, 7, '病理工作台', 'MENU', '/pathology/workbench', 'path:work', 'Files', 30),`；V156__v52_menus.sql:24-25 `insert into sys_role_menu (role_id, menu_id)` / `select r.id, 168 from sys_role r where r.code in ('ADMIN', 'TECHNICIAN', 'DOCTOR_OUTP')`；行内动作 TechOrderPanel.vue:142-145 `<el-button link type="primary" size="small" :disabled="row.status !== 'ORDERED'"` / `@click="done(row)">完成</el-button>` / `<el-button link type="danger" size="small" :disabled="row.status !== 'ORDERED'"` / `@click="cancel(row)">取消</el-button>`
- [✓] ②「状态追踪」：ORDERED/DONE/CANCELLED 状态机，完成/取消同事务写流转节点，屏上成功提示与库内备注是同一个字符串（含「已出块 N」执行证据） —— PathologyReportController.java:1092-1095 `String doneRemark = "确认完成特检医嘱 " + label + "，已出块 " + sampledBlockCount` / `+ " / 挂接 " + slideCount + " 片 / 已染色 " + stainedCount` / `+ (gap == null ? "" : "；" + gap + "（「" + gateTierName(gate) + "」档放行）");` / `logProcess(asLong(f.get("specimen_id")), "TECH_DONE", uid, doneRemark);`；:1112 `body.put("doneRemark", doneRemark);`；TechOrderPanel.vue:300 `ElMessage.success(String(d.doneRemark ?? ''))`；DiagnosisPanel.vue:967 `const d = (await client.put(\`/pathology/report/tech-orders/${Number(row.id)}/done\`, null)).data.data as Row`；TrailPanel.vue:189 `<div v-if="n.remark" class="muted">{{ n.remark }}</div>`
- [✓] ③「可实时调阅」：每次请求现查库、现算，不是快照 —— PathologyReportController.java:1269-1270 `round((extract(epoch from (now() - t.ordered_at)) / 3600)::numeric, 1)` / `as hours_since_ordered`；:1324-1327 `for (var r : rows) {` / `r.put("tech_type_name", TECH_TYPE_NAMES.getOrDefault(String.valueOf(r.get("tech_type")), null));` / `applyTechProgress(r);   // v60：读 sampled_block_count 派生第六态 SAMPLED`；TechOrderPanel.vue:303 `await load()`（完成后立即重查）
- [✓] ④ 明细涵盖「技术分类」，且清单/诊断工位中文同源 —— TechOrderPanel.vue:63 `{{ techName(row.tech_type) }}`，字典 :212-213 `const d = (await client.get('/pathology/report/tech-orders/dict')).data.data as Row` / `techTypes.value = (d.techTypes ?? []) as Row[]`；DiagnosisPanel.vue:264 `<template #default="{ row }">{{ techName(row.tech_type) }}</template>`，:916-917 `const d = (await client.get('/pathology/report/tech-orders/dict')).data.data as Row` / `techTypes.value = (d.techTypes ?? []) as Row[]`
- [✓] ⑤ 明细涵盖「具体检测项目」 —— TechOrderPanel.vue:64 `<span v-if="row.tech_item" class="muted">　{{ row.tech_item }}</span>`；DiagnosisPanel.vue:267 `<template #default="{ row }">{{ fmt(row.tech_item) }}</template>`；PathologyReportController.java:1252 `t.tech_type, t.tech_item, t.reason, t.status,`
- [✓] ⑥-a 明细涵盖「关联蜡块编号」：⑤ 特检工作台、④ 诊断页、③ 制片挂接下拉三个消费方对同一条医嘱取同一列同一顺序（v64 打回的 coalesce 短路第三消费方本轮确已修，并配了真库态对拍用例） —— ProcessingPanel.vue:500 `  const blockCode = t.blocks_derived ?? t.block_code`（v1.6.4 是 `t.block_code ?? t.blocks_derived`，git diff v1.6.4..v1.6.5 可见）；TechOrderPanel.vue:103 `<span class="code">{{ fmt(row.blocks_derived ?? row.block_code) }}</span>`；DiagnosisPanel.vue:274 `{{ fmt(row.blocks_derived ?? row.block_code) }}`；并集 SQL PathologyReportController.java:329-336 `(select string_agg(x.block_code, '、' order by x.block_no, x.block_code)` … `from path_block pb where pb.tech_order_id = t.id` … `where sl.tech_order_id = t.id) x)            as blocks_derived,`；对拍用例 server/src/test/java/cn/hip/server/V62TechRemarkTest.java（v65 新增 `threeScreensReadTheSameBlockColumnForOneTechOrder`）：`assertEquals(Boolean.TRUE, gr.get("techOrderBlockBackfilled"),` / `assertEquals(3, union.split("、").length, "「蜡块」列应把三块逐块列全：" + union);` / `assertNotEquals(backfilled, union,` / `assertEquals(1, new HashSet<>(exprs.values()).size(),`——一头真造库态、一头剥注释扫源码，活对照组 `V64_PROC_BLOCK_EXPR = "  const blockCode = t.block_code ?? t.blocks_derived"`
- [✗] ⑥-b 「关联蜡块编号」的第四个消费方——② 取材工位「挂接补取材医嘱」下拉——对同一条医嘱仍只印回写进 block_id 的那一块、无并集回落：v65 规划节自己把它列进车道 B 任务书①，但 GrossingPanel.vue 被划给车道 A、车道 B 提交未碰它，本轮 GrossingPanel.vue 的全部改动只在 2530 的修订弹窗；CHANGELOG/交接单/登记簿三处均未记 —— GrossingPanel.vue:575-580 `function resampleLabel(o: Row): string {` / `  const item = o.tech_item ? \` ${String(o.tech_item)}\` : ''` / `  const block = o.block_code ? String(o.block_code) : '未指定'` / `  const reason = o.reason ? \`　原因：${String(o.reason)}\` : ''` / `  return \`#${fmt(o.id)}　补取材${item}　下达 ${fmt(o.ordered_by_name)} ${fmtTime(o.ordered_at)}　蜡块 ${block}${reason}\`` / `}`；它列的是 :563-568 `params: { specimenId, status: 'ORDERED', techType: 'RESAMPLE' },` / `.filter((o) => o.tech_type === 'RESAMPLE' && o.status === 'ORDERED')`，即第一次补取材后仍待执行、可二次挂接的同一条医嘱；回写侧 PathologyProcessController.java:572-576 `boolean orderBlockBackfilled = false;` / `if (techOrder != null && techOrder.get("block_id") == null) {` / `orderBlockBackfilled = jdbc.update(` / `"update path_tech_order set block_id = ? where id = ? and block_id is null and status = 'ORDERED'",`——于是 ② 的下拉印「蜡块 P-3」而同一条医嘱在 ⑤/④/③ 印「P-3、P-4」（V62TechRemarkTest 新用例已在真库上坐实 block_code 是一块、并集是多块）；GrossingPanel.vue 全文 grep `blocks_derived` 零命中。规划节 docs/下一步开发规划.md:74 `…（v60 在 SQL、v61 在两个面板都修过，这处一轮没跟）；\`GrossingPanel\` 的补取材下拉更彻底，只读 \`block_code\` 连回落都没有；② 演示脚本…` 而同一行「文件独占」列为 `PathologyReportController`、`ProcessingPanel.vue`、`TechOrderPanel.vue`、`DiagnosisPanel.vue`、`tools/demo-pathology.py`（不含 GrossingPanel.vue，它在 :73 车道 A 的独占列）；`git show --stat cf4dd50`（车道 B）只改 登记簿.tsv / ProcessingPanel.vue / V62TechRemarkTest.java / demo-pathology.py；`git diff v1.6.4..v1.6.5 -- GrossingPanel.vue` 三段 hunk 全在 :741-776（reviseFormFields/reviseSubmitNote）、:788-797（reviseCoverNote）、:851-852（submitRevise）；CHANGELOG.md:36 `- **2563**：\`coalesce\` 短路的第三个消费方（\`ProcessingPanel\` 的 \`block_code ?? blocks_derived\`）改与⑤④同源；`——对 GrossingPanel 一字未提
- [✓] ⑦-a 「当前执行进度」六态由挂接切片/补取材已出块事实派生，两屏页首宣告的档位由后端 progressStates 下发、与派生同源 —— PathologyReportController.java:1383-1391 `public static String techProgress(Object status, Object slideCount, Object stainedCount, Object sampledBlockCount) {` … `if (slides == 0) return sampled > 0 ? "SAMPLED" : "PENDING_SECTION";` / `return stained < slides ? "SECTIONING" : "STAINED";`；:243-250 `public static List<Map<String, Object>> techProgressStates() {` … `"inProgress", TECH_DONE_VERDICTS.containsKey(c)))`；TechOrderPanel.vue:230-232 `? \`「进度」由挂接切片与补取材已出块派生，不是手工标记，未完成的分 ${inProgressNames.value.length} 档：\`` / `+ \`${inProgressNames.value.join(' / ')}。\``；DiagnosisPanel.vue:891-893 同形并加 `「已出块」一列就是其中「已补取材待切片」所依据的那个数。`；页首完成规则 :1435-1450 `techDoneGateRule()` 由 TECH_DONE_VERDICTS 生成，TechOrderPanel.vue:9 `<el-alert v-if="gateRule" … :title="gateRule" />`、:288 `gateRule.value = String(d.techDoneGateRule ?? '')`
- [✗] ⑦-b 屏上对「当前执行进度」的每一处宣告都须与实现一致——② 取材工位「挂接补取材医嘱」下方那句无条件承诺「医嘱进度随之变为「已补取材待切片」」（v64 复核审计者镜头的主证据）在当前主树逐字未动，而只要该医嘱已有挂接切片（先挂片再补取材，或二次补取材），提交后 ⑤/④ 打出的是「切片中」。这条既不在 v65 任一车道任务书、也不在登记簿、也不在交接单 —— GrossingPanel.vue:273-277（活模板文本）`<span v-else-if="form.techOrderId" class="muted">` / `<!-- v64 合并后补齐：原写「已补取材、待切片」多一个顿号。全仓其余每一处（含唯一事实源` / `PathologyReportController.TECH_PROGRESS_NAMES）都是「已补取材待切片」，屏上口径要逐字同源；` / `这句恰是本轮新加的，守卫测试比的是键与入口、抓不到文案里多一个标点。 -->` / `提交后本次新蜡块记到该医嘱名下，医嘱进度随之变为「已补取材待切片」</span>`；渲染条件 :265 `<el-form-item v-if="mode === 'GROSSING' && form.append" label="挂接补取材医嘱">`。实现是有条件的：PathologyReportController.java:1390-1391 `if (slides == 0) return sampled > 0 ? "SAMPLED" : "PENDING_SECTION";` / `return stained < slides ? "SECTIONING" : "STAINED";`；仓库自己的测试把反例钉死：server/src/test/java/cn/hip/server/V60ResampleProgressTest.java:259 `assertEquals("SECTIONING", PathologyReportController.techProgress("ORDERED", 1L, 0L, 1L));`（1 片 + 1 块 = 切片中，不是已补取材待切片）。反例路径从菜单走得通：③ 挂接下拉列出该 RESAMPLE 医嘱（ProcessingPanel.vue:479-480 `params: { specimenId: Number(row.specimen_id), status: 'ORDERED' },`），选中即锁 HE（:509-511 `const TECH_TO_STAIN: Record<string, string> = {` / `IHC: 'IHC', SPECIAL_STAIN: 'SPECIAL', MOLECULAR: 'MOLECULAR', DEEP_CUT: 'HE', RECUT: 'HE', RESAMPLE: 'HE',`），后端两道校验放行 PathologyProcessController.java:1204-1207 `Long orderBlock = asLongObj(techOrder.get("block_id"));` / `Long blockOwnerOrder = asLongObj(block.get("tech_order_id"));` / `if (orderBlock != null && !orderBlock.equals(req.blockId())` / `&& !req.techOrderId().equals(blockOwnerOrder)) {`（block_id 为空整条不成立；已回写时挂到为它补出的块同样放行）与 :1218-1222 `String expectStain = TECH_TO_STAIN.get(techType);` / `if (expectStain != null && !expectStain.equals(stainType)) {`（HE==HE）；而 ② 下拉仍列它（GrossingPanel.vue:563-568 只看 RESAMPLE+ORDERED，不看是否已挂片）。历史：`git log -S"随之变为" -- GrossingPanel.vue` 只命中 4f9f0f1（v64 车道 A），v65 对该文件的三段 hunk 全在 :741 以后；docs/屏上断言登记簿.tsv 的 GrossingPanel 三行是 :762 / :793 / :796，无 :277；docs/技术债交接单.md 全文无此项；docs/下一步开发规划.md:73-75 三条车道任务书无此项
- [✓] ⑧ 演示可达：零种子库跑平台自带演示脚本后，⑤ 默认「待执行」视图非空，四个未完成档各一实例，「已出块」与「派生」标有真实例，且脚本自己回读核对 —— tools/demo-pathology.py:337-338 `plan = ([LADDER[i % len(LADDER)] for i in range(ARGS.count)]` / `+ [('REJECTED', '已拒收'), ('TECH_BACKLOG', '特检在办（四档未完成）')])`；:245-279 `def tech_backlog(sid, block_ids, idx):` … `backlog['PENDING_SECTION'] = …` / `rs = do('下补取材医嘱（不指定蜡块）', …` / `blocks += len(resample_append(sid, rs, ['切缘一（补取材）', '切缘二（补取材）'], idx))` / `backlog['SAMPLED'] = rs` / `slides += attach_slides(block_ids[-1], sect, 'IHC', 'CK20', 2, 1, '切片中')` / `slides += attach_slides(block_ids[-1], stained, 'SPECIAL', 'PAS', 1, 1, '已染色待确认')`；回读核对 :480-487 `states = do('进度字典（⑤ 页首照它列档）', 'GET', '/pathology/report/tech-orders/dict').get('progressStates') or []` … `body = do('⑤ 默认视图（全院待执行）', 'GET', '/pathology/report/tech-orders')` / `assert body.get('status') == 'ORDERED', …` / `rows = body.get('items') or []` / `assert rows, '⑤ 特检工作台默认第一屏是空表——这正是复核打回的那一条（评委落地看不到任何待执行医嘱）'`；:496-497 `assert row.get('progress') == st['code'], \` / `f"医嘱 #{tid} 的进度应是「{st['name']}」，实得「{row.get('progress_name')}」"`；:502-505 `assert sampled, …` / `assert derived, …` / `assert r.get('blocks_derived'), f'标了「派生」就必须列得出块号：{r}'`；E2E 钉住 tools/e2e-v58-audit.py:378-382 `assert m and int(m.group(1)) == 5, (` … `assert 'tech_backlog=4' in summary, …`。派生标为 DERIVED 而非 MIXED 由 SQL 保证：PathologyReportController.java:343 `from (select b.block_code, (b.tech_order_id is distinct from t.id) as ordered_src`（回写进 block_id 的首块 tech_order_id = t.id，算派生）

**审计者主证据**：【缺口一句话】② 取材工位勾「补取材」并选中一条补取材医嘱时，屏上同一块区域说了两句与库内事实对不上的话：下拉行把这条医嘱的「关联蜡块」只印成回写进 block_id 的那一块（「蜡块 P-3」），而 ⑤/④/③ 对同一条医嘱印的是并集「P-3、P-4」；下拉底下那行灰字无条件承诺「提交后…医嘱进度随之变为「已补取材待切片」」，而只要这条医嘱已挂过一张切片（先挂片再补取材，或二次补取材），提交后 ⑤/④ 打出的「当前执行进度」是「切片中」。这两处正是 v64 复核 2563 三个镜头里审计者镜头的主证据（⑦-d）与数据镜头的后半句（GrossingPanel:577），v65 一处没动。

【1】屏上那两句（当前主树，活模板文本与活函数）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:265-277
```
      <el-form-item v-if="mode === 'GROSSING' && form.append" label="挂接补取材医嘱">
        <div style="width: 100%">
          <el-select v-model="form.techOrderId" clearable placeholder="不挂接（自由补取材，医嘱进度不随本次取材推进）"
                     style="width: 100%" :loading="resampleLoading">
            <el-option v-for="o in resampleOrders" :key="Number(o.id)" :value="Number(o.id)" :label="resampleLabel(o)" />
          </el-select>
          ...
          <span v-else-if="form.techOrderId" class="muted">
            <!-- v64 合并后补齐：原写「已补取材、待切片」多一个顿号。... -->
            提交后本次新蜡块记到该医嘱名下，医嘱进度随之变为「已补取材待切片」</span>
```
GrossingPanel.vue:575-580
```
function resampleLabel(o: Row): string {
  const item = o.tech_item ? ` ${String(o.tech_item)}` : ''
  const block = o.block_code ? String(o.block_code) : '未指定'
  const reason = o.reason ? `　原因：${String(o.reason)}` : ''
  return `#${fmt(o.id)}　补取材${item}　下达 ${fmt(o.ordered_by_name)} ${fmtTime(o.ordered_at)}　蜡块 ${block}${reason}`
}
```
下拉数据源 :563-568 只按 RESAMPLE + ORDERED 过滤，不看是否已补过块、是否已挂片：
```
      params: { specimenId, status: 'ORDERED', techType: 'RESAMPLE' },
    })).data.data as Row
    resampleOrders.value = ((d.items ?? []) as Row[])
      .filter((o) => o.tech_type === 'RESAMPLE' && o.status === 'ORDERED')
```
GrossingPanel.vue 全文 `grep blocks_derived` 零命中——没有任何兜底把并集补回来。

【2】库内事实：block_code 是系统回写的一块，并集是多块；进度在有片时永远不是 SAMPLED
modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:572-576
```
        boolean orderBlockBackfilled = false;
        if (techOrder != null && techOrder.get("block_id") == null) {
            orderBlockBackfilled = jdbc.update(
                    "update path_tech_order set block_id = ? where id = ? and block_id is null and status = 'ORDERED'",
                    asLongObj(created.get(0).get("id")), req.techOrderId()) == 1;
```
modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:1383-1391
```
    public static String techProgress(Object status, Object slideCount, Object stainedCount, Object sampledBlockCount) {
        String st = String.valueOf(status);
        if (!"ORDERED".equals(st)) return st;
        ...
        if (slides == 0) return sampled > 0 ? "SAMPLED" : "PENDING_SECTION";
        return stained < slides ? "SECTIONING" : "STAINED";
    }
```
仓库自己的测试把反例钉死：server/src/test/java/cn/hip/server/V60ResampleProgressTest.java:259
```
        assertEquals("SECTIONING", PathologyReportController.techProgress("ORDERED", 1L, 0L, 1L));
```
v65 新增的对拍用例又在真库上坐实了「block_code 是一块、并集是多块」（V62TechRemarkTest#threeScreensReadTheSameBlockColumnForOneTechOrder）：
```
        assertNotNull(row.get("block_code"), "回写确实发生了，block_code 非空（?? 在这里短路）");
        String backfilled = String.valueOf(row.get("block_code"));
        assertFalse(backfilled.contains("、"), "回写进 block_id 的只有本次首块这一块：" + backfilled);
        assertNotEquals(backfilled, union,
```
——同一批数据喂给 GrossingPanel.vue:577，下拉印的就是 `backfilled`，不是 `union`。

【3】反例路径从菜单走得通、不用改配置、不用第二个账号
ProcessingPanel.vue:479-480 `params: { specimenId: Number(row.specimen_id), status: 'ORDERED' },`（③ 挂接下拉列出该 RESAMPLE 医嘱）；:509-511 `RESAMPLE: 'HE'`（选中即锁 HE）；PathologyProcessController.java:1204-1207
```
            Long orderBlock = asLongObj(techOrder.get("block_id"));
            Long blockOwnerOrder = asLongObj(block.get("tech_order_id"));
            if (orderBlock != null && !orderBlock.equals(req.blockId())
                    && !req.techOrderId().equals(blockOwnerOrder)) {
```
（block_id 为空整条不成立；已回写时挂到为它补出的块同样放行）；:1218-1222 `if (expectStain != null && !expectStain.equals(stainType)) {`（HE==HE 通过）。ProcessingPanel.vue:81 `<el-button link type="primary" size="small" @click="openSlide(row)">切片</el-button>` 无任何 disabled 条件。第二次补取材同样被后端接受（5275 四条只查 存在/RESAMPLE/同标本/ORDERED，:464-486）。

【4】v65 对这两处的处置：一条在规划节里点了名但没做，一条压根没进规划
docs/下一步开发规划.md:74（车道 B 任务书）
```
| **B** | 2563 三条 | ① `coalesce` 短路的**第三个**消费方：`ProcessingPanel.vue` 的 `t.block_code ?? t.blocks_derived`——…；`GrossingPanel` 的补取材下拉更彻底，只读 `block_code` 连回落都没有；② 演示脚本… | `PathologyReportController`、`ProcessingPanel.vue`、`TechOrderPanel.vue`、`DiagnosisPanel.vue`、`tools/demo-pathology.py` |
```
——任务书写了 GrossingPanel，独占文件列没有它（它在 :73 车道 A 的独占列）。`git show --stat cf4dd50` 车道 B 只改 4 个文件（登记簿.tsv / ProcessingPanel.vue / V62TechRemarkTest.java / demo-pathology.py）；`git diff v1.6.4..v1.6.5 -- GrossingPanel.vue` 三段 hunk 全在 :741-776 / :788-797 / :851-852（2530 的修订弹窗）。CHANGELOG.md:36 `- **2563**：`coalesce` 短路的第三个消费方（`ProcessingPanel` 的 `block_code ?? blocks_derived`）改与⑤④同源；` 对 GrossingPanel 一字未提。
v64 复核审计者镜头的主证据（GrossingPanel.vue:273-277 那句承诺）在 :73-75 三条车道任务书里都不存在；docs/屏上断言登记簿.tsv 的 GrossingPanel 三行是 :762 / :793 / :796；docs/技术债交接单.md 全文没有这两条；docs/下一步开发规划.md:24 的「结转 v66」三项也没有。

【5】守卫为什么抓不到：这两句都在 .vue 普通文本节点/模板串里，棘轮按设计只抽 + 行里的引号字面量
tools/claim-ratchet.py:193 `LIT = re.compile('|'.join('%s((?:[^%s\\\\]|\\\\.)*)%s' % (q, q, q) for q in _Q))`；:219-220 `for ln in lines for m in LIT.finditer(strip_comments(ln)))` / `if SENTENCE.search(joined) and len(joined) > 12:`——GrossingPanel.vue:277 是裸文本节点，且本轮不在 diff 的 + 行里（形态 b + c 同时命中）。登记簿自己承认这一点：docs/屏上断言登记簿.tsv:55-57 `# 注：这两句在 .vue 的**普通文本节点**里（<span class="muted"> 的正文），不是带引号的字面量，` / `#     claim-ratchet.py --since 的 diff 抽取只收字面量，抓不到它们（见簿首 limits (c)）。` / `#     本轮是**照规矩手工补登**…`。V62TechRemarkTest 的 HARDCODED_PROGRESS_LIST 要两个态名以「/」相连才命中（v64 复核已指出），:277 只有一个态名，照旧放行。

【6】本轮确实修好、我背书的（不凑数）：③ 制片挂接下拉的蜡块段 ProcessingPanel.vue:500 `const blockCode = t.blocks_derived ?? t.block_code` 与 ⑤/④ 逐字同一表达式，对拍用例真造库态并扫源码、活对照组是 v64 原文；演示脚本 tools/demo-pathology.py:337-338 固定加「特检在办」标本、:245-279 留四条待执行、:468-512 回读 ⑤ 默认视图逐档核对且档位清单读后端 progressStates；tools/e2e-v58-audit.py:378-382 把 specimens=5 与 tech_backlog=4 钉在端到端层。参数点名的四项明细四列都在 ⑤（TechOrderPanel.vue:61-73、:97-107）。我的指控只落在「② 取材工位对『关联蜡块编号』与『当前执行进度』两处说的话与库内事实不符」。

**演示路径（审计者）**：前提：全新零种子库（Flyway 到 V168，无演示数据）、服务在 8080、单个 ADMIN 账号、全程只点菜单。零种子主数据里已有病理收费项目：server/src/main/resources/db/migration/V21__phase24_specialty.sql:66 `('C0301', '组织病理学检查(活检)', 'EXAM', '次', 260.00, null);`。

第 0 步（造一条病理申请，绕不开）：患者登记菜单建患者（V2__empi_patient.sql:32 授 ADMIN 菜单 5、6）→「门诊业务 → 医生排班」（V3__outpatient_registration.sql:41 菜单 9）建今日排班 →「门诊挂号」（:40 菜单 8）给该患者挂号 →「门诊医生站」（V5__doctor_charge_dispense.sql:64 菜单 10；DoctorStationController.java:24 `@PreAuthorize("hasAnyRole('ADMIN','DOCTOR_OUTP')")`）接诊，开检查医嘱选「组织病理学检查(活检)」→「门诊收费」（V5:65 菜单 11）结算。不结算 ① 的「待登记申请」不出现。

第 1 步：「病理工作台」（V144:198 菜单 168；V156:24-25 授 ADMIN）→ ① 登记 → 待登记申请点「登记」填标本描述 → 标本检索点「接收核对」。

第 2 步：② 取材 → 该标本「取材登记」→ 填一两个字段 + 自由描述，加 2 个蜡块（记 P-1、P-2）→ 提交。

第 3 步：④ 诊断 / 报告追溯 → 打开该标本抽屉 →「特检技术医嘱」页签 →「下达特检技术医嘱」→ 类型「补取材」，蜡块留空 → 下达。记下「医嘱号」列的 #N。

第 4 步（正常的第一次补取材，此时屏上都是对的，先让评委看到好的一面）：② 取材 → 同一标本「取材登记」→ 勾「补取材」→「挂接补取材医嘱」下拉出现「#N　补取材　下达 admin …　蜡块 未指定」→ 选中，灰字「提交后本次新蜡块记到该医嘱名下，医嘱进度随之变为「已补取材待切片」」→ 加 2 块（P-3、P-4）→ 提交。切到 ⑤ 特检工作台（默认「待执行」）：该行进度「已补取材待切片　已染 0 / 挂接 0　已出块 2」，蜡块列「P-3、P-4」带「派生」标（TechOrderPanel.vue:69-72、:103-104）。这一步屏上说的都成立。

第 5 步（把医嘱推进到「切片中」，全在菜单里）：③ 制片 → 蜡块 P-3 行点「切片」（ProcessingPanel.vue:81，无需先包埋）→「挂接特检医嘱」下拉选「#N 补取材（已出块 2）（P-3、P-4）」（本轮修好的那一处，括注确是两块）→ 染色类型自动锁 HE → 数量 1 → 保存，**不要染色**。⑤ 该行现在是「切片中　已染 0 / 挂接 1　已出块 2」。

第 6 步（当场证伪，两句同屏）：回 ② 取材 → 同一标本「取材登记」→ 勾「补取材」→「挂接补取材医嘱」下拉：该行文案是「#N　补取材　下达 admin …　蜡块 P-3」（GrossingPanel.vue:577-579）——同一条医嘱 ⑤ 刚显示的是「P-3、P-4」；选中 #N → 灰字仍说「提交后本次新蜡块记到该医嘱名下，医嘱进度随之变为「已补取材待切片」」（:277）→ 加 1 块（P-5）→ 提交 → 切到 ⑤：该行进度「**切片中**　已染 0 / 挂接 1　已出块 3」，蜡块列「P-3、P-4、P-5」。屏上刚承诺的那个态没有出现；④ 诊断抽屉同一行同样是「切片中」。（v64 复核审计者给的「先挂片再补取材」顺序在当前主树同样走得通：把第 4 步跳过、先做第 5 步再做第 4 步，结果一样。）

第 7 步（干净对照 + 本轮修好的部分演给评委）：另下一条补取材 #M，只做第 4 步 → ⑤ 该行「已补取材待切片」；页首第二条黄条（TechOrderPanel.vue:9，后端 techDoneGateRule）写着「…判无缺口、三档都放行的进度：「已补取材待切片」（补取材已出块，蜡块就是这类医嘱的交付物）…」→ 点「完成」：零告警，成功提示逐字「确认完成特检医嘱 #M 补取材，已出块 2 / 挂接 0 片 / 已染色 0」（:300 原样印 doneRemark）→ ⑥ 流转与异常「查看轨迹」TECH_DONE 备注逐字相同（TrailPanel.vue:189）。

演示陷阱：
1. **本轮起可以先跑 `tools/demo-pathology.py --count 5`（docs/培训脚本.md:83 指定的零种子路径）**：它现在固定多造一个「特检在办」标本，⑤ 默认视图落地就有四行（待切片 / 已补取材待切片 / 切片中 / 已染色待确认各一，tools/demo-pathology.py:245-279）。它造出的那条补取材医嘱（已出块 2、P-3、P-4 派生）正好是第 5、6 步的起点，可省掉第 0-4 步。但脚本跑完 ⑤ 里那四条要留着别点「完成」，一点就把那一档演示没了（脚本自己的 verify_tech_backlog 也是这么设计的，:468-512）。
2. **脚本不会演示缺口本身**：它只做「先补取材、再挂片」一次（tech_backlog 里 RESAMPLE 医嘱补出 2 块后不再挂片），第 5、6 步必须手工做。
3. **第 6 步做过之后 #N 就脏了**：它永远带「挂接 1 片」，点「完成」按「切片中」判有缺口，出厂「提示」档下会多弹一条告警（PathologyReportController.java:1075-1078）；干净对照一定用另一条 #M。
4. 第 5 步保存切片时不要顺手「染色登记」：染完会弹「医嘱 #N 补取材 进度：已染色待确认（已染色 1 / 挂接 1 片）」（ProcessingPanel.vue:646-651），该提示不报「已出块」，会把第 6 步的对照搅乱。
5. **同一实例反复跑脚本会把 ⑤ 默认视图撑爆**：每跑一次留下 4 条永不完成的待执行医嘱，⑤ 与脚本自检都只取前 100 行（PathologyReportController.java:387 `DEFAULT_LIMIT = 100`，排序 :1319 `order by t.status = 'ORDERED' desc, s.urgent desc, t.ordered_at asc` 新的排最后），约第 25 次起 verify_tech_backlog 会以「不在 ⑤ 默认视图里」非 0 退出，屏上也要「收窄条件」才看得到新造的四条。脚本 docstring 说「可在同一实例上反复跑」（:49）没提这一点。
6. 「拦截」档演不出来：`emr.gate.pathology.techdone` 无前端编辑界面，只能 PUT /api/config 改；而拦截消息仍带配置键与英文档位名（PathologyReportController.java:1070-1072 `+ "（gate " + TECH_DONE_GATE_KEY + "=block）"`），评委若要求按页首规则演「拦截」，菜单上做不到。
7. 第 0/1 步评委必经的 ① 登记「手工登记」提示仍逐字打出库列名与约束名（RegistryPanel.vue:185-186 `path_specimen 无 patient_id 列、chk_path_specimen_source 要求门诊/住院恰有其一`），交接单 :155-156 已记，不属本条但就在评委眼前。

**审计者自列的保留项**：我没核到的：
1. **一行代码都没跑。** 全程只读当前主树 bbf8012（tag v1.6.5）的源码、迁移、测试与脚本文本，没编译、没起服务、没连库、没开浏览器。上面所有「屏上会显示什么」都是顺着 .vue 模板与后端字符串推出来的；CHANGELOG 自述的 1076 测试 / 46 次 E2E 全绿、以及「浏览器实测坐实」我一条未复现。
2. V62TechRemarkTest 新增用例（threeScreensReadTheSameBlockColumnForOneTechOrder）、demo-pathology.py 的 verify_tech_backlog 我只读了文本，判断它们「不空洞、活对照组是历史形态、抽取器找不到锚点会红」，没跑。V62TechRemarkTest 其余 ~900 行、V63FrontendLabelGuardTest、V58TechProgressTest 只按关键词定位，不排除漏了某条与 GrossingPanel:277 相关的断言——但 HARDCODED_PROGRESS_LIST 按设计只抓「态名/态名」双态句，这点有把握。
3. demo-pathology.py 的 TECH_BACKLOG 标本在 build() 里先跑 technical()（脱水/包埋/2 张 HE 染色）再 tech_backlog()，其中 attach_slides 挂在 block_ids[-1]（已包埋的 P-2）上；我推断 ③ 对未包埋的 P-3 也能开切片（ProcessingPanel.vue:81 无 disabled、PathologyProcessController.slides 无 embedded_at 校验），未实测。

存疑 / 刻意没计入缺口的：
4. **本结论的两条硬伤都不是 v65 新造的**：:277 那句是 v60 写、v64 改硬（`git log -S`），:577 是 v60 原样。我按「屏上说假话 → 维持」口径计入，依据是它们正是 v64 复核 2563 三镜头里审计者主证据与数据镜头后半句，即本轮「冲着九条打回去修」的九条之内：一条在规划节 :74 点了名但因文件划给车道 A 而无人做（v61「只做一半」形态回潮），一条压根没进三条车道任务书（打回项在立项时被丢）。若复核口径只认 CHANGELOG 自述的「2563 两点」，本条会被读成「可上调」，我不认那种读法，但把分歧点写在这里。
5. 「二次补取材」「先挂片再补取材」是否算合理操作可争。我认为算：② 下拉主动把已补过块/已挂片的同一条 ORDERED 医嘱再列一次（:563-568 只按类型与状态过滤），③ 下拉也主动列它并锁 HE，前后端一路放行，没有一个字提醒不该这么走——屏把路铺好了就得对铺完后说的话负责。
6. 5273 拦截消息仍带配置键与英文档位名（PathologyReportController.java:1070-1072），且 V58TechProgressTest / V62TechRemarkTest / e2e-v58-audit.py 三处断言把它锁成契约；本轮车道 B 声明「无后端改动」，它原样留着。菜单到不了「拦截」档，我不拿它当主证据。
7. ④ 诊断页至今不印 techDoneGateRule（DiagnosisPanel.vue:922-926 的 loadTech 丢掉返回体的 note 与 techDoneGateRule）却有「完成」按钮，属「少说」不是「说错」，未计入。

对本轮三件「根治手段」本身的核验：
8. **尺子能被绕过，且本条恰好落在它的两个盲区里**：(a) touched_claims 只抽 + 行中的引号字面量（claim-ratchet.py:193、:219-220），.vue 普通文本节点整类不在扫描域——登记簿 :55-57 自己承认车道 B 那两句是手工补登；病理三个工位里最会说话的 `<span class="muted">` 灰字（GrossingPanel.vue:272、:277；ProcessingPanel.vue:269-272）全在域外，这就是簿首 limits (c) 的常态而非例外。(b) diff 驱动看不见「文案没动」的句子（limits (b)），:277 正是。(c) CI 基线取 `git describe --tags --abbrev=0 --match 'v[0-9]*' HEAD`（.github/workflows/ci.yml:56、:70）：在定版提交上（tag 落在 HEAD 自己）基线即 HEAD，`--since v1.6.5 --strict` 的 diff 为空、恒绿；若一轮的车道提交与 tag 在一次 push 里推上去，CI 只跑 HEAD，棘轮这一轮什么都没看。我没有去查这轮的 push 记录，只指出机制。(d) 缩进切子树 / 段落式 caveat 两条形态我在 2563 的改动里没找到实例（TechOrderPanel/DiagnosisPanel 页首用的是 :title 绑定 computed 字面量，本轮未动）。
9. 登记簿 2563 两行：877594f144c3（FACT）配的用例确在真实库态求值（造 RESAMPLE 医嘱 → append 三块 → 读标本分支与全院默认视图两行）并附源码扫描，不是纯源码扫描，这一条我背书；bcc0d677b7c3 默认档、v60 既有句只补句号，不需对拍。但 877594f144c3 的「它在什么状态下是假的」只答了「任一屏把表达式改回 block_code 优先」——我试着造别的反例（③ 传 status=ORDERED、④ 不传 status、⑤ 传查询态：同一 SQL 同一列；③ 弹窗打开时取、⑤ 页签打开时取的时差：仍同一列），没造出让「三屏取自同一列」为假的库态。真正的问题不在这句的真假，而在它把断言收窄到「三屏」：平台上对同一条医嘱印「关联蜡块」的是四块屏，第四块（② 取材下拉）给的是另一套答案。这是「把话说成只覆盖修好的那部分」的形态，登记簿的对拍抓不到，因为句子本身是真的。
10. 第五种形态（生成器说假话）我专门找了：⑤/④ 页首「未完成的分 N 档：…」由 progressStates 生成（TechOrderPanel.vue:230-232）、techDoneGateRule 由 TECH_DONE_VERDICTS 生成（:1435-1450）且 techDoneGap 读同一张表（:1409-1410）、脚本汇总行由同一批 rows 生成（demo-pathology.py:507-512，d0 的已出块与派生取同一行）——都没发现分支漏算或两次查询混用。找到的是三处**文档/帮助文案陈旧**，不上评委屏，不计缺口：tools/demo-pathology.py:69 argparse help 仍写「另固定加 1 个拒收」（docstring :30-31 已是 +2）；docs/演示/病理演示数据.md:98 仍写 `ordered=2、done=1、cancelled=1`（本轮起脚本必留 4 条 ordered）；docs/培训脚本.md:82 把同一句无条件承诺「挂上后新蜡块回写到医嘱、进度变「已补取材待切片」」写进了培训稿。
11. 已知结转项（AnesQcView 合计块无标题、ANOMALY_NOTE 无对拍、兄弟 caveat「当日」）都不在 2563 的屏上，未影响本条结论。

本结论的边界：
12. 我判的是当前主树 bbf8012（tag v1.6.5），不是「v65 干了什么」。v64 打回 2563 的三个镜头里，③ 制片 coalesce 短路与演示脚本空表两条是真修了、守卫是真扫源码且真造库态、脚本自检读后端字典不写死——这两条我愿意背书，维持的理由不在它们身上。
13. 我没核 2530 与 2576。它们与本条共用 GrossingPanel.vue、PathologyReportController.java；若另两位审计者发现本轮对这两个文件的改动另有问题，我的 ①-⑤、⑥-a、⑦-a 背书可能需要重看。

**反驳者·子要求拆解**（推翻）：

「关联蜡块编号」这条子要求在四个消费方里只修到了三个：② 取材工位「挂接补取材医嘱」下拉（GrossingPanel.vue:577）仍只印 o.block_code、空则印「未指定」，而同一条补取材医嘱、同一条 API 行（后端无条件带 blocks_derived / blocks_derived_source）在 ⑤ 特检工作台、④ 诊断页、③ 制片下拉已统一改印并集列并标「派生」——在 v65 自己新造的演示数据上（tech_backlog 的补取材医嘱补出 2 块、block_id 被系统回写为首块、留在待执行），⑤ 的蜡块列写「P-3、P-4」带「派生」标，评委回到 ② 打开同一标本勾「补取材」，下拉里同一条医嘱却写「蜡块 P-3」，与「医师下达时指定了这一块」屏上完全同形；这一处正是 v65 规划节给车道 B 点名的「GrossingPanel 的补取材下拉更彻底，只读 block_code 连回落都没有」，但 GrossingPanel.vue 被分给车道 A、车道 A 本轮唯一一次提交只动了 2530 修订弹窗，该行自 v1.6.3 起一个字节没变；本轮新建的守卫 threeScreensReadTheSameBlockColumnForOneTechOrder 只扫 ③⑤④ 三屏，全仓测试零处提到 resampleLabel，技术债交接单与屏上断言登记簿都没记它。同一个表单项往下两行，:277 那句无条件承诺「医嘱进度随之变为「已补取材待切片」」（v64 审计者对本条的主证据，在该医嘱已被挂片时为假）也原样留着、未登记——「当前执行进度」子要求同样欠着半条。

【证据】【1】坏的那一处（当前主树 bbf8012 = tag v1.6.5）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:575-580
```
function resampleLabel(o: Row): string {
  const item = o.tech_item ? ` ${String(o.tech_item)}` : ''
  const block = o.block_code ? String(o.block_code) : '未指定'
  const reason = o.reason ? `　原因：${String(o.reason)}` : ''
  return `#${fmt(o.id)}　补取材${item}　下达 ${fmt(o.ordered_by_name)} ${fmtTime(o.ordered_at)}　蜡块 ${block}${reason}`
}
```
它上屏的位置（② 取材登记弹窗，勾「补取材」后的表单项，el-option 的 label 就是它）：
GrossingPanel.vue:265-269
```
      <el-form-item v-if="mode === 'GROSSING' && form.append" label="挂接补取材医嘱">
        <div style="width: 100%">
          <el-select v-model="form.techOrderId" clearable placeholder="不挂接（自由补取材，医嘱进度不随本次取材推进）"
                     style="width: 100%" :loading="resampleLoading">
            <el-option v-for="o in resampleOrders" :key="Number(o.id)" :value="Number(o.id)" :label="resampleLabel(o)" />
```
数据来源就是同一条清单 API（并集列已经在 o 里，这屏不读）：GrossingPanel.vue:563-567
```
    const d = (await client.get('/pathology/report/tech-orders', {
      params: { specimenId, status: 'ORDERED', techType: 'RESAMPLE' },
    })).data.data as Row
    resampleOrders.value = ((d.items ?? []) as Row[])
      .filter((o) => o.tech_type === 'RESAMPLE' && o.status === 'ORDERED')
```
后端对该端点所有分支无条件拼进派生列：modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:1251 `select t.id, t.specimen_id, t.block_id, b.block_no, b.block_code,` … :1265 `.append(TECH_DERIVED_COLUMNS)   // v60：sampled_block_count / blocks_derived / attached_stain_name`

【2】三个已修好的同义入口（并集列在前）
frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue:103 `<span class="code">{{ fmt(row.blocks_derived ?? row.block_code) }}</span>`
frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:274 `{{ fmt(row.blocks_derived ?? row.block_code) }}`
frontend/shell/src/views/medtech/pathology/ProcessingPanel.vue:500 `const blockCode = t.blocks_derived ?? t.block_code`
v61 在 ⑤ 上写明为什么只读 block_code 是错的——TechOrderPanel.vue:100-102
```
             v61（2563 复核）：「派生」标改读后端与并集同一处 SQL 算出的 blocks_derived_source，
             不再按 !block_code 二次推断——补取材挂接会把 block_id 回写为本次首块，那样判恒为 false，
             于是补出 2 块时显示「P-3、P-4」却不带「派生」标，与「医师指定了这两块」同形。 -->
```

【3】block_code 为什么必然非空且只有一块（回写侧）
modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:573-576
```
        if (techOrder != null && techOrder.get("block_id") == null) {
            orderBlockBackfilled = jdbc.update(
                    "update path_tech_order set block_id = ? where id = ? and block_id is null and status = 'ORDERED'",
                    asLongObj(created.get(0).get("id")), req.techOrderId()) == 1;
```
二次补取材仍放行（:464-488 只校验 存在 / RESAMPLE / 同标本 / ORDERED，不看已出块数），且 :573 的 `block_id == null` 条件让第二次不再回写——block_code 永远停在首块。

【4】v65 自己的演示数据当场造出这个态、且把医嘱留在待执行
tools/demo-pathology.py:261-265
```
    rs = do('下补取材医嘱（不指定蜡块）', 'POST', '/pathology/report/tech-orders',
            {'specimenId': sid, 'techType': 'RESAMPLE',
             'reason': '切缘情况需补充取材（演示：取材已补出块、尚未切片）'})['id']
    blocks += len(resample_append(sid, rs, ['切缘一（补取材）', '切缘二（补取材）'], idx))
    backlog['SAMPLED'] = rs
```
tools/demo-pathology.py:224-225
```
    assert g.get('techOrderBlockBackfilled') is True, \
        f'医嘱 block_id 应由空回写为本次首块（演示要的就是这个态）：{g}'
```
即库态：block_code = 首块、blocks_derived = 两块「、」相连、blocks_derived_source = DERIVED、status = ORDERED（verify_tech_backlog :496 断言 progress == 'SAMPLED' 且在 ⑤ 默认视图里）。② 的下拉按 status=ORDERED&techType=RESAMPLE 取，这条医嘱必在其中。

【5】规划节点了名、文件却归了别的车道，一轮没跟
docs/下一步开发规划.md:74（v65 车道 B 行，节选）
```
| **B** | 2563 三条 | ① `coalesce` 短路的**第三个**消费方：`ProcessingPanel.vue` 的 `t.block_code ?? t.blocks_derived`……；`GrossingPanel` 的补取材下拉更彻底，只读 `block_code` 连回落都没有；② 演示脚本…… | `PathologyReportController`、`ProcessingPanel.vue`、`TechOrderPanel.vue`、`DiagnosisPanel.vue`、`tools/demo-pathology.py` |
```
文件独占列没有 GrossingPanel.vue（它在车道 A 的 2530 清单里）。
`git log --oneline v1.6.4..v1.6.5 -- frontend/shell/src/views/medtech/pathology/GrossingPanel.vue` 只有一条：`b21e6c5 v65 车道A：2530 三条——覆盖提示由表单状态生成 / 备注印模板中文名 / 退化告警说实话且只留一个基准`；`git diff v1.6.4..v1.6.5 -- …GrossingPanel.vue` 三个 hunk 全在 reviseFormFields / reviseSubmitNote / reviseCoverNote / submitRevise（:738-856），没碰 :265-280 与 :575-580。
三个 tag 同位置逐字相同：`git show v1.6.3:…GrossingPanel.vue` :555、`v1.6.4` :577、`v1.6.5` :577 均为 `  const block = o.block_code ? String(o.block_code) : '未指定'`。

【6】守卫扫不到它
server/src/test/java/cn/hip/server/V62TechRemarkTest.java（v65 新增用例 threeScreensReadTheSameBlockColumnForOneTechOrder）
```
        for (String rel : List.of(PROC_PANEL, TECH_PANEL, DIAG_PANEL)) {
            exprs.put(rel, blockExprOf(rel));
        }
```
抽取器 `BLOCK_EXPR = "[A-Za-z_$][\\w$]*\\.(blocks_derived|block_code)\\s*\\?\\?\\s*…"` 只认 `a.x ?? b.y` 形态，② 那行的三目表达式即使被扫也抓不到。`git grep -n "resampleLabel\|'未指定'" -- server/src/test tools` 零命中。
登记簿 docs/屏上断言登记簿.tsv 的 GrossingPanel 行只有 :762 / :793 / :796（2530 修订弹窗）；docs/技术债交接单.md grep `GrossingPanel|补取材下拉|resampleLabel|随之变为` 仅命中 :82 的 v59 模板管理备注。

【7】同一表单项两行之下、「当前执行进度」子要求的半条（v64 审计者主证据，原样留着）
GrossingPanel.vue:273-277
```
          <span v-else-if="form.techOrderId" class="muted">
            <!-- v64 合并后补齐：原写「已补取材、待切片」多一个顿号。…… -->
            提交后本次新蜡块记到该医嘱名下，医嘱进度随之变为「已补取材待切片」</span>
```
实现有条件：PathologyReportController.java:1389-1390
```
        if (slides == 0) return sampled > 0 ? "SAMPLED" : "PENDING_SECTION";
        return stained < slides ? "SECTIONING" : "STAINED";
```
该医嘱只要先在 ③ 被挂过 HE 片（ProcessingPanel.vue:510 `RESAMPLE: 'HE'`，后端 5272/5274 均放行），提交后 ⑤ 打出「切片中」。这句是普通文本节点、本轮未动，登记簿簿首 limits (b)(c) 明写这两种形态棘轮抓不到，登记簿里也没有它。

【反向核对】先试着打破自己这条指控：(1)「o.block_code 会不会是空、下拉印『未指定』反而无害？」——不会：RESAMPLE 医嘱下达时块还不存在（demo :261-263 不传 blockId），第一次补取材 :573-576 条件回写必成功，demo :224-225 在真库上断言 techOrderBlockBackfilled is True。(2)「两个值会不会本来相等？」——只有补出 1 块时相等；demo tech_backlog 固定补 2 块（:264），并集列两块、block_code 一块。(3)「② 下拉是不是真在上屏路径上？」——是：:265 的 el-form-item 在 mode==='GROSSING' && form.append 时渲染，:269 el-option 的 label 直接用 resampleLabel(o)；:563-567 拉的正是与 ⑤④③ 同一条 GET /pathology/report/tech-orders，后端 :1265 无条件把 TECH_DERIVED_COLUMNS 拼进 select，所以 blocks_derived 就在 o 里，是这屏不读。(4)「demo 那条补取材医嘱会不会已完成、不在 ② 下拉里？」——不会：tech_backlog 一条都不 /done 或 /cancel（:246-247 docstring 与代码一致），verify_tech_backlog :484-497 在 ⑤ 默认 ORDERED 视图里断言它 progress=='SAMPLED'；② 按 status=ORDERED&techType=RESAMPLE 取，它必在。(5)「是不是已登记的结转项？」——不是：规划节 :74 把它写进车道 B 要修的 ①，但车道 B 文件独占没有 GrossingPanel.vue；CHANGELOG 1.6.5 的 2563 段只说修了 ProcessingPanel 与演示脚本；交接单与登记簿零命中。(6)「② 印『医嘱下达在哪块』语义上是否说得通？」——说不通：空时印「未指定」，就是把 block_code 当「医师指定的块」在用，回写后系统填的首块以同一形态印出，正是 TechOrderPanel.vue:100-102 v61 为 ⑤ 修掉的那个「与医师指定了这两块同形」；且 ② 下拉的读者是要决定给哪条医嘱再补块的技师，少列的正是已补出的块。

我核过、这次确实是好的（不凑数）：① ③⑤④ 三屏取值表达式逐字同源（ProcessingPanel.vue:500 / TechOrderPanel.vue:103 / DiagnosisPanel.vue:274），新守卫的库端一半是真库态（process.grossing 三块 → listRow / defaultDeskRow 比对 blocks_derived 与 block_code 不等），屏端一半是剥注释源码扫描，登记簿 877594f144c3 行对此描述诚实。② 演示脚本 tech_backlog 四档各一条 + verify_tech_backlog 读字典端点 progressStates 逐档在 ⑤ 默认视图求值，是真库态回读不是源码扫描；e2e-v58-audit.py:378 钉 tech_backlog=4。③ doneRemark 一个变量既落库又回体（PathologyReportController.java:1092-1095、:1112），两个完成入口原样印（TechOrderPanel.vue:300、DiagnosisPanel.vue:971）。④ techDoneGateRule 单一来源 :1435-1451，⑤ 页首 :288/:9 消费，模板里无写死规则。⑤ 5273 消息仍带配置键与英文档位（:1071-1072 `+ "（gate " + TECH_DONE_GATE_KEY + "=block）"`），但全前端无 sys_config 编辑页、菜单走不到，我没拿它当主证据。⑥ ProcessingPanel.vue:649-650 染色后提示对补取材医嘱不报「已出块」，只是少说不是说错，不计。

**反驳者·演示可行性**（推翻）：

评委照培训脚本跑完 demo-pathology.py 后，⑤ 特检工作台把那条补取材医嘱的「关联蜡块编号」列成两块「…-3、…-4」并标「派生」，而培训脚本紧接着教评委去的 ② 取材工位「挂接补取材医嘱」下拉，对同一条医嘱只打出「蜡块 …-3」——单独一块、没有「派生」标、看上去像医师下达时指定的块（实际该医嘱下达时未指定蜡块，-3 是补取材自己出的第一块被系统回写进 block_id）。这是 v60（SQL）、v61（⑤④）、v65（③）三轮修同一个 coalesce 短路后仍原样留着的第四个消费方：v65 规划节自己点了名（「GrossingPanel 的补取材下拉更彻底，只读 block_code 连回落都没有」），却把它写在车道 B 的格子里、而文件独占归车道 A，两个车道都没改，CHANGELOG 1.6.5 与技术债交接单都没登记；守卫只扫 ③⑤④ 三个文件，② 无任何测试或 E2E 触及。同一条医嘱在评委必经的两块屏上「关联蜡块编号」两套答案，参数要求的「完整明细」在 ② 这一屏站不住。

【证据】【1】② 下拉只读 block_code、无派生回落（当前主树，与 v60 时一字未变：git log -S 该行仅命中 028ac46 v60）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:575-580
```
function resampleLabel(o: Row): string {
  const item = o.tech_item ? ` ${String(o.tech_item)}` : ''
  const block = o.block_code ? String(o.block_code) : '未指定'
  const reason = o.reason ? `　原因：${String(o.reason)}` : ''
  return `#${fmt(o.id)}　补取材${item}　下达 ${fmt(o.ordered_by_name)} ${fmtTime(o.ordered_at)}　蜡块 ${block}${reason}`
}
```
上屏位置 GrossingPanel.vue:265-269
```
      <el-form-item v-if="mode === 'GROSSING' && form.append" label="挂接补取材医嘱">
        <div style="width: 100%">
          <el-select v-model="form.techOrderId" clearable placeholder="不挂接（自由补取材，医嘱进度不随本次取材推进）"
                     style="width: 100%" :loading="resampleLoading">
            <el-option v-for="o in resampleOrders" :key="Number(o.id)" :value="Number(o.id)" :label="resampleLabel(o)" />
```
它取的是同一个端点的同一行（含 blocks_derived / blocks_derived_source，但被忽略）GrossingPanel.vue:563-566
```
    const d = (await client.get('/pathology/report/tech-orders', {
      params: { specimenId, status: 'ORDERED', techType: 'RESAMPLE' },
    })).data.data as Row
    resampleOrders.value = ((d.items ?? []) as Row[])
```

【2】三个已修好的消费方（同一行数据、另一套答案）
frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue:103-105
```
        <span class="code">{{ fmt(row.blocks_derived ?? row.block_code) }}</span>
        <el-tag v-if="row.blocks_derived_source === 'DERIVED'" size="small" type="info">派生</el-tag>
        <el-tag v-else-if="row.blocks_derived_source === 'MIXED'" size="small" type="warning">含派生</el-tag>
```
frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:274-276 同形；frontend/shell/src/views/medtech/pathology/ProcessingPanel.vue:501
```
  const blockCode = t.blocks_derived ?? t.block_code
```
ProcessingPanel.vue:271-272 本轮新增并登记为 FACT 的那句
```
          补取材医嘱标「已出块 N」（取材工位补取材时挂接到该医嘱的蜡块数，v60）：N=0 表示还没为它补出块，先去取材工位补取材再来切片。
          括注里的块号是这条医嘱已关联的蜡块，与 ⑤ 特检工作台、④ 诊断页的「蜡块」列取自同一列，三屏给同一套答案。</span>
```

【3】演示数据必然造出「block_code 一块 vs 并集两块」这个态
tools/demo-pathology.py:257-261
```
    rs = do('下补取材医嘱（不指定蜡块）', 'POST', '/pathology/report/tech-orders',
            {'specimenId': sid, 'techType': 'RESAMPLE',
             'reason': '切缘情况需补充取材（演示：取材已补出块、尚未切片）'})['id']
    blocks += len(resample_append(sid, rs, ['切缘一（补取材）', '切缘二（补取材）'], idx))
    backlog['SAMPLED'] = rs
```
tools/demo-pathology.py:219-220
```
    assert g.get('techOrderBlockBackfilled') is True, \
        f'医嘱 block_id 应由空回写为本次首块（演示要的就是这个态）：{g}'
```
回写的后端 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:565-571
```
        // v60：医嘱 block_id 为空则回写为本次首块——条件 update（and block_id is null）不覆盖已有值；
        // 下达时块还不存在，RESAMPLE 医嘱的 block_id 只可能在这里落
        boolean orderBlockBackfilled = false;
        if (techOrder != null && techOrder.get("block_id") == null) {
            orderBlockBackfilled = jdbc.update(
                    "update path_tech_order set block_id = ? where id = ? and block_id is null and status = 'ORDERED'",
                    asLongObj(created.get(0).get("id")), req.techOrderId()) == 1;
```
块码形态 PathologyProcessController.java:555-556（pathNo + '-' + n）
```
                    insert into path_block(specimen_id, block_no, block_code, tissue_desc, created_by, tech_order_id)
                    select ?::bigint, nb.n, ?::text || '-' || nb.n::text, ?::varchar, ?::bigint, ?::bigint
```
真库态钉死「block_code = 首块、blocks_derived = 首块、次块」tools/e2e-v60-audit.py:450-452
```
assert row.get('block_id') == nb1 and row.get('block_code') == code1, f'**医嘱 block_id 回写为本次首块**：{row}'
assert_progress(sA, rs, 'SAMPLED', '已补取材待切片', 0, 0)
assert_derived(sA, rs, code1 + '、' + code2, 2, None)
```
server/src/test/java/cn/hip/server/V62TechRemarkTest.java:719-727 同样在真库上断言 backfilled != union。

【4】② 这条路从菜单走得到，且是培训脚本点名要评委走的
docs/培训脚本.md:82（节选）
```
→ ② 取材：…**补取材可挂接 RESAMPLE 特检医嘱**（挂上后新蜡块回写到医嘱、进度变「已补取材待切片」）→ ③ 制片：…
```
docs/培训脚本.md:83
```
   零种子演示先跑 `tools/demo-pathology.py --count 5`（走真实 API，前两条同患者可演示既往对比）。
```
② 列表「全部」范围含已有蜡块的标本 PathologyProcessController.java:298-308
```
        String mode = "all".equalsIgnoreCase(trim(scope)) ? "all" : "pending";
        ...
                .append(" where s.received_at is not null and s.rejected_at is null ");
        ...
        if ("pending".equals(mode)) {
            sql.append(" and s.diagnosed_at is null ")
               .append(" and not exists (select 1 from path_block b where b.specimen_id = s.id) ");
```
「取材登记」按钮无条件出现 GrossingPanel.vue:76
```
        <el-button link type="primary" size="small" @click="openGrossing(row)">取材登记</el-button>
```
打开即预取该标本的补取材医嘱 GrossingPanel.vue:637
```
  void loadResampleOrders(Number(row.id))   // v60：勾「补取材」后可挂接的 RESAMPLE 医嘱，先取好、不让人勾了再等
```
工作台页签无角色门槛 frontend/shell/src/views/medtech/pathology/PathologyWorkbenchView.vue:17-28（② 与 ⑤ 都是普通 el-tab-pane）。

【5】v65 自己点了名、却没人修，也没登记
docs/下一步开发规划.md:74（车道 B 格子，节选）
```
| **B** | 2563 三条 | ① `coalesce` 短路的**第三个**消费方：`ProcessingPanel.vue` 的 `t.block_code ?? t.blocks_derived`……`GrossingPanel` 的补取材下拉更彻底，只读 `block_code` 连回落都没有；② 演示脚本…… | `PathologyReportController`、`ProcessingPanel.vue`、`TechOrderPanel.vue`、`DiagnosisPanel.vue`、`tools/demo-pathology.py` |
```
（GrossingPanel.vue 在车道 A 的文件独占列 :73，车道 A 任务书只有 2530 三条。）
`git diff v1.6.4..v1.6.5 -- GrossingPanel.vue` 三个 hunk 全在 :738-856（reviseFormFields / reviseSubmitNote / reviseCoverNote / submitRevise），未触及 :265-277 与 :575-580。
车道 B 提交 cf4dd50 正文只说 ProcessingPanel：「ProcessingPanel「切片登记」弹窗挂接下拉的蜡块段由 `t.block_code ?? t.blocks_derived` 改为 `t.blocks_derived ?? t.block_code`」；CHANGELOG.md 1.6.5 段 2563 条同；docs/技术债交接单.md grep `GrossingPanel|补取材|resample|block_code|下拉` 零命中本处。

【6】守卫扫不到 ②
server/src/test/java/cn/hip/server/V62TechRemarkTest.java:733-737
```
        var exprs = new LinkedHashMap<String, String>();
        for (String rel : List.of(PROC_PANEL, TECH_PANEL, DIAG_PANEL)) {
            exprs.put(rel, blockExprOf(rel));
        }
        assertEquals(1, new HashSet<>(exprs.values()).size(),
```
全仓 grep `resampleLabel`（server/src/test、tools、frontend）除 GrossingPanel.vue 自身外零命中。docs/屏上断言登记簿.tsv:58 对 ③ 那句「它在什么状态下是假的」只答「任一屏把取值表达式改回 block_code 优先」，把「还有一屏从来就是 block_code」这个态漏掉了。

【反向核对】（A）先试着打破自己这条指控。①「block_code 会不会其实是空，两屏根本不冲突？」——不会：PathologyProcessController.java:565-571 是 `block_id is null` 条件回写，演示的 RESAMPLE 医嘱下达时不指定蜡块（demo-pathology.py:257-259），首次 append 必然回写成功，脚本 :219-220 还硬断言 techOrderBlockBackfilled 为 True，e2e-v60-audit.py:450 在真库上钉了 block_code == code1、:452 钉了并集 = code1、code2；V62TechRemarkTest.java:719-727 又在真库上断言回写块 != 并集。②「② 下拉会不会不在上屏路径、或有别处补回块号？」——GrossingPanel.vue:265-269 是勾「补取材」后真实渲染的 el-option label；选中后同区唯一附加文字是 :273-277 那句进度承诺，不列块；「查看大体所见」的蜡块表（:161-183）列的是 块号/蜡块编码/组织描述/建块/包埋/切片，没有归属医嘱列，补不回「这两块都是 #rs 的」。③「② 的『蜡块 …-3』能不能读成『只是 block_id』而非『关联蜡块编号』？」——不能：⑤ 页首 headNote（TechOrderPanel.vue:235）宣告「蜡块」列在医嘱未指定蜡块时按补取材已出块派生并标「派生」，而 ② 对同一条未指定蜡块的医嘱打「蜡块 …-3」、无任何派生标记，与「医师指定了 -3」同形——这正是 v61 在 ⑤ 修掉的那个「同形」（TechOrderPanel.vue:100-102 注释原话）。④「是不是已登记结转项？」——技术债交接单与 CHANGELOG 1.6.5 均无；规划节 :74 点了名但归错车道，属未处置而非已登记。⑤「③ 那句『三屏给同一套答案』本身是不是假话？」——它字面只说 ③⑤④，那三处确实逐字同一表达式（:501 / TechOrderPanel:103 / DiagnosisPanel:274），我不指控这句；我指控的是参数要求的「关联蜡块编号」在 ② 这一屏与其它三屏不同，而 ② 是培训脚本点名的必经工位。

（B）核过、这次确实是好的，不凑数：①「⑤ 第一屏空表」已修——demo-pathology.py:244-282 tech_backlog 造四条 ORDERED、:435-480 verify_tech_backlog 回读全院默认视图逐档核对（档位清单读 /tech-orders/dict 的 progressStates，不写死），e2e-v58-audit.py 改断言 `tech_backlog=4`；② 四档各有实例的派生我按 SQL 推过：Ki-67 的 slide_count 是 `sl.tech_order_id = t.id`（PathologyReportController.java:1256），块上既有的 2 张 HE 不算进去，故为 PENDING_SECTION；RESAMPLE 的 blocks_derived_source：b（回写块）的 `b.tech_order_id is distinct from t.id` 为 false、pb 两行为 false → bool_and/bool_or 皆 false → DERIVED（:339-352），屏上确实带「派生」；③ ⑤ 页首「未完成的分 N 档」由 progressStates.inProgress = TECH_DONE_VERDICTS.containsKey 生成（:243-249、:278-283），4 档与判定表逐字同源；④ ③ 的 coalesce 修复真实、且 V62TechRemarkTest#threeScreensReadTheSameBlockColumnForOneTechOrder 一头实查库一头扫源码、带修复前原文对照组，不是恒绿。

（C）顺带核到、不作主证据的两条：① GrossingPanel.vue:273-277 那句无条件承诺「医嘱进度随之变为「已补取材待切片」」（v64 复核 ⑦-d 主证据）在 v65 一字未动、也不在规划节九条里；在演示数据上它恰好为真（该医嘱挂接切片数为 0），只在「先在 ③ 给补取材医嘱挂片再回 ② 补取材」的路上为假，故不拿它独扛。② docs/演示/病理演示数据.md 最后一次改动是 cda099f（v64），仍写「固定再加 1 个拒收标本，故实际登记 N+1 个」（:51）、阶梯表只有 6 行（:60-67）、WORKLOAD_TECH「ordered=2、done=1、cancelled=1」（:98），与 v65 脚本的 N+2 / 第 7 个「特检在办」标本 / 4 条待执行不符——评委按这份文档不会知道 ⑤ 有东西可看，但脚本汇总行自己会打印 tech_line，不算走不到。

（D）我没跑一行代码：全程只读主树 bbf8012 的源码、脚本、测试与文档；「屏上会打出什么」是顺着 .vue 模板与后端 SQL 推出来的。

**反驳者·数据真实性与口径**（推翻）：

② 取材工位「取材登记」弹窗勾「补取材」后的「挂接补取材医嘱」下拉，对一条补取材医嘱的「关联蜡块编号」仍只读 block_code（系统回写的首块）、不读并集列 blocks_derived：演示库里那条已补出 2 块、仍待执行的补取材医嘱，在这里印「蜡块 P-3」，而同一条医嘱在 ⑤ 特检工作台 / ④ 诊断页 / ③ 制片下拉印「P-3、P-4」并标「派生」——同一事实四个入口两套答案，且把系统回写的块印成与「医师下达时指定 P-3」完全同形（后端自己的口径明写回写块算派生不算指定）。v65 规划节把这一处逐字写进车道 B 的修项，但车道 B 的文件独占清单不含 GrossingPanel.vue、车道 A 的任务书也没有它，交付对这一行零改动、无守卫、登记簿与交接单都没记——正是 v62「同义入口原样留着」的形态又一次从车道缝里漏过去。

【证据】【1】屏上那一行，当前主树、v65 未动（git blame 停在 2026-09-11 v60 提交 028ac460）
frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:575-580
```
function resampleLabel(o: Row): string {
  const item = o.tech_item ? ` ${String(o.tech_item)}` : ''
  const block = o.block_code ? String(o.block_code) : '未指定'
  const reason = o.reason ? `　原因：${String(o.reason)}` : ''
  return `#${fmt(o.id)}　补取材${item}　下达 ${fmt(o.ordered_by_name)} ${fmtTime(o.ordered_at)}　蜡块 ${block}${reason}`
}
```
上屏路径 GrossingPanel.vue:265-269
```
      <el-form-item v-if="mode === 'GROSSING' && form.append" label="挂接补取材医嘱">
        <div style="width: 100%">
          <el-select v-model="form.techOrderId" clearable placeholder="不挂接（自由补取材，医嘱进度不随本次取材推进）"
                     style="width: 100%" :loading="resampleLoading">
            <el-option v-for="o in resampleOrders" :key="Number(o.id)" :value="Number(o.id)" :label="resampleLabel(o)" />
```
数据源 GrossingPanel.vue:563-567（同一端点、同一行同时带 block_code 与 blocks_derived）
```
    const d = (await client.get('/pathology/report/tech-orders', {
      params: { specimenId, status: 'ORDERED', techType: 'RESAMPLE' },
    })).data.data as Row
    resampleOrders.value = ((d.items ?? []) as Row[])
      .filter((o) => o.tech_type === 'RESAMPLE' && o.status === 'ORDERED')
```
`git diff v1.6.4..v1.6.5 -- frontend/shell/src/views/medtech/pathology/GrossingPanel.vue | grep -n "resampleLabel\|block_code\|blocks_derived"` → 空。

【2】另外三屏读的是并集列（v65 车道 B 把 ③ 拉齐后，仓库里只剩 ② 这一处反着读）
frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue:103 `        <span class="code">{{ fmt(row.blocks_derived ?? row.block_code) }}</span>`
frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:274 `                {{ fmt(row.blocks_derived ?? row.block_code) }}`
frontend/shell/src/views/medtech/pathology/ProcessingPanel.vue:500 `  const blockCode = t.blocks_derived ?? t.block_code`

【3】block_code 为什么必然只有首块：列表端点 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:1251 `select t.id, t.specimen_id, t.block_id, b.block_no, b.block_code,` / :1275 `left join path_block b on b.id = t.block_id`；回写 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:562-566
```
        boolean orderBlockBackfilled = false;
        if (techOrder != null && techOrder.get("block_id") == null) {
            orderBlockBackfilled = jdbc.update(
                    "update path_tech_order set block_id = ? where id = ? and block_id is null and status = 'ORDERED'",
                    asLongObj(created.get(0).get("id")), req.techOrderId()) == 1;
```
并集列 PathologyReportController.java:329-337
```
                       (select string_agg(x.block_code, '、' order by x.block_no, x.block_code)
                          from (select b.block_code, b.block_no where b.id is not null
                                union
                                select pb.block_code, pb.block_no
                                  from path_block pb where pb.tech_order_id = t.id
                                union
                                select b2.block_code, b2.block_no
                                  from path_slide sl join path_block b2 on b2.id = sl.block_id
                                 where sl.tech_order_id = t.id) x)            as blocks_derived,
```
后端自己的「指定」口径 PathologyReportController.java:313-315 `<b>「指定」的口径</b>：{@code t.block_id} 指向的块<b>且该块不是为本医嘱补出的</b>` / `（{@code b.tech_order_id} 与 {@code t.id} 不同）才算医师指定——补取材回写进 {@code block_id} 的首块是系统写的、` / `不是医师下达时指定的，算派生；`——② 却把它印成「蜡块 P-3」，与医师指定同形。

【4】v65 自己的守卫在库里坐实了「回写块 ≠ 并集」，但只扫三屏、不扫 ②
server/src/test/java/cn/hip/server/V62TechRemarkTest.java:143-146
```
    static final String TECH_PANEL = "frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue";
    static final String DIAG_PANEL = "frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue";
    /** v65：coalesce 短路的第三个消费方所在的屏（③ 制片工位「切片登记」弹窗的挂接下拉） */
    static final String PROC_PANEL = "frontend/shell/src/views/medtech/pathology/ProcessingPanel.vue";
```
:722-727
```
        assertNotNull(row.get("block_code"), "回写确实发生了，block_code 非空（?? 在这里短路）");
        String backfilled = String.valueOf(row.get("block_code"));
        assertFalse(backfilled.contains("、"), "回写进 block_id 的只有本次首块这一块：" + backfilled);
        assertNotEquals(backfilled, union,
                "**修复前 ③ 下拉印这一块、⑤ 与 ④ 印那三块——同一条医嘱两套答案**："
                        + backfilled + " vs " + union);
```
:731 `        for (String rel : List.of(PROC_PANEL, TECH_PANEL, DIAG_PANEL)) {`
`grep -n "resampleLabel" server/src/test/java/cn/hip/server/*.java tools/e2e-v60-audit.py` → 零命中。

【5】演示库里正有这个态、且从菜单可达
tools/demo-pathology.py:261-265
```
    rs = do('下补取材医嘱（不指定蜡块）', 'POST', '/pathology/report/tech-orders',
            {'specimenId': sid, 'techType': 'RESAMPLE',
             'reason': '切缘情况需补充取材（演示：取材已补出块、尚未切片）'})['id']
    blocks += len(resample_append(sid, rs, ['切缘一（补取材）', '切缘二（补取材）'], idx))
    backlog['SAMPLED'] = rs
```
:224-225 `    assert g.get('techOrderBlockBackfilled') is True, \` / `        f'医嘱 block_id 应由空回写为本次首块（演示要的就是这个态）：{g}'`；该医嘱一条不完成（:245-247 docstring「一条都不完成、一条都不取消」）。
② 列表 scope=all 含已有蜡块标本：PathologyProcessController.java:301-309 `where s.received_at is not null and s.rejected_at is null` / `if ("pending".equals(mode)) {` / `.append(" and not exists (select 1 from path_block b where b.specimen_id = s.id) ");`（all 档无此过滤）；「取材登记」按钮无禁用条件 GrossingPanel.vue:76 `        <el-button link type="primary" size="small" @click="openGrossing(row)">取材登记</el-button>`，打开即预取 :636 `  void loadResampleOrders(Number(row.id))`。

【6】规划节写了、交付没做、也没记债
docs/下一步开发规划.md:74（v65 车道表）`| **B** | 2563 三条 | ① `coalesce` 短路的**第三个**消费方：`ProcessingPanel.vue` 的 …；`GrossingPanel` 的补取材下拉更彻底，只读 `block_code` 连回落都没有；② … | `PathologyReportController`、`ProcessingPanel.vue`、`TechOrderPanel.vue`、`DiagnosisPanel.vue`、`tools/demo-pathology.py` |`——修项点名 GrossingPanel，文件独占列却没有它；:73 车道 A 独占 `GrossingPanel.vue` 但任务书三条全是 2530。`git show cf4dd50 --stat`（车道 B）只有 登记簿 / ProcessingPanel.vue / V62TechRemarkTest / demo-pathology.py 四个文件。`grep -n "GrossingPanel\|resampleLabel" docs/技术债交接单.md` 仅 :82 的模板管理旧债；docs/屏上断言登记簿.tsv 三行 GrossingPanel 全是 2530 修订弹窗（:35-37），无此行。

【反向核对】先确认不是我读错：(a) resampleLabel 里根本没有 `??`，是 `o.block_code ? String(o.block_code) : '未指定'`，blocks_derived / blocks_derived_source 两键在该函数与整个 GrossingPanel.vue 零引用（grep 全文件只有 :168/:415 的蜡块表与 :577 这三处 block_code）；(b) 它确在上屏路径：el-option :label 直接绑 resampleLabel（:269），渲染条件只有 mode==='GROSSING' && form.append（:265），数据来自 tech-orders 标本分支、该分支每行同时下发 block_code 与 blocks_derived（PathologyReportController.java:1251 + :1265 append TECH_DERIVED_COLUMNS）；(c) 触发态可达且演示库自带：补取材医嘱 append 后仍 ORDERED（回写 update :565 只改 block_id 不改 status），下拉按 status=ORDERED 过滤所以第二次补取材必看到它，demo 的 SAMPLED 那条正是「不指定蜡块下达 → 补出 2 块 → 不完成」；(d) 没有别处兜底：同一弹窗其它块码只在 :168/:415 的本标本蜡块表（不是该医嘱的关联块）。反例试打：若医师下达时就指定了蜡块且从未补出块，block_code 与并集只有一块、两处逐字相同——但这恰是 v60 之前的老场景，v60 起的旗舰场景（不指定蜡块 → append 回写）必然分叉，V62TechRemarkTest:722-727 自己在真库态断言了 backfilled != union。反向核对这次是好的地方（试着打没打破）：⑤/④/③ 三屏都是 `blocks_derived ?? block_code`（TechOrderPanel.vue:103 / DiagnosisPanel.vue:274 / ProcessingPanel.vue:500），「派生」标读 blocks_derived_source 且 ordered_src = `b.tech_order_id is distinct from t.id`（:343）把回写首块正确判成 DERIVED；WORKLOAD_TECH 穿透复用 TECH_DERIVED_COLUMNS 并走四参 techProgress（PathQcController.java:1737、1768-1770），与清单同口径；doneRemark 落库与回体同一变量（PathologyReportController.java:1092-1095、:1112）；techDoneGateRule / progressStates 由 TECH_DONE_VERDICTS 与 TECH_PROGRESS_ORDER 生成（:243-250、:1435-1450），生成器逐条遍历无漏分支；demo verify_tech_backlog（demo-pathology.py:480-495）读后端 progressStates 并逐条对 ⑤ 默认视图真实返回行的 progress 求值，不是源码扫描。这些我没能打破，缺口只落在 ② 这一个未被任何车道、守卫、登记簿或交接单覆盖的入口。

---

### 2576

**参数原文（CSV 逐字）**：支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。 +---------------------------------------------------------------------------------------------------------------------------------------------------------

**CSV 响应结论列**：部分响应

**CSV 说明列**：本条所述病理基础工作量自动统计（病理登记总量、蜡块产出数、报告签发量等多维度汇总）功能当前版本未提供，列入后续开发计划。

**审计者**：维持。

子要求核对：

- [✓] 「支持基础工作量自动化统计」：有承载页面、有菜单入口、单 ADMIN 进得去、打开即算不需手工传参 —— server/src/main/resources/db/migration/V144__pathology_base.sql:199 `    (169, 7, '病理质控',   'MENU', '/pathology/qc',        'path:qc',   'DataAnalysis', 31)`；V156__v52_menus.sql:32 `select r.id, 169 from sys_role r where r.code in ('ADMIN', 'QUALITY')`；父目录补授 V156:126 `where rm.menu_id in (168, 169, 170, 171, 172, 173, 174, 175, 176, 177, 178, 179)`；路由 frontend/shell/src/router/index.ts:62 `{ path: 'pathology/qc', component: () => import('../views/medtech/pathology/PathQcView.vue') }`；后端 modules/medtech/src/main/java/cn/hip/medtech/web/PathQcController.java:84-85 `@RequestMapping("/api/path-qc")` / `@PreAuthorize("hasAnyRole('ADMIN','QUALITY')")`；自动：PathQcView.vue:420-423 `onMounted(async () => {` / `  await loadCatalog()` / `  await load()` / `})`；缺省窗 PathQcController.java:604-605 `t = (to == null || to.isBlank()) ? BusinessDates.today() : LocalDate.parse(to.trim());` / `f = (from == null || from.isBlank()) ? t.minusDays(29) : LocalDate.parse(from.trim());`。全部取值由 SQL 聚合（rowsOf :985-1229、summaryOf :1329-1463），无手工录入汇总值的表。
- [✓] 「涵盖病理登记总量」——这个数算得出 —— PathQcController.java:484 `def("WORKLOAD_REGISTER", "登记总量（按日，含拒收与加急构成）", BY_STAT_DAY,`；按日 SQL :1121-1123 `case "WORKLOAD_REGISTER" -> query("""` / `select s.collected_at::date                                         as stat_day,` / `count(*)                                                      as registered,`；合计 SQL :1393-1401 `case "WORKLOAD_REGISTER" -> one(q("""` / `select count(*)                                                as registered,` … `from path_specimen s` / `where {wc}`（无 group by，整窗）；中文 format.ts:476 `  registered: '登记标本数',`。
- [✗] 「涵盖病理登记总量」——上屏时口径说的是真话（同一屏内同一中文标签不得是两个口径；这是本项目连判三轮、v64 为蜡块列自己立的判据） —— 差在：合计格正上方的黄色 caveat 仍把整窗合计键定义成「当日」，而 v65 新加的合计格标题在同一屏说的是相反的话。caveat 原文 PathQcController.java:486 `+ " 分母口径：registered 是当日登记的**标本条数**，不是申请单数——"`、:488-489 `+ "这正是病理科的真实工作量单位。rejected 一列是当日登记的标本里**后来**被拒收的条数"` / `+ "（按登记日归集，不是按拒收日），要看「本期拒了多少」请走 PROCESS_TAT 的 REJECT 节点。"`；而 `registered` / `rejected` 同时是合计格的键（summaryOf :1394-1396 `as registered,` / `as rejected,`，`where {wc}` 整窗）。新标题对同一格说：PathQcController.java:874-875 `sb.append(w.days() > 1` / `? "这一格的每个数都按整个统计区间算，不是某一天的数；"`。两句同屏相邻：caveat 经 PathQcView.vue:91-92 `<el-alert v-if="ind.caveat" type="warning" :closable="false" class="cav"` / `:title="mdText(ind.caveat)" />` 上屏，紧接着 :99-100 `<el-descriptions v-if="ind.summary" :column="4" border size="small" class="cav"` / `:title="mdText(ind.summaryTitle)">`。合计格与按日表仍共用 registered/urgent/rejected/outp_source/inp_source 五个键、同一 `colLabel`（PathQcView.vue:102 与 :116）→ 同一中文「登记标本数」既是 30 天合计又是单日数；「合计行与按日行不得有同名列」这条规矩只对 WORKLOAD_BLOCK 施加：server/src/test/java/cn/hip/server/V62BlockCaliberTest.java:404-405 `assertEquals(Set.of(), common,` / `"**合计行与按日行不得有同名列**——合计落窗整个统计区间、按日行才是「这一天」，"`（:398-399 只喂 `"WORKLOAD_BLOCK"`）。平台自己在 docs/技术债交接单.md:164-167 承认「兄弟 caveat 正文里的「当日」措辞仍未清，这一条照旧挂着」。这句 caveat 本轮一字未动，v64 复核已判 ✗，v65 结转未修。
- [✓] 「蜡块产出数」——算得出，且上屏的两套口径（本期/当日）与 v64 新造的两句「必然」都已说真话 —— 合计四列 PathQcController.java:1425-1429 `select count(*)                                                as blocks_produced_in_period,` … `as blocks_per_specimen_in_period`；按日四列 :1167-1171 `as blocks_produced,` … `as blocks_per_specimen_of_day,`；中文 format.ts:426 `  blocks_produced: '当日产出蜡块数',`、:434 `  blocks_produced_in_period: '本期产出蜡块数',`。v64 那两句「必然」已删：`git diff v1.6.4..v1.6.5` 删去 `必然小于` / `不等于按日表里的任何一行`，现为条件句 :530-531 `+ " 「本期涉及标本数(去重)」与按日各行「当日涉及标本数(去重)」之和，"` / `+ "**只在本区间内没有一份标本的蜡块落进两个以上统计日时才相等**：同一份标本的蜡块可以分落在多天"`；本次实际关系由 summaryVsDailyNote 按库态现算 :925-965（相等档 :941-943 `if (periodSpecimens == daySpecimens) {` / `sb.append(" 等于按日各行「当日涉及标本数(去重)」之和 ")`），上屏 PathQcView.vue:108-109 `<el-alert v-if="ind.summaryVsDailyNote" type="info" :closable="false" class="cav"` / `:title="mdText(ind.summaryVsDailyNote)" />`，CSV 页脚 :1879-1880。真库态对拍：V62BlockCaliberTest.java:640 `void theSummaryVsDailyNoteTellsTheTruthInBothLibraryStates() {`，一根柱 :668-669 `assertTrue(note1.contains("「本期涉及标本数(去重)」1 等于按日各行「当日涉及标本数(去重)」之和 1"),`，跨日 :693-694 `assertTrue(note2.contains("「本期涉及标本数(去重)」1 小于按日各行「当日涉及标本数(去重)」之和 2"),`，v64 原话作活对照组 :681-683。
- [✓] 「报告签发量」——这个数算得出 —— PathQcController.java:565 `def("WORKLOAD_REPORT", "报告签发量（首次报告 / 补充报告分列）", BY_STAT_DAY,`；按日 SQL :1194-1198 `case "WORKLOAD_REPORT" -> jdbc.queryForList("""` / `select d.day::date                                                        as stat_day,` / `(select count(*) from path_specimen s` / `where s.report_issued_at >= d.day` / `and s.report_issued_at <  d.day + interval '1 day')            as issued_reports,`；合计 :1444-1451 `case "WORKLOAD_REPORT" -> one(q("""` / `select (select count(*) from path_specimen s where {wi})                    as issued_reports,`；中文 format.ts:484 `  issued_reports: '首次报告签发',`。
- [✗] 「报告签发量」——上屏时口径说的是真话 —— 差在：同一屏合计格正上方的 caveat 把合计格的整窗键 `diagnosed` 说成「当日」，并逐字直呼内部 JSON 键：PathQcController.java:568 `+ " issued_reports（首次报告签发）与 supplement_reports（补充报告）**分列不合并**："`、:570-571 `+ "两列相加当「报告总数」会把工作量算高。diagnosed 一列是当日写完诊断的标本数"` / `+ "（按 diagnosed_at 落窗），与 issued_reports 不是同一批——两者的差就是卡在签名 / 签发环节的量。");`；而 `diagnosed` / `issued_reports` / `supplement_reports` 三个键在合计格里都是整窗值（summaryOf :1445-1450 三个子查询各跨 `{wi}` / `?::date` 两端）。新标题对同一格说 :875 `"这一格的每个数都按整个统计区间算，不是某一天的数；"`——同一屏、相邻两块、同一个键，一句「当日」一句「整个统计区间」。「口径说明里不得直呼内部 JSON 键」那把尺只喂 WORKLOAD_BLOCK：V62BlockCaliberTest.java:433 `String caveat = caveatOf("WORKLOAD_BLOCK", from, to);` / :445 `assertFalse(RAW_KEY_IN_CAVEAT.matcher(caveat).find(),`。本轮一字未动（`git diff v1.6.4..v1.6.5` 对 :565-571 无改动）。
- [✓] 「等多维度的业务数据汇总」——多维度确有，且每条维度屏的「汇总」块标题说的是真话（v64 新造那句「下面那张表才按日拆分」已修） —— 维度：送检科室 PathQcController.java:495 `def("WORKLOAD_DEPT", "送检科室工作量", BY_DEPT,`、染色类型 :465 `def("SLIDE_QUALITY", … BY_STAIN_TYPE,`、技术类型 :582 `def("WORKLOAD_TECH", … BY_TECH_TYPE,`、病理医师 :573。标题由分组维度生成并先拿本次真交出去的行验：:880-893 `RowGroup g = d.rowGroup();` / `if (g == null || !groupsOnePerRow(shown, g.column())) {` … `sb.append("下面那张表不按日拆分，按").append(g.label())` / `.append("分组，每行一个").append(g.label());` / `return sb.append("，共 ").append(shown.size()).append(" 行。").toString();`；`groupsOnePerRow` :897-903 逐行取值互不相同、`hasDayColumn` :906-911 查无按日列；空行 :877-879 `if (shown.isEmpty()) {` / `return sb.append("本区间没有分组行，下面没有表。").toString();`。前端全局 computed 已删（`git diff v1.6.4..v1.6.5 -- PathQcView.vue` 删去 `const summaryTitle = computed(() =>`），只印 :100 `:title="mdText(ind.summaryTitle)"`。真库态逐指标穷举：V62BlockCaliberTest.java:534 `void everySummaryTitleDeclaresTheDimensionItsOwnTableIsGroupedBy() {`，12 条登记 :488-500，五个非按日维度各造一行 :540-547，非按日维度断言 :611-613 `assertTrue(title.contains("下面那张表不按日拆分，按" + exp.label() + "分组，每行一个" + exp.label()),`，行数 :617 `assertTrue(title.contains("共 " + rows.size() + " 行"),`，v64 原话活对照组 :567-571。ROW_LIMIT=500（:174）> MAX_SPAN_DAYS=366（:164），按日指标截断不可达，「共 N 行」与屏上表恒同批。

**审计者主证据**：【gap 一句话】病理质控看板上，参数点名的「登记总量」与「报告签发量」两条指标，合计格（标题逐字写「这一格的每个数都按整个统计区间算，不是某一天的数」）正上方的黄色口径说明仍逐字写着「registered 是当日登记的标本条数」「rejected 一列是当日登记的标本里后来被拒收的条数」「diagnosed 一列是当日写完诊断的标本数」——而 registered / rejected / diagnosed 正是那一格里的整窗合计键；同一屏、相邻两块，对同一个数一句说「当日」、一句说「整个统计区间」，且合计格与按日表仍共用同一套键与同一中文表头。这正是 v63/v64 在蜡块列上被判、v64 只为 WORKLOAD_BLOCK 一条立规矩修掉的同一种病；v65 车道 C 把 v64 新造的两条假话（全局 summaryTitle、两句「必然」）修好了，但参数点名的这两项本轮一字未动，平台自己在交接单里承认「照旧挂着」。

【证据链】
A. caveat 原文（一字未动，`git diff v1.6.4..v1.6.5` 对这几行无改动）
modules/medtech/src/main/java/cn/hip/medtech/web/PathQcController.java:484-489
```
        def("WORKLOAD_REGISTER", "登记总量（按日，含拒收与加急构成）", BY_STAT_DAY,
                "path_specimen.collected_at", ANCHOR_COLLECTED, WORKLOAD_NOTE
                + " 分母口径：registered 是当日登记的**标本条数**，不是申请单数——"
                + "多部位送检一份申请对应多条标本（同一份申请下按部位序号各自唯一），"
                + "这正是病理科的真实工作量单位。rejected 一列是当日登记的标本里**后来**被拒收的条数"
                + "（按登记日归集，不是按拒收日），要看「本期拒了多少」请走 PROCESS_TAT 的 REJECT 节点。"
```
PathQcController.java:568-571
```
                + " issued_reports（首次报告签发）与 supplement_reports（补充报告）**分列不合并**："
                + "补充报告是免疫组化结果回来后的追加意见，不代表一次新的诊断结案，"
                + "两列相加当「报告总数」会把工作量算高。diagnosed 一列是当日写完诊断的标本数"
                + "（按 diagnosed_at 落窗），与 issued_reports 不是同一批——两者的差就是卡在签名 / 签发环节的量。");
```
B. 这几个键同时是合计格（整窗）的键
PathQcController.java:1393-1401
```
            case "WORKLOAD_REGISTER" -> one(q("""
                    select count(*)                                                as registered,
                           count(*) filter (where s.urgent)                        as urgent,
                           count(*) filter (where s.rejected_at is not null)       as rejected,
                           count(*) filter (where s.order_id is not null)          as outp_source,
                           count(*) filter (where s.inp_order_id is not null)      as inp_source
                    from path_specimen s
                    where {wc}
                    """), w.args());
```
PathQcController.java:1444-1451（issued_reports / diagnosed / supplement_reports 三个子查询各跨整窗）
```
            case "WORKLOAD_REPORT" -> one(q("""
                    select (select count(*) from path_specimen s where {wi})                    as issued_reports,
                           (select count(*) from path_specimen s
                             where s.diagnosed_at >= ?::date and s.diagnosed_at < ?::date + 1)  as diagnosed,
```
C. 同一屏上、紧挨着的新标题说的是相反的话
PathQcController.java:871-876
```
    private static String summaryTitle(Def d, List<Map<String, Object>> shown, Window w) {
        var sb = new StringBuilder("本期合计　").append(w.f()).append(" 至 ").append(w.t())
                .append("（共 ").append(w.days()).append(" 天）。");
        sb.append(w.days() > 1
                ? "这一格的每个数都按整个统计区间算，不是某一天的数；"
                : "这一格按 " + w.f() + " 这一天算（本区间只有这一天）；");
```
渲染顺序 frontend/shell/src/views/medtech/pathology/PathQcView.vue:91-92 → :99-100（caveat 紧压在合计格上方）
```
        <el-alert v-if="ind.caveat" type="warning" :closable="false" class="cav"
                  :title="mdText(ind.caveat)" />
```
```
        <el-descriptions v-if="ind.summary" :column="4" border size="small" class="cav"
                         :title="mdText(ind.summaryTitle)">
```
D. 合计格与按日表同键同中文（两处都走同一个 colLabel）：PathQcView.vue:102 `:label="colLabel(k, keysOf(ind.summary as Row))"` 与 :116 `:label="colLabel(c, columnsOf(rowsOf(ind)))"`；format.ts:476 `  registered: '登记标本数',`、:484 `  issued_reports: '首次报告签发',`。「不得同名」的规矩只对 WORKLOAD_BLOCK：server/src/test/java/cn/hip/server/V62BlockCaliberTest.java:398-405
```
        Set<String> rowCols = rowCols("WORKLOAD_BLOCK", from, to);
        Set<String> sumCols = summaryCols("WORKLOAD_BLOCK", from, to);
...
        assertEquals(Set.of(), common,
                "**合计行与按日行不得有同名列**——合计落窗整个统计区间、按日行才是「这一天」，"
```
E. 平台自认结转：docs/技术债交接单.md:163-167
```
- `WORKLOAD_REGISTER` / `WORKLOAD_REPORT` 等**兄弟指标的 caveat 正文**仍把区间合计说成「当日」
  （`registered 是当日登记的标本条数` 等）。（v65 更新：v64 那个对所有指标生效的全局 `summaryTitle`
  本身就是一句假话……v65 已改为按各指标的分组维度逐指标生成、且说出口前拿真实行数验过。但兄弟 caveat 正文里的
  「当日」措辞仍未清，这一条照旧挂着；）
```
docs/下一步开发规划.md:24 `**结转 v66**：…兄弟指标 caveat 的「当日」措辞。`

【本轮三件事的核验】
1. 尺子能否被绕过——能，且本轮改动恰好落进它自列的第 (a) 条：我用 tools/claim-ratchet.py 的 touched_claims('v1.6.4')（只读）复算，指纹 d8b4b42e3500 把 summaryTitle + summaryVsDailyNote 的 14 个句读、约 10 句模板合成**一行**登记（登记簿 docs/屏上断言登记簿.tsv 第 d8b4b42e3500 行），c7b11fee96ef 把 WORKLOAD_BLOCK caveat 的 4 句合成一行；其中 PathQcController.java:537 `+ "「本期产出蜡块数」是逐块计数、没有去重，它等于按日各行之和。"` 与 :949-950 `sb.append(" 大于按日各行「当日涉及标本数(去重)」之和 ").append(daySpecimens)` / `.append("——整窗去重数不该大于按日之和，出现即是缺陷，请报修；");` 没有任何对拍用例求值（V62BlockCaliberTest#assertCaliberClaimMatchesData :726-741 只查「必然小于」「也不等于按日表里的任何一行」「 等于按日各行「当日涉及标本数(去重)」之和」三个串）。另一处结构性盲区：`:title="mdText(ind.summaryTitle)"` 因为引用了循环变量 `ind` 而天然通过 --detect（tools/claim-ratchet.py:159-161 `names = set(IDENT.findall(expr))` / `if names & loop_vars:` / `continue`），后端哪怕对每条指标回同一个常量，探测器也是绿的——挡它的只剩 V62BlockCaliberTest 那一条 Java 用例。还有一条它文件头没列的：静态属性 `title="…"`（不带冒号）不在 TEXT_BIND（:74 `TEXT_BIND = re.compile(r':(?:title|label|content|description)="([^"]*)"')`）扫描域，PathQcView.vue:78 `title="本指标缺数据源，本平台不给近似值、不显示为 0"` 与 :81 `title="需要补的字段（补齐后本指标即可按现成口径出）"` 都在 `v-for="ind in indicators"` 子树里、无条件说给每条不可用指标听。实跑三把（--selftest / --detect / --since v1.6.4 --strict）当前主树全绿。
2. 登记簿「对拍用例」是否真在真实库态下逐档求值——2576 相关三行是：everySummaryTitleDeclaresTheDimensionItsOwnTableIsGroupedBy（@SpringBootTest，:540-547 真插标本/蜡块/切片/特检医嘱后调 `pathQc.indicators(from, to, null)` 逐指标核）、theSummaryVsDailyNoteTellsTheTruthInBothLibraryStates（:650-651 `update path_block set created_at = now() - interval '200 days'` 造一根柱，:687-688 再挪一块造跨日）——确是真库态；V63FrontendLabelGuardTest#theRangeTotalBlockTitleIsPerIndicatorAndNeverAFileLevelConstant 是源码扫描（按设计）。但 af073fb0d8dd / c7b11fee96ef 两行的「对拍」对象是静态 caveat 字符串，`assertCaliberClaimMatchesData` 对它只做串缺席检查，静态文本在两种库态下当然同一结果——它证明的是「不再含 v64 那两句」，不是「条件句被逐档求值」。
3. 反例——我试了「截断让 summaryVsDailyNote 的『大于…请报修』分支被触发」：不可达，ROW_LIMIT=500（:174）> MAX_SPAN_DAYS=366（:164）。真正可达的是**两次查询之间的写入**：indicatorBody :830 `List<Map<String, Object>> rows = rowsOf(d.code(), w);` 与 :840 `Map<String, Object> summary = summaryOf(d.code(), w);` 是两条独立 SQL，控制器无 @Transactional（grep 零命中）；期间另一工位插入一块新标本的蜡块，则 periodSpecimens > daySpecimens，屏上打出「整窗去重数不该大于按日之和，出现即是缺陷，请报修」——一句由竞态生成的假缺陷报告；同理 :938 `periodBlocks == dayBlocks ? " 等于" : " 不等于"` 的「不等于」分支无任何解释，却与 :537 那句无条件「它等于按日各行之和」同屏。这就是「由状态生成」自带的第五种病的一个实例：生成器把「不可能」写成「缺陷」，而它的两个输入并非同一快照。此外 CSV 路径 :1874-1880 重新查询，代码注释 :1877-1878 说「与屏上同一个函数、同一批（截断后的）行算出来，两处逐字同源」——同一函数是真的，同一批行不是（页面加载与导出各查一次）；这句在注释里不在屏上，故不计入 gap。

**演示路径（审计者）**：【前提】全新零种子库（Flyway 到 V168）、后端 8080、**只用一个 ADMIN 账号**、全程左侧菜单、**不跑任何铺数脚本**。菜单 id 与 ADMIN 授权已回查迁移：V2__empi_patient.sql:28 菜单 6 / :32 `where r.code = 'ADMIN' and m.id in (5, 6);`；V3__outpatient_registration.sql:41 菜单 9 / :46 `m.id in (7, 8, 9)`；V5__doctor_charge_dispense.sql:64-65 菜单 10、11 / :70 `m.id in (10, 11, 12)`；V144__pathology_base.sql:198-199 菜单 168、169；V156__v52_menus.sql:32 与 :126。

一、造数（全在菜单内）
1. 「门诊业务 → 医生排班」（菜单 9）：给任一科室建**今天**的排班（零种子库无号源，跳过则挂不了号）。
2. 「患者服务 → 患者建档」（菜单 6）：新建一名患者。
3. 「门诊业务 → 挂号」（菜单 7）：用第 1 步排班挂号。
4. 「门诊业务 → 门诊医生站」（菜单 10）：接诊，开检查项目「组织病理学检查(活检)」C0301（种子 V21__phase24_specialty.sql:66 `('C0301', '组织病理学检查(活检)', 'EXAM', '次', 260.00, null);`）。
5. 「门诊业务 → 门诊收费」（菜单 11）：结算。病理登记只认已收费且项目名含「病理/活检」的门诊申请——PathologyController.java:39 `where o.status = 'CHARGED' and (o.item_name like '%病理%' or o.item_name like '%活检%')`。
6. 「门诊业务 → 病理工作台」（菜单 168）→ 页签「① 登记」（PathologyWorkbenchView.vue:14）：登记标本（类别 ROUTINE，填部位/固定液）→ 核收。
7. 「② 取材」（:17）：给该标本登记 **2 块**蜡块。
8. 「③ 制片」（:20）：脱水篮 → 包埋确认 → **只从第 1 块**建 2 张 HE 切片 → 染色登记（这样「本期产出蜡块数 2」与切片段「涉及蜡块数(去重) 1」是两个不同的数）。
9. 「④ 诊断 / 报告追溯」（:23）：书写首次报告 → 「初诊签名」（DiagnosisPanel.vue:149）→ 跳过复诊直接「正式签发」（:161，见陷阱 3）。

二、看数
10. 「门诊业务 → 病理质控」（菜单 169，路由 /pathology/qc）。**打开即自动查近 30 天**（PathQcView.vue:420-423），不点任何按钮。
11. 自上而下：统计口径 alert → 时限阈值与统计区间 → 本时段字段录入覆盖率（蜡块段「本期产出蜡块数（本段分母）2 / 本期涉及标本数(去重) 1」）→ 逐指标。
12. 参数点名三项：WORKLOAD_REGISTER 合计格「登记标本数 1」；WORKLOAD_BLOCK 合计格「本期产出蜡块数 2 / 本期涉及标本数(去重) 1 / 本期蜡块/标本 2.00」，其下蓝色一行「本期合计与按日各行的实际对账（按本次查询的库态现算，不是通例）：「本期产出蜡块数」2 等于按日各行「当日产出蜡块数」之和 2；「本期涉及标本数(去重)」1 等于按日各行「当日涉及标本数(去重)」之和 1——本区间内没有一份标本的蜡块落进两个以上统计日，两数相等；「本期蜡块/标本」2.00 与按日表里 1 行的数值相同（它用的是整窗分母，相同是本次库态使然）。」（这一行在演示库态下如实说「等于」，是 v65 修好的部分，可以当正面镜头）；WORKLOAD_REPORT 合计格「首次报告签发 1」。多维度：WORKLOAD_DEPT 合计格标题「本期合计　X 至 Y（共 30 天）。这一格的每个数都按整个统计区间算，不是某一天的数；下面那张表不按日拆分，按送检科室分组，每行一个送检科室，共 1 行。」——v64 那句「下面那张表才按日拆分」已不在屏上；每行可「该科室明细」穿透；每条可「穿透明细」「导出汇总 / 导出明细」。

三、演示陷阱
- **陷阱 1（本条判「维持」的现场，单标本单日当场可见）**：WORKLOAD_REGISTER 的黄色 caveat 逐字打出「registered 是当日登记的标本条数」「rejected 一列是当日登记的标本里后来被拒收的条数」，紧接着下面那格标题写「这一格的每个数都按整个统计区间算，不是某一天的数」，格里的「登记标本数 1」用的正是 registered 键；往下 WORKLOAD_REPORT 同型（「diagnosed 一列是当日写完诊断的标本数」+ 屏上直呼 issued_reports / supplement_reports）。评委只要把 caveat 读完再看合计格，就会问「到底是当日还是本期」。别让评委读那两段 caveat，或先说明它讲的是按日表。
- **陷阱 2（第一屏迁移号）**：页面第一条指标 SPECIMEN_RECEIVE 的 caveat 仍带 `V21 建表 default now()`（PathQcController.java:380），v64 车道 C 自称「全量 grep 已删迁移号」，本轮仍在。
- **陷阱 3（单账号签发）**：单 ADMIN 做不了复诊签名——PathologyReportController.java:629 `if (first.equals(uid)) return R.fail(5263, "复诊签名人不能与初诊签名人为同一人（一个人签两次不构成双签）");`；双签 gate 默认 warn（V144__pathology_base.sql:188 `('emr.gate.pathology.doublesign', 'warn',`），跳过复诊直接签发放行、报告签发量出得来，但 REPORT_DOUBLE_SIGN 是 0%、ISSUE 节点记「缺复诊签名（放行）」。要主动说明。
- **陷阱 4（别用脚本铺数）**：tools/demo-pathology.py 幂等新建 demo_pathdoc 与 demo_quality 两个账号，用它就不是单账号演示；v65 起它固定多造 1 个「特检在办」标本、留 4 条待执行特检医嘱（plan :335-336），WORKLOAD_TECH 的 pending 会是 4，评委问「为什么 4 条没做完」要能答。
- **陷阱 5（文档陷阱，v63/v64 两轮点名、v65 仍一字未动）**：docs/演示/病理演示数据.md:95 仍写 `| WORKLOAD_BLOCK 蜡块产出 | blocks=6、embedded=6、specimens=3、blocks_per_specimen=2.0 |`——四个键里 blocks / specimens / blocks_per_specimen 三个在当前主树已不是任何行的列名（format.ts:436-440、:452）；:86 还教演示者「用 demo_quality（脚本建的）…登录」，与单 ADMIN 演示冲突。该文件最后提交停在 v59 `0c62d99`。照它准备台词会念出屏上不存在的名字。
- **陷阱 6（30 行零表）**：WORKLOAD_REPORT 按日表由 generate_series 生成，30 天窗口必是 30 行、29 行全 0（PathQcController.java:1206 `from generate_series(?::date, ?::date, interval '1 day') as d(day)`），标题「共 30 行」是真话，但一屏零行看着像没数据；别的按日指标只有今天一行，评委会问为何不一致。
- **陷阱 7（换账号就没菜单）**：菜单 169 刻意不授 TECHNICIAN（V156__v52_menus.sql:29-30 注释「给了就是『菜单点得进、接口 403』」）。
- **陷阱 8（数据形态）**：手工造数不能回溯日期，30 天趋势只有今天一根柱；各 TAT 都是「几秒走完」的演示值。

**审计者自列的保留项**：1. **纯静态审计**：未跑 mvn、未起服务、未连库、未开浏览器、未改文件。唯一动态动作是用只读方式实跑 tools/claim-ratchet.py 三个模式（selftest / detect / strict）与其 touched_claims 函数（只做 git diff 与读文件），当前主树全绿；我用它复算了 8 条指纹的句读数以坐实「一槽多句」。所有「屏上会显示什么」都是沿 SQL 别名 → JSON 键 → PathQcView 的 colLabel/mdText → el-alert/el-descriptions 这条链推出来的，不是看到的像素。
2. **未能坐实的一环**：Element Plus `el-descriptions` 的 `:title` 能否把约 80 字的标题完整渲染不截断（PathQcView.vue:100）。规划文档 docs/下一步开发规划.md:23 自述「浏览器实测坐实」，是被审对象自述，我未复验。若标题被样式裁到只剩「本期合计　X 至 Y」，子要求 7 要重判，但主缺口（caveat「当日」）不受影响。
3. **口径边界**：CSV 说明列的锚点「功能当前版本未提供」在当前主树已不成立——登记总量、蜡块产出数、报告签发量三项都算得出、菜单进得去、打开即算，多维度也在。我判「维持」是按本项目自 v58 起一以贯之的严口径（屏上说假话 → 维持），且缺口正落在参数点名的两项上。若评分只认说明列字面，这条应上调。
4. **主缺口的严重度可争**：那两段 caveat 的「当日」是描述按日表列的措辞，被同一键的合计格连带；v65 新标题已把危害压低。我按 v63/v64 判蜡块列的同一把尺判（同一屏同一标签两个口径），不另立标准；但它是措辞级缺陷，不是数值错。
5. **我没核的**：① CHANGELOG 自述的 1076 测试、46 次 E2E、sys_config 98 键，一条未验；② V62BlockCaliberTest 三条新用例只读源码没跑——尤其 :650-651 / :687-688 用 `now() - interval '200 days'` 与 Java 侧 `BusinessDates.today()` 配窗，库端会话时区与业务日若相差超过一天会炸（本仓「测试时间字面量」已炸三次，这里用的是相对表达式并留了 ±1 天余量，风险小但我没实证）；③ 2530 与 2563 一字未核；④ AnesQcView 合计块无标题的结转项属麻醉域，不在本参数范围，只记不判；⑤ ANOMALY_NOTE 那行 FACT 无机器对拍，属时效域，同上。
6. **我找到但刻意不当主判据的**（供下一轮）：(a) summaryVsDailyNote 两个输入取自两次独立查询（PathQcController.java:830 与 :840，无 @Transactional），竞态下会打出「出现即是缺陷，请报修」的假缺陷；(b) 「不等于」分支（:938）无解释而与 :537 无条件「它等于按日各行之和」同屏；(c) 「导出汇总」CSV 只含按日行，合计数只以页脚说明文字出现（toCsv :1929-1937 只写 rows）；(d) 标题「这一格的每个数都按整个统计区间算」对 WORKLOAD_DEPT 的三列存量（issued_of_registered / in_progress 是截至查询时刻的状态，:1405-1408）是「整窗登记 + 此刻状态」，caveat :500-503 有解释，我不算它假话；(e) 静态 `title="…"` 不在探测器扫描域（PathQcView.vue:78、:81）；(f) 「每行一个送检科室」对「（未知科室）」兜底行不严格成立。
7. **反例的边界**：我造的「截断触发『大于…报修』」反例不可达（ROW_LIMIT 500 > MAX_SPAN_DAYS 366），已在 evidence 里如实标明；可达的只有跨查询写入竞态，出现概率取决于科室并发，我无法给出频率。

**反驳者·子要求拆解**（推翻）：

病理质控看板上，参数逐字点名的三项里只有「蜡块产出数」一条把合计格口径修真了，「病理登记总量」（WORKLOAD_REGISTER）与「报告签发量」（WORKLOAD_REPORT）两条在默认 30 天窗口下仍是同一屏两句相反的话：合计格正上方的黄色 caveat 逐字把 registered / rejected / diagnosed 定义成「当日登记的标本条数」「当日登记的标本里后来被拒收的条数」「当日写完诊断的标本数」，而紧贴其下、v65 新生成的合计格标题写「这一格的每个数都按整个统计区间算，不是某一天的数」，合计格里挂着同名同中文「登记标本数」「拒收数」「写完诊断数」的正是整窗合计（summaryOf 的 WORKLOAD_REGISTER / WORKLOAD_REPORT 两个 case 全无 group by、谓词是整个 {wc} / {wi}）——同一个键、同一个中文标签、上下两行字给出两个互斥口径，评委照 caveat 读合计格读到的就是错的；这三句 caveat 自 v48（180ef66）起一字未改，v64 只在蜡块那条上消灭了这个形态、v65 又只改了标题一句而没动 caveat 正文，仓内零测试、零 E2E 对这三句求值，平台自己在技术债交接单 :163-167 承认「那句话本身仍不准…照旧挂着」。另一个同义入口也没跟：「导出汇总」的 indicators.csv 根本不含合计格（toCsv 只写 rows），页脚却原样带着「最上面那一格是本期合计…下面那张表才是按日拆分」这段 caveat 与「本期产出蜡块数 N 等于…」的对账句，描述的是一格文件里不存在的数。

【证据】【一、caveat 把合计格的键定义成「当日」，且本轮未动】
modules/medtech/src/main/java/cn/hip/medtech/web/PathQcController.java:484-489
        def("WORKLOAD_REGISTER", "登记总量（按日，含拒收与加急构成）", BY_STAT_DAY,
                "path_specimen.collected_at", ANCHOR_COLLECTED, WORKLOAD_NOTE
                + " 分母口径：registered 是当日登记的**标本条数**，不是申请单数——"
                + "多部位送检一份申请对应多条标本（同一份申请下按部位序号各自唯一），"
                + "这正是病理科的真实工作量单位。rejected 一列是当日登记的标本里**后来**被拒收的条数"
                + "（按登记日归集，不是按拒收日），要看「本期拒了多少」请走 PROCESS_TAT 的 REJECT 节点。"
PathQcController.java:565-571
        def("WORKLOAD_REPORT", "报告签发量（首次报告 / 补充报告分列）", BY_STAT_DAY,
                ...
                + "两列相加当「报告总数」会把工作量算高。diagnosed 一列是当日写完诊断的标本数"
                + "（按 diagnosed_at 落窗），与 issued_reports 不是同一批——两者的差就是卡在签名 / 签发环节的量。");
`git log -S "registered 是当日登记" --oneline -- modules/medtech/src/main/java/cn/hip/medtech/web/PathQcController.java` → 唯一一条 `180ef66 1.4.8 v48 病理 PIS（…）`；`git log -S "diagnosed 一列是当日写完诊断"` 同样只有 180ef66。v64/v65 的 3af9d24、7f499d9 都没碰这三句。

【二、同一个键在合计格里是整窗值】
PathQcController.java:1393-1401
            case "WORKLOAD_REGISTER" -> one(q("""
                    select count(*)                                                as registered,
                           count(*) filter (where s.urgent)                        as urgent,
                           count(*) filter (where s.rejected_at is not null)       as rejected,
                           ...
                    from path_specimen s
                    where {wc}
                    """), w.args());
PathQcController.java:1444-1451
            case "WORKLOAD_REPORT" -> one(q("""
                    select (select count(*) from path_specimen s where {wi})                    as issued_reports,
                           (select count(*) from path_specimen s
                             where s.diagnosed_at >= ?::date and s.diagnosed_at < ?::date + 1)  as diagnosed,
                           ...
                    """), rep(w, 3));
PathQcController.java:285-286
    private static final String W_COLLECTED = "s.collected_at    >= ?::date and s.collected_at    < ?::date + 1";
    private static final String W_ISSUED = "s.report_issued_at >= ?::date and s.report_issued_at < ?::date + 1";
（两个 ? 是窗口的 from / to，即整个统计区间；缺省 30 天：PathQcController.java:605 `f = (from == null || from.isBlank()) ? t.minusDays(29) : LocalDate.parse(from.trim());`）

【三、v65 新标题在同一块上宣告相反口径】
PathQcController.java:871-876
    private static String summaryTitle(Def d, List<Map<String, Object>> shown, Window w) {
        var sb = new StringBuilder("本期合计　").append(w.f()).append(" 至 ").append(w.t())
                .append("（共 ").append(w.days()).append(" 天）。");
        sb.append(w.days() > 1
                ? "这一格的每个数都按整个统计区间算，不是某一天的数；"
                : "这一格按 " + w.f() + " 这一天算（本区间只有这一天）；");
装配（两句话对每条有合计格的指标都上屏）PathQcController.java:827 / :840-843
        m.put("caveat", d.caveat());
        ...
        Map<String, Object> summary = summaryOf(d.code(), w);
        if (summary != null) {
            m.put("summary", summary);
            m.put("summaryTitle", summaryTitle(d, shown, w));

【四、屏上顺序：caveat 紧压在合计格上方，格子标签与按日表同名】
frontend/shell/src/views/medtech/pathology/PathQcView.vue:91-92
        <el-alert v-if="ind.caveat" type="warning" :closable="false" class="cav"
                  :title="mdText(ind.caveat)" />
PathQcView.vue:99-103
        <el-descriptions v-if="ind.summary" :column="4" border size="small" class="cav"
                         :title="mdText(ind.summaryTitle)">
          <el-descriptions-item v-for="k in keysOf(ind.summary as Row)" :key="k"
                                :label="colLabel(k, keysOf(ind.summary as Row))">
            {{ fmt((ind.summary as Row)[k]) }}
frontend/shell/src/views/medtech/pathology/format.ts:476 `  registered: '登记标本数',`；:336 `  rejected: '拒收数',`；:372 `  diagnosed: '写完诊断数',`
format.ts:219-225（mdText 只剥 ** 与反引号，「当日」原样上屏）
export function mdText(v: unknown): string {
  if (v === null || v === undefined) return ''
  return String(v)
    .replace(/\*\*/g, '')
    .replace(/\x60/g, '')
    .replace(/^[ \t]*-[ \t]+/gm, '')
}

【五、只有蜡块那一条立了「合计/按日不得同名同口径」的规矩，测试也只喂那一条】
server/src/test/java/cn/hip/server/V62BlockCaliberTest.java:351 `    void summaryColumnsAreWindowCaliberAndNeverShareANameWithTheDailyTable() {`（全程只查 WORKLOAD_BLOCK）。
`grep -rn "当日登记\|当日写完\|registered 是" server/src/test modules/*/src/test tools/*.py` → 零命中；对 REGISTER caveat 的唯一断言是 V62BlockCaliberTest.java:338 `        assertFalse(caveatOf("WORKLOAD_REGISTER", from, to).contains("不可复现"),`——与「当日」无关。

【六、平台自认】
docs/技术债交接单.md:163-167
- `WORKLOAD_REGISTER` / `WORKLOAD_REPORT` 等**兄弟指标的 caveat 正文**仍把区间合计说成「当日」
  （`registered 是当日登记的标本条数` 等）。（v65 更新：…但兄弟 caveat 正文里的
  「当日」措辞仍未清，这一条照旧挂着；）
docs/下一步开发规划.md:24 `**结转 v66**：…兄弟指标 caveat 的「当日」措辞。`

【七、同义入口：「导出汇总」CSV 不含合计格，页脚却在描述它】
PathQcController.java:1874-1880
        List<Map<String, Object>> rows = rowsOf(d.code(), w.window());
        ...
        return toCsv(d, w.window(), "指标汇总", null,
                summaryVsDailyNote(d, shown, summaryOf(d.code(), w.window())), shown, cut, ROW_LIMIT);
PathQcController.java:1929-1937（toCsv 的表体只有 rows，没有任何 summary 输出）
        if (rows.isEmpty()) {
            sb.append(csv("（该统计区间内无数据）")).append('\n');
        } else {
            var cols = new ArrayList<>(rows.get(0).keySet());
            sb.append(String.join(",", cols.stream().map(c -> csv(zh(c))).toList())).append('\n');
            for (var r : rows) {
PathQcController.java:1945-1946（页脚原样带 caveat 与对账句）
        if (d.caveat() != null) sb.append(csv("口径：" + d.caveat())).append('\n');
        if (derivedNote != null) sb.append(csv(derivedNote)).append('\n');
而 WORKLOAD_BLOCK caveat 原文 PathQcController.java:517-518
                + " **本指标一屏两套口径，列名与中文都已分开**：最上面那一格是**本期合计**"
                + "（整个统计区间只出一个数），四列一律以「本期」起头；下面那张表才是**按日拆分**，"
仓内测试坐实 CSV 里只有按日列：V62BlockCaliberTest.java:463-464
        // ---- CSV：导出的是按日行，表头一律「当日…」，不会再有一个含义是 30 天合计的「当日产出蜡块数」 ----
        assertCsvHeader("WORKLOAD_BLOCK", from, to, "当日产出蜡块数");
前端按钮文案 PathQcView.vue:66-67 `          <el-button link type="primary" size="small" @click="exportCsv('indicators', ind)">` / `            导出汇总</el-button>`

【反向核对】先试着推翻自己的主缺口：(a) caveat 会不会不上屏或按指标分流？indicatorBody :827 对所有 available 指标无条件 `m.put("caveat", d.caveat())`，PathQcView.vue:91-92 只看 `v-if="ind.caveat"`，全文件 grep 没有任何按 code 改写 caveat 的分支；format.ts 的 mdText 只剥 ** 与反引号，「当日」两字原样到屏。(b) 「registered 是当日登记的标本条数」会不会只是在说按日表？caveat 定义的是键名本身，而 :1394 合计 SQL 用的正是同一个别名 registered、:101-102 合计格标签走同一个 colLabel→zh→format.ts:476「登记标本数」；且这条 alert 的位置压在合计格正上方而不是按日表上方，v65 标题又逐字说「这一格的每个数都按整个统计区间算」——两句互斥，不是读法之争。(c) 它在什么状态下是真的？只有 days==1 时（标题走 :876 分支「这一格按 X 这一天算」，此时当日==整窗）；默认打开即 30 天（:605），演示与验收都走默认窗，所以缺口在真实默认路径上成立、只在单日窗这一特例上不成立——正是 v64 型「一句话在某些真实路径上是假的」。(d) v65 是否在别处修了？`git log -S` 两句只命中 180ef66（v48），3af9d24/7f499d9 未碰；交接单 :163-167 与规划 :24 都写明「仍未清、结转 v66」。(e) 有没有守卫能替它兜底？全仓测试与 E2E 对「当日登记 / 当日写完」零命中，唯一喂 caveatOf("WORKLOAD_REGISTER") 的断言（V62BlockCaliberTest:338）查的是「不可复现」。——主缺口站得住。对照组（我核过、判为好的、刻意不指控）：① v65 生成器本身：summaryTitle 的 RowGroup 列名与 12 条有合计格指标的 rows SQL 别名逐一对得上（:1021/:1040 specimen_type、:1086 stain_type、:1156 dept_name、:1224 tech_type、:1064/:1251 issue_day、:1136/:1175/:1190/:1195 stat_day），groupsOnePerRow/hasDayColumn 拿的是截断后真正返回的 shown，与前端 rowsOf(ind) 同一数组；② 截断不会让对账句撒谎：按日行 ≤ MAX_SPAN_DAYS 366（:164）< ROW_LIMIT 500（:174）；③ summaryVsDailyNote 的「不等于 / 大于…请报修」分支只能在 rowsOf(:830) 与 summaryOf(:840) 两条独立语句之间有人写库时触发，是竞态不是确定性缺口，故不记；④ 两条 FACT 对拍用例（V62BlockCaliberTest:514-573、:635-705）确实在真库态下造行、造「一根柱 / 跨日两根柱」逐档求值，并带 v64 原话活对照组，不是源码扫描——登记簿这一部分的自述属实；⑤ 「导出汇总」不含合计格这一条我按项目自己的测试（V62BlockCaliberTest:463-464）与 toCsv 无 summary 形参坐实，但参数原文没点名导出，所以只作为「同义入口没跟」的佐证放在 gap 末尾，不当主判据。

**反驳者·演示可行性**（推翻）：

评委从菜单点进「病理质控」、默认近 30 天窗口、一个按钮都不点，参数点名的「病理登记总量」与「报告签发量」两块，压在合计格正上方的黄色口径条逐字把合计格里的数定义成「registered 是当日登记的标本条数」「rejected 一列是当日登记的标本里后来被拒收的条数」「diagnosed 一列是当日写完诊断的标本数」，而它正下方那格 v65 新生成的标题说「这一格的每个数都按整个统计区间算，不是某一天的数」、格子里「登记标本数 / 拒收数 / 写完诊断数」是整窗 30 天的 count(*)——同一屏相邻两句对同一个数给出相反口径，且 registered / rejected / diagnosed / issued_reports / supplement_reports / molecular_specimens 六个内部 JSON 键原样上屏；v65 只把「蜡块产出数」那一块的 caveat 改对，而它改过并登记为 FACT 档「已配对拍用例」的那段 caveat 里，仍留着一句从未被任何用例求值的假话「下面那张表才是按日拆分，各列一律以「当日」起头」——按日表实际渲染出「日期」「脱水篮批次数」两列不以「当日」起头。参数点名的三项里两项的屏上口径说明在最常见路径上说反话，第三项的口径说明被登记簿标成机器验过而实际没验，「已满足」站不住。

【证据】【A. 两块参数点名指标的 caveat 把整窗合计定义成「当日」——文案在当前主树】
modules/medtech/src/main/java/cn/hip/medtech/web/PathQcController.java:484-489
        def("WORKLOAD_REGISTER", "登记总量（按日，含拒收与加急构成）", BY_STAT_DAY,
                "path_specimen.collected_at", ANCHOR_COLLECTED, WORKLOAD_NOTE
                + " 分母口径：registered 是当日登记的**标本条数**，不是申请单数——"
                + "多部位送检一份申请对应多条标本（同一份申请下按部位序号各自唯一），"
                + "这正是病理科的真实工作量单位。rejected 一列是当日登记的标本里**后来**被拒收的条数"
                + "（按登记日归集，不是按拒收日），要看「本期拒了多少」请走 PROCESS_TAT 的 REJECT 节点。"
PathQcController.java:565-571
        def("WORKLOAD_REPORT", "报告签发量（首次报告 / 补充报告分列）", BY_STAT_DAY,
                "path_specimen.report_issued_at 与 path_report.signed_at", ANCHOR_ISSUED,
                ISSUE_VS_DIAGNOSE_NOTE
                + " issued_reports（首次报告签发）与 supplement_reports（补充报告）**分列不合并**："
                + "补充报告是免疫组化结果回来后的追加意见，不代表一次新的诊断结案，"
                + "两列相加当「报告总数」会把工作量算高。diagnosed 一列是当日写完诊断的标本数"
                + "（按 diagnosed_at 落窗），与 issued_reports 不是同一批——两者的差就是卡在签名 / 签发环节的量。");

【B. 同名键在合计格里是整窗值，不是当日】
PathQcController.java:1393-1401
            case "WORKLOAD_REGISTER" -> one(q("""
                    select count(*)                                                as registered,
                           count(*) filter (where s.urgent)                        as urgent,
                           count(*) filter (where s.rejected_at is not null)       as rejected,
                           count(*) filter (where s.order_id is not null)          as outp_source,
                           count(*) filter (where s.inp_order_id is not null)      as inp_source
                    from path_specimen s
                    where {wc}
                    """), w.args());
PathQcController.java:1444-1450
            case "WORKLOAD_REPORT" -> one(q("""
                    select (select count(*) from path_specimen s where {wi})                    as issued_reports,
                           (select count(*) from path_specimen s
                             where s.diagnosed_at >= ?::date and s.diagnosed_at < ?::date + 1)  as diagnosed,
                           (select count(*) from path_report r
                             where coalesce(r.signed_at, r.created_at) >= ?::date
                               and coalesce(r.signed_at, r.created_at) < ?::date + 1)      as supplement_reports
                    """), rep(w, 3));
缺省窗 30 天：PathQcController.java:547 附近 `f = (from == null || from.isBlank()) ? t.minusDays(29) : ...`（v64 复核已引，本轮未动）。

【C. v65 新生成的合计格标题，正说相反的话，且两者相邻上屏】
PathQcController.java:871-877
    private static String summaryTitle(Def d, List<Map<String, Object>> shown, Window w) {
        var sb = new StringBuilder("本期合计　").append(w.f()).append(" 至 ").append(w.t())
                .append("（共 ").append(w.days()).append(" 天）。");
        sb.append(w.days() > 1
                ? "这一格的每个数都按整个统计区间算，不是某一天的数；"
                : "这一格按 " + w.f() + " 这一天算（本区间只有这一天）；");
上屏顺序 frontend/shell/src/views/medtech/pathology/PathQcView.vue:91-92（caveat）→ :99-105（合计格）：
        <el-alert v-if="ind.caveat" type="warning" :closable="false" class="cav"
                  :title="mdText(ind.caveat)" />
        ...
        <el-descriptions v-if="ind.summary" :column="4" border size="small" class="cav"
                         :title="mdText(ind.summaryTitle)">
          <el-descriptions-item v-for="k in keysOf(ind.summary as Row)" :key="k"
                                :label="colLabel(k, keysOf(ind.summary as Row))">
            {{ fmt((ind.summary as Row)[k]) }}
          </el-descriptions-item>
        </el-descriptions>
mdText 只剥 ** 与反引号，不改一字：format.ts:219-225
export function mdText(v: unknown): string {
  if (v === null || v === undefined) return ''
  return String(v)
    .replace(/\*\*/g, '')
格子中文：format.ts:476 `  registered: '登记标本数',`；PathQcController.java:1997 `            case "rejected" -> "拒收数";`、:2033 `            case "diagnosed" -> "写完诊断数";`。
装配：PathQcController.java:822 `        m.put("caveat", d.caveat());`、:840-843 `        Map<String, Object> summary = summaryOf(d.code(), w);` / `        if (summary != null) {` / `            m.put("summary", summary);` / `            m.put("summaryTitle", summaryTitle(d, shown, w));`。

【D. 平台自认未清】docs/技术债交接单.md:163-167
- `WORKLOAD_REGISTER` / `WORKLOAD_REPORT` 等**兄弟指标的 caveat 正文**仍把区间合计说成「当日」
  （`registered 是当日登记的标本条数` 等）。（v65 更新：……但兄弟 caveat 正文里的
  「当日」措辞仍未清，这一条照旧挂着；）

【E. 第三项（蜡块产出数）：v65 登记为 FACT 的那段 caveat 里一句从未求值的假话】
PathQcController.java:517-519
                + " **本指标一屏两套口径，列名与中文都已分开**：最上面那一格是**本期合计**"
                + "（整个统计区间只出一个数），四列一律以「本期」起头；下面那张表才是**按日拆分**，"
                + "各列一律以「当日」起头。**合计那一格按整个区间算、不按某一天算**："
按日表真实列：PathQcController.java:1165-1176
            case "WORKLOAD_BLOCK" -> query("""
                    select coalesce(b.embedded_at, b.created_at)::date                    as stat_day,
                           count(*)                                                       as blocks_produced,
                           ...
                           count(distinct b.dehydrate_batch)                              as dehydrate_batches
两列的上屏中文：format.ts:474 `  stat_day: '日期',`；format.ts:419 `  dehydrate_batches: '脱水篮批次数',`（后端同 :2075 `case "dehydrate_batches" -> "脱水篮批次数";`）。表头由 PathQcView.vue:115-116 `<el-table-column v-for="c in columnsOf(rowsOf(ind))" ... :label="colLabel(c, columnsOf(rowsOf(ind)))"` 逐列渲染，columnsOf = Object.keys(rows[0])（format.ts:256-259），六列全上屏。
登记簿把这句判成 FACT「成立」：docs/屏上断言登记簿.tsv:41 `af073fb0d8dd	FACT	...WORKLOAD_BLOCK caveat（合计格口径）	...对拍用例：V62BlockCaliberTest#theSummaryVsDailyNoteTellsTheTruthInBothLibraryStates 的库态一`。实跑 tools/claim-ratchet.py（只读）抽出的 af073fb0d8dd 正文以「各列一律以「当日」起头。」开头——这句就在该指纹槽里。而两条对拍用例都不碰它：V62BlockCaliberTest.java:144-145 `    private static final List<String> DAY_COLS = List.of(` / `            BLOCKS_PRODUCED, "embedded", "specimens_of_day", "blocks_per_specimen_of_day");`，:453-457 只对这四列 `assertTrue(label.startsWith("当日"), ...)`；:714-731 assertCaliberClaimMatchesData 只查「必然小于」「等于…之和」。正是登记簿自己写的 limits (a)（docs/屏上断言登记簿.tsv:25-26「段落式 caveat 一个槽位载多句断言时，被求值的只有登记的那句」）在本轮 FACT 行上发作。

【反向核对】【先证明我没读错、没有别处兜底】① caveat 与合计格的渲染路径只有 PathQcView.vue:91-92 与 :99-105 一条，caveat 无按 code 分流、无插槽覆盖；`indicatorBody` 对所有 available 指标无条件 `m.put("caveat", d.caveat())`（:822）。② WORKLOAD_REGISTER / WORKLOAD_REPORT 都有 summaryOf 分支（:1393、:1444），不落 default→null，合计格必渲染。③ 零种子库单 ADMIN 路径本轮未变：无新迁移（仍 V168），菜单 169 与 ADMIN 授权、PathologyController 的 CHARGED 过滤、GrossingPanel 出块入口均不在 v65 改动的语义范围（v65 只动 GrossingPanel 修订弹窗、ProcessingPanel 挂接下拉、PathQcView 标题绑定），v64 复核给出的 12 步菜单路径仍成立；第一条标本登记当天即可在默认 30 天窗看到 A/C 两句并排。④「各列一律以「当日」起头」的反例我按前端真实渲染链核过：rows[0] 六个键全进 columnsOf，`dehydrate_batches` 在两侧字典都是「脱水篮批次数」，不存在带「当日」的前端覆盖。

【最可能出问题而这次是好的（我试着打破、没打破）】(a) v65 的标题生成器：12 条有合计格的指标我逐条对了 rowsOf 的 group by 列是否真出现在行里且逐行唯一（specimen_type :989、stain_type :1069、dept_name :1147、tech_type :1212、stat_day/issue_day 各按日指标；WORKLOAD_REPORT 用 generate_series 每日一行、天然唯一），byDay 分支与非按日分支的宣告都与真实表一致；「分组维度未登记」与「大于…请报修」两条假分支在真实路径上不可达——MAX_SPAN_DAYS=366（:164）< ROW_LIMIT=500（:174），按日行永不截断，WORKLOAD_PATHOLOGIST / PROCESS_TAT 没有合计格。(b)「现算说明与旁边的数取自两次查询」：屏上 summaryVsDailyNote 用的正是同一次装配里放进返回体的 `summary` 与 `shown`（:838-845），无漂移；CSV 端 summaryOf 只查一次、仅供该句（:1875-1876）。(c)「本期产出蜡块数等于按日各行之和」两边都是 count(*) 且无截断，造不出反例；单日窗分支「这一格按 X 这一天算」也对。(d) 棘轮本身：只读实跑 `--since v1.6.4` 8/8 已登记、`--detect` 1 条（ANOMALY_NOTE）、`--selftest` 全 OK；但它按「连续 + 行」合并指纹，d8b4b42e3500 一枚指纹覆盖整个 summaryTitle+summaryVsDailyNote 方法块，af073fb0d8dd 覆盖整段 caveat——这正是我在 E 里抓到的一槽多句。(e) v64 复核陷阱 5 的 docs/演示/病理演示数据.md 仍停在 v59（`git log -1` = 0c62d99），:95 仍教 `blocks=6、embedded=6、specimens=3、blocks_per_specimen=2.0`、:85 教用 demo_quality 登录——本轮一字未动，但它是文档不是屏，故不作主判据。(f) 「导出汇总」CSV（:1868-1876 → toCsv :1917-1935）只写 rows 与页脚，本期合计四个数不进文件——是新发现的可用性缺口，但参数未点名导出，只作旁证。

【口径边界】A 项的数值本身没错，错的是同屏对同一个数的两句相反定义 + 六个裸内部键；这是本项目 v58 起六轮判 2576 的同一把尺（屏上宣告与实现不符即维持），也是本次任务书明确要求「登记了不等于不算缺口」的那条结转项。若评分只认 CSV 说明列「功能未提供」的字面，这条应上调；按项目自用严口径，维持。

**反驳者·数据真实性与口径**（推翻）：

病理质控看板上，参数点名的「病理登记总量」那一格（WORKLOAD_REGISTER 合计格：整窗 count(*)，表头「登记标本数」「拒收数」，与它下面按日表的列逐字同名）正上方的黄色口径说明至今写着「registered 是当日登记的标本条数」「rejected 一列是当日登记的标本里后来被拒收的条数」，而紧贴其下 v65 新生成的标题却说「这一格的每个数都按整个统计区间算，不是某一天的数」——同一个数在相邻两行被定义成两种口径，「报告签发量」那格同病（「diagnosed 一列是当日写完诊断的标本数」压在整窗合计上）；而这句「不是某一天的数」本身又被 v65 无条件说给 30 天窗口里只有一根柱的「蜡块产出数」合计格（平台自己的演示库态），其正下方 v65 自己现算的对账行写着「本期产出蜡块数 N 等于按日各行之和 N……与按日表里 1 行的数值相同」——v65 登记簿 af073fb0d8dd 已判这句话在该库态下不成立，它只是从 caveat 挪进了标题，配的对拍用例从没在那个库态下读过 summaryTitle。

【证据】【A. 登记总量：口径说明把整窗合计格定义成「当日」，且未被 v65 动过】
modules/medtech/src/main/java/cn/hip/medtech/web/PathQcController.java:484-489
        def("WORKLOAD_REGISTER", "登记总量（按日，含拒收与加急构成）", BY_STAT_DAY,
                "path_specimen.collected_at", ANCHOR_COLLECTED, WORKLOAD_NOTE
                + " 分母口径：registered 是当日登记的**标本条数**，不是申请单数——"
                + "多部位送检一份申请对应多条标本（同一份申请下按部位序号各自唯一），"
                + "这正是病理科的真实工作量单位。rejected 一列是当日登记的标本里**后来**被拒收的条数"
                + "（按登记日归集，不是按拒收日），要看「本期拒了多少」请走 PROCESS_TAT 的 REJECT 节点。"
同一个键就是合计格那一列、落窗整个区间、无 group by —— PathQcController.java:1393-1401
            case "WORKLOAD_REGISTER" -> one(q("""
                    select count(*)                                                as registered,
                           count(*) filter (where s.urgent)                        as urgent,
                           count(*) filter (where s.rejected_at is not null)       as rejected,
                           count(*) filter (where s.order_id is not null)          as outp_source,
                           count(*) filter (where s.inp_order_id is not null)      as inp_source
                    from path_specimen s
                    where {wc}
                    """), w.args());
按日行同名同中文 —— PathQcController.java:1122-1125
                    select s.collected_at::date                                         as stat_day,
                           count(*)                                                      as registered,
                           count(*) filter (where s.urgent)                              as urgent,
                           count(*) filter (where s.rejected_at is not null)             as rejected,
中文表头两处同一个字典项（合计格 :101-102 与按日表 :115-116 都走 colLabel→zh）—— frontend/shell/src/views/medtech/pathology/format.ts:476 `  registered: '登记标本数',`；:336 `  rejected: '拒收数',`；后端 CSV 同源 PathQcController.java:2110 `            case "registered" -> "登记标本数";`
`git log --oneline -S"registered 是当日登记" -- modules/medtech/.../PathQcController.java` → 只有 `180ef66 1.4.8 v48`，v65 diff 中 :486-489 为未改动的上下文行（diff-驱动棘轮看不见，登记簿 docs/屏上断言登记簿.tsv 无此行）。

【B. 渲染顺序：caveat 紧贴在合计格之上，中间只有 v65 的新标题】
frontend/shell/src/views/medtech/pathology/PathQcView.vue:91-92
        <el-alert v-if="ind.caveat" type="warning" :closable="false" class="cav"
                  :title="mdText(ind.caveat)" />
PathQcView.vue:99-104
        <el-descriptions v-if="ind.summary" :column="4" border size="small" class="cav"
                         :title="mdText(ind.summaryTitle)">
          <el-descriptions-item v-for="k in keysOf(ind.summary as Row)" :key="k"
                                :label="colLabel(k, keysOf(ind.summary as Row))">
            {{ fmt((ind.summary as Row)[k]) }}
mdText 只去 ** / 反引号 / 行首 "- "，一个字不删 —— format.ts:219-225。
标题生成器（v65 新增）—— PathQcController.java:871-876
    private static String summaryTitle(Def d, List<Map<String, Object>> shown, Window w) {
        var sb = new StringBuilder("本期合计　").append(w.f()).append(" 至 ").append(w.t())
                .append("（共 ").append(w.days()).append(" 天）。");
        sb.append(w.days() > 1
                ? "这一格的每个数都按整个统计区间算，不是某一天的数；"
                : "这一格按 " + w.f() + " 这一天算（本区间只有这一天）；");
→ 屏上相邻两行：caveat「registered 是当日登记的标本条数」 / 标题「这一格的每个数都按整个统计区间算」 / 格子「登记标本数 N」（整窗）。

【C. 报告签发量同病】PathQcController.java:565-571
        def("WORKLOAD_REPORT", "报告签发量（首次报告 / 补充报告分列）", BY_STAT_DAY,
        ...
                + "两列相加当「报告总数」会把工作量算高。diagnosed 一列是当日写完诊断的标本数"
                + "（按 diagnosed_at 落窗），与 issued_reports 不是同一批——两者的差就是卡在签名 / 签发环节的量。");
其合计格 diagnosed 是整窗 —— PathQcController.java:1444-1451
            case "WORKLOAD_REPORT" -> one(q("""
                    select (select count(*) from path_specimen s where {wi})                    as issued_reports,
                           (select count(*) from path_specimen s
                             where s.diagnosed_at >= ?::date and s.diagnosed_at < ?::date + 1)  as diagnosed,
平台自认：docs/技术债交接单.md:163 `- \`WORKLOAD_REGISTER\` / \`WORKLOAD_REPORT\` 等**兄弟指标的 caveat 正文**仍把区间合计说成「当日」`；CHANGELOG.md:47 `兄弟指标 caveat 正文的「当日」措辞；`

【D. v65 新标题把「不是某一天的数」无条件说给「一根柱」库态——登记簿自己判过这句不成立】
docs/屏上断言登记簿.tsv:41
af073fb0d8dd	FACT	...PathQcController.java:WORKLOAD_BLOCK caveat（合计格口径）	**v64 判定：不成立**——原话「合计那一格不是任何一天的数」在演示库态（区间内只有一天有产出）下是假的，合计与按日唯一那一行逐字相等。v65 改成「按整个区间算、不按某一天算」并写明相等的情形。...
caveat 确按此改了 —— PathQcController.java:519-521
                + "各列一律以「当日」起头。**合计那一格按整个区间算、不按某一天算**："
                + "「本期产出蜡块数」与某一天的「当日产出蜡块数」不是同一个口径——"
                + "区间内只有一天有产出时两数会逐字相等，那也不表示它是「今天的产量」。"
但标题 :875 `? "这一格的每个数都按整个统计区间算，不是某一天的数；"` 只按 w.days()>1 分支，不看数据是否只落一天；`git log -S"不是某一天的数"` → 只有 `7f499d9 v65 车道 C`。
登记簿对标题这句的「什么状态下是假的」只想到窗口长度 —— docs/屏上断言登记簿.tsv:40（d8b4b42e3500）`...区间只有一天时不再说「不是某一天」；没有行时不提「下面那张表」。`
对拍用例只测一天窗口 —— server/src/test/java/cn/hip/server/V62BlockCaliberTest.java:565-570
        String oneDay = BusinessDates.today().toString();
        String single = String.valueOf(indicator("WORKLOAD_BLOCK", oneDay, oneDay).get("summaryTitle"));
        ...
        assertFalse(single.contains("不是某一天的数"),
「一根柱」库态用例从不读 summaryTitle —— V62BlockCaliberTest.java:655-669 只取 `one.get("summaryVsDailyNote")` 与 `caveatOf("WORKLOAD_BLOCK", ...)`；而 :515-521 的维度用例本身就造出「3 天窗口、数据只落今天」的库态（rows=1）却不断言这一句。
演示库态即此形态 —— tools/demo-pathology.py:577 `        print('  注意：所有时刻都是运行当下，30 天趋势图只有今天这一根柱——脚本不能也不该回溯日期。')`；默认窗 30 天 format.ts:710-712。
同屏正下方的现算行 —— PathQcView.vue:108-109 `<el-alert v-if="ind.summaryVsDailyNote" ... :title="mdText(ind.summaryVsDailyNote)" />`；文案 PathQcController.java:937-943、960-962
        sb.append("「本期产出蜡块数」").append(periodBlocks)
                .append(periodBlocks == dayBlocks ? " 等于" : " 不等于")
                .append("按日各行「当日产出蜡块数」之和 ").append(dayBlocks).append("；");
        ...
                    : " 与按日表里 " + same + " 行的数值相同（它用的是整窗分母，相同是本次库态使然）。");
→ 演示屏上 WORKLOAD_BLOCK 块自上而下：标题「（共 30 天）。这一格的每个数都按整个统计区间算，不是某一天的数；下面那张表按日拆分，每行一个统计日，共 1 行。」→ 合计格 → 对账行「…等于按日各行之和 N；…与按日表里 1 行的数值相同」。

【反向核对】【先试着推翻自己】(1) caveat 会不会不在合计格上方、或被前端裁掉？——PathQcView.vue 同一个 `<template v-else>`（:88）内顺序是 :89-90 锚点 alert → :91-92 caveat → :99-105 合计格 → :108-109 对账行，无 v-if 分流；mdText（format.ts:219-225）只去 **、反引号、行首 "- "。(2) 合计格的 registered 会不会其实是按日值？——:1393-1401 `where {wc}` 无 group by，`one()` 取整窗单行；rows :1122-1136 才 group by 1。(3) 有没有别处纠正这句「当日」？——WORKLOAD_REGISTER 全段 caveat（:484-492）无一句说合计格口径；唯一说的是 v65 标题 :875，而它与 caveat 正相反。(4) 有没有守卫钉住？——V62BlockCaliberTest 的 RAW_KEY_IN_CAVEAT 与合计/按日列名互斥断言只喂 `caveatOf("WORKLOAD_BLOCK")`（:433-446）；登记簿无 WORKLOAD_REGISTER/REPORT caveat 行；棘轮是 diff 驱动，这两段 v65 一字未动（git log -S 只命中 v48 的 180ef66），正落进它自陈的盲区 (b)。平台自己在交接单 :163 与 CHANGELOG :47 承认「那句话本身仍不准」——我引用的是它承认的原文，不是转述。(5) 标题那句「不是某一天的数」会不会只是「不是按日口径」的意思、算不上假话？——我按的是本项目自己的判据：登记簿 :41 已把 v64 同义原话「合计那一格不是任何一天的数」判为在演示库态下「不成立」，车道 C 据此把 caveat 改成「按整个区间算、不按某一天算」并补写相等情形（:519-521），却在同一提交（7f499d9）里让标题 :875 保留原断言且只按窗口天数分支；两处措辞由同一车道、同一轮、同一判据处置而结果相反，这不是我另立尺子。

【核过、这次确实是好的（不指控）】① summaryTitle 的维度分支对 12 条有合计格的指标都成立：groupsOnePerRow（:897-903）逐行去重、hasDayColumn（:906-911）查 stat_day/issue_day；WORKLOAD_DEPT 按 dept_name、SPECIMEN_RECEIVE/FIXATION 按 coalesce 后的 specimen_type、SLIDE_QUALITY 按 stain_type、WORKLOAD_TECH 按 tech_type，行间取值确不重复；PROCESS_TAT/WORKLOAD_PATHOLOGIST 无 summary 故无标题；行数取截断后的 shown（:833、:893），与屏上表同一批。② summaryVsDailyNote 逻辑无错：整窗 count(distinct specimen_id) 与各日之和相等 ⇒ 确无标本跨两统计日（否则和至少多 1），「小于」分支解释正确，比值用 BigDecimal.compareTo（:978-981）避开标度差；CSV 页脚与屏上走同一函数、同一批 shown（:1874-1880）。③ 「本期产出蜡块数 = 按日各行之和」两侧同谓词 W_BLOCK、同 count(*)，逐块无去重，恒等——唯一例外是 rowsOf（:830）与 summaryOf（:840）是两条自动提交查询、无 @Transactional，中间若有蜡块落库会打出「不等于」甚至「大于…请报修」（:948-950），这是「现算说明与旁边的数取自两次查询」的形态，但只在并发窗口内出现、不可稳定复现，故不作主指控。④ coverage.blocks 与 WORKLOAD_BLOCK 合计同谓词同聚合（:757-764 vs :1424-1432），前端 COVERAGE_SECTIONS.labels（PathQcView.vue:274）与合计格中文同源。⑤ WORKLOAD_DEPT caveat「三列之和等于 registered」在 SQL 上成立（:1404-1408 三态互斥穷尽）。⑥ 合计四列 *_in_period / 「本期…」两侧字典逐字同源（format.ts:434-435、453-456；PathQcController.java:2089-2095），CSV 与页面同字。⑦ 登记簿三行（d8b4b42e3500 / af073fb0d8dd / c7b11fee96ef）指向的用例确是 @SpringBootTest+@Transactional 真库态求值（V62BlockCaliberTest.java:120-123，:650-653 用库端相对表达式造两种库态），不是源码扫描——只是对 summaryTitle 漏了「多日窗口、数据一根柱」这一档。

---
