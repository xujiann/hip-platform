# v61 交付后复核（2026-09-14，main=14e70bf） —— 结论：**3 条中 0 条上调**（上调：无；维持「部分响应」：[2530, 2563, 2576]）

方法与 2553 那次相同：只传序号，审计者自读 `技术偏离表-v3.csv` 回填参数原文并核当前主树，每条再过三个独立反驳者（子要求拆解 / 演示可行性 / 数据真实性与口径），**零推翻才上调**。
证据落 `audit_r2_wave14.json`，本轮不出改写。

## 结论表

| 序号 | 审计者 | 子要求拆解 | 演示可行性 | 数据真实性与口径 | 结论 |
|---|---|---|---|---|---|
| 2530 | 维持 | 推翻 | 推翻 | 推翻 | 维持部分响应 |
| 2563 | 维持 | 推翻 | 推翻 | 推翻 | 维持部分响应 |
| 2576 | 可上调 | 推翻 | 推翻 | 推翻 | 维持部分响应 |

## 逐条：参数原文、审计者的主张、反驳者的硬缺口（原话，未转述）

### 2530

**参数原文（CSV 逐字）**：支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。

**审计者**：维持。最接近缺口的一点：v61 把「补取材追加后口径与事实相反」这条打回只修了后端一半：控制器新增了 textIsCumulative 并把 fieldsCurrent 改判 false（PathologyProcessController.java:1594-1599），但全仓 Grep textIsCumulative 的 9 处命中里没有一处在 frontend/——前端零消费。于是评委在「查看大体所见」抽屉里切到上一版时，屏上仍写「被取代版本（第 1 版，N 项）：已被第 2 版字段取代，仅供调阅」（GrossingPanel.vue:123 无条件拼串），而那一版字段的文字此刻正原封不动地留在当前 gross_finding 的累积全文里，根本没被取代；更糟的是诚实的那条 fieldsNote 黄条 v-if 绑了 viewVersionIsLatestFields（:128-129），一切到旧版就消失——说真话的提示和说假话的标签恰好互斥出现。叠加同一抽屉里被取代版本的「录入」列恒为「—　—」（后端 byRevision 字段行只取 revision_seq/seq/label/value，:1540-1545，前端却照样渲染 operatorName/createdAt，:142-144），子要求④「调阅」在被取代版本上既缺人缺时刻、又会陈述与库内事实相反的结论。按本轮口径（只做了后端 / 只做了一半 → 维持），2530 不具备上调条件。

子要求核对：
- [✓] 子要求①「支持文字化记录」——取材工位要有真实的文字录入入口（不是 v3 说明点名的「固定预设值」） —— frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:74「取材登记」按钮（工作列表每行）→ :196 取材登记/修订对话框 → :253-263 取材模板下拉、:273-279 逐字段 el-input（maxlength=300，可删自定义项）、:299-302 自由描述 textarea；后端 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:345 起 POST /api/pathology/process/grossing（GrossingReq.gross/grossText/templateCode/blocks[].tissueDesc/remark，记录见 :290-299）。零固定预设值：拼装
- [✓] 子要求②「完整的取材过程信息」——首写 + 补取材追加 + 修订，含字段、自由描述、模板码、每块组织描述、备注、取材人与时刻 —— 首写/追加同一端点：PathologyProcessController.java:441-476（append=false 且已有内容→5222；append=true→grossToWrite = 既有 + 「。补取材：」+ 本次，并复检总长）；修订入口 PUT /grossing/{id}/fields（GrossingPanel.vue:76「修订取材描述」→ :356「提交修订」→ :692-706 submitRevise）；修订留痕 insertGrossRevision（:2143 起，changed_at 由库端 now()）；每版字段行落 path_gross_field（:2120-2135 storeGrossFields，带 operator_id、created_at default now()，见 V164__path_gross_fields.sql:26-
- [✓] 子要求③「确保描述内容的结构化存储」——逐字段落库、版本化，且字段名集合不被模板封闭 —— 迁移 server/src/main/resources/db/migration/V164__path_gross_fields.sql:26-36 建 path_gross_field(specimen_id, seq, label(32), value(300), created_at, operator_id)；V166__path_gross_revise.sql 加 revision_seq、唯一约束改 (specimen_id, revision_seq, seq)、修订行加 template_code。v61 车道 A（提交 80edea7）补上此前缺的前端自定义字段名入口：GrossingPanel.vue:285-294「添加字段」输入框 + :470-479 addCustomField（校验与后端 assembleGross 同源：非空 / ≤32 字 / trim
- [✗] 子要求④「确保描述内容的调阅」——从菜单可调阅当前版与被取代版本的结构化记录，且屏上所述与事实一致 —— 调阅入口在（GrossingPanel.vue:86-150「查看大体所见」抽屉、TrailPanel.vue:195-250 轨迹「大体所见修订」表），但被取代版本这一半是残的且会说假话：(a) 后端 fieldsByRevision 的字段行只 select revision_seq/seq/label/value（PathologyProcessController.java:1540-1545），不带 operator_id/created_at，而前端对所有版本渲染同一张表、含「录入」列 {{ fmt(row.operatorName) }}　{{ fmtTime(row.createdAt) }}（GrossingPanel.vue:142-144）→ 切到被取代版本该列恒显示「—　—」（最新版走 view.fields，:1489-1503 带 operatorName/cr

演示路径（审计者）：全新零种子库走得通，单个 ADMIN 账号一次登录即可（病理工作台菜单 168 授 ADMIN/TECHNICIAN/DOCTOR_OUTP：V144__pathology_base.sql:198 + V156__v52_menus.sql:22-26；路由 frontend/shell/src/router/index.ts:59-62 /pathology/workbench）。**不要先跑 tools/demo-pathology.py**：该脚本取材只传 grossText、不传结构化字段（tools/demo-pathology.py:160-163、:296），用它铺的标本打开「查看大体所见」会显示「历史标本或纯自由文本，无字段级记录」，正好把要演示的东西演示没了——结构化字段必须现场手工录入。步骤：(1) 门诊业务 → 排班 / 挂号 / 门诊医生站：给患者开一条项目名含「病理」或「活检」的检查项（PathologyRegistryController.java:169-170 待登记判据是 outp_order.status='CHARGED' 且 item_name like '%病理%' or '%活检%'）→ 收费结算。(2) 门诊业务 → 病理工作台 → ①登记：待登记里选该申请，**务必手工填「标本描述」**（RegistryPanel.vue:206-208 该项没有 required，后端 PathologyRegistryController.java:781 只校验 ≤255 字，不填则后续六处展示全是「—」，正撞 v3 说明的缺项②）→ 登记 → 同页接收核对。(3) ②取材：范围「待取材」找到该标本（行上「标本描述」列可见，GrossingPanel.vue:53-55）→「取材登记」→ 选模板（如乳腺肿物，字段自动出若干输入框）→ 逐字段填 → 在「添加字段」里手输一个模板外的字段名（如「淋巴结清扫组数」）并「添加」，再填值 → 自由描述补一句 → 加 2 块蜡块并各写组织描述 → 登记取材。(4) 弹出「取材完成」：下半「已落库的字段级记录」是回读 GET /grossing/{id} 的库内行（GrossingPanel.vue:600-612 loadResultFields），能看到自定义字段确实落库。(5) **必须手动把「范围」从「待取材」改成「全部」**——submit 成功后 await load() 仍用 scope='pending'（GrossingPanel.vue:400、:826），而后端 pending 判据是「一块蜡块都没有」（PathologyProcessController.java:261-263），刚取完材的标本立刻从默认列表消失，且「查看大体所见 / 修订取材描述」两个按钮都长在行上（:74-77），结果弹窗也不给跳转，不切范围后面三步无处可点。(6) 切到「全部」后点「查看大体所见」：抽屉上半是标本描述 + 大体所见全文，下半「字段级记录」标「最新字段版（第 1 版，N 项），与当前文本同版」，来源「取材」、模板码（**显示的是英文码如 BREAST_MASS**，:125）。(7) 点「修订取材描述」→ 表单按库里第 1 版字段预填 → 改一个值 → 提交 → 提示「已生成第 2 版取材描述」。(8) 再开「查看大体所见」，版本下拉切到第 1 版：可看到旧版字段值（调阅成立），但「录入」列两格都是「—」。(9) 补取材场景（要验 v61 那条口径修补）：再点

审计者自列的保留项：
- 本次为只读审计：未跑 mvn、未起服务、未连库，全部结论来自当前主树（main=14e70bf）的源码、迁移、测试与 E2E 脚本文本；演示路径是按代码推演的可行性，不是实机跑通。
- v61 车道 A 确实解掉了 v60 复核最硬的那条（字段名集合封闭、没有自定义字段名入口）——GrossingPanel.vue:285-294 + :470-479，并由 V61GrossFieldEntryTest 带正反对照组守卫。子要求①②③我核为 met，本条维持只卡在④。
- v60 复核「演示可行性」镜头提出的四点里，只有第 (1) 点（自定义字段名入口）在 v61 被修；第 (2) 点（取材完成后标本从默认 pending 范围消失、后续按钮无处可点）、第 (3) 点（被取代版本缺录入人/时刻）、第 (4) 点（英文码上屏：结果弹窗 codePrefixSource 原样打 PATH_NO|BARCODE，GrossingPanel.vue:341；模板列只显英文码 :125、TrailPanel.vue:240-241，中文模板名已在 templates 里却不用）我复核后确认**均仍在**。同一轮车道 B 为 2563 做的正是「节点备注去裸英文枚举」，口径在 2530 这一侧没有拉齐。
- 被取代版本的「谁何时录」并非全仓缺失：TrailPanel.vue:243-248 的修订表按版给了时刻与操作人（后端 revisions 带 changedAt/changedByName，PathologyProcessController.java:1524-1526），且 fieldsByRevision 的版本头也已带 changedAt/changedByName（:1552-1553）——只是 GrossingPanel 的版本头（:124-126）只渲染来源与模板、不渲染这两项。也就是说这一项是「前端没接」，不是「后端没有」，修起来很轻。
- GrossingPanel.vue:466 的注释写「自定义字段名本身非法 5276」，与提交 dcbf470 及 docs/错误码分段.md:59「5276 未启用，仍空置」冲突——非法字段名实际统一返 5222。注释不上屏，不影响演示，但属遗留的不实注释。
- GrossingPanel.vue:53 注释仍称标本描述是「登记时强制录入（4554 必填）」，实测 4554 只在旧端点 PathologyController.java 的 /pathology/specimens 上；工作台走的 PathologyRegistryController 只校验长度（:781），前端 RegistryPanel.vue:206 也未标 required。演示时若忘填，v3 说明点名的缺项②「标本描述在流转列表中的调阅展示」六处会全显示「—」。
- tools/demo-pathology.py 至今只传 grossText、不传结构化字段（:160-163、:296），先跑脚本再演示会看到「无字段级记录」的空表——这是演示陷阱，不是功能缺陷，但必须写进演示须知。
- V164/V166 两条迁移零回填（迁移头注释 V164__path_gross_fields.sql:20 自陈历史标本零行），旧库上的存量标本调阅恒显示「无字段级记录」。全新库演示不涉及。
- 本条为单 agent 只读审计，★ 实质性条目上调另需负责人签认与多维复核，本结论不等于已改表。

**反驳者·数据真实性与口径**（推翻）：

补取材追加之后打开「修订取材描述」，弹窗顶上那条黄色警示写的是一句自相矛盾的假话：`库里的字段行属于第 2 版，与当前第 2 版文本不同版，未按它预填`（同一个版号被说成「不同版」）；同时该弹窗把库里第 2 版的结构化字段全部丢弃、只把累积全文整段塞进「自由描述」，提交后落的新版字段行数为 0——参数摘要要求「确保描述内容的结构化存储」的那条路，在最典型的补取材场景下被这个入口反向拆掉了。审计者只堵了查看抽屉（fieldsByRevision 缺 operator/createdAt、「被取代版本」标签与 textIsCumulative 零消费），没查修订入口这一处。

证据：【参数摘要逐字（我自己从 CSV 第 2530 行第 4 列读出）】「支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。 +---------------------------------------------------------------------------------------------------------------------------------------------------------+」（响应结论列＝部分响应；说明列另述缺项①取材过程描述录入入口为固定预设值、②标本描述在流转列表中的调阅展示）。

【根因】v61 改了 fieldsCurrent 的语义，但只改了后端一处消费者。modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:1594-1599：
  boolean sameSeq = !fields.isEmpty() && fieldsRevisionSeq != null && fieldsRevisionSeq.equals(textRevisionSeq);
  boolean fieldsCurrent = sameSeq && !lastIsAppend;
即 v61 起「fieldsCurrent=false」不再蕴含「两个版号不同」——补取材追加时两版号相等但 fieldsCurrent=false。前端有两处写死了旧蕴含关系，其中修订入口是审计者未涉及的：

【缺口一：修订弹窗的警示语句本身是假的】frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:662-664（openRevise）：
  revisePrefillNote.value = split.parsed
    ? (d.fieldsAvailable === true && d.fieldsCurrent !== true
        ? `库里的字段行属于第 ${fmt(d.fieldsRevisionSeq)} 版，与当前第 ${fmt(d.textRevisionSeq)} 版文本不同版，未按它预填；已把当前文本整段放入自由描述`
        : '')
    : ...
渲染在 :222-223 的 el-alert(type=warning)。补取材追加场景下 fieldsRevisionSeq === textRevisionSeq（都是第 2 版）、fieldsAvailable=true、fieldsCurrent=false，于是屏上原样出现「第 2 版……与第 2 版文本不同版」。屏上所述与库内事实互相打脸，正是本镜头要抓的口径失真。

【缺口二：同一入口把结构化记录清零】GrossingPanel.vue:644 `const fields = (d.fieldsCurrent === true ? (d.fields ?? []) : []) as Row[]` → 追加后 fieldsCurrent=false，第 2 版真实存在的字段行被当成不可用、一条不预填；:620-628 splitGross 拿到空 fields 直接 `return { gross, free: text, parsed: true }`，全文进自由描述、form.gross={}。submitRevise（:672-706）于是只送 grossText，后端 storeGrossFields（PathologyProcessController.java:2122-2135）`if (gross == null || gross.isEmpty()) return 0`——第 3 版零字段行。再打开查看抽屉：fieldsRevisionSeq=2、textRevisionSeq=3、fieldsCurrent=false、提示「以文本为准」（fieldsStaleNote :2166-2175 的 GROSSING_EDIT 分支）。也就是说：补取材后想把累积全文理顺的唯一入口，必然把当前文

核过的点：自行从 docs/验收/技术偏离表-v3.csv 按序号列取 2530 行，逐字读出参数摘要与响应说明（未采信审计者转述）；PathologyProcessController.java:1484-1620 grossingView：fields/fieldsRevisionSeq/textRevisionSeq/fieldsCurrent/textIsCumu；PathologyProcessController.java:1594-1599 fieldsCurrent = sameSeq && !lastIsAppend 的 v61 语义变更及其蕴含关系被打破；PathologyProcessController.java:345-560 POST /grossing：首写/追加分支、grossWritten 乐观比对、insertGrossRevision + storeGrossFields ；PathologyProcessController.java:571-626 PUT /grossing/{id}/fields 修订端点：GROSSING_EDIT 版本、字段随版落库、空 gross 即 0 行；PathologyProcessController.java:2076-2101 assembleGross 的 20 项/32 字/300 字/2000 字校验与去重（越界抛错转 5222，非静默截断）；PathologyProcessController.java:2122-2135 storeGrossFields（gross 为空即 return 0）与 :2152-2162 insertGrossRevision（seq 子查询、c；PathologyProcessController.java:2166-2175 fieldsStaleNote 三档措辞与 lastSource 的对应；GrossingPanel.vue:110-150 查看抽屉字段级记录区：版本下拉、viewVersionIsLatestFields 判定、「被取代版本」标签、fieldsNote 警示条的 v-if 条件；GrossingPanel.vue:608-670 openRevise：fieldsCurrent 门控的字段预填、splitGross 回退、revisePrefillNote 文案（:662-664）；GrossingPanel.vue:222-223 revisePrefillNote 的渲染位置（修订弹窗顶部 warning alert）；GrossingPanel.vue:672-706 submitRevise 送参与提示文案

**反驳者·子要求拆解**（推翻）：

子要求③「确保描述内容的结构化存储」在**补取材追加后的第一次「修订取材描述」**上整段失守，且 v61 自己的修复把屏上口径改成了自相矛盾的假话——这两点审计者的四条证据一条都没碰（他只查了「查看大体所见」抽屉，没查「修订取材描述」入口）。(a) 功能：GrossingPanel.vue:644 `const fields = (d.fieldsCurrent === true ? (d.fields ?? []) : []) as Row[]` —— 而 v61 恰恰把补取材追加场景的 fieldsCurrent 硬判为 false（PathologyProcessController.java:1596-1597 `fieldsCurrent = sameSeq && !lastIsAppend`，V61GrossFieldEntryTest.java:153 钉死 FALSE）。于是追加后点「修订取材描述」，fields=[] → splitGross 走 :623 `if (!fields.length) return { gross, free: text, parsed: true }` → 库里已落的两版共四项字段**一项都不预填**，累积全文整段被塞进「自由描述」textarea；用户照单提交，后端 reviseGrossFields 对 gross=null 不设任何防线（:585 assembled 只要求「字段与自由描述至少填一项」，:621 storeGrossFields 落 0 行），新的第 3 版 = 当前 gross_finding 全文、结构化字段行数 0。即：走完平台自己提供的唯一取材端修订入口，结构化存储就**退回一段非结构化文本**，正是参数摘要要消灭的形态，全程零警示。(b) 口径：同一路径上 :662-664 那条 el-alert（:222 渲染）打印「库里的字段行属于第 {fieldsRevisionSeq} 版，与当前第 {textRevisionSeq} 版文本**不同版**」——而追加场景这两个值**相等**（V61GrossFieldEntryTest.java:150-151 assertEquals(2, fieldsRevisionSeq) 与 assertEquals(2, textRevisionSeq) 逐位钉死），屏上原样显示「属于第 2 版，与当前第 2 版文本不同版」。这句话是 v59 按「fieldsCurrent=false ⇒ 版号必不同」写的，v61 单方面翻转 fieldsCurrent 语义时没有跟着改文案，属于 v61 修复自身引入的新假话。

证据：【CSV 原文（自读，python csv + utf-8-sig，只按序号列定位）】序号 2530｜实质性★｜重要列空｜参数摘要逐字：「支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。 +---------------------------------------------------------------------------------------------------------------------------------------------------------+」｜响应结论：部分响应｜响应说明：「已提供：病理标本描述字段的存储与保存接口。尚未提供：①取材登记界面的取材过程描述录入入口（当前描述内容为固定预设值）；②标本描述在流转列表中的调阅展示。上述缺项列入后续开发计划。」

【拆解】名词/动词各自独立：①文字化记录 ②完整的取材过程信息 ③结构化存储 ④调阅 ⑤（说明点名）标本描述在**流转列表**中的调阅展示。

① 成立：GrossingPanel.vue:74「取材登记」→ :253-263 模板下拉 → :273-279 逐字段 el-input(maxlength=300) → :285-294「添加字段」+ :470-479 addCustomField → :299-302 自由描述；后端 POST /api/pathology/process/grossing 全部取自入参（assembleGross :2085 起），无固定预设值。

② 基本成立：首写/追加同端点（:441-476，append=false 且已有内容→5222；append=true 拼「。补取材：」并复检总长）；修订 PUT /grossing/{id}/fields（:571 起）；每版字段行落 path_gross_field（V164:26-36，带 operator_id/created_at default now()）；修订留痕 insertGrossRevision(:2152)；诊断端覆盖也留痕（PathologyController.java:157-162，source=DIAGNOSE，覆盖前 for update 锁行读原值）。

③ **不成立（新缺口 a）**：见 hardest_gap。补充证据链——V61GrossFieldEntryTest.appendMakesTextCumulativeSoFieldsAreNotCurrent 只测后端读端点（fieldsCurrent/textIsCumulative/fieldsByRevision），**没有任何测试覆盖「追加后走修订」这条写路径**；V61 的前端守卫（:171-182）只源码扫描「添加字段/addCustomField」两个串，扫不到 openRevise 的预填逻辑。

④ 不成立（审计者的两条我复核属实，且更严重）：(a) fieldsByRevision 的 allFieldRows 只 `select f.revision_seq, f.seq, f.label, f.value`（PathologyProcessController.java:1543-1548），不带 operator_id/created_at，而前端对所有版本渲染同一张含「录入」列的表（GrossingPanel.vue:142-144 `{{ fmt(row.operatorName) }}　{{ fmtTime(row.createdAt) }}`，fmt 空值出「—」见 format.ts:195-200）→ 历史版该列恒「—　—」，最新版走 view.fields（:1494-1503 带 operatorName/createdAt）正常，同屏一眼见缺数据。(b) `Grep textIsCumulative` 全仓 9 处：CHANGELOG.md:16、控制器 :1599/:1619、V59GrossReviseTest:361/401、V61GrossFieldEntryTest:45/137/155、tools/e2e-v59-audit.py:301-303——**前端零消费**，于是 :12

核过的点：docs/验收/技术偏离表-v3.csv 序号 2530 行三列原文（python csv, io.open(encoding='utf-8-sig', newline='')，只按序号列匹配，参数摘要含尾部 + 分隔线逐字抄录）；frontend/shell/src/views/medtech/pathology/GrossingPanel.vue 全文 865 行：列表列 :31-72（含 :53-55 标本描述列）、操作列 :73-79（取材登记/修订取材描述/；modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java：grossing() :430-540（uid 前置 5224、append ；modules/medtech/src/main/java/cn/hip/medtech/web/PathologyController.java:110-175 diagnose()（coalesce 保留空白、for update 锁行；modules/medtech/src/main/java/cn/hip/medtech/web/PathologyRegistryController.java:765-800 checkLengths（标本描述只查 ≤255 字、不查非；frontend/shell/src/views/medtech/pathology/RegistryPanel.vue:146-148（标本描述只读列）、:195-215（登记表单：标本类别 required、标本描述无 required；frontend/shell/src/views/medtech/pathology/ProcessingPanel.vue 全部 el-table-column 清单（:47-130 蜡块表与批次表、:200-234 切片表）与操作列按钮；frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue 全部 el-table-column 清单 :51-144；grep -c 'specimen_desc|gross；frontend/shell/src/views/medtech/pathology/TrailPanel.vue:165-166（标本描述回落取值）、:195-250（大体所见修订表）、:357-392；frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:125-126、:185-205、:343-344、:400-433、:455-463、:778-796、:987（；frontend/shell/src/views/medtech/pathology/format.ts:195-200 fmt（null/undefined/'' → 「—」）、:285 specimen_desc 中文名；server/src/test/java/cn/hip/server/V61GrossFieldEntryTest.java:40-46 类注释、:96-127 自定义字段名落库与非法拒绝、:128-165 appendMakesTextC

**反驳者·演示可行性**（推翻）：

「查看大体所见」的字段级记录区，对**只写了自由描述、没有结构化字段**的标本（零种子库里最普通的一种，也是本仓自己的演示脚本 tools/demo-pathology.py 铺出来的全部标本），会在屏上打出一条**假话 + 裸破折号**：「被取代版本（第 1 版，0 项）：已被第 — 版字段取代，仅供调阅」。它既没被任何版本取代（它就是当前版、唯一版），「第 — 版」也不是版本号而是 fmt(null)。审计者主张的诚实兜底「历史标本或纯自由文本，无字段级记录」在当前代码里对全新库标本**永远不可达**——这条是 v60 引入 fieldsByRevision 时的回归，审计者四条证据里一条都没碰到，反而在演示路径里把它当成仍然存在的行为写进了叮嘱。

证据：【我自己从 CSV 读到的参数摘要（逐字）】
序号 2530｜实质性★｜响应结论「部分响应」
参数摘要：`支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。 +---------------------------------------------------------------------------------------------------------------------------------------------------------+`
（响应说明为裸正文：已提供标本描述字段的存储与保存接口；尚未提供①取材登记界面的取材过程描述录入入口（当前描述内容为固定预设值）②标本描述在流转列表中的调阅展示。）
子要求拆三条：①文字化记录完整的取材过程信息 ②结构化存储 ③调阅。我的缺口全部落在③。

【缺口一（hardest）：自由文本标本的调阅屏上是假话 —— v60 回归，审计者四条证据零覆盖，且其演示叮嘱本身已过期】
· 后端：只要本次取材写成了 gross_finding（含**只传 grossText、一个结构化字段都不传**的情形），`grossing()` 必落一行修订 —— PathologyProcessController.java:497-501 `revisionSeq = insertGrossRevision(...,"GROSSING",uid,templateCode); grossFieldCount = storeGrossFields(...)`；insertGrossRevision（:2152-2161）无条件 insert path_gross_revision 并 returning seq。字段行则因 `req.gross()==null` 一行都不落（storeGrossFields :2117「if (gross == null || gross.isEmpty()) return 0;」）。
· 于是读端点 grossingView 回出：`fieldsRevisionSeq = null`（:1596 `fields.isEmpty() ? null : ...`，因为 `select max(revision_seq) from path_gross_field` 为空）、`fields = []`、而 `fieldsByRevision` 由 revisions 逐版预置（:1543-1554 对每条 revision 都 `byRevision.put(seq, one)`，fields 初始化为空 list），**非空**：[{revisionSeq:1, source:'GROSSING', sourceName:'取材首写', fields:[]}]。
· 前端 GrossingPanel.vue:714-717 `viewFieldVersions`：`Array.isArray(raw) && raw.length` 成立 → 返回那一条 → `:112 <template v-if="viewFieldVersions.length">` 判真 → **:148 的诚实兜底「历史标本或纯自由文本，无字段级记录（不从文本反解析）」这条 v-else 分支对任何有修订行的标本永远进不去**（全新零种子库里每一个标本都有修订行）。
· 落到屏上的是 :117-123 那只 tag 的 else 支：`viewVersionIsLatestFields`（:727-731）要求 `view.fieldsRevisionSeq != null`，此处为 null → 恒 false → 渲染 `被取代版本（第 ${fmt(viewVersion?.revisionSeq)} 版，${viewFields.length} 项）：已被第 ${fmt(view.fieldsRevisionSeq)} 版字段取代，仅供调阅`，即屏上逐字是「被取代版本（第 1 版，0 项）：已被第 — 版字段取代，仅供调阅」（format.ts `fmt(null)` 返回 '—'）。下拉同时写「第 1 版 · 取材首写（本版未填写字段）」（:763），:131

核过的点：docs/验收/技术偏离表-v3.csv 序号 2530 行：自己用 python csv + utf-8-sig 读出参数摘要与响应说明原文（不信审计者转述）；frontend/shell/src/views/medtech/pathology/GrossingPanel.vue 全文 1029 行通读（模板 1-330、脚本 330-865）；GrossingPanel.vue:112-148 字段级记录区的版本下拉 / tag / 兜底 v-else 分支条件逐条推演；GrossingPanel.vue:714-745 viewFieldVersions / viewVersion / viewVersionIsLatestFields / viewFields / latestVersionWithFi；GrossingPanel.vue:338-346 取材完成弹窗 codePrefixSource 的渲染（无中文映射）；GrossingPanel.vue:470-490 addCustomField / removeCustomField 的四条校验与后端 assembleGross 是否同源；GrossingPanel.vue:601/:672 每次开表单清空 customFields；onTemplateChange 换模板时空值自定义项被丢；modules/medtech/.../PathologyProcessController.java:247-290 取材工作列表 pending/all 判据与 note；PathologyProcessController.java:345-560 POST /grossing 全流程：5222/5223/5221/5224/5275 的拒绝次序、首写 vs 追加的 update where 子句、inse；PathologyProcessController.java:497-501 —— 只传 grossText（零字段）时仍落 1 行修订、0 行字段（本轮关键推演起点）；PathologyProcessController.java:2117-2135 storeGrossFields 的空 map 早退；PathologyProcessController.java:2142-2161 insertGrossRevision 无条件 insert + returning seq

### 2563

**参数原文（CSV 逐字）**：支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。

**审计者**：维持。最接近缺口的一点：「状态追踪」的可读面只修了一半，而且守卫被写成抓不到剩下那一半。v60 复核推翻本条的第三条理由逐字点名两个位置——SECTION 备注（PathologyProcessController.java:1076）「更是纯裸码无中文」、GROSSING 备注（:527）「直接拼『，补取材医嘱#12 RESAMPLE』」；v61 车道 B 的提交自述「节点备注去裸英文枚举……三处节点同时生效」，实测只改了 PathologyReportController.techLabel（:1409-1412）覆盖的 TECH_ORDER/TECH_DONE/TECH_CANCEL 三个节点，被点名的那两处一字未动，STAIN 备注（:1174）同样还是原始 stain_type。评委按演示路径第 3、4 步走完，④「流转节点」同一张表里会并排出现「下达特检医嘱 #12 免疫组化 CK7」（已中文）与「切片 2 张（P-1，IHC CK7），特检医嘱#12 IHC」「取材产出 2 块，补取材医嘱#13 RESAMPLE」（仍裸码）——同一屏两个口径，比全裸更难解释。新加的 V61TechLabelTest.java:61 用 `\\([A-Z][A-Z_]*\\)` 只匹配括号里的裸码，结构上永远抓不到无括号的这两处；而 V57TechTraceTest.java:258 反过来断言 SECTION 备注必须含「特检医嘱#id IHC」，把裸码钉成了现行契约——要修这两处得先改这条旧断言。次硬缺口：④ DiagnosisPanel.vue:253-254 的进度图例仍写 v58 三态、漏 SAMPLED，而同页 :282 就会显示「已补取材待切片」，页面与自述不符（v60 反驳者已点名的同族残留，本轮亦未动）。

子要求核对：
- [✓] 支持特检医嘱的「集中化管理」（不分标本的全院集中清单，可筛选、可在清单上处理） —— 后端 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:1060 GET /api/pathology/report/tech-orders——不传 specimenId 即全院分支，默认只看 ORDERED、status=ALL 看全状态，支持 techType(白名单 5267，:1087)/urgentOnly/dateField(ORDERED|DONE)/from/to/keyword(条码·病理号·患者名·患者号·项目名 5 字段)/limit+truncated；写侧 :835 POST /tech-orders 下达、:912 PUT /{id}/done、:990 PUT /{id}/cancel。类级 @PreAuthorize ADMIN/TECHNICIAN
- [✗] （特检医嘱的）状态追踪 —— 状态机本身完整：三态 chk_path_tech_status（V144:146）；三端点同事务打点 TECH_ORDER/TECH_DONE/TECH_CANCEL（PathologyReportController.java:878 / :933 / :1013，V165 放开 chk_path_process_node）；取消三列 cancelled_at/by/reason 与 status 同一条 update，V163 前历史行零回填、前端显式标「历史取消（时刻未采集）」（TechOrderPanel.vue 取消列）。缺口在追踪的可读面：v61 车道 B 声称「节点备注去裸英文枚举……三处节点同时生效」，实际只改了 techLabel 覆盖的三个 TECH_* 节点（PathologyReportController.java:1409-1412 已成「#12 免疫组化 CK
- [✓] 可实时调阅（查询即读当前库事实） —— PathologyReportController.java:1060 每次请求直查 path_tech_order + 派生子查询，hours_since_ordered 由 now() 现算；TechOrderPanel.vue 「查询」按钮与工位头部刷新触发重取，done/cancel 后 load() 重查；④ DiagnosisPanel 特检页签按 specimenId 调同一端点；质控 WORKLOAD_TECH 穿透 PathQcController.java:1431 拼同一份 TECH_DERIVED_COLUMNS，三处同源。
- [✓] 明细涵盖「技术分类」 —— path_tech_order.tech_type + chk_path_tech_type 六档 DEEP_CUT/RECUT/RESAMPLE/IHC/SPECIAL_STAIN/MOLECULAR（V144__pathology_base.sql:143-145）；PathologyReportController.java:251 TECH_TYPES / :254 TECH_TYPE_NAMES 中文，清单行回 tech_type_name（:1179）；前端「类型 / 项目」列 TechOrderPanel.vue:57-61、④ 「类型」列 DiagnosisPanel.vue:259-261；筛选下拉取自 GET /tech-orders/dict（:818）。
- [✓] 明细涵盖「具体检测项目」 —— path_tech_order.tech_item（V144:141）；IHC/SPECIAL_STAIN/MOLECULAR 必填项目（TECH_ITEM_REQUIRED :262，createTechOrder :851 回 5267）；清单同列显示并可按项目名关键词搜；另有 attached_stain_name 中文汇总列（PathologyReportController.java:235-241 SQL 内 case 译中文，如「免疫组化 CK7 ×2」），TechOrderPanel.vue「挂接切片染色」列读 attached_stain_name ?? attached_stain；挂接时项目一致性硬校验 5274（PathologyProcessController.java TECH_TO_STAIN :164-166）。注：④ 特检页签仍无此列（Diagnosis
- [✓] 明细涵盖「关联蜡块编号」 —— 值：TECH_DERIVED_COLUMNS（PathologyReportController.java:209-242）的 blocks_derived 为三源并集——t.block_id 指定块 ∪ path_block.tech_order_id=t.id（V167 补取材出块）∪ 挂接切片所在块，按块号去重「、」相连。判定：v61 新增 blocks_derived_source（:227-234，同一段 SQL 算 ORDERED/DERIVED/MIXED/null），且「指定」口径排除回写块（`b.tech_order_id is distinct from t.id`），补取材回写进 block_id 的首块算派生。前端只读不推断：TechOrderPanel.vue:92-101、DiagnosisPanel.vue:266-274 两处同标（④ 此前连标签都没有）；质
- [✗] 明细涵盖「当前执行进度」 —— 后端与 ⑤ 成立：techProgress 四参只读派生六态（PathologyReportController.java:1230，ORDERED 且 0 片→有挂接蜡块 SAMPLED 否则 PENDING_SECTION；有片未全染 SECTIONING；全染 STAINED；DONE/CANCELLED 照状态；非法状态原样回不猜），applyTechProgress :1244 为两清单与质控穿透共用，中文 TECH_PROGRESS_NAMES :175-177；⑤ TechOrderPanel.vue:62-70 进度列 +「已染 x / 挂接 y」+ RESAMPLE 行「已出块 N」，页首说明四态齐（含「已补取材待切片」）。缺口在 ④：DiagnosisPanel.vue:253-254 特检页签的说明仍是 v58 三态「「进度」由挂接切片派生（v58）：待切片 / 切片

演示路径（审计者）：前提：零种子新库上 ⑤ 首屏是空表（TechOrderPanel.vue 空态文案「该条件下无特检技术医嘱」），必须先造数。
第 0 步（造数）：起服务后 `HIP_E2E_BASE=http://localhost:8080/api python tools/demo-pathology.py --count 2`（纯走 API；病理收费项目 C0301 由 V21 已 seed；会建 demo_pathdoc/demo_quality 账号）。它只造 1 条 IHC CK7（挂 2 片、染色、完成）与 1 条 SPECIAL_STAIN PAS（带原因取消），见 tools/demo-pathology.py:184-209；**全脚本零 RESAMPLE**（grep 无命中），且两条终态都是 DONE/CANCELLED。
第 1 步：ADMIN/TECHNICIAN/DOCTOR_OUTP 登录 →「门诊业务 → 病理工作台」（菜单 168，/pathology/workbench）→ 第 ⑤ 页签「特检工作台」。默认「待执行」筛选下仍是空表，必须把「状态」切到「全部状态」再点「查询」，两条才出；同屏可见 状态 / 类型·项目（免疫组化 CK7、特殊染色 PAS）/ 进度（已完成，已染 2 / 挂接 2）/ 病理号·条码 / 患者 / 类别 / **医嘱号 #id（v61 新列）** / 蜡块 / 开单人·时刻 / 距开单小时 / 完成人·时刻 / 下达原因 / 取消人·时刻·原因 / 挂接切片数 / 挂接切片染色「免疫组化 CK7 ×2」。
第 2 步（活进度）：④「诊断 / 报告追溯」打开另一例 → 特检页签「下达」→ 免疫组化 / CK7 / 选蜡块 / 填原因 → ⑤ 切「待执行」见「待切片 0/0」→ ③「制片」对该蜡块开切片对话框，「挂接特检医嘱」选中该条（染色类型锁 IHC、项目只读 CK7）切 2 张 → ⑤「切片中 已染 0 / 挂接 2」→ ③ 逐张染色登记 → ⑤「已染色待确认 2/2」→ ⑤ 点「完成」→「已完成」。
第 3 步（补取材派生蜡块，v61 修复点）：④ 下一条「补取材」医嘱（蜡块留空）→ ② 取材工位打开该标本，勾「补取材」→「挂接补取材医嘱」下拉选中（GrossingPanel.vue:240-249）→ 加 2 块提交 → ⑤ 该行进度「已补取材待切片 已出块 2」、蜡块列显示两块块码并带 **「派生」标**（v61 起判定读后端 blocks_derived_source，回写首块也算派生，故此标现在会出——v60 树上不会出）。
第 4 步（状态追踪留痕）：④「流转节点」页签或 ⑥「流转与异常」→ 三条 TECH_* 节点备注已是中文「下达特检医嘱 #12 免疫组化 CK7：…」；**但同一张表里第 2/3 步产生的 SECTION 行会显示「切片 2 张（P-…-1，IHC CK7），特检医嘱#12 IHC」、GROSSING 行显示「取材产出 2 块，补取材医嘱#13 RESAMPLE」、STAIN 行显示「染色 …（IHC CK7）」——裸英文枚举原样在屏。**
旁路：「病理质控」（菜单 169，ADMIN/QUALITY）→ 特检技术医嘱量指标穿透，同带 进度 / 关联蜡块 / 关联蜡块来源 / 挂接切片染色 列（PathQcController.java:1431 复用同一份派生列）。
不跑 demo 脚本也走得通，但要 ADMIN 自己从挂号→接诊→开病理医嘱→结算→登记→接收→

审计者自列的保留项：
- 本次审计只读：未起服务、未跑 mvn、未实跑 E2E、未点页面；所有结论来自当前工作树（main=14e70bf + 未跟踪的 V61QcCaliberTest.java）的源码、迁移、测试与 E2E 脚本文本。「全量 1037 绿 / 46 套 E2E 全绿」是任务书给的前提，非本人复现。
- v61 对 2563 的修复（blocks_derived_source）只有 Java 单测 V61TechLabelTest 守，E2E 未跟：tools/e2e-v60-audit.py:176-186 的 assert_derived 只断言 blocks_derived / sampled_block_count / attached_stain_name 三列，不含 blocks_derived_source——v60 刚教训过「改派生列时 SQL / 单测 / E2E 三处必须同改」，这一轮 E2E 那一处又没跟（5ece29d 只同步了 2530/2576 相关断言与 e2e-v58 的 TECH_ORDER 中文断言）。
- blocks_derived_source 的 MIXED 档在真实业务路径下不可达（指定了蜡块的医嘱不许往别的块挂片，5272），V61TechLabelTest:140-153 是直接造库状态证明 SQL 判定正确并如实注明——这是诚实钉法，但意味着前端「含派生」标在演示里永远不会出现。
- 零回填口径依旧：V163 前的切片 tech_order_id、V167 前补取材出的块 tech_order_id、V163 前取消的三列均恒 NULL，旧库演示时存量 RESAMPLE 医嘱恒「—」「待切片」；gate emr.gate.pathology.techdone 出厂 warn，0 片可点完成（返回体与 TECH_DONE 备注留 warnings），且平台无系统配置页，改 block 只能走接口。
- ⑤ 全院清单无翻页，只有 limit + 「命中超过 N 条」告警条，大院演示要先收窄日期区间。
- ④ 特检页签比 ⑤ 少「挂接切片染色」与「已出块」两列，两处明细口径仍不等宽（不影响参数摘要点名的四项本身）。
- tools/demo-pathology.py 全脚本无 RESAMPLE，且造出的两条医嘱终态都是 DONE/CANCELLED——⑤ 默认「待执行」首屏仍是空表，v61 未动脚本；演示路径第 2、3 步必须手工走。
- CSV 参数摘要单元格末尾的「 +---（140 个 -）」是原表排版残留，param_verbatim 已逐字保留。

**反驳者·数据真实性与口径**（推翻）：

「当前执行进度」的完成判定与留痕只认「挂接切片」一种事实，不认平台自己在 v60/v61 才建起来的「补取材已出块」事实（path_block.tech_order_id / sampled_block_count / SAMPLED）：同一屏上进度列写着「已补取材待切片　已出块 2」，点「完成」时后端 doneTechOrder 的事实查询里根本没有这一列，于是判 gap=「无挂接切片（slide_count=0）」——warn 档（V165 出厂默认）下弹「无已染色挂接切片即确认完成」并把「挂接 0 片 / 已染色 0；无挂接切片（slide_count=0）（gate=warn 放行）」永久写进 TECH_DONE 流转节点备注（等于给这条医嘱留下一条「无证据完成」的假账，而证据就在隔壁列）；block 档（V165 注释里明文支持「以 5273 拦完成」）下补取材医嘱**永远完不成**，六个技术分类里有一类的执行进度卡死在「已补取材待切片」。六态派生（techProgress）已读四参、完成 gate 仍是 v58 的两参口径，两套口径在同一条医嘱上自相矛盾。

证据：CSV 原文（自读，docs/验收/技术偏离表-v3.csv 序号 2563，参数摘要逐字）：「支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。」结论列=部分响应。

1) 完成判定漏掉蜡块事实：modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:919-926 的 facts SQL 只取 slide_count / stained_count，没有 `(select count(*) from path_block pb where pb.tech_order_id = t.id)`；:935-937 `gap = slideCount == 0 ? "无挂接切片（slide_count=0）" : ...`，全程无 tech_type 分支，补取材不豁免。:939-941 block 档直接 R.fail(5273)；:958-960 logProcess("TECH_DONE", ...) 把「挂接 N 片 / 已染色 M；<gap>（gate=… 放行）」原样写进 path_process.remark（不可改），这条备注就是 ④「流转节点」/⑥ TrailPanel 上给评委看的那行。
2) 同一份代码知道这个事实：TECH_DERIVED_COLUMNS（同文件 :209 起）第一列就是 sampled_block_count；techProgress 四参形态 :1230-1237 用它派出 SAMPLED；前端 TechOrderPanel.vue:66-68 对 RESAMPLE 行专门显示「已出块 N」。即证据存在、只是完成这一步不读。
3) gate 三档是产品化配置：V165__path_tech_progress.sql:39-42 seed 'warn'，remark 逐字「off 旁路 / warn …（默认）/ block 以 5273 拦完成」，理由只讨论了「历史医嘱普遍无挂接切片」，从未考虑补取材的交付物是蜡块不是切片。
4) 没有任何测试/E2E 走过这条路：tools/e2e-v60-audit.py:479-499 完成 RESAMPLE 之前先补了两张 HE 挂接切片并染色，注释自承「切片全染色，任何 gate 档都无缺口」（:493）；仓库里没有 e2e-v61-audit.py（ls tools/ 最新只到 e2e-v60-audit.py），v61 的 blocks_derived_source 只在 server/src/test/java/cn/hip/server/V61TechLabelTest.java:209-214 有 Java 断言，无 E2E、无演示脚本覆盖。
5) 现实路径就会撞上：补取材出块后切的是常规 HE 片，③ 制片的 techOrderId 是可选入参（PathologyProcessController POST /slides），技师不勾「挂接特检医嘱」这条医嘱就再也拿不到「已染色挂接切片」——warn 档留假账、block 档完不成。

旁证（同一镜头的次级问题，未计为 hardest）：
- 零种子演示确实只能看空表：tools/demo-pathology.py 全脚本 `grep -c RESAMPLE` = 0，只造 1 条 IHC CK7（:186-196 下达即完成）与 1 条 SPECIAL_STAIN PAS（:200-207 带原因取消），两条终态都是 DONE/CANCELLED。⑤ 默认筛选是「待执行」，所以跑完项目自带的演示数据脚本后，集中化工作台首屏**仍是空表**，必须手动切「全部状态」才见两行；而 SAMPLED 态、「已出块」列、v61 本轮主修的「派生 / 含派生」标在任何种子数据里都产不出来，只能人工跨 ②③④⑤ 四个工位手搓。
- hours_since_ordered（:1131-1132）对 DONE/CANCELLED 行照样算 now()-ordered_at 且无条件回列，演示时那两条终态医嘱的「距开单(小时)」会一直往上涨，与

核过的点：自读 CSV 序号 2563 参数摘要/结论/说明（utf-8-sig，控制台乱码改写文件再读），确认四个子要求：集中化管理、状态追踪、可实时调阅、明细含技术分类/具体检测项目/关联蜡块编号/当前执行进度；PathologyReportController.TECH_DERIVED_COLUMNS(:209-243)：sampled_block_count / blocks_derived 三源并集 / blocks_derived_sour；techProgress 三参与四参形态(:1216-1237)、applyTechProgress(:1245)、TECH_PROGRESS_NAMES(:175)：进度确为只读派生、不是库列；doneTechOrder(:913-970) 全文：facts SQL 不含 sampled_block_count，gap 只按 slide/stained 算，无 tech_type 分支，block 档 5273、warn 档 wa；V165__path_tech_progress.sql:29-45：chk_path_process_node 放开三个 TECH_* 节点、gate seed 'warn' 与三档 remark 原文；全院/标本清单 SQL(:1104-1200)：slide_count、stained_count、hours_since_ordered=now()-ordered_at、七项筛选、keyword 五字段、order by、truncat；path_slide 表结构 V144:94-110（stain_type 四值白名单 → attached_stain_name 的 else 分支不构成猜值）、path_block/path_process 结构；PathologyProcessController 取材 append(:380-545)：techOrderId 四路 5275 校验（必 RESAMPLE、同标本、待执行、存在）、新块写 tech_order_id、block_id ；PathQcController WORKLOAD_TECH 三处(:913 汇总 / :1148 概览 / :1412 穿透)：穿透复用 PathologyReportController.TECH_DERIVED_COLUMNS、wit；前端 TechOrderPanel.vue:55-110（进度列/已出块仅 RESAMPLE 可见/医嘱号列/蜡块列读 blocks_derived_source）、DiagnosisPanel.vue:268-273 同标、format.；tools/demo-pathology.py:184-209 与全文 RESAMPLE 计数=0；两条医嘱终态 DONE/CANCELLED；tools/e2e-v60-audit.py:397-513 的 RESAMPLE 全程（含 :493 注释与 :497-499 完成断言 warnings==[]）；ls tools/ 确认无 e2e-v61-audit.py；grep 

**反驳者·子要求拆解**（推翻）：

「完整明细」在第三处调阅口（质控 WORKLOAD_TECH 穿透明细的 CSV 导出）根本不完整、且是新造的裸英文枚举：导出行里**压根没有中文「技术分类」列**（穿透明细 SQL 只选 t.tech_type，中文 tech_name 只存在于汇总行的 case 分支里），表头却写作「技术类型编码」——暗示有一列成对的「技术类型」而它不存在；同一份 CSV 里「状态」列值是 ORDERED/DONE/CANCELLED，v61 车道 B 刚加的「关联蜡块来源」列值是 ORDERED/DERIVED/MIXED，**两列在同一行同时出现 `ORDERED` 而含义完全相反**（待执行 vs 下达时指定）；更有「挂接切片染色」这一个中文表头**出现两次**（attached_stain 英文版「IHC CK7 ×2」与 attached_stain_name 中文版「免疫组化 CK7 ×2」被 zh() 映射成同一个名字）。屏上抽屉靠前端 cellText 逐列翻译掩盖了这一切，导出一离开页面就原形毕露——而 v61 自己的代码注释（format.ts:548）正把「原样显示 ORDERED/DERIVED 就是新的裸英文枚举」定为本轮要消灭的形态，却只消灭了屏上那一半。次强缺口：完成校验 gate 只认切片、不认蜡块，把「补取材」这一整个技术分类的执行进度做成半截。

证据：参数摘要（自行从 docs/验收/技术偏离表-v3.csv 按序号列逐字读出，序号 2563，实质性★）：「支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。 +----（原文此处一长串连字符）」；响应结论「部分响应」。

拆解为 7 个子要求：①集中化管理 ②状态追踪 ③可实时调阅 ④技术分类 ⑤具体检测项目 ⑥关联蜡块编号 ⑦当前执行进度，且 ④⑤⑥⑦ 必须是「完整明细」。

【缺口一（审计者未堵）：第三处调阅口的明细在导出上不完整且裸码】
- C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathQcController.java:1412-1445 WORKLOAD_TECH 穿透明细 SQL 的 select 列表逐列读过：tech_order_id, tech_type, tech_item, reason, status, ordered_at, done_at, ordered_by_name, done_by_name, cancelled_at, cancelled_by_name, cancel_reason, slide_count, stained_count, attached_stain, + TECH_DERIVED_COLUMNS(sampled_block_count, blocks_derived, blocks_derived_source, attached_stain_name), block_code, specimen_id, path_no, barcode, patient_name。**无 tech_name**。而同文件 :914-918 的汇总分支有 `case t.tech_type … as tech_name`——中文技术分类只在汇总层存在，穿透明细层没有。
- :1458-1465 withTechProgress 只补 progress / progress_name 两个键，不补 tech_type_name / status_name。
- :1810-1812 zh(): `case "tech_type" -> "技术类型编码"; case "tech_name" -> "技术类型";`——表头承诺了一个明细里不存在的列。
- :1832 与 :1838 zh(): `case "attached_stain_name" -> "挂接切片染色";` 与 `case "attached_stain" -> "挂接切片染色";` 两键同名，两列都在明细行里 → CSV 同名表头出现两次。
- :1607-1620 toCsv()：表头 `csv(zh(c))`、**取值 `csv(r.get(c))` 原始值、无任何值翻译**；列序取 rows.get(0).keySet()。故导出文件里 状态=ORDERED、关联蜡块来源=ORDERED/DERIVED/MIXED、技术类型编码=IHC/RESAMPLE、执行进度编码=SAMPLED 与执行进度=已补取材待切片 并排。
- 屏上为何看不出：frontend/shell/src/views/medtech/pathology/PathQcView.vue:141-144 穿透抽屉是通用表（表头 zh(c)、取值 cellText(c,…)），而 frontend/shell/src/views/medtech/pathology/format.ts:532-562 cellText 对 'status' 走 techStatusName、对 'tech_type' 走 techTypeName、对 'blocks_derived_source' 走 BLOCKS_DERIVED_SOURCES——翻译全在前端。同文件 :548 注释自陈：「v61（2563 复核）：新列若原样显示 ORDERED/DERIVED 就是新的裸英文枚举——正是本轮要消灭的形态」。抽屉里 v61 刚加的「导出本次明细」按钮（PathQcView.vue:132-136）导出的正是

核过的点：CSV 原文：按序号列取 2563 行，逐字抄出参数摘要（含尾部连字符串），结论列为「部分响应」；PathologyReportController.java:1060-1206 全院/按标本两分支 SQL 逐列读过：筛选 status/techType(5267 白名单)/urgentOnly/dateField/from/to/ke；PathologyReportController.java:209-242 TECH_DERIVED_COLUMNS：blocks_derived 三源并集、blocks_derived_source 的 ORDERED/MIXED/DE；PathologyReportController.java:1225-1247 techProgress 三参/四参与 applyTechProgress；TECH_PROGRESS_NAMES 六态中文；PathologyReportController.java:919-975 doneTechOrder 完成校验 gate：事实查询列、gap 判定、5273/warn 分支、TECH_DONE 节点备注；V165__path_tech_progress.sql:39 gate emr.gate.pathology.techdone 默认 warn；PathologyReportController.java:251-262 TECH_TYPES/TECH_TYPE_NAMES/TECH_ITEM_REQUIRED；PathQcController.java:905-930 WORKLOAD_TECH 汇总分支（有 tech_name）；PathQcController.java:1412-1445 WORKLOAD_TECH 穿透明细 SQL 全部 select 列（无 tech_name）；PathQcController.java:1458-1465 withTechProgress 只补 progress/progress_name，且确已走四参（审计者「三处同源」在 JSON 层属实）；PathQcController.java:1795-1845 zh() 表头映射：tech_type→技术类型编码、attached_stain 与 attached_stain_name 同名、blocks_derived_source；PathQcController.java:1607-1625 toCsv()：表头翻译、取值不翻译、列序取首行 keySet

**反驳者·演示可行性**（推翻）：

「状态追踪」的可读面在评委真正会看的那张表上仍然破着，而且比审计者列的还多一处。v61 的修复说明（PathologyReportController.java:1400-1407）自己引用了打回原话「评委在 ④ 抽屉『流转节点』/⑥『流转与异常』看到的备注正文里同时有内部主键与裸英文枚举」，但只改了 techLabel 覆盖的 TECH_ORDER/TECH_DONE/TECH_CANCEL 三个节点。演示路径第 2、3 步必然产生的另三个节点仍在同一张表里逐字打裸码，其中一处是审计者没点到的：STAIN 节点备注把染色质量枚举直接拼进去（PathologyProcessController.java:1174-1176：`"染色 " + slide_code + "（" + stain_type + " " + stain_item + "）" + "，质量 " + quality`，quality ∈ GOOD/FAIR/POOR）——第 2 步「③ 逐张染色登记」每染一张就落一行「染色 2026-C-000001-1-1（IHC CK7），质量 GOOD」，一行里两个裸英文枚举；SECTION 节点（:1074-1076）落「切片 2 张（P-1，IHC CK7），特检医嘱#12 IHC」，同时带内部主键与两个裸码；第 3 步的 GROSSING 节点（:527）落「…，补取材医嘱#12 RESAMPLE」。四处都在 ④ DiagnosisPanel.vue:244-246 与 ⑥ TrailPanel.vue:188 直接 `fmt(row.remark)` / `{{ n.remark }}` 原样回显，与已中文化的三条 TECH_* 节点上下相邻。守卫抓不到也拦不住：V61TechLabelTest 的 RAW_ENUM = `\([A-Z][A-Z_]*\)` 只匹配括号内裸码（上述四处的裸码都不在括号里），且只对 TECH_* 三节点断言；反向地 V57TechTraceTest.java:258 现役断言 `remark.contains("特检医嘱#" + ihcOnA + " IHC")`——裸码被绿测钉成契约，改它就红。也就是说 v61 修的是「同一张表里的三行」，剩下三行照旧，而这三行恰是演示脚本走不出来、必须现场手工走第 2/3 步才出现的那几行。

证据：【参数摘要逐字（自读 CSV 第 2563 行第 4 列，未信审计者抄件）】「支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。 +----（后接一串连字符）」；响应结论列「部分响应」。子要求拆开是五个：①集中化管理 ②状态追踪 ③实时调阅 ④明细含技术分类/具体检测项目/关联蜡块编号 ⑤明细含当前执行进度。

【我自己核到、能立住的部分】④⑤ 的值侧确实做实了：path_tech_order.tech_type + chk_path_tech_type 六档（V144__pathology_base.sql:143-145）、tech_item（:141）、TECH_DERIVED_COLUMNS（PathologyReportController.java:208-242）里 blocks_derived 三源并集 + blocks_derived_source（ORDERED/MIXED/DERIVED 与并集同一处 SQL 算出）+ attached_stain_name（SQL 内 case 译中文「免疫组化 CK7 ×2」）；进度六态由 techProgress(:1219-1247) 只读派生；hours_since_ordered 已 round(...,1)（:1123）不是长小数；fmtTime 委托 fmtDateTime（format.ts:202-204 → utils/date.ts），带偏移才换算到 Asia/Shanghai，⑤ 各时刻列无「早 8 小时」问题。审计者说「④ 特检页签仍无项目列」这条是错的——DiagnosisPanel.vue:262-264 明明有「项目」列（读 tech_item），这处不算缺口。UI 联动也真接上了：③ ProcessingPanel.vue:264-268 有「挂接特检医嘱」下拉，openSlide(:468-474) 用 row.specimen_id 取本标本 ORDERED 医嘱（GET /blocks 确实回 b.specimen_id，PathologyProcessController.java:899，不会 NaN），onTechOrderPick(:517-522) 按 TECH_TO_STAIN 锁染色类型、项目只读；② GrossingPanel.vue:240-249 有「挂接补取材医嘱」下拉，只列 ORDERED+RESAMPLE。

【缺口一（最硬，见 hardest_gap）】流转节点备注四处裸码 + 内部主键，v61 只修三分之三里的 TECH_* 那组；守卫正则与 V57 现役断言双重失效。

【缺口二：演示第一屏就是空表，且 v61 的招牌修复根本演不出来】tools/demo-pathology.py 全脚本 grep 只有两处下医嘱：:186-188 IHC/CK7（挂 2 片、染色、:196 完成 → DONE）与 :202-206 SPECIAL_STAIN/PAS（:205 带原因取消 → CANCELLED）。**零 RESAMPLE、零 DEEP_CUT、零 RECUT、零 MOLECULAR**（grep 全文无命中）。而 TechOrderPanel.vue:186 的 `query = reactive({ status: 'ORDERED', ... })` 是硬编码默认，:302 空态文案「该条件下无特检技术医嘱」。于是零种子新库跑完造数脚本后，评委点开 ⑤「特检工作台」——这条应答的主场——看到的是空表，必须先知道要把「状态」切「全部状态」再点查询。更要命的是第 3 步：v61 这一轮 2563 的招牌修复就是「派生」标（blocks_derived_source），它只在 RESAMPLE 医嘱经取材挂接出块后才出现（DERIVED），而造数脚本一条 RESAMPLE 都不造，六档技术分类里演示能覆盖的只有 2 档。要看到「派生」标，评委得现场手工跨 ④→②→⑤ 三个工位约十步再走一遍——「建了功能 ≠ 能演示」正卡在这里。

【缺口三：「医嘱号」不是业务号，是数据库代理主键】V144__pathology_base.sql:137-1

核过的点：自读 docs/验收/技术偏离表-v3.csv 第 2563 行，逐字取参数摘要与结论列（未采信审计者抄件）；PathologyReportController.java:1060 起全院清单分支：不传 specimenId、status=ALL、techType 白名单 5267、dateField/from/to/keyword/limit+t；PathologyReportController.java:208-242 TECH_DERIVED_COLUMNS：sampled_block_count / blocks_derived 三源并集 / blocks_derived_s；逐行推演 blocks_derived_source 在补取材回写 block_id 后的取值：b.tech_order_id is distinct from t.id → false → 'DERIVED'，确认第 3 步「派生」标确实；PathologyReportController.java:1123 hours_since_ordered 已 round(...,1)，非长小数；PathologyReportController.java:1219-1247 techProgress 六态派生 + TECH_PROGRESS_NAMES 中文；PathologyReportController.java:1409-1411 techLabel 已中文化，且注释自认「#id 刻意保留」；PathologyReportController.java:835-895 createTechOrder：TECH_ITEM_REQUIRED、5267/5269、logProcess TECH_ORDER；PathologyProcessController.java:1074-1077 SECTION 备注拼 stain_type 与 tech_type 裸码 + 「特检医嘱#id」；PathologyProcessController.java:1174-1176 STAIN 备注拼 stain_type 裸码 + 「质量 GOOD」裸码（审计者未列出的第 4 处）；PathologyProcessController.java:525-528 GROSSING 备注拼「补取材医嘱#id RESAMPLE」；PathologyProcessController.java:894-933 GET /blocks 返回列含 b.specimen_id（确认 ③ 挂接下拉不会传 NaN）

### 2576

**参数原文（CSV 逐字）**：支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。

**审计者**：可上调。最接近缺口的一点：最硬的一处不是缺口而是被平台自己写在屏幕上的口径限制：**科室维度只挂在「登记量」上，平台拿不出「本期签发量按科室分」**（PathQcController.java:413-415 的 caveat 原话：「平台目前拿不出『本期签发量按科室分』：REPORT_* 只按日分组、没有科室维度，本指标的 dept 过滤也只对自己生效——要这个口径须另开指标，本版不做」）。配套地，WORKLOAD_DEPT 的 issued_of_registered / in_progress 是**存量**：同一个已结束的历史区间今天导与下周导数字会变（上期登记、本期才签发的标本被追加进上期那一行），所以「一张发给科室做工作量考核的表不可复现」这个事实 v61 只做了**正名 + 口径下发**，并没有消除。若评委把「多维度」理解成「每个工作量口径都要能按科室切」，这一点会被咬。但序号 2576 的参数摘要逐字只点名「登记总量、蜡块产出数、报告签发量」三项与「多维度汇总」，未点名科室维度；三项各自在菜单里走得到、出的是真数、锚点与口径都标注到位，8 个分组维度也远超「多维度」的字面要求，故仍判可上调。次硬一处：蜡块产出数只有「按日」一个分组维度（:867 group by 1），无科室/技师分组（技师只出现在穿透明细的 embedded_by_name 列）。

子要求核对：
- [✓] 支持基础工作量统计（有承载页面、有菜单入口、目标角色进得去） —— 菜单 sys_menu 169「病理质控」/pathology/qc：C:\Users\drxuj\医院信息化\server\src\main\resources\db\migration\V144__pathology_base.sql:199；授权 ADMIN+QUALITY：V156__v52_menus.sql:31-33；父目录 DIR 7 补授（不补则子菜单在导航里一条都渲染不出）：V156__v52_menus.sql:124-127；路由：C:\Users\drxuj\医院信息化\frontend\shell\src\router\index.ts:62 → PathQcView.vue；后端类级 @RequestMapping("/api/path-qc") + @PreAuthorize("hasAnyRole('ADMIN','QUALITY')")：C:\Users
- [✓] 自动化统计（打开即算，不需人工汇总／不需手工传参） —— PathQcView.vue:342-345 onMounted 先 loadCatalog() 再 load()，进页面即出数；默认统计区间近 30 天由 format.ts:595-601 defaultRange() 给（本地日期，不用 toISOString 以免北京 0-8 点切到 UTC 昨天）；后端 parseWindow 缺省 to=今天、from=to-29（PathQcController.java:459-475）。全部指标由 SQL 聚合出，无任何手工录入汇总值的表。
- [✓] 涵盖病理登记总量 —— 指标 WORKLOAD_REGISTER「登记总量（按日，含拒收与加急构成）」定义 PathQcController.java:396-402；按日行 SQL :828-845（registered/urgent/rejected/outp_source/inp_source + ROUTINE/FROZEN/CYTOLOGY/CONSULT/MOLECULAR/type_unfilled 构成列）；区间汇总 :1098；穿透明细 :1338-1342（复用 SPEC_SELECT，同窗同锚点）。锚点 path_specimen.collected_at。demo-pathology.py:383 把 WORKLOAD_REGISTER.registered 列为必须非零的三项之一，为 0 即退出码 2（:430-434）。
- [✓] 涵盖蜡块产出数 —— 指标 WORKLOAD_BLOCK「蜡块产出数（按日）」定义 PathQcController.java:418-422；按日行 SQL :867-880（blocks/embedded/specimens/blocks_per_specimen/dehydrate_batches，按 coalesce(embedded_at, created_at) 落窗）；汇总 :1120；穿透 :1358-1372（蜡块编码/组织描述/脱水篮/包埋人/所属标本）。demo-pathology.py:384 钉 WORKLOAD_BLOCK.blocks 非零。
- [✓] 涵盖报告签发量 —— 指标 WORKLOAD_REPORT「报告签发量（首次报告 / 补充报告分列）」定义 PathQcController.java:428-435；按日行 SQL :896-910（issued_reports 锚 report_issued_at、diagnosed 锚 diagnosed_at、supplement_reports 锚 path_report.coalesce(signed_at,created_at)，generate_series 补齐无数据日）；汇总 :1140；穿透 :1376-1405（首次报告 union all 补充报告，带签名人/seq_no/原因）。口径明写终点取 report_issued_at 不拿 diagnosed_at 冒充（ISSUE_VS_DIAGNOSE_NOTE :108-116），并给 coverage.specimens.diagn
- [✓] 等多维度的业务数据汇总（不止一个分组维度） —— 同一看板 8 个可用维度：按日（WORKLOAD_REGISTER :828 / BLOCK :867 / SLIDE :881 / REPORT :896）、按标本类别与来源与加急（:828 的 filter 列族）、按染色类型（WORKLOAD_SLIDE :881 he/ihc/special_stain/molecular）、按病理医师（WORKLOAD_PATHOLOGIST :911 → pathologistRows，写诊断/初签/复签/补充/取材/特检开单六列各按自己的时刻列落窗）、按特检技术类型（WORKLOAD_TECH :913-925 深切/重切/补取材/免疫组化/特殊染色/分子）、按送检科室（WORKLOAD_DEPT :404-415 定义、:853-866 SQL、:1108 汇总、:1344-1357 穿透，门诊取 outp_registration.dep
- [✓] 出的数据真、口径对（同名表头不得两个口径；明细/导出与屏幕同过滤） —— v61 车道 C 两处收口：①存量列正名 issued/在办数 → issued_of_registered「本期登记中已签发」、in_progress「本期登记中在办」（PathQcController.java:856-860、:1110-1114、zh() :1803-1804；前端 format.ts:449-450；流量列 zh("issued")="签发份数" :1703 不动），caveat 明写「这三列是存量不是流量……据此做的科室工作量表不可复现，发出去之前请连同查询时刻一起注明」（:409-413）；②穿透抽屉加「导出本次明细」并把当前 dept 传下去（PathQcView.vue:132-137、:336-340、exportCsv :321-333），指标头部那个明示「（全科室）」（:69-71）。判据测试 server\src\test\java\cn\hip\
- [✓] 零种子库上演示走得通（不是只能看空表） —— tools\demo-pathology.py：纯走真实 API 造数（登录→建患者→排班→挂号→接诊→开 C0301→结算→登记→核收→取材→脱水→包埋→切片→染色→特检医嘱→诊断→初签→复签→签发→补充报告/拒收），不连库不写 insert；:375-386 qc_overview 用 QUALITY 令牌回读三项，:430-434 三项任一为 0 即以退出码 2 失败。已挂 CI：.github\workflows\ci.yml:183 `python3 tools/demo-pathology.py --count 2 --quiet`。实测数字与看板对照见 docs\演示\病理演示数据.md:95-107。另有纯菜单手工路径（菜单 168 病理工作台 ①登记/②取材/③制片/④诊断），不依赖脚本。

演示路径（审计者）：零种子库演示（两段，第一段造数、第二段看数）：

【第一段 造数 — 二选一】
(A) 脚本（推荐，一条命令）：后端起在 8080 后执行
    HIP_E2E_BASE=http://localhost:8080/api python tools/demo-pathology.py --count 5
    它用 admin 登录并顺带建 demo_pathdoc(DOCTOR_OUTP) 与 demo_quality(QUALITY) 两个账号，造 5 个阶梯标本 + 1 个拒收（全部挂在 sys_dept id=1 的挂号科室下，tools/demo-pathology.py:110-112）。跑完打印「2576 钉死的三项」，任一为 0 即退出码 2。
(B) 纯菜单手工：ADMIN 或 DOCTOR_OUTP 登录 → 门诊业务 → 门诊医生站，给一名患者开「组织病理学检查(活检)」(C0301) 并收费结算 → 门诊业务 → 病理工作台(菜单168) → ①登记（填类别 ROUTINE/部位/固定液）→ 接收核对 → ②取材（登记 2 块）→ ③制片（脱水篮→包埋→新建 HE 切片→染色登记）→ ④诊断（书写首次报告→初签）→ 换第二个医师账号复签 → 签发。约 18 步、两次登录。

【第二段 看数 — 全程菜单】
1. 用 demo_quality（或任一 QUALITY / ADMIN 账号）登录 → 左侧「门诊业务 → 病理质控」（sys_menu 169，/pathology/qc）。
2. 页面打开即自动查询近 30 天（PathQcView.vue:342 onMounted），无需点任何按钮——「自动化统计」在这一步就看得到。顶部先出统计口径 alert、时限阈值与统计区间、「本时段字段录入覆盖率」三段。
3. 往下逐指标看（每条先显示归集锚点与口径 caveat，再显示汇总行，再显示明细表）：
   - WORKLOAD_REGISTER 登记总量 → 汇总 registered=6、rejected=1、outp_source=6；按日 1 行（今天），行内还有 ROUTINE/FROZEN/CYTOLOGY/加急/来源构成列。
   - WORKLOAD_BLOCK 蜡块产出数 → blocks=6、embedded=6、specimens=3、blocks_per_specimen=2.0。
   - WORKLOAD_REPORT 报告签发量 → 30 行里只有今天非零：issued_reports=1、diagnosed=2、supplement_reports=1（脚本不能回溯日期，其余 29 行是真实的 0，不是缺陷）。
   - 另可看 WORKLOAD_SLIDE 切片产出（he/ihc 分列）、WORKLOAD_TECH 特检医嘱量、WORKLOAD_PATHOLOGIST 医师工作量（admin 与 demo_pathdoc 两行）、WORKLOAD_DEPT 送检科室工作量（一行，dept 1 的科室名，列为 登记标本数/本期登记中已签发/拒收数/本期登记中在办，三列之和=登记数）。
   - 三条符合率（冰冻-石蜡 / 临床 / 外院会诊）显示红色「缺数据源」并列出要补哪几列，不画全 0 表——这是刻意的诚实标注。
4. 顶部下拉「全部指标」可切到单条指标；日期选择器改区间后点「查询」。
5. 穿透与导出：任一指标右上「穿透明细」→ 抽屉逐条列到标本/患者/科室/时刻；WORKLOAD_

审计者自列的保留项：
- 演示前必须先造数：零种子库上这三项都是 0/空表。允许的两条路径都写在 demo_path 里（脚本 tools/demo-pathology.py 已挂 CI；或菜单 168 手工约 18 步、两次登录）。若评委要求「全程不碰命令行」，就只能走手工那条。
- 脚本不能回溯日期（tools/demo-pathology.py 文件头与 docs/演示/病理演示数据.md「四、脚本做不到的事」均如实写明）：所有时刻是运行当下，30 天趋势只有今天一根柱，WORKLOAD_REPORT 另 29 行仍是真实的 0；报告及时率 100%、各环节 TAT 0.0 小时都是「几秒内走完」的演示值。想要多天柱子只能多天各跑一次。
- 脚本只造门诊来源、ROUTINE、非加急标本：inp_source=0、frozen/cytology/consult/molecular=0、urgent=0、REPORT_FROZEN 无行。住院来源与冰冻分支要手工走。
- docs/演示/病理演示数据.md 停在 v58：第三节「跑完之后看什么」的实测表里**没有** WORKLOAD_DEPT 送检科室工作量这一行，也没提 v61 新增的抽屉「导出本次明细」。文档陈旧不影响功能（脚本造的数全落在 dept 1，科室行会自动出现），但按演示脚本照念会漏掉这两处新亮点——建议定版联动时补。
- WORKLOAD_REPORT 用 generate_series 补齐无数据日，所以即使区间内零签发也会画出 30 行全 0 的表；这与本页对 available=false 指标「绝不画全 0 表」的doctrine 不完全一致。属既有设计（趋势线要连续），不是本轮引入。
- 口径覆盖面限制（DATA_CAVEAT，PathQcController.java:88-95）：v48 之前入库的病理标本 病理号/类别/取材部位/固定液/固定时刻/拒收信息/双签/签发时刻全空且刻意不回填，依赖这些字段的指标只覆盖 v48 之后真正录了字段的标本，不是全院全历史口径；上线后头几个月的数会偏低。这一条已随返回体与 CSV 页脚下发，不算隐瞒。
- 本次为只读审计：未跑 mvn、未起服务、未改任何文件。绿灯数字（全量 1037 例、全新库 46 套 E2E 全绿）取自 CHANGELOG.md 1.6.1 定版验收段，非我实测。
- V61QcCaliberTest.java 在会话开头的 git status 快照里显示为未跟踪（??），实查 HEAD=14e70bf 下 git ls-files 已跟踪、工作树干净——快照陈旧，无欠账。

**反驳者·子要求拆解**（推翻）：

子要求「蜡块产出数」在同一块看板上有两个不同口径的「蜡块数」，且两个数在演示数据下就不相等——正是 v61 车道 C 刚刚为 issued 修掉的那一类缺陷（同名表头两口径），在 blocks 这一列上原封未动、无 caveat、无测试。WORKLOAD_BLOCK 的 blocks 是 count(*) from path_block、锚 coalesce(embedded_at, created_at)，意思是「当日产出蜡块数」；WORKLOAD_SLIDE 的 blocks 是 count(distinct sl.block_id)、锚 coalesce(stained_at, created_at)，意思是「当日染色切片涉及的蜡块数（去重）」。两者都经同一张扁平字典渲染成中文表头「蜡块数」，并排显示在同一页同一天的行里。按脚本演示路径：每个标本取材 2 块、切片只从 block_ids[0] 出，所以今天这一行 WORKLOAD_BLOCK「蜡块数」=6、WORKLOAD_SLIDE「蜡块数」=3——评委在一屏之内就能读到同名表头的两个数，而 WORKLOAD_SLIDE 的 caveat 里一个字都没提这一列。同一形态还有 molecular→「分子病理」（WORKLOAD_REGISTER 数的是 specimen_type='MOLECULAR' 的标本数，WORKLOAD_SLIDE 数的是 stain_type='MOLECULAR' 的切片数）。这不是「没做」，是「做了一半」：v61 只把 issued 一列正了名，没有把同一判据推到列名族的其余同名列，也没有任何机械断言拦住下一个。按题面口径（同名表头不得两个口径、口径对），维持「部分响应」。

证据：param_verbatim（CSV 序号 2576，逐字）：「支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。 +---------------------------------------------------------------------------------------------------------------------------------------------------------」（响应结论=部分响应）

主缺口证据链：
1) C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathQcController.java:867-879 WORKLOAD_BLOCK 按日 SQL，:869 `count(*) as blocks`，窗口 {wb} 见定义 :418-422「coalesce(path_block.embedded_at, created_at)」。
2) 同文件 :881-894 WORKLOAD_SLIDE 按日 SQL，:889 `count(distinct sl.block_id) as blocks`，窗口 {ws} 见定义 :424-426「coalesce(path_slide.stained_at, created_at)」；该指标 caveat 只有 WORKLOAD_NOTE（:426），对 blocks 这一列零说明。
3) 表头中文同源同值：后端 zh() :1759 `case "blocks" -> "蜡块数"`；前端 C:\Users\drxuj\医院信息化\frontend\shell\src\views\medtech\pathology\format.ts:391 `blocks: '蜡块数'`；format.ts:518-520 `zh(col) = ZH[col] ?? col` 是**按列名的扁平字典、不带指标上下文**；渲染处 C:\Users\drxuj\医院信息化\frontend\shell\src\views\medtech\pathology\PathQcView.vue:101 `:label="zh(c)"`，两个指标在同页顺序渲染（:58-115），未选指标时默认全出。
4) 演示即可复现：C:\Users\drxuj\医院信息化\tools\demo-pathology.py:296 每标本取材 2 块（['肿物中心','肿物周边']），:175-176 切片只从 block_ids[0] 出、:189-191 IHC 也挂 block_ids[0] —— 故同一天 WORKLOAD_BLOCK.blocks=6（与题面演示路径给的 blocks=6/specimens=3 一致）而 WORKLOAD_SLIDE.blocks=3。
5) 无人拦得住：C:\Users\drxuj\医院信息化\server\src\test\java\cn\hip\server\V61QcCaliberTest.java:70 只钉 STOCK_COL="issued_of_registered"，:145-150 只断言 issued / issued_of_registered / in_progress 三个中文；V59QcLabelsTest 只断言「前端 ZH ⊇ 后端 zh() 同名 case 且中文逐字一致」（:78、:82-92），断的是两侧同名键中文一致，恰恰**强制**了同名列必须同一个中文，反而把这个缺陷固化了；全仓仅 PathQcController / demo-pathology.py / e2e-v58-audit.py 提到 WORKLOAD_SLIDE，没有任何测试或 E2E 碰这一列。
6) 现有口径文本不覆盖：WINDOW_ANCHOR_NOTE（:79-86）说的是「各指标不共用归集时刻、跨指标横向相加没有意义」，说的是相加，不是「同一个中文表头在两条指标下是两个计数对象」——v61 自己就是在 WINDOW_ANCHOR_NOTE 早已存在的前提下仍判定 issued

核过的点：从 CSV 原文重读 2576 参数摘要（utf-8-sig + CRLF，按序号列取行，逐字抄入 param_verbatim；控制台乱码系 codepage，改写文件后读取确认编码无误）；拆子要求：支持 / 基础工作量 / 自动化 / 统计 / 涵盖 / 病理登记总量 / 蜡块产出数 / 报告签发量 / 等多维度 / 业务数据汇总；PathQcController.java 全部 WORKLOAD_* 定义（:396-448）与 anchor/caveat 文本；WORKLOAD_REGISTER 按日 SQL :828-845 与 summary :1098-1106（发现列不同构）；WORKLOAD_DEPT 按日 SQL :853-865 与 summary :1108-1119，含 v61 正名后的 issued_of_registered / in_progress；WORKLOAD_BLOCK 按日 :867-879 与 summary :1120-1128；WORKLOAD_SLIDE 按日 :881-894（发现 count(distinct sl.block_id) as blocks）与 summary :1129-1138；WORKLOAD_REPORT 按日 :896-910（generate_series 补零日）与 summary :1140-1147；确认 path_report 确系补充报告专表（V144__pathology_base.sql:15；WORKLOAD_TECH :913-931、PROCESS_TAT :800-826 的列名复用情况；后端 zh() 全表 :1647-1830，逐列比对是否存在同名不同义列（blocks / molecular / specimens / urgent 四处命中）；前端 format.ts ZH 字典 :299-513 与 zh() :518-520，确认是扁平按列名查表、无指标上下文；cellText :532-560 只翻译取值不改表头；PathQcView.vue 全文：onMounted 自动查询 :342-345、表头绑定 :101、汇总 el-descriptions :93-97、穿透抽屉与 exportDetailInDrawer :132-137/:336-3

**反驳者·数据真实性与口径**（推翻）：

「蜡块产出数」的按日归集是**代填 + 可回溯变动**的：未包埋的蜡块被拿 created_at 顶替 embedded_at，先算进取材日的「产出」，等包埋登记落下去又从取材日消失、跳到包埋日——同一个已关闭区间今天导与明天导的按日数不一样（某天变小），而 caveat 只写了「回落建档时刻」，**一个字没提这个数事后会变、不可复现**（v61 刚给 WORKLOAD_DEPT 存量列写明的正是同一类缺陷，BLOCK/SLIDE 漏了）。这直接违反 V144 自立的「零回填纪律：宁可少算，不可假算」，且全仓无任何测试盯它。

证据：param_verbatim（CSV 序号 2576 第 4 列逐字）：「支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。 +---------------------------------------------------------------------------------------------------------------------------------------------------------」；结论列「部分响应」。

【缺口一：蜡块/切片「产出数」是代填的，且日归集可回溯变动（主因）】
1) 事实链：取材时 insert path_block 只落 created_at，embedded_at 为空——C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathologyProcessController.java:503-510（insert 列表里没有 embedded_at）；包埋确认才 `update path_block set embedded_at = now()`（同文件 :852-856）。同文件 :1080 明写「制片要求 block.embedded_at 不为空」——即平台自己承认未包埋的蜡块还不是可用产出。
2) 口径：WORKLOAD_BLOCK 锚点 = coalesce(b.embedded_at, b.created_at)（PathQcController.java:418-420 定义、:240-241 W_BLOCK 谓词、:867-880 按日 SQL、:1120-1128 汇总、:1372 穿透 order by）。于是未包埋的蜡块**先记在取材日**的「蜡块产出数」里。
3) 后果：包埋登记一落，同一块蜡块的归集日从 created_at::date 跳到 embedded_at::date——昨天那一行的 blocks **会变小**、包埋日那一行变大。脱水过夜是病理常规（平台自己建了 dehydrate_batch 脱水篮字段、PathologyProcessController.java:743 的批次 update），跨日是常态不是边角。
4) 未披露：PathQcController.java:418-424 的 caveat 只说了「未录包埋时刻的回落建档时刻」和 blocks_per_specimen 不是每标本块数，**没有任何「事后会变/不可复现」的提示**。全文 grep「不可复现/会变大/随查询时刻」只命中 WORKLOAD_DEPT 一处（:411-413、:849-852）——v61 车道 C 认定这类「同一区间两次查不一样」必须写明并加判据测试，却只给 DEPT 写了，BLOCK（:418-424）与 SLIDE（:426、:238-239 W_SLIDE，path_slide 同样是制片时 insert 无 stained_at、染色登记才落 stained_at）原样漏掉。
5) 与自立纪律冲突：V144__pathology_base.sql:5-10「【零回填纪律】……绝不拿 collected_at 去填 received_at……宁可少算，不可假算」。这里正是拿 created_at 去填 embedded_at 的位置，把尚未发生的「产出」算成当日产出，而指标名叫「蜡块产出数」。
6) 无判据：server/src/test 全仓 grep「WORKLOAD_BLOCK」零命中（V61QcCaliberTest.java 只有 stockColumnIsNotAFlowAndIsNamedAccordingly 与 drawerExportCarriesTheDeptFilter 两个用例，都只管 DEPT）；唯二断言是 tools/demo-pathology.py:384 与 tools/e2e-v58-audit.py:356-361 的「同一次运行内 blocks>0」，分钟级同日跑完，**结构上检测不到跨日迁移**。审计者引的正是这两条。

【缺口二：「签发量」只有按日一个

核过的点：从 docs/验收/技术偏离表-v3.csv 按序号 2576 重读参数摘要与结论列（UTF-8-BOM，逐字抄录，未信审计者转述）；PathQcController.java:418-424 WORKLOAD_BLOCK 定义与 caveat 全文；PathQcController.java:236-241 W_BLOCK / W_SLIDE 时间窗谓词（coalesce 锚点）；PathQcController.java:867-880 蜡块按日 SQL、:1120-1128 汇总、:1358-1372 穿透；PathologyProcessController.java:503-510 取材 insert path_block（无 embedded_at）；PathologyProcessController.java:848-870 包埋确认 update embedded_at = now()；PathologyProcessController.java:1064-1082 制片 insert path_slide（无 stained_at）+ 要求 block 已包埋；PathQcController.java:426 WORKLOAD_SLIDE 锚点与 caveat（同款回落，无不可复现提示）；全文 grep「回落 / 不可复现 / 会变大 / 随查询时刻」——不可复现只写在 WORKLOAD_DEPT（:411-413、:849-852）；V144__pathology_base.sql:1-10 零回填纪律原文；PathQcController.java:396-416 WORKLOAD_REGISTER / WORKLOAD_DEPT 定义，含「平台目前拿不出本期签发量按科室分」自认；PathQcController.java:428-435 + :896-910 + :1140-1147 报告签发量锚点与 SQL

**反驳者·演示可行性**（推翻）：

同一块看板上两个「蜡块数」表头、两个口径，且在审计者自己那份演示数据里就是两个不同的数（6 与 3）——v61 车道 C 刚把 issued 正名成 issued_of_registered 治的正是这个病，隔壁指标原样留着，而且这次撞的还是 2576 参数摘要点名的「蜡块产出数」。WORKLOAD_BLOCK 的 blocks 是 count(*)（当日产出蜡块数），WORKLOAD_SLIDE 行里的 blocks 是 count(distinct sl.block_id)（当日染色切片涉及的蜡块数），前后端字典都只有一条 blocks→「蜡块数」，两处指标的 caveat 一个字都没提这件事。评委按演示路径第二段往下滚，先看到「蜡块数 6」，再看到「蜡块数 3」，问「蜡块到底做了 6 块还是 3 块」时，页面上没有任何一行字能回答——只能去读 SQL。同型的还有「分子病理」（登记总量里是标本类别 MOLECULAR 条数、切片产出里是染色类型 MOLECULAR 张数，同一条 molecular→「分子病理」）。

证据：param_verbatim（自 docs/验收/技术偏离表-v3.csv 按序号 2576 逐字读出，utf-8-sig + CRLF）：「支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。 +---------------------------------------------------------------------------------------------------------------------------------------------------------」（响应结论=部分响应）

一、同名两口径（主缺口，落在「蜡块产出数」这一子要求上）
- C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathQcController.java:867-880 WORKLOAD_BLOCK 按日行：`count(*) as blocks`（产出，锚 coalesce(embedded_at, created_at)）。
- 同文件 :881-894 WORKLOAD_SLIDE 按日行末列：`count(distinct sl.block_id) as blocks`（涉及，锚 coalesce(stained_at, created_at)）。同一查询族里 count(distinct) 的列本来有命名惯例——:870 的 `count(distinct b.specimen_id) as specimens` 叫「涉及标本数」——这一列却仍叫 blocks。
- 表头字典只有一条：后端 :1759 `case "blocks" -> "蜡块数"`；前端 C:\Users\drxuj\医院信息化\frontend\shell\src\views\medtech\pathology\format.ts:390 `blocks: '蜡块数'`。CSV 同源：PathQcController.java:1618 表头一律 `csv(zh(c))`，导出文件里也是两个「蜡块数」。
- caveat 不救场：def("WORKLOAD_BLOCK", …) :418-422 的 caveat 只解释 blocks_per_specimen；def("WORKLOAD_SLIDE", …) :424-426 只挂通用 WORKLOAD_NOTE（:124-127，讲的是「不折算工时」）；顶层 WINDOW_ANCHOR_NOTE :79-86 只说锚点不同、不可横向相加，没有一个字说这两列一个是 count(*) 一个是 count(distinct)。
- 演示数值实算（按审计者给的 `--count 5`，tools\demo-pathology.py:169-182 technical() 只在 block_ids[0] 上开 2 张 HE、:186-200 ihc_attach 也挂在同一块上、:203-212 tech_cancel 那单被取消不出片）：达到 STAINED/DIAGNOSED/ISSUED 的 3 个标本各取材 2 块 → WORKLOAD_BLOCK.blocks=6、embedded=6、specimens=3；而有切片的蜡块只有 3 块 → WORKLOAD_SLIDE 行 slides=8、he=6、ihc=2、blocks=3。屏幕上「蜡块数 6」与「蜡块数 3」相隔一屏。
- 无测试守这条：V59QcLabelsTest.java:71 只查「后端每个列键都有 zh 标签」（ZH ⊇ zh()），没有任何测试查「同一中文标签不得对应两个口径」；V61QcCaliberTest.java:110 只钉了 issued_of_registered 这一条。

二、同型未清（同一块看板、同一次演示可见）
- `molecular`：PathQcController.java:840（登记总量，specimen_type='MOLECULAR' 的标本条数）与 :887/:1135（切片产出与汇总，stain_type='MOLECULAR' 的切片张数

核过的点：按序号 2576 从 docs/验收/技术偏离表-v3.csv 自行读出参数摘要与响应结论（utf-8-sig，csv.reader，未信审计者转述）；PathQcController.java 指标定义段 :396-445（WORKLOAD_REGISTER / DEPT / BLOCK / SLIDE / REPORT / PATHOLOGIST / TECH 的名称、锚点、cavea；按日行 SQL :828-931（六条 WORKLOAD_* 的列清单，逐列比对表头字典）；区间汇总 SQL :1098-1156（确认汇总是重算不是按日相加；WORKLOAD_SLIDE 汇总无 blocks 列）；穿透明细 SQL :1256-1450（SPEC_SELECT 复用、WORKLOAD_DEPT 的 dept 过滤与 stage 派生、WORKLOAD_REPORT 首次/补充 union、WORKLOAD_TECH 的 attache；SPEC_SELECT :185-222 与 TYPE_NAME :224-232（确认明细里 specimen_type / source 回的是编码，屏幕靠 cellText 翻译）；后端表头字典 zh() :1640-1845 全表（blocks / molecular / stained / attached_stain / attached_stain_name / issued / issued_of_regis；CSV 导出 :1560-1630（表头一律 zh(col)、取值原样、filterNote 页脚、截断说明）；parseWindow :463-479 与 MAX_SPAN_DAYS / DETAIL_LIMIT / ROW_LIMIT 常量；PathQcView.vue 全文 364 行（渲染顺序、缺数据源分支、穿透抽屉、两个导出按钮、onMounted）；format.ts 的 ZH 字典 :260-520、cellText :520-560、isIdColumn/idColumnsLast/colWidth、defaultRange :580-601；PathologyReportController.java TECH_DERIVED_COLUMNS :209-241（attached_stain_name 中文映射、blocks_derived_source）

## 下一轮清单（主控从未推翻/推翻原话里提炼；修前逐条实测）

**这轮的总模式：v61 的每一条修复都只做了一半，而复核者正好从没做的那一半进来。** 三条全维持，9 个镜头 9 个推翻。
值得单独记一笔：**审计者自己就判了 2530 / 2563「维持」**（前三轮都是 3/3「可上调」）——判据比以前严了，但仍漏掉了反驳者找到的入口。

- **2530（3/3 推翻）** ①**补取材追加后点「修订取材描述」，库里已落的字段一项都不预填**：前端 `fields = (d.fieldsCurrent === true ? d.fields : [])`，
  而 v61 恰恰把追加场景的 `fieldsCurrent` 改判为 false——自己的修复把自己的预填拆了；累积全文整段塞进「自由描述」，
  提交后新版字段行为 0，「结构化存储」在最典型的补取材路径上被这个入口反向拆掉。后端 `reviseGrossFields` 对 `gross=null` 无任何防线。
  ②同一弹窗的黄条写「库里的字段行属于第 2 版，与当前第 2 版文本不同版」——同一个版号被说成「不同版」。
  ③只写自由描述的标本（演示脚本铺出来的全部标本）在查看抽屉里被打成「被取代版本（第 1 版，0 项）：已被第 — 版字段取代」——
  它没被任何版本取代，「第 —」是 `fmt(null)`；审计者以为存在的诚实兜底「无字段级记录」在全新库标本上**永远不可达**。
  ④`textIsCumulative` 后端建好了、**前端零消费**；诚实的黄条 `v-if` 绑了「是最新版」，一切到旧版就消失——说真话的提示与说假话的标签互斥出现。
  ⑤被取代版本的「录入」列恒为「— —」：后端 `fieldsByRevision` 只 select `revision_seq/seq/label/value`，前端照样渲染 `operatorName/createdAt`。
- **2563（3/3 推翻）** ①**质控穿透 CSV 导出里没有中文「技术分类」列**，表头却叫「技术类型编码」（暗示有一列成对的中文而它不存在）；
  同一行「状态」与 v61 新加的「关联蜡块来源」**同时出现 `ORDERED` 而含义完全相反**（待执行 vs 下达时指定）；
  「挂接切片染色」这个中文表头**出现两次**（英文版与中文版被映射成同一个名字）。屏上靠 `cellText` 逐列翻译掩盖，导出一离开页面就原形毕露。
  ②v61 只改了 `techLabel` 覆盖的三个节点，**同一张表里另外三处仍打裸码**：STAIN 备注「质量 GOOD」、SECTION 备注「（IHC CK7），特检医嘱#12 IHC」、GROSSING 备注「补取材医嘱#12 RESAMPLE」。
  ③**完成校验 gate 只认切片、不认蜡块**：进度列写着「已补取材待切片 已出块 2」，点完成时后端的事实查询里根本没有这一列，
  判 gap=「无挂接切片」——warn 档把「无证据完成」的假账永久写进流转节点，block 档下补取材医嘱**永远完不成**。六态派生已读四参、完成 gate 仍是两参口径。
- **2576（3/3 推翻）** ①**同一块看板上两个「蜡块数」**：`WORKLOAD_BLOCK` 是 `count(*)`（当日产出），`WORKLOAD_SLIDE` 是 `count(distinct block_id)`（当日染色涉及），
  演示数据下一屏之内就是 6 与 3 两个数，两处 caveat 一个字没提——正是 v61 刚为 `issued` 修掉的那类缺陷，在 `blocks` 上原封未动，而它恰是参数摘要点名的「蜡块产出数」。
  同型的还有 `molecular`→「分子病理」（一个数标本、一个数切片）。
  ②**「蜡块产出数」的按日归集是代填 + 可回溯变动**：未包埋的蜡块拿 `created_at` 顶替 `embedded_at` 先算进取材日，等包埋登记落下又从取材日消失、跳到包埋日——
  同一个已关闭区间今天导与明天导不一样，caveat 只写了「回落建档时刻」，没提这个数事后会变。违反 V144 自立的「零回填纪律：宁可少算，不可假算」，全仓无测试盯它。

**方法上这轮的教训**：v61 车道纪律里写了「改『值』的同时必须改『判定该值的条件』」，结果车道 A 自己踩了同一个坑
（改了 `fieldsCurrent` 的判定，没改消费它的预填逻辑）。下一轮把它升级成可机械检查的形式：
**凡改动某个返回键的语义，先 grep 出它的全部消费方（前端 + 测试 + E2E），逐个确认或改，列进车道任务书的验收项。**

