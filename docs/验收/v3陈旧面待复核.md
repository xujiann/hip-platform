# v3 偏离表「陈旧面」待复核清单

## 这份文件解决什么

`技术偏离表-v3.csv` 是 2026-09-01 依据当时代码现状重出的。此后交付了
**v46（手术室护理与麻醉质控）与 v47（写入校验收口）**，其中一批条目的
「部分响应」结论**可能已经陈旧**——不是当时写错了，是代码后来补上了。

陈旧的应答同样是失真，只是方向相反：**少报**。它一样会在验收会上出问题
（评委按矩阵抽查，发现"你说没有的其实有"，会连带质疑其余条目的可信度）。

## 当前状态：**未完成，不得据此改动 v3**

首轮自动复核（2026-09-05）**因网络故障大面积失败**：38 个复核 agent 中 36 个
撞证书/连接错误，只有 1446、1447 两条跑完。

**而这两条的三个反驳者全部挂掉**——按本项目的复核纪律，
**没过反驳的判定不算数**。故它们目前是「待确认」而非「可上调」。

### 已抽验（主控手工，2026-09-06）

| 序号 | 自动判定 | 主控抽验结论 | 状态 |
|---|---|---|---|
| 1446★ | 可上调为正偏离/无偏离 | `AnesQcController.java:260` 确为 `def("1446", …)` 即 `available:true`，与 1435/1444/1445 的 `unavailable()` 分属两类；`:668` 有分组统计 SQL、`:1037` 有穿透明细分支。**代码事实与判定一致** | **待补反驳者**，不改 v3 |
| 1447★ | 可上调为正偏离/无偏离 | `AnesQcController.java:261` 同为 `def(…)`，`:684` 统计、`:1038` 明细 | **待补反驳者**，不改 v3 |

抽验只证明「代码里确实有」，**不等于「验收会上演示得出来」**——
后者正是反驳者要打的地方（子要求是否逐条覆盖、演示路径是否走得通、
数据是否真实而非空表算指标）。

## 待复核范围（38 条）

v46 相关 20 条（★ 14）：1397、1421–1426、1429、1433、1434、1437、1446、1447、
2019、1787、2922、2928、2934、3111
v47 相关 18 条：493、495、520、624、684、686、798、1701、1704、1708、1835、1837、
2072、2308、2333、2396、2430、2474、2901

## 复核纪律（不得简化）

1. **每条过三个独立反驳者**，分别打三个角度：子要求拆解、演示可行性、数据真实性。
   反驳者的默认立场是「推翻」，不确定即判推翻。
2. **上调只针对「代码后来补上了」**。若某条本就属外部边界或配套产品，
   **维持原答**——补开发救不了它，改答才对。
3. **★ 条目上调必须留签认人**，与 `偏离表重出-签认清单.md` 同一纪律。
4. v3 在 449 条签认完成前**整体不得对外**；本清单的上调项即便复核通过，
   也只是进入签认队列，不是直接生效。

## 下一步

网络恢复后重跑复核工作流：
`workflows/scripts/hip-v3-staleness-wf_482e592b-abf.js`（args 见该次运行记录）。
**必须确认三个反驳者都真的跑完**，只要有一个是 error 就不算过审。
