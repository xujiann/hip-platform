# v60 交付后复核（2026-09-12，main=b694eea） —— 结论：**3 条中 0 条上调**（上调：无；维持「部分响应」：[2530, 2563, 2576]）

方法与 2553 那次相同：只传序号，审计者自读 `技术偏离表-v3.csv` 回填参数原文并核当前主树，每条再过三个独立反驳者（子要求拆解 / 演示可行性 / 数据真实性与口径），**零推翻才上调**。
证据落 `audit_r2_wave13.json`，本轮不出改写。

## 结论表

| 序号 | 审计者 | 子要求拆解 | 演示可行性 | 数据真实性与口径 | 结论 |
|---|---|---|---|---|---|
| 2530 | 可上调 | — | 推翻 | 推翻 | 维持部分响应 |
| 2563 | 可上调 | 推翻 | 推翻 | 推翻 | 维持部分响应 |
| 2576 | 可上调 | 未推翻 | 推翻 | 推翻 | 维持部分响应 |

## 逐条：参数原文、审计者的主张、反驳者的硬缺口（原话，未转述）

### 2530

**参数原文（CSV 逐字）**：支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。

**审计者**：可上调。最接近缺口的一点：不阻断本条、但对外文案必须收住的一处：**结构化只覆盖取材端，不覆盖报告端**。病理医师在 ④ 诊断里改写大体所见时（PathologyController.java:129-171），只落 path_gross_revision 的文本修订行（source=DIAGNOSE，无 template_code），**不落字段行**；读端点因此回 fieldsCurrent=false、fieldsNote 提示「文本已在第 N 版修订，以文本为准」（PathologyProcessController.java:1586-1590），界面上「字段级记录」停在取材那一版。也就是说「描述内容的结构化存储」在取材工位是逐字段落库的，到了报告环节退化为扁平文本 + 留痕。条款问的是「取材过程信息」，取材侧字段完整且旧版可调阅，故不构成缺口；但不能对外写成「大体描述全生命周期结构化」或「报告修改同步结构化字段」。

子要求核对：
- [✓] 子要求①「支持文字化记录」——取材工位要有真正的文字录入入口（而不是 v3 点名的「固定预设值」） —— 入口：frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:74「取材登记」按钮（工作列表每行）→ :196 取材登记对话框 → :253-263 取材模板下拉、:274-276 逐字段 el-input（maxlength=300）、:279-280 自由描述 textarea（拼成一段大体所见，总长上限 2000 字）。后端 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyProcessController.java:345 POST /api/pathology/process/grossing（GrossingReq.gross/grossText/templateCode/blocks[].tissueDesc/remark）。原「固定预设值」路
- [✓] 子要求②「完整的取材过程信息」——不止一段话：结构化字段 + 自由描述 + 模板 + 每块组织描述 + 备注 + 取材人/时刻，且可修订、可补取材追加，超长报错不静默截断 —— 字段+自由文本拼装 PathologyProcessController.java:2058-2094 assembleGross（字段上限 20 项、label 32 字、value 300 字、总长 2000 字，越界 5222 报错而不截断；重复字段名拒绝）；每块 tissue_desc 上限 500 字 :143/:356-360；GROSSING 节点带 operator_id + 库端 now() :529-534；取材修订入口 :571 PUT /grossing/{specimenId}/fields（GrossingPanel.vue:76「修订取材描述」按钮 + :314「提交修订」）；补取材追加 :443-453（append=true 时文本追加为新版本，总长复检）；内置模板 8 套含字段清单与示例 :1976-1999（GI_BIOPSY/BREAST_MASS/
- [✓] 子要求③「确保描述内容的结构化存储」——不是拍扁成一段文本的单列 —— 迁移 server/src/main/resources/db/migration/V164__path_gross_fields.sql 建 path_gross_field（specimen_id, seq 按入参顺序, label(32), value(300), operator_id）与 path_gross_revision（seq, old_text, new_text, source, changed_by/at）；V166__path_gross_revise.sql 给字段行加 revision_seq、唯一约束改 (specimen_id, revision_seq, seq)、修订行加 template_code、source 白名单增 GROSSING_EDIT。写入 PathologyProcessController.java:2106-2118 storeG
- [✓] 子要求④「确保描述内容的调阅」——含被取代版本与修订留痕，从菜单看得见 —— 读端点 PathologyProcessController.java:1444 GET /grossing/{specimenId} 回 grossFinding / fields / fieldsAvailable / fieldsRevisionSeq / textRevisionSeq / fieldsCurrent / fieldsNote / fieldsByRevision（:1537-1573 全部版本按 revision_seq 归组，无字段行的版本 fields=[]，不猜来源）/ revisions（:1508-1531 含 sourceName 与 templateCode）。前端：GrossingPanel.vue:96-148「查看大体所见」抽屉（:113-126 按版下拉切换、标「最新字段版／被取代版本」、显示来源与模板码；:130 字段与文本分叉时明说以文本
- [✓] 当前说明所列缺项①「取材登记界面的取材过程描述录入入口（当前描述内容为固定预设值）」 —— 同子要求①：GrossingPanel.vue:74/196/253-281 为真实录入入口；固定预设值路径已删（SpecialtyView.vue 零 grossFinding 命中），并由 V59GrossReviseTest:459 源码扫描守卫锁死
- [✓] 当前说明所列缺项②「标本描述在流转列表中的调阅展示」 —— 后端六个读端点都 select s.specimen_desc（PathologyProcessController.java:1448 取材视图头、:1627 轨迹头、:1954 取材工作列表；PathologyRegistryController.java:686 检索/既往；PathologyReportController.java:298 阅片 worklist、:696 prior、:1264 reports 头）。前端只读展示：RegistryPanel.vue:147（检索表列）与 :352（既往抽屉列）、GrossingPanel.vue:54（取材工作列表列）与 :98（取材视图头）、TrailPanel.vue:166（轨迹头）、DiagnosisPanel.vue:126（报告抽屉头）与 :391/:419（既往对比两栏）。守卫 V60SpecimenDescSho

演示路径（审计者）：全新零种子库可走通，单个 ADMIN 账号一次登录即可（排班/挂号 OutpRegistrationController、OutpScheduleController 限 ADMIN/CASHIER，医生站 DoctorStationController.java:24 含 ADMIN，结算 ChargeController.java:23 含 ADMIN，病理工作台菜单 168 授 ADMIN/TECHNICIAN/DOCTOR_OUTP，V144__pathology_base.sql:198 + V156__v52_menus.sql:24-27，路由 frontend/shell/src/router/index.ts:59）。**不需要先跑 tools/demo-pathology.py**；反而要注意该脚本造的标本只传 grossText、不传 gross 字段（tools/demo-pathology.py:160-163），用它造的数打开「查看大体所见」会显示「无字段级记录」——要演示结构化字段必须现场手工录入。步骤：(1) 门诊排班/挂号/接诊，医生站给患者开「组织病理学检查(活检)」类项目 → 收费结算（菜单：门诊业务 → 排班/挂号/门诊医生站/收费）。(2) 菜单「门诊业务 → 病理工作台」（/pathology/workbench）→ ① 登记：待登记申请里选该已收费申请，填部位序号/标本类别/**标本描述**（如「左乳肿物切除标本，灰白结节一枚」）/取材部位/临床诊断/固定液 → 登记 → 同页「接收核对」。看到：检索表出现该标本，行上「标本描述」列原样显示刚填的文字（RegistryPanel.vue:147）。(3) ② 取材：范围「待取材」能看到该标本，行上「标本描述」列同样可见（GrossingPanel.vue:54）→ 点「取材登记」→ 取材模板选「乳腺肿物」（字段自动出 7 个输入框）→ 逐字段填（标本大小 5×4×3cm、肿物大小 2×1.5×1.5cm、边界 界欠清…）+ 自由描述补一句 → 蜡块加 2 块并各写组织描述 → 提交。看到：结果区回显 grossFindingWritten=已写入、「已落库的字段级记录」表按入参顺序列出各字段（GrossingPanel.vue:335-352）。(4) 点「查看大体所见」：抽屉上半是标本描述与拼好的大体所见全文，下半「字段级记录」标「最新字段版（第 1 版，N 项），与当前文本同版」，并显示来源「取材首写」与模板码 BREAST_MASS。(5) 点「修订取材描述」→ 表单已按库里第 1 版字段预填 → 改一个字段值、加一个字段 → 「提交修订」→ 提示「已生成第 2 版取材描述（字段 N 项）」。(6) 再点「查看大体所见」：版本下拉可切到「被取代版本（第 1 版）：已被第 2 版字段取代，仅供调阅」——**旧版字段原样可调阅**。(7) ⑥ 流转与异常 → 该标本「查看轨迹」→ 抽屉头显示标本描述，「大体所见修订」表按 seq 列出第 1 版（取材首写，old_text 空）与第 2 版（取材修订，old→new 全文对照、修订人与时刻、模板码），每行可展开看该版字段行。(8)（可选）④ 诊断：写报告时留空大体所见 → 返回 grossKept=true，取材原文一字不动；若改写大体所见则轨迹里多出一条来源「诊断修订」的修订行，原取材版本仍在。该链路的每一步都有 CI 全新库 E2E 对账：tools/e2e-v60-audit.py 段 [1]（

审计者自列的保留项：
- 演示陷阱：tools/demo-pathology.py:160-163 造数只传 grossText，不传结构化字段——用它铺数据后打开「查看大体所见」会显示「无字段级记录（历史标本或纯自由文本）」。演示结构化字段必须现场从 ② 取材登记手工录入，不要先跑脚本再指着空表讲。
- 零回填是刻意的：V164/V166 两条迁移全无 update（V58GrossFieldsTest:404 migrationV164IsZeroBackfill、V59GrossReviseTest:517 同款守卫），V164 之前登记的标本在 path_gross_field / path_gross_revision 里恒零行，读端点显式 fieldsAvailable=false。全新库演示不涉及，但若在旧库上演示老标本会看到「无字段级记录」——这是诚实口径，不是缺陷，讲解时要先说。
- 模板管理没有界面：字段清单只能由 sys_config 键 path.grossing.template.<CODE> 覆盖/新增（PathologyProcessController.java:172、:2004-2052），示例文本是硬编码常量、配置覆盖不了；无模板写端点、无按科室/医师维护、无版本与停用（:640-646 自陈）。条款未要求，但别答成「支持取材模板自定义管理」。
- 标本描述在工作台登记路径上不是必填：4554 强制只在旧端点 PathologyController.java:58-59（/pathology/specimens collect），工作台走的 PathologyRegistryController 只校验长度 ≤255（:781），前端 RegistryPanel.vue:206 该项也未标 required。演示时要记得真填，否则后面六处「标本描述」都显示「—」，正好撞上 v3 说明里点名的那一项。
- ④ 诊断抽屉只显示标本描述与大体所见全文，不显示字段级记录与修订表；字段与修订的调阅入口在 ② 取材的「查看大体所见」与 ⑥ 流转与异常的轨迹抽屉两处。演示要按这个顺序点，别在 ④ 里找字段。
- 上调仍须走 2553 那套流程：本条为单 agent 只读审计，按 §二·五 ★ 条目上调需负责人签认与三方复核（读 CSV 原文、param_verbatim、A/B/C/D/E 五维），本结论不等于已改表。

**反驳者·演示可行性**（推翻）：

演示第 (5) 步「加一个字段」在界面上根本做不到：结构化字段的**字段名集合是封闭的**——GrossingPanel.vue 里输入框由 `grossFields`（=当前模板 fields ∪ 表单里已有的键，:436-440）逐个渲染（:274-276 `v-model="form.gross[f]"`），全文件唯一的「增加」按钮是 :286「增加一块」（蜡块），**没有任何自定义字段名的入口**；而模板字段清单只能由运维直改 sys_config（后端自己写明「本版没有模板写端点、没有模板管理界面」，PathologyProcessController.java:644-648）。于是评委现场问「模板里没有『淋巴结清扫组数』这一项怎么办」，唯一答案是写进「自由描述」——即回落成一段非结构化文本，正是参数摘要「确保描述内容的结构化存储」要消灭的形态。后端明明支持任意 label（20 项 / 32 字，assembleGross :2058-2094），前端却不给录入口，这是「只做了后端 / 演示走不完」的典型。

证据：【我自己从 CSV 读到的原文】序号 2530，实质性★。参数摘要逐字：「支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。 +---------------------------------------------------------------------------------------------------------------------------------------------------------+」；响应结论「部分响应」；响应说明「已提供：病理标本描述字段的存储与保存接口。尚未提供：①取材登记界面的取材过程描述录入入口（当前描述内容为固定预设值）；②标本描述在流转列表中的调阅展示。上述缺项列入后续开发计划。」

按评委现场走菜单（门诊业务 → 病理工作台 /pathology/workbench），我复核到四处会被当场问住的地方：

(1) 字段名集合封闭（见 hardest_gap）。frontend/shell/src/views/medtech/pathology/GrossingPanel.vue:253-263 模板下拉 → :273-277 逐字段 el-input，字段来自 `grossFields`（:436-440）；`onTemplateChange`(:443-448) 也只在模板字段之间搬运。想加模板外的一项，只能换成另一个模板（字段名仍限于 9 套内置清单的并集），或让运维去改 sys_config。审计者演示脚本第 (5) 步「改一个字段值、加一个字段」按现有界面执行不了。

(2) 第 (3) 步提交完，第 (4)(5)(6) 步无处可点：submit() 成功后 `await load()`（GrossingPanel.vue:770），默认范围是 `scope:'pending'`（:400），而后端 pending 判据是「无蜡块」（PathologyProcessController.java:261-264 `and not exists (select 1 from path_block …)`）。刚取完材的标本立刻从列表消失，零种子库里这就是整张表「该范围内无标本」（:79 空态文案）；「查看大体所见 / 修订取材描述」两个按钮都长在行上（:74-77），评委必须自己把「范围」切成「全部」才接得下去——演示脚本里没有这一步，而「取材完成」弹窗只有「知道了」，不给任何跳转。

(3) 「调阅被取代版本」当场缺人缺时刻：切到被取代版本时 `viewFields` 取 `fieldsByRevision[i].fields`（:686-692），而后端 byRevision 的字段行只 select `revision_seq, seq, label, value`（PathologyProcessController.java:1534-1539），**没有 operator/created_at**；同一张表却仍渲染「录入」列 `{{ fmt(row.operatorName) }}　{{ fmtTime(row.createdAt) }}`（GrossingPanel.vue:142-144）→ 被取代版本恒显示「—　—」。后端在同一个对象里其实带了 changedAt/changedByName（:1545-1551），前端版本头（:124-127）只渲染「来源 / 模板」，不渲染它们。即：最新版看得见谁何时录，历史版看不见——「调阅」这一子要求在被取代版本上是半截的（要补人和时刻得另开工位六 TrailPanel:243-248）。

(4) 英文码上屏 + 一处不实注释：取材完成弹窗把 `codePrefixSource` 原样打成标签（GrossingPanel.vue:311，值域 PATH_NO|BARCODE，后端 :534）；「查看大体所见」与轨迹「模板」列只显示英文模板码 BREAST_MASS（GrossingPanel.vue:125、TrailPanel.vue:240-241），中文模板名「乳腺肿物」明明已在 templates 里却不用。另外 GrossingPanel.vue:53 与 tools/e2e-v6

核过的点：自己用 python csv 从 docs/验收/技术偏离表-v3.csv 按序号列读出 2530 行，逐字取参数摘要与响应说明（未采信审计者转述）；GrossingPanel.vue 全文 815 行：工作列表列（含 :55 标本描述）、取材登记/修订对话框、结果弹窗、查看大体所见抽屉、全部 script 逻辑；确认 GrossingPanel.vue 中 form.gross 的所有写点（:275/:438/:444-447/:615/:630/:757）与全文件「增加/新增/添加」仅 :286「增加一块」——无自定义字段入口；PathologyProcessController.java grossingWorklist :247-288 的 pending 判据（无蜡块）与 submit 后 load() 的联动 → 取材完成即从默认范围消失；PathologyProcessController.java grossingView :1444-1600：fields（带 operatorName/createdAt）与 fieldsByRevision（只 revision_se；GrossingPanel.vue :112-146 版本下拉与字段表渲染，确认被取代版本「录入」列恒为「—　—」、版本头不渲染 changedAt/changedByName；assembleGross :2058-2094 与 storeGrossFields :2106-2118：后端支持任意 label（20 项/32 字/300 字/2000 字，越界 5222 不截断）；grossingTemplates :627-660 与 BUILTIN_TEMPLATES :1976-1999：9 套内置模板、字段清单可由 sys_config 覆盖，但注释自述无模板写端点/无模板管理界面；reviseGrossFields PUT /grossing/{id}/fields :571-626 的前置条件（未诊断/未拒收/已核收/已有大体所见/内容有变化）；PathologyRegistryController pending :149-197（CHARGED + 项目名含病理/活检）与 doRegister :269-345，确认 specimenDesc 非必填、只查长度；PathologyController.java :46-58 的 4554 只属旧端点；前端 grep pathology/specimens 确认工作台不走该端点，SpecialtyView.vue:285 只剩 GET；V21__phase24_specialty.sql:66 已种 C0301 组织病理学检查(活检)，零种子库第 (1) 步可走通

**反驳者·数据真实性与口径**（推翻）：

补取材追加（append=true 带描述）后，「当前大体所见文本」是累积全文而「结构化字段」只有最后一次追加那一版，且系统把这种覆盖不全的状态明确标成「与当前文本同版」、把仍在文本里的上一版字段标成「已被取代」——口径与事实相反，且没有任何一处能调阅出与当前全文对应的完整结构化记录。参数摘要要的是「完整的取材过程信息…结构化存储与调阅」，这一条在补取材场景下不成立。

证据：param_verbatim（自读 CSV 第 2530 行「参数摘要」列逐字）：支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。 +---------------------------------------------------------------------------------------------------------------------------------------------------------+
（响应结论=部分响应；说明列缺项①取材过程描述录入入口为固定预设值、②标本描述在流转列表中的调阅展示。）

【缺口一：补取材后「同版」是对覆盖度的过度声明，且没有并集视图】
· C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathologyProcessController.java:455-457 追加时文本是累积拼接：grossToWrite = existingGross + "。补取材：" + grossAssembled；:486-489 只把本次 req.gross() 落在新 revisionSeq 下（storeGrossFields(specimenId, revisionSeq, req.gross(), uid)），上一版字段行不动、不做并集。
· 同文件 :1489-1500 读端点 fields 只取「max(revision_seq) 那一版」；:1578-1581 fieldsCurrent = fieldsRevisionSeq.equals(textRevisionSeq) → 补取材场景两者都=2 → fieldsCurrent=true、fieldsNote=null（fieldsStaleNote 在 current=true 时直接返回 null，:2148-2156），唯一的分叉告警机制在这里根本不触发。
· 前端两处照此措辞：frontend\shell\src\views\medtech\pathology\GrossingPanel.vue:121-123 打「最新字段版（第 2 版，2 项），与当前文本同版」，并把第 1 版标为「被取代版本（第 1 版，N 项）：已被第 2 版字段取代，仅供调阅」——而第 1 版的「大小：2×1cm」原封不动仍在当前 gross_finding 里，根本没被取代；TrailPanel.vue:199-202 同样打「第 2 版字段级记录（2 项），与当前文本同版」。
· 这个口径已被绿测与 E2E 固化，不是我推演：server\src\test\java\cn\hip\server\V59GrossReviseTest.java:355-359（append 后 fieldsRevisionSeq=2 / textRevisionSeq=2 / fieldsCurrent=TRUE / fields 只剩「块数、最大径」，而 :338 断言全文是 expected1 + "。补取材：…"，含第 1 版的「大小」）；tools\e2e-v59-audit.py:298-301 同断言。
· 更硬的一例：V59GrossReviseTest.java:381-393 历史标本（gross_finding 有一整段文本、零字段行）走 append 带字段 → fieldsRevisionSeq=1、fieldsCurrent=TRUE，UI 直接说「与当前文本同版」，而当前文本的主体那段历史文字从未被字段化。这正是迁移头注释自己立的规矩「宁可少算，不可假算」（V164__path_gross_fields.sql:16-27）在展示层被反向违反：行没造假，覆盖面的陈述造了假。

【缺口二：缺项② 依赖的「登记必填」在演示所用的登记路径上不成立】
· 审计者与 GrossingPanel.vue:53 注释都称标本描述是「登记时强制录入（4554 必填）」。实测：4554 只在旧端点 PathologyController.java:59（POST /api/pathology/specimens）；病理工作

核过的点：自读 docs/验收/技术偏离表-v3.csv 第 2530 行四列（参数摘要逐字抄回，未采信审计者转述）；V164__path_gross_fields.sql / V166__path_gross_revise.sql 全文：确认零 update、零反解析、唯一约束由 (specimen_id,seq) 改为 (specimen_id,rev；PathologyProcessController.assembleGross(:2057-2093)：字段上限/标签 32/值 300/总长 2000 越界报 5222 而非静默截断、同名字段拒绝、空值字段整条略去；PathologyProcessController.grossing(:345-548)：append 累积拼接 grossToWrite、乐观比对 update、uid 判定前置、字段只落本次一版、GROSSING 节点与蜡块由库端 n；PathologyProcessController.reviseGrossFields(:575-640)：GROSSING_EDIT 整体替换文本，字段与文本同版（此路径无覆盖度问题）；读端点 grossingView(:1444-1600)：fields 只回 max(revision_seq) 那一版、fieldsCurrent 判定、fieldsNote/fieldsStaleNote 在 current=true ；GrossingPanel.vue:53/96-148/665-714：标本描述列、查看大体所见抽屉的版本下拉与「与当前文本同版 / 被取代版本」措辞、viewFieldVersions/viewVersionIsLatestFields ；TrailPanel.vue:195-243：大体所见修订表、每版字段展开、同样的「与当前文本同版」标签；V59GrossReviseTest.java:321-395（append 出新版本与历史标本 append 两段断言）、V58GrossFieldsTest 引用点、V60ResampleProgressTest:405-420（fie；tools/e2e-v59-audit.py:285-305 与 tools/e2e-v60-audit.py:265-285：绿 E2E 固化了 append 后 fieldsCurrent=true 的口径；PathologyRegistryController.doRegister/checkLengths（:236-340、:779-786）与 PathologyController:48-59：确认 4554 必填只在旧 collect ；grep 全仓 gross_finding 写入点（三处）与 4554 出现点，核对审计者关于写入面与必填面的主张

### 2563

**参数原文（CSV 逐字）**：支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。

**审计者**：可上调。最接近缺口的一点：不是功能缺口，是演示首屏观感：tools/demo-pathology.py 造出的两条特检医嘱一条置 DONE、一条 CANCELLED，全院清单默认口径是「只看待执行」，所以跑完脚本直接进 ⑤ 特检工作台首屏仍是空表「该条件下无特检技术医嘱」，必须手动把状态切到「全部状态」才看得见数据；且脚本不留任何在途（ORDERED）医嘱，六态进度里的 待切片/已补取材待切片/切片中/已染色待确认 四态在脚本造的数据上一态都看不到，要演示进度推进必须现场按演示路径第 2、3 步自己下一条医嘱并走制片。功能与数据本身无假算（历史行零回填、进度全部只读派生），此缺口靠一次点击或脚本补一条在途医嘱即可消除。

子要求核对：
- [✓] 支持特检医嘱的「集中化管理」（不分标本的全院集中清单 + 下达/完成/取消闭环在同一处操作） —— 后端全院分支：modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:1015 GET /api/pathology/report/tech-orders（不传 specimenId 即全院清单，默认只看 ORDERED，status=ALL 看全状态；状态/类型/仅加急/日期口径 ORDERED|DONE/日期区间/关键词 条码·病理号·患者·患者号·项目 七类筛选，limit+truncated）；下达 :795 POST /tech-orders，完成 :872 PUT /tech-orders/{id}/done，取消 :950 PUT /tech-orders/{id}/cancel（reason 必填 5271）。前端集中工作台：frontend/shell/src/view
- [✓] 状态追踪（医嘱状态与流转留痕可追） —— 状态三档 ORDERED/DONE/CANCELLED（V144__pathology_base.sql:146 chk_path_tech_status）+ 下达/完成/取消三类流转节点：V165__path_tech_progress.sql 放开 chk_path_process_node 追加 TECH_ORDER/TECH_DONE/TECH_CANCEL（V144 原 12 档一个不删），三个端点同事务打点；节点中文名 frontend/shell/src/views/medtech/pathology/format.ts:78-80。取消留痕三列 cancelled_at/cancelled_by/cancel_reason 由 V163__path_tech_trace.sql 加列，与 status 同一条 update 落库、不覆盖下达原因 reason；前端 Tech
- [✓] 可实时调阅（查询即读当前库事实，不是离线报表） —— PathologyReportController.java:1015 每次请求直接 jdbc 查询 path_tech_order 及其派生子查询，hours_since_ordered 由 now() 现算（:1081-1083）；TechOrderPanel.vue:224 「查询」按钮与 PathologyWorkbenchView.vue 头部「刷新当前工位」触发重取；ProcessingPanel.vue:630 染色登记后按 slideId 回查该片所挂医嘱的最新进度。
- [✓] 涵盖「技术分类」 —— 库列 path_tech_order.tech_type + chk_path_tech_type 六类 DEEP_CUT/RECUT/RESAMPLE/IHC/SPECIAL_STAIN/MOLECULAR（V144__pathology_base.sql:143-145）；端点回 tech_type + tech_type_name 中文（PathologyReportController.java:1130 TECH_TYPE_NAMES :213）；清单列「类型 / 项目」TechOrderPanel.vue:56-61，筛选下拉取自 /tech-orders/dict（:186）；下达表单类型下拉 DiagnosisPanel.vue:493-497。
- [✓] 涵盖「具体检测项目」 —— 库列 path_tech_order.tech_item（V144:141）；清单同列显示 TechOrderPanel.vue:59；下达时 IHC/SPECIAL_STAIN/MOLECULAR 必填项目（前端 DiagnosisPanel.vue:885-888 techItemRequired，后端 createTechOrder :795 白名单校验）；另有 attached_stain_name「挂接切片染色」列回挂接切片的实际染色类型/项目中文汇总（如「免疫组化 CK7 ×2」，PathologyReportController.java:196-203 TECH_DERIVED_COLUMNS；TechOrderPanel.vue:120-125），可核对与医嘱项目是否一致（挂接时 5274 硬校验，PathologyProcessController 一致性判定 + V5
- [✓] 涵盖「关联蜡块编号」 —— 下达时指定块取 path_block.block_code（left join，PathologyReportController.java:1065 区段）；未指定块时 blocks_derived 由三来源并集派生——下达指定块 ∪ 为本医嘱补出的蜡块（path_block.tech_order_id，V167__path_block_tech_order.sql）∪ 挂接切片所在块，按块号去重「、」相连（PathologyReportController.java:184-194，v60 复核修补：原 coalesce 短路只显示第 1 块已改为 UNION）；前端 TechOrderPanel.vue:87-93 显示并加「派生」标，DiagnosisPanel.vue:262-265 同口径。写侧回挂：取材 append=true 带 techOrderId 时四路校验（不存在/
- [✓] 涵盖「当前执行进度」 —— 六态只读派生、无状态列无回填：PathologyReportController.techProgress 四参 :1182（ORDERED 且 0 片→有挂接蜡块 SAMPLED / 否则 PENDING_SECTION；有片未全染 SECTIONING；全染 STAINED；DONE/CANCELLED 照状态；非法状态原样返回不猜），applyTechProgress :1196 为两个清单分支与质控 WORKLOAD_TECH 穿透共用（PathQcController.java:1401/1447 同源，同用 TECH_DERIVED_COLUMNS :1420）。中文 TECH_PROGRESS_NAMES :162-164。前端进度列 + 「已染 x / 挂接 y」「已出块 N」TechOrderPanel.vue:62-69。完成动作有事实校验 gate emr.gate.
- [✓] 以上四项构成「完整明细」可一处同屏调阅 —— 同一行同屏给出：状态/类型·项目/进度(+已染·挂接·已出块)/病理号·条码/患者/类别·加急/蜡块(含派生)/开单人·时刻/距开单小时/完成人·时刻/下达原因/取消人·时刻/取消原因/挂接切片数/挂接切片染色（TechOrderPanel.vue:48-136）；标本维度同明细在 ④ 诊断抽屉「特检技术医嘱」页签 DiagnosisPanel.vue:251-300。

演示路径（审计者）：零种子新库可走，建议先造数再演示。
第 0 步（造数，CI 已跑通）：起服务后 `HIP_E2E_BASE=http://localhost:8080/api python tools/demo-pathology.py --count 2`（纯走真实 API 不写库；病理收费项目 C0301 由 V21 已 seed）。它会造出 2 例阶梯标本，并在已签发那例上留下「IHC CK7 医嘱挂 2 张切片并完成」与「特殊染色 PAS 医嘱带原因取消」各一条。
第 1 步：ADMIN/TECHNICIAN/DOCTOR_OUTP 登录 → 菜单「门诊业务 → 病理工作台」（菜单 168，路由 /pathology/workbench）→ 点第 ⑤ 页签「特检工作台」。首屏默认「待执行」，demo 造的两条一条已完成一条已取消，故首屏为空表「该条件下无特检技术医嘱」——把「状态」下拉切到「全部状态」点「查询」，两条即出：可同屏看到 类型/项目（免疫组化 CK7、特殊染色 PAS）、蜡块（块码）、进度（已完成 / 已取消，含「已染 2 / 挂接 2」）、开单人时刻、完成人时刻、取消人·时刻·取消原因、挂接切片染色「免疫组化 CK7 ×2」。可再演示按技术类型、仅加急、日期口径（按开单/按完成）+ 区间、关键词（条码/病理号/患者/项目）筛选。
第 2 步（演示「当前执行进度」逐态推进，全程点得到）：回 ④「诊断 / 报告追溯」打开 demo 造的第二例（已切片已染色未诊断）→「特检技术医嘱」页签 →「下达」→ 技术类型选「免疫组化」、具体项目填 CK7、蜡块下拉选一块、填下达原因 → 提交。转 ⑤ 切「待执行」→ 该行进度「待切片」（0/0）。转 ③「制片」→ 查该标本蜡块 → 切片对话框里「挂接特检医嘱」下拉选中这条（染色类型自动锁 IHC、项目锁 CK7）→ 切 2 张 → 回 ⑤ 进度变「切片中」（已染 0 / 挂接 2）→ ③ 逐张「染色登记」→ ⑤ 进度「已染色待确认」→ ⑤ 点「完成」→「已完成」。
第 3 步（演示补取材 RESAMPLE 的「关联蜡块编号」与 SAMPLED，v60 新增）：④ 下达一条「补取材」医嘱（蜡块留空）→ ⑤ 看到进度「待切片」、蜡块「—」→ ② 「取材」工位打开该标本，勾「补取材」→ 出现「挂接补取材医嘱」下拉，选中该医嘱 → 填本次描述、加 2 个蜡块 → 提交 → 回 ⑤：进度「已补取材待切片」（已出块 2）、蜡块列显示两块块码并带「派生」标；再去 ③ 对这两块切 HE 片即推进到「切片中」。
第 4 步（状态追踪留痕）：④ 抽屉「流转节点」页签或 ⑥「流转与异常」可看到 下达特检医嘱 / 确认完成特检医嘱 / 取消特检医嘱 三类节点（含人、时刻、备注，补取材还带「补取材医嘱#N」）。
不跑 demo 脚本也走得通，但需 ADMIN 自己从挂号→接诊→开病理医嘱→结算→登记→接收→取材→制片一路造到能下特检医嘱（约 18 步），首屏同样是空表。

审计者自列的保留项：
- V163/V165/V167 三条迁移均为零回填：V163 之前取消的医嘱三列 cancelled_* 恒 NULL（页面显式标「历史取消（时刻未采集）」）、V163 之前的切片 tech_order_id 恒 NULL、V167 之前补取材产出的蜡块 tech_order_id 恒 NULL（历史 RESAMPLE 医嘱蜡块列仍「—」、进度仍「待切片」），历史医嘱也无 TECH_* 流转节点。新库/新数据不受影响，但若甲方拿旧库演示，这几列会是空——这是事实不是 bug，答辩时需主动说明。
- 完成校验 gate emr.gate.pathology.techdone 默认 warn：无已染色挂接切片仍可点「完成」，只在返回体与 TECH_DONE 节点留 warnings。要「无切片不许完成」需院方把该键改 block（5273）。
- 全院清单无翻页，只有 limit + 「命中超过 N 条仅显示前 N 条」的告警条，需靠收窄筛选条件；大院一天的特检量下演示要先设日期区间。
- blocks_derived 是只读派生，不回写 path_tech_order.block_id（除补取材 append 那一次首块回写），所以「蜡块」列可能与库列 block_id 不同源——页面用「派生」标区分，抽查时别按库列核对。
- 本次审计为只读静态核查：读代码/迁移/测试/E2E 脚本与菜单 seed，未起服务、未跑 mvn、未实际点页面；演示路径是按代码与 E2E 断言推出的，首次向甲方演示前建议按上述路径实机走一遍。
- 本条参数摘要末尾的 +---- 分隔符为原表排版残留，已逐字保留在 param_verbatim 中。

**反驳者·子要求拆解**（推翻）：

子要求「关联蜡块编号」的派生溯源标在 v60 自己的旗舰场景（补取材 RESAMPLE）下恒不渲染，演示脚本第 3 步承诺的画面做不出来。TechOrderPanel.vue:92 的标签条件是 `v-if="!row.block_code && row.blocks_derived"`，而 PathologyProcessController.java:515-519 的补取材挂接会把医嘱 block_id 由空「回写为本次首块」——回写一旦发生，清单端点的 `left join path_block b on b.id = t.block_id` 就让 `row.block_code` 非空，标签条件恒为 false。于是补取材出 2 块时，⑤「蜡块」列显示「P-3、P-4」却**不带「派生」标**：第 1 块是回写来的、第 2 块纯属挂接派生，屏幕上却与「下达时医师指定了这两块」完全同形。v60 复核修补只改了值（coalesce→UNION，PathologyReportController.java:184-194），没有跟着改判定「是否派生」的前端条件——这正是「做了一半」。连带后果有三处：(1) 任务给的演示脚本第 3 步逐字写「蜡块列显示两块块码并带「派生」标」，照着走演示不出来；(2) TechOrderPanel.vue:4 的页首说明也逐字承诺「按挂接蜡块（补取材已出块）与挂接切片所在块派生并标「派生」」，页面与自述不符；(3) ④ DiagnosisPanel.vue:262-265 同一列连标签都没有，两处口径本就不齐。另有两处同族残留：DiagnosisPanel.vue:254 的「进度」说明仍写 v58 三态（待切片/切片中/已染色待确认），漏掉 v60 新增的 SAMPLED「已补取材待切片」，补取材医嘱停在该态时屏上legend 解释不了自己；demo-pathology.py 全脚本无 RESAMPLE（只造 IHC CK7 与 SPECIAL_STAIN PAS，两条都显式带 blockId），零种子造数演示里「派生」能力一次都露不出来，必须手工走第 3 步——而第 3 步恰恰是坏的那条。

证据：【参数摘要逐字（自读 CSV 第 2563 行第 3 列）】「支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。」（响应结论列=部分响应）

【逐子要求核账，我自己读到的】
1) 集中化管理 — 部分成立但审计者证据有一处**事实错误**。全院分支确在 PathologyReportController.java:1015（不传 specimenId 默认 ORDERED，status=ALL 全状态），前端 TechOrderPanel.vue load() 注释「刻意不传 specimenId」属实。但审计者写的「下达/完成/取消闭环在同一处操作」不成立：TechOrderPanel.vue 操作列（:127-136）只有「完成 / 取消 / 打开报告」三个按钮，**没有「下达」**；下达只在 ④ DiagnosisPanel.vue:165「下达特检技术医嘱」按钮 + :491 弹框，是按标本的。（我判断这不足以单独维持部分响应——下达本就是医师按标本/按蜡块的行为——但审计者的证据句与代码不符，不能照收。）另审计者称关键词「七类」，实读 PathologyReportController.java:1113-1117 只有 5 个字段（barcode / path_no / p.name / p.patient_no / t.tech_item）。
2) 状态追踪 — 成立。三档 chk_path_tech_status（V144:146）；三个端点同事务 logProcess：createTechOrder TECH_ORDER(:838)、doneTechOrder TECH_DONE(:915)、cancelTechOrder TECH_CANCEL(:972)；节点中文名后端 PathologyProcessController.java:175-181 与前端 format.ts:76-78 两处都补齐了三档；取消三列 cancelled_at/by/reason 与 status 同一条 update 落库（:958-966），前端 TechOrderPanel.vue:104-113 对 V163 前历史取消显式标「历史取消（时刻未采集）」。跳节点/停滞不受影响：PathologyProcessController.java:1930 的 last_node_name 走同一份 NODE_NAMES，三个新节点都有名字。
3) 可实时调阅 — 成立。每次请求直查 path_tech_order，hours_since_ordered 由 now() 现算（:1080-1082）。
4) 技术分类 — 成立。chk_path_tech_type 六类（V144:143-145），端点回 tech_type_name（:1134），清单列 :56-61，筛选下拉取 /tech-orders/dict。我额外验了六类**都能真正执行**：TECH_TO_STAIN（PathologyProcessController.java:164-166）把 MOLECULAR 映射到 stain_type=MOLECULAR，而 chk_path_slide_stain（V144:106）四档含 MOLECULAR、前端 format.ts STAIN_TYPES 四档也含 MOLECULAR——不存在「某一类永远推不动进度」。
5) 具体检测项目 — 成立。tech_item 列 + IHC/SPECIAL_STAIN/MOLECULAR 必填（后端 :813-816，前端 :885-888）；attached_stain_name 中文汇总（:196-203）可核对一致性，挂接时 5274 硬校验（:1051-1052）。
6) 关联蜡块编号 — **数据对、标签错**（见 hardest_gap）。UNION 三来源派生本身我逐句读过并认为正确：`select b.block_code,b.block_no where b.id is not null` ∪ `path_block.tech_order_id=t.id` ∪ 挂接切片所在块，PostgreS

核过的点：自读 docs/验收/技术偏离表-v3.csv 第 2563 行（utf-8-sig + newline='' + csv.reader），逐字取第 3 列参数摘要与第 4/5 列结论说明，未采信审计者转抄；PathologyReportController.java:1015-1160 全院/按标本两分支 SQL 与参数校验逐句读（status=ALL、techType 白名单 5267、urgentOnly、dateField ORDERE；PathologyReportController.java:184-205 TECH_DERIVED_COLUMNS 三段派生列逐句读：sampled_block_count / blocks_derived 的三来源 UNION / a；PathologyReportController.java:1171-1199 techProgress 三参与四参、applyTechProgress 收口；确认 SAMPLED 只在 slides==0 且 sampled>0 时派出；PathologyReportController.java:795-990 createTechOrder / doneTechOrder / cancelTechOrder 三端点：必填项目、blockId 归属校验 5269、TECH；PathologyReportController.java:136-142 类级 @PreAuthorize 含 TECHNICIAN，并 grep 全文 @PreAuthorize 确认 tech-orders 四端点无更严方法级注解；PathologyProcessController.java:360-530 取材 append 分支：techOrderId 四条 5275 被拒路径全在写入之前、append=false 带 techOrderId 回 5222、新块；PathologyProcessController.java:164-166 TECH_TO_STAIN 六类映射 + :1051-1052 挂接一致性 5274；PathologyProcessController.java:175-181 NODE_NAMES 含三个 TECH_* 节点；:1930 异常行 last_node_name 走同一份表，新节点不致空名；PathQcController.java:1401-1435 WORKLOAD_TECH 穿透 SQL：确认 :1420 拼入 TECH_DERIVED_COLUMNS 处 :1427 确有 left join path_block b（；frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue 全文：筛选表单、15 列表格、:91-92「蜡块」列与「派生」标条件、:127-136 操作列（确认无「下达」按钮）；frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:165/:252-310/:491-520/:831-905：下达按钮无状态禁用、特检页签 11 列（含进度/蜡块，

**反驳者·数据真实性与口径**（推翻）：

「关联蜡块编号」这一列的口径在本树里是**自相矛盾的两份**：CI 里的 2563 尾 E2E（tools/e2e-v60-audit.py）把被修掉的缺陷当成契约钉死——四处 assert_derived 严格断言 blocks_derived **只等于首块码**（451/460/478/495 行，assert_derived 用的是 `==` 严格相等，176-186 行），而 HEAD 的 SQL 与 Java 单测断言的是三源并集「首块、次块、第三块」（V60ResampleProgressTest 181/193/200 行）。二者不可能同时绿：91fcd5d「v60 复核修补」只改了 PathologyReportController + V60ResampleProgressTest 两个文件（git show --stat 确认），没有碰 E2E，而 .github/workflows/ci.yml:186 明确跑 tools/e2e-v60-audit.py。也就是说 2563 唯一的端到端演示证据要么跑的是修补前的树（「全新库 46 套 E2E 全绿」不成立于当前 HEAD），要么现在必炸——这一条的「数据口径已被端到端核过」不成立。附带同根缺陷：v60 把执行侧派生值回写进「下达意图」列 path_tech_order.block_id（PathologyProcessController.java:515-520），前端唯一的出处标记「派生」恰恰以 `!row.block_code` 为条件（TechOrderPanel.vue:92），回写后必然不渲染——审计者给的演示第 3 步「蜡块列显示两块块码并带「派生」标」在真机上不会发生，屏幕上三个块码被一律呈现为医师下达时指定的关联蜡块。

证据：CSV 原文（docs/验收/技术偏离表-v3.csv，按序号列取行）——参数摘要逐字：「支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。 +--------------------------------------------------------------------------------------------------------------------------------------------」；响应结论「部分响应」。

1) E2E 与代码口径对不上（硬缺口）
- C:\Users\drxuj\医院信息化\tools\e2e-v60-audit.py:176-186 assert_derived 三处均为严格相等：`assert r.get('blocks_derived') == blocks_derived`；调用点 451 `assert_derived(sA, rs, code1, 2, None)`、460/478/495 `assert_derived(sA, rs, code1, 3, ...)`——期望「补出 2/3 块时蜡块列只回首块码」。
- C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathologyReportController.java:184-194 TECH_DERIVED_COLUMNS 现为三源 UNION（下达指定块 ∪ path_block.tech_order_id ∪ 挂接切片所在块），string_agg 按块号「、」相连。
- C:\Users\drxuj\医院信息化\server\src\test\java\cn\hip\server\V60ResampleProgressTest.java:181/193/200 断言 `code1+"、"+code2`、`code1+"、"+code2+"、"+code3`。
- `git show --stat 91fcd5d` 只含 PathologyReportController.java 与 V60ResampleProgressTest.java 两个文件；`git log --oneline -- tools/e2e-v60-audit.py` 最新仍是 921b42d（修补前）。`.github/workflows/ci.yml:186` 跑 `python3 tools/e2e-v60-audit.py`。两份互斥断言同在 CI 里 → 2563 的端到端口径证据不可信。

2) 「关联蜡块编号」的出处被抹掉（同根）
- PathologyProcessController.java:515-520：`update path_tech_order set block_id = ? where id = ? and block_id is null and status='ORDERED'`——把执行侧首块回写进下达意图列；V167__path_block_tech_order.sql 文件头自陈「下达时块还不存在，必然为空」「同理不回写历史医嘱的 block_id」，即本仓「零回填」纪律只覆盖历史行，新行照回写，且行上没有任何「此值系回写」的标记（返回体 techOrderBlockBackfilled 是一次性的，不落库）。
- frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue:91-92：`fmt(row.blocks_derived ?? row.block_code)` + `v-if="!row.block_code && row.blocks_derived"` 才出「派生」标 → 回写后 block_code 非空，标必不出；同页 89-90 行注释仍写着回写前的旧语义。DiagnosisPanel.vue:264 干脆没有任何出处标记。V60ResampleProgressTest.java:151/166 正面确认 `techOrderBlockBa

核过的点：CSV 原文逐字重读（docs/验收/技术偏离表-v3.csv，utf-8-sig + CRLF，按序号列取 2563 行：参数摘要 / 结论 / 说明三列）；TECH_DERIVED_COLUMNS 三源 UNION 的 SQL 语义与 javadoc（PathologyReportController.java:168-200）；GET /tech-orders 全院与标本两分支 SQL、筛选、limit/truncated、note 口径（PathologyReportController.java:1015-1158）；techProgress 三参/四参与 applyTechProgress 六态派生（:1171-1200）；doneTechOrder 完成校验 gate 三档与 warn 默认（:872-930，V165__path_tech_progress.sql:39）；cancelTechOrder 留痕同一条 update（:950-985）与 V163 零回填；createTechOrder 的 blockId 可空 + 5269 归属校验、tech_item 必填白名单（:795-855）；取材 append 挂接 RESAMPLE 的 5275 四路径与新块 tech_order_id、医嘱 block_id 回写（PathologyProcessController.java:407-430、502-520）；切片挂接 5272/5274 校验与 path_slide insert（PathologyProcessController.java:1007-1070）；全仓 `update path_slide` 仅两处、均不写 tech_order_id（:1152、:1271）——事后无补挂通道；V144__pathology_base.sql 表结构（path_slide 无作废列、path_tech_order 三态、chk 约束）、V163/V165/V167 三条迁移全文；V167 文件头自陈的零回填口径与「不回写历史医嘱 block_id」

**反驳者·演示可行性**（推翻）：

审计者自己写的第 4 步（「状态追踪留痕」专演步骤）一走就翻车：评委在 ④ 抽屉「流转节点」/⑥「流转与异常」看到的备注正文里同时有**内部主键**与**裸英文枚举**，而且全平台没有任何页面能把这个 #id 对回一条医嘱。逐字来源：PathologyReportController.java:1351-1354 `techLabel` 拼 `"#" + id + " " + 中文 + "(" + 英文码 + ")" + item`，三处节点备注全用它——:838「下达特检医嘱 #12 免疫组化(IHC) CK7：…」、:917「确认完成特检医嘱 #12 免疫组化(IHC) CK7，挂接 2 片 / 已染色 2」、:973「取消特检医嘱 #13 特殊染色(SPECIAL_STAIN) PAS：…」；PathologyProcessController.java:1076 的 SECTION 备注更是**纯裸码无中文**「切片 2 张（P…-1，IHC CK7），特检医嘱#12 IHC」（stain_type 与 tech_type 两处都裸）；v60 为第 3 步新加的 GROSSING 备注 :527 直接拼「，补取材医嘱#12 RESAMPLE」。同时 ⑤ TechOrderPanel.vue 十六列、④ DiagnosisPanel.vue 特检页签十一列**都没有医嘱 id 列**（我逐列读过两个模板），所以「流转节点」里的 #12 在清单上无从对应——同一标本先取消一条 IHC CK7 再下一条时，评委只能看着 #12/#13 而清单上两行长得一模一样。v59 复核把「⑤『挂接切片染色』列显示裸 SPECIAL PAS ×1」这一处观感级英文枚举判为推翻、团队随后补了 attached_stain_name 中文列；同一病灶在演示路径必经的节点备注里原样保留，口径不能一列算数一列不算。

证据：param_verbatim（docs/验收/技术偏离表-v3.csv 序号 2563 参数摘要列逐字，utf-8-sig/CRLF，csv 模块按序号列取行）：「支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。 +--------------------------------------------------------------------------------------------------------------------------------------------」；响应结论「部分响应」；响应说明「平台当前提供病理标本基础信息列表（标本编号、检查项目、患者、标本状态）；本条所述特检医嘱的集中化管理与状态追踪，含技术分类、具体检测项目、关联蜡块编号及执行进度的完整明细功能尚未提供，列入后续开发计划。」

我亲核走通的部分（不否认）：菜单 168 V144__pathology_base.sql:198 seed 挂 DIR 7，V156__v52_menus.sql:25-27 授 ADMIN/TECHNICIAN/DOCTOR_OUTP，:124-127 另补授父目录 7（父 DIR 缺失会让子菜单一条都渲染不出，这一条已做）；⑤ 页签 PathologyWorkbenchView.vue:26-28；⑤ 清单七类筛选与全部列 TechOrderPanel.vue:6-138；全院分支 PathologyReportController.java:1015+（specimenId 为空即全院、默认 ORDERED、ALL 全状态、now() 现算 hours_since_ordered）；进度六态派生 :1182-1195 + applyTechProgress :1196；blocks_derived 三源 UNION :184-194；attached_stain_name 中文 :195-203；时间显示 fmtTime→fmtDateTime（utils/date.ts:71-78，带偏移才换算到 Asia/Shanghai），⑤ 三处时刻列都走它；下达表单蜡块下拉按 block_code 显示（DiagnosisPanel.vue:505-509，loadSpecimenBlocks 在 openReport:614 被调）；③ 挂接下拉锁染色类型/项目（ProcessingPanel.vue:513-519）；② 补取材挂接下拉（GrossingPanel.vue:240-249 + :558 openGrossing 预取）；tools/demo-pathology.py 确如审计者所述（LADDER:241-247，--count 2 → plan=[ISSUED, STAINED, REJECTED]，三个 variants 全落在 idx 0，ihc_attach:184-198 / tech_cancel:200-209）。

缺口证据（全部来自我自己读的当前树）：
1) 节点备注内部主键 + 裸英文枚举：PathologyReportController.java:1351-1354 techLabel、:838/:893+917/:973 三处 logProcess；PathologyProcessController.java:1076 SECTION 备注「，特检医嘱#id IHC」、:527 GROSSING 备注「，补取材医嘱#id RESAMPLE」。对照面：format.ts:49-55 STAIN_TYPES 与 PathologyReportController.java:213 TECH_TYPE_NAMES 明明都有中文，节点备注一处没用。清单无 id 列：TechOrderPanel.vue:51-136、DiagnosisPanel.vue:254-308 逐列读过，零处 row.id。
2) 第 3 步承诺的现场观感与代码不符：取材挂接时后端把医嘱 block_id 回写为本次首块（PathologyProcessController.java:515-519 条件 update），于是 

核过的点：docs/验收/技术偏离表-v3.csv 序号 2563 六列逐字（python csv + utf-8-sig，参数摘要末尾 +--- 残留原样保留）；frontend/shell/src/views/medtech/pathology/TechOrderPanel.vue 全文 290 行：筛选表单、十六列绑定、派生标条件 :92、done/cancel 后 load() 重查 :251；frontend/shell/src/views/medtech/pathology/PathologyWorkbenchView.vue 全文：⑤⑥ 页签 lazy、openSpecimen 跳 ④ 抽屉；frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue：范围四档 :5-10、下达按钮 :165、特检页签全部列 :251-309（确认无 id 列、无挂接切片染色列）、下；frontend/shell/src/views/medtech/pathology/ProcessingPanel.vue：切片对话框挂接下拉与 techOrderLabel '#id …' :483-488、TECH_TO_STAIN ；frontend/shell/src/views/medtech/pathology/GrossingPanel.vue：范围两档 :5-8、补取材开关 :226-228、挂接补取材医嘱下拉 :240-249、下拉文案 '#id 补取材…'；frontend/shell/src/views/medtech/pathology/format.ts：PROCESS_NODES 含三个 TECH 节点中文 :64-80、STAIN_TYPES/SPECIMEN_TYPES/TECH_；frontend/shell/src/utils/date.ts 全文：partsOf 只对带偏移串换算、fmtDateTime/fmtDate/fmtDateTimeSec 口径；modules/medtech/.../PathologyReportController.java：类级 @PreAuthorize :142、TECH_PROGRESS_NAMES :159-164、TECH_DERIVED_COLUM；modules/medtech/.../PathologyProcessController.java：NODE_NAMES 十五档 :174-181、grossingWorklist pending 条件 :253-263、grossin；server/src/main/resources/db/migration/V144__pathology_base.sql:193-200 菜单 168/169 seed 与 parent 7；server/src/main/resources/db/migration/V156__v52_menus.sql:24-27 授 168 三角色、:118-127 补授父 DIR 7

### 2576

**参数原文（CSV 逐字）**：支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。

**审计者**：可上调。最接近缺口的一点：最硬的一处不在三个被点名的计数上，而在计数单位的口径声明与演示前置：①WORKLOAD_REPORT 的 caveat（PathQcController.java:423-430 + ISSUE_VS_DIAGNOSE_NOTE :108-113）没有像 WORKLOAD_REGISTER（:396-402 明写「标本条数不是申请单数」）那样声明 issued_reports 的计数单位是标本/部位而非申请单——多部位申请下医生站是「1 单已执行」、质控是「2 份签发」，两个数现在时间上已一致（全部部位签发才 EXECUTED，v60 修）但单位不同，对外文案必须自己说清，否则评委会认为对不上账。②零种子库上必须先跑 tools/demo-pathology.py（需 ADMIN 口令、会建两个演示账号）或手工约 18 步，否则 QUALITY 打开质控页只见 0/空表；且脚本不能回溯日期，「按日」维度在演示库上永远只有当天一根柱。两者都不构成参数摘要里任何名词/动词「只做了一半」，故不阻断上调。

子要求核对：
- [✓] 支持基础工作量自动化统计（系统按业务表自动聚合、非手工填报，且用户能从菜单走到） —— modules/medtech/src/main/java/cn/hip/medtech/web/PathQcController.java:72 @RequestMapping("/api/path-qc")、:73 @PreAuthorize(hasAnyRole('ADMIN','QUALITY'))、:492 GET /catalog、:530 GET /indicators（from/to 可选，缺省近 30 天）、:1191 GET /detail 穿透、:1546 /indicators.csv、:1561 /detail.csv；全部指标由 SQL 直接聚合 path_specimen/path_block/path_slide/path_report/path_tech_order/path_process（汇总行 :823-960、区间合计 :1087-1150、穿透 
- [✓] 涵盖病理登记总量 —— PathQcController.java:396-402 def("WORKLOAD_REGISTER","登记总量（按日，含拒收与加急构成）") 锚 path_specimen.collected_at，caveat 明写「registered 是标本条数不是申请单数」；按日行 :823-841（registered/urgent/rejected/outp_source/inp_source/五类标本型，group by collected_at::date）；区间汇总 :1087-1096；穿透 :1327-1332 走 SPEC_SELECT（:184-210 含病理号/条码/患者/科室/初复签名人）。E2E tools/e2e-v58-audit.py:352-358 断言 WORKLOAD_REGISTER.summary.registered>0；tools/e2e-v60
- [✓] 蜡块产出数 —— PathQcController.java:413-417 def("WORKLOAD_BLOCK","蜡块产出数（按日）") 锚 coalesce(path_block.embedded_at, created_at)；按日行 :856-869（blocks/embedded/specimens/blocks_per_specimen/dehydrate_batches）；汇总 :1109-1117；穿透 :1347-1363。蜡块由取材端点 insert path_block 产生（PathologyProcessController 取材 → tools/demo-pathology.py:160-166 取材后回读 blocks 断言数量）。E2E e2e-v58-audit.py:352-358 断言 WORKLOAD_BLOCK.summary.blocks>0；tools/d
- [✓] 报告签发量（且与平台其他入口的「报告已出」口径一致） —— PathQcController.java:423-430 def("WORKLOAD_REPORT","报告签发量（首次报告 / 补充报告分列）") 锚 path_specimen.report_issued_at 与 path_report.signed_at；按日 generate_series :885-899 逐日 issued_reports/diagnosed/supplement_reports；汇总 :1129-1136；穿透首次/补充 union :1365-1399。report_issued_at 唯一写入点 PathologyReportController.java:560-562 issue()（双签 gate 后）；v59 起 diagnose 不再置 outp_order EXECUTED；v60 把申请级结算收紧并抽成 PathologyReportCo
- [✓] 等多维度的业务数据汇总（多指标多维度、页面能看、能穿透、能导出、中文可读） —— PathQcController.java 工作量共 7 条 def：WORKLOAD_REGISTER :396（日×加急/拒收/门诊住院来源/五类标本型）、WORKLOAD_DEPT :404（送检科室×registered/issued/rejected/in_progress，SQL :842-854，汇总 :1097-1108，穿透含 dept 过滤 :1333-1346 且 :1241 只对该指标生效）、WORKLOAD_BLOCK :413、WORKLOAD_SLIDE :419（日×HE/IHC/SPECIAL/MOLECULAR，:870-884）、WORKLOAD_REPORT :423、WORKLOAD_PATHOLOGIST :431（医师×六类活动，pathologistRows）、WORKLOAD_TECH :440（技术类型×开/完/待/取消）。页面 Path

演示路径（审计者）：前提（零种子库必做）：仓库无任何病理种子（`grep -r "insert into path_" server/src/main/resources/db/migration | wc -l` = 0），空库打开质控页只见覆盖率全 0 + WORKLOAD_REPORT 30 行全 0 + 其余「该统计区间内无数据」。所以先造数，二选一：(A) 后端已起、admin/admin123 可登录，跑 `HIP_E2E_BASE=http://localhost:8080/api python tools/demo-pathology.py`（纯走真实 API 不写库；幂等建 demo_pathdoc(DOCTOR_OUTP)/demo_quality(QUALITY) 两账号 tools/demo-pathology.py:394-397；建档→排班→挂号→接诊→开病理医嘱→结算→登记→核收→取材出蜡块→脱水/包埋/切片/染色→IHC 挂接/特检取消→诊断→初签→复签→正式签发→补充报告，:110-230；默认 5 阶梯标本 + 1 拒收；末行打印 `demo-pathology OK specimens=6 … path-qc registered=6 blocks=N issued_reports=1`，三项任一为 0 以退出码 2 失败 :431-434；CI .github/workflows/ci.yml:183 在全新库上先跑 `--count 2 --quiet`，再跑 e2e-v59/v60 :185-186）。(B) 手工：ADMIN/DOCTOR_OUTP 走「门诊业务→门诊医生站」开「组织病理学检查(活检)」并结算 →「门诊业务→病理工作台」(菜单 168) 登记→核收→取材→脱水/包埋/切片/染色→诊断→初签→复签(需第二个 DOCTOR_OUTP 账号)→正式签发，约 18 步。
演示：1) 用 demo_quality（或 admin）登录 → 左侧「门诊业务」→「病理质控」（sys_menu 169，路由 /pathology/qc）→ 页面 onMounted 自动按近 30 天查询（PathQcView.vue:319-322）。2) 自上而下：统计口径与时限阈值 alert → 「本时段字段录入覆盖率」四段 → 逐指标区块：WORKLOAD_REGISTER「登记标本数 6 / 加急 0 / 拒收 1 / 门诊来源 6 / 住院来源 0」+ 按日表（首列「日期」=今天）；WORKLOAD_DEPT「送检科室工作量」一科一行（本演示全走 dept 1 的挂号科室），列为登记/已签发/已拒收/进行中，三列之和 = registered；WORKLOAD_BLOCK 蜡块数/已包埋/涉及标本/每标本蜡块数；WORKLOAD_SLIDE HE/IHC 分列；WORKLOAD_REPORT「首次报告签发 1 / 写诊断 N / 补充报告 1」+ 30 行按日表（仅今天非零）；WORKLOAD_PATHOLOGIST 按医师六类活动；WORKLOAD_TECH 按技术类型含 1 条带原因取消。3) 点 WORKLOAD_DEPT 某行「该科室明细」→ 抽屉按该科室过滤（后端 :1241 dept 只对该指标生效）；点任一指标「穿透明细」看中文表头、来源「门诊」、状态/节点中文、主键列置灰在最后；点「导出汇总/导出明细」得 UTF-8 BOM CSV，表头「日期,登记标本数,…」。4) 口径交叉核对（本轮修复点）：多部位申请在

审计者自列的保留项：
- 本审计为只读：未起服务、未跑 mvn、未实测页面；结论全部来自当前工作树（main，最新提交 c85e8b6，CHANGELOG.md 有未提交改动 M，非本审计所改）的源码、迁移、JUnit 与 E2E 脚本。
- 穿透明细「首次报告」的 signer_name 取 coalesce(su2.real_name, su1.real_name, pu.real_name)（PathQcController.java:1372），双签 gate 默认 warn（V144__pathology_base.sql:189）允许零签名签发，此时把诊断医师显示成「签名人」——是替代值不是事实，v59 反驳者点过、本轮未改；不影响三个被点名的计数，但穿透页对外演示时勿说「签名人列即法定签署人」。
- WORKLOAD_DEPT 行级「该科室明细」按 dept 过滤，但页眉的「导出明细」按钮（PathQcView.vue:305-317 exportCsv）只传 indicator/from/to，不带 dept——导出的是全科室明细，与抽屉里看到的按科室过滤不一致（抽屉本身没有导出按钮）；后端 /detail.csv 支持 dept（e2e-v60-audit.py:627 直接调用验证过），是前端按钮没接。
- 演示脚本只造门诊来源、ROUTINE、非加急、单一科室的标本：inp_source / urgent / frozen / cytology / consult / molecular 全 0，WORKLOAD_DEPT 在演示库上只有一行——来源/类型/科室维度在演示数据上只有一档非零，对外演示要么承认，要么手工补一条住院来源。
- 质控页共 16 条指标，其中三条符合率类恒 available:false（页面标「缺数据源」不画 0 表），不属本条要求，但勿把「16 条指标全部有数」写进应答。
- JUnit 级精确数值断言目前只覆盖 WORKLOAD_DEPT（V60DeptDimensionTest:153-183 逐格断言 3/1/1/1、4/1/1/2、1/0/0/1 并与 WORKLOAD_REGISTER 对分母）；登记总量/蜡块产出/报告签发量三项在 E2E 层只有「>0」断言（e2e-v58-audit.py:352-358）与 demo 脚本自检，对外勿写「三项计数经单元测试逐值核对」。
- 质控页仅 ADMIN 与 QUALITY 可进（V156__v52_menus.sql:32 与后端 :73 @PreAuthorize 一致），TECHNICIAN/DOCTOR_OUTP 演示须切账号。
- 登记总量口径是「标本条数」而非「申请单数」（caveat 随返回体下发），多部位送检一份申请对应多条标本，对外文案须明说。
- param_verbatim 末尾的「 +-----…」为 CSV 参数摘要列原文自带，按要求逐字保留。

**反驳者·子要求拆解**（未推翻）：

最硬的一处残留是「科室过滤后的明细导出只做了后端」：PathQcController.java:1561 `detail.csv` 接受 `dept` 参数并在页脚写「科室过滤：X」（:1573-1576、toCsv 的 filterNote 形参 :1600-1606），但前端 PathQcView.vue:305-318 的 exportCsv() 只传 indicator/from/to，从不传 dept——用户在 WORKLOAD_DEPT 行级穿透进「该科室明细」抽屉后，抽屉里没有导出按钮，只能回到指标头上点「导出明细」，拿到的是全科室明细。同一类残留还有两处：(a) 按人维度只覆盖病理医师（WORKLOAD_PATHOLOGIST 的六类活动全部来自 pathologist_id/first_signer_id/second_signer_id/path_report.signer_id/GROSSING operator/tech ordered_by），技术员的包埋（path_block.embedded_by）、切片/染色（path_slide 的操作人）有数据落库却没有任何按人聚合的指标；(b) 住院来源签发后不结算 inp_order（PathologyReportController.java:607-620 settleOutpOrderExecuted 明写 order_id 为空即 return false），门诊「报告已出」的两套口径已在 v60 合一、住院侧没有对应动作。但三处都不是参数摘要里点名的子要求：摘要只点名「登记总量 / 蜡块产出数 / 报告签发量」三项 + 「等多维度」+「自动化统计」，导出、技术员绩效、住院医嘱执行态都不在其中；住院侧 inp_order 的 EXECUTED 本来就由护士执行（InpatientService.java:253 inp_order_exec DONE 时置位）而非报告签发决定，属两条业务线的不同语义而非口径分裂。逐条拆解后没有一个点名子要求落在「只做后端 / 只做一半 / 演示走不完」上。

证据：param_verbatim（docs/验收/技术偏离表-v3.csv 序号列=2576「参数摘要」列逐字，utf-8-sig + CRLF，python csv 读）：「支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。 +---------------------------------------------------------------------------------------------------------------------------------------------------------」；「响应结论」列＝「部分响应」，「响应说明」列裸正文＝「本条所述病理基础工作量自动统计（病理登记总量、蜡块产出数、报告签发量等多维度汇总）功能当前版本未提供，列入后续开发计划。」

逐名词/动词拆解与亲核结果：

【自动化统计】C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathQcController.java 全部指标由 SQL 直接聚合业务表（rowsOf 的 switch :823-960、summaryOf :1087-1150、detailRows :1327-1450），来源只有 path_specimen / path_block / path_slide / path_report / path_tech_order / path_process；全仓无任何手工填报表（迁移里 `insert into path_` 零命中，属零种子）。类级 `@RequestMapping("/api/path-qc")` + `@PreAuthorize("hasAnyRole('ADMIN','QUALITY')")`（:72-73）。前端 frontend/shell/src/views/medtech/pathology/PathQcView.vue:337-340 onMounted 先 loadCatalog() 再 load()，进页面即按 defaultRange() 近 30 天自动出数，无需手工点查询。

【菜单可达】V144__pathology_base.sql:198-199 sys_menu(168 病理工作台 /pathology/workbench，169 病理质控 /pathology/qc，父 DIR 7)；V156__v52_menus.sql:28-32 授 169 给 ADMIN/QUALITY（注释明写刻意不给 TECHNICIAN，与后端一致，不留「菜单点得进、接口 403」），:124-127 为持有 168-179 的角色补父 DIR 7（menuTree 先 filter DIR 再挂 children，缺父目录子菜单渲染不出）。

【病理登记总量】:396 def("WORKLOAD_REGISTER","登记总量（按日，含拒收与加急构成）")，锚 path_specimen.collected_at；按日行 :824-841（registered/urgent/rejected/outp_source/inp_source + 五类标本型 + type_unfilled，group by collected_at::date）；区间汇总 :1087-1096；穿透 :1327-1331 走 SPEC_SELECT（:185-222，含 path_no/barcode/patient_no/patient_name/dept_name/三个签名人）。caveat 明写 registered 是标本条数不是申请单数、rejected 按登记日归集。

【蜡块产出数】:413 def("WORKLOAD_BLOCK","蜡块产出数（按日）")，锚 coalesce(path_block.embedded_at, created_at)（W_BLOCK :238-239）；按日行 :856-869（blocks/embedded/specimens/blocks_per_specimen/dehydrate_batches）；汇

核过的点：docs/验收/技术偏离表-v3.csv 序号 2576 的参数摘要/响应结论/响应说明三列逐字重读（python csv, utf-8-sig, newline='' 保 CRLF），未用审计者转述；PathQcController.java 类级 @RequestMapping /api/path-qc 与 @PreAuthorize hasAnyRole('ADMIN','QUALITY')（:72-73）；7 条 WORKLOAD_* def 定义与 caveat 文本（:396 REGISTER / :404 DEPT / :413 BLOCK / :419 SLIDE / :423 REPORT / :431 PATHOLOGIST / ；rowsOf switch 的按日/按维 SQL（:824-925）与 summaryOf 汇总 SQL（:1087-1145）逐指标比对，确认分组列、filter 条件与锚点谓词一致；W_COLLECTED / W_ISSUED / W_BLOCK / W_SLIDE 四个时间窗谓词（:234-240）与各 SQL 的表别名（s / b / sl）对应关系；detailRows 的穿透 SQL（:1327-1450），含 WORKLOAD_DEPT 的 stage 三态判据与 dept 过滤参数化（where x.dept_name = ? 走占位符，非字符串拼接）；deptFilterFor(:1244-1247)：科室过滤只对 WORKLOAD_DEPT 生效、空白视为不过滤；detail 返回体只在生效时回带 dept 键；DEPT_JOINS(:163-171) / DEPT_NAME(:180-182) / SPEC_SELECT(:185-222) 三者的联接链一致性，确认 left join 全为一对一、无行放大，空白科室名归「（未知科室）」不丢行；parseWindow(:459-475)：缺省近 30 天、起止倒置/格式非法/跨度超 MAX_SPAN_DAYS=366 三路同 5280；ROW_LIMIT=500 / DETAIL_LIMIT=200 与 truncated 标记；indicators.csv(:1546) / detail.csv(:1561) 两个 CSV 端点：错误时导错误码 CSV 而非空表、unavailableCsv 导缺失字段、toCsv 的 filterNote 与口径页脚；机械核对 zh() 的 170 个 case 与 frontend format.ts 的 231 个 ZH 键：zh()−ZH = 空集；再把全文件 SQL 的 `as <alias>` 抽出做差，剩余未标注项全是模板占位符/CTE 内部；frontend/shell/src/views/medtech/pathology/PathQcView.vue 全文 340 行：onMounted 自动查询(:337-340)、逐指标 summary/rows 渲染、缺数据源指标不画

**反驳者·演示可行性**（推翻）：

v60 新增的「送检科室」维度在页面上只做了一半：能看、能行级穿透，**不能按科室导出**。后端 `PathQcController.java:1561-1577 detailCsv(indicator, from, to, dept)` 专门为它加了 `dept` 形参，`toCsv` 还为它加了 `filterNote`（:1596、:1601 写出「科室过滤：X」页脚），但**全前端没有任何调用方传 dept**：`PathQcView.vue:306-317 exportCsv()` 的 params 只有 `{indicator, from, to}`；两个导出按钮在指标头部（:66-69），穿透抽屉（:119-139）里根本没有导出按钮。评委按最自然的动作走——WORKLOAD_DEPT 行点「该科室明细」看到某科室 N 条（:104-109 → openDetail(ind, row.dept_name)），关掉抽屉点「导出明细」——拿到的是**全科室最多 200 条**、页脚不写任何过滤说明的文件，与屏幕上刚看的那份不是同一份，而抽屉顶部的 alert 恰恰写着「明细与汇总同时间窗、同锚点、同过滤条件——对不上账即为缺陷，请据此核对」。这条「按科室导出」链路唯一走得通的地方是 `tools/e2e-v60-audit.py:627 csv_det = csv_text('/path-qc/detail.csv?...&dept=...')`——**只有 E2E 调得到，用户从菜单永远走不到**，正是「建了功能≠应答可上调 / 只做了后端」的形态。次硬两处：(1) v59 同镜头已点名的「页内表首列是内部主键」在 v60 未收口——`idColumnsLast` 只用于抽屉的 `detailColumns`（PathQcView.vue:271），页内指标表用的是裸 `columnsOf`（:99），于是 WORKLOAD_PATHOLOGIST 的「各病理医师工作量」表首列仍是 `user_id`（后端 pathologistRows :1004 `select a.user_id` 打头），评委第一眼看到的是一列内部主键数字（表头虽已中文「用户ID」）；(2) 演示脚本的官方剧本 `docs/演示/病理演示数据.md` §三「跑完之后看到什么」那张表是 **v59 实测**（2026-09-09），全文零处 WORKLOAD_DEPT（grep docs/ 只在《下一步开发规划》里命中），`tools/demo-pathology.py` 的自检也只卡 registered/blocks/issued_reports 三项——照文档演示的人不会被引导到本轮新增的科室维度，且脚本造的数全走 dept 1 一个科室，「多科室对比」这个维度在演示库上永远只有一行。

证据：param_verbatim（docs/验收/技术偏离表-v3.csv 序号 2576「参数摘要」列，python csv/utf-8-sig/CRLF 逐字）：「支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。 +---------------------------------------------------------------------------------------------------------------------------------------------------------」；响应结论列「部分响应」；说明列「本条所述病理基础工作量自动统计（病理登记总量、蜡块产出数、报告签发量等多维度汇总）功能当前版本未提供，列入后续开发计划。」

亲核为真、审计者主张成立的部分（不再作为缺口）：【菜单可达】V144__pathology_base.sql:197-200 sys_menu(169,父7,'病理质控','/pathology/qc')；V156__v52_menus.sql:30-33 授 ADMIN/QUALITY、:124-127 补父 DIR 7；QUALITY 角色确实存在（V36__phase38_role_matrix.sql:3-7）；ModuleGate.MODULES 无病理条目 → 不会被模块开关 404。【时间不会错】application.yml:15 `connection-init-sql: set time zone 'Asia/Shanghai'` 钉死会话时区，`collected_at::date` 等 timestamptz 切日与业务日同钟；前端 format.ts defaultRange() 用本地日期而非 toISOString()；utils/date.ts fmtDateTime 只对带偏移串换算到 Asia/Shanghai。【不会看到英文枚举（页面内）】format.ts ZH 247 键 ⊇ 控制器全部 SQL 别名（我用正则把 PathQcController 里 146 个 `as xxx` 与 ZH 逐键比对，未登记的只剩 `{sfx}` 占位符碎片与 CTE 别名 d/n/tat 等非列名）；cellText 对 source/specimen_type/stain_type/tech_type/node/status 逐族翻中文；mdText（format.ts）已把后端 `**强调**`/反引号去标记，v59 的裸星号确已修。【口径不假】path_report 仅存补充报告（V144:157-175，全仓唯一 insert 在 PathologyReportController.java:663），首次报告在 path_specimen，两列不重复；DEPT_JOINS/DEPT_NAME 与 SPEC_SELECT 同一条联接链（:162-190），汇总与穿透同口径，`（未知科室）`两侧一致故行级穿透不会空；RegistrationService.java:42/85 `reg.setDeptId(schedule.getDeptId())` → 门诊标本能取到真科室名，不会整片落「未知科室」；V21__phase24_specialty.sql:66 有 'C0301 组织病理学检查(活检)'，演示脚本的收费项断言不会踩空。

缺口证据（从菜单出发实际会撞上的）：① PathQcView.vue:306-311 `client.get('/path-qc/detail.csv', {params:{indicator, from, to}})` —— 无 dept；抽屉 :119-139 无导出按钮；对照 PathQcController.java:1561-1577 detailCsv 有 dept 形参、:1596/:1601 有「科室过滤：」页脚，仅 tools/e2e-v60-audit.py:627 调得到。② PathQcView.vue:99 `v-for="c in columnsOf(rowsOf(ind))"`（页内表未过 idColumnsLast，对比 :2

核过的点：docs/验收/技术偏离表-v3.csv 序号 2576 六列逐字自读（python csv, utf-8-sig, newline='' 保 CRLF），未采信审计者转述；PathQcController.java 全文相关段：类级 /api/path-qc + hasAnyRole('ADMIN','QUALITY')、/indicators(:529)、/catalog、/detail(:1192-123；七条 WORKLOAD_* def（:396-455）与 caveat 原文；按日/按维 rows SQL（:823-925）；summaryOf（:1087-1145）；detailRows（:1248-1420）；pathologist；SPEC_SELECT / PAT_JOINS / DEPT_JOINS / DEPT_NAME / UNKNOWN_DEPT / W_COLLECTED,W_ISSUED,W_BLOCK,W_SLIDE 与 q() 占位符展开（:150-；用正则机械比对：PathQcController 全部 `as <alias>`（146 个）vs format.ts ZH（247 键），确认无真实列名落成英文表头；frontend/shell/src/views/medtech/pathology/PathQcView.vue 全文（模板 :26-139、script :160-320）：onMounted 自动查询、mdText 绑定、缺数据源不画；frontend/shell/src/views/medtech/pathology/format.ts 全文：ZH 表、cellText 枚举翻译族、isIdColumn/idColumnsLast、mdText 三条规则、default；frontend/shell/src/utils/date.ts fmtDateTime/partsOf：带偏移才换算到 Asia/Shanghai，纯日期串原样；frontend/shell/src/api/client.ts 响应拦截器 blob 分支（下载不会被信封判定误杀、后端 JSON 错误不会被下载成 .csv）；server/src/main/resources/application.yml:1-30（hikari connection-init-sql 钉死会话时区 Asia/Shanghai；无自定义 jackson 配置）；V144__pathology_base.sql：path_report 表语义(:157-175)、各时刻列均 timestamptz、sys_menu 168/169(:197-200)、双签 gate 默认 warn、迁移内零病理种子；V156__v52_menus.sql 全文（169 授 ADMIN/QUALITY、§四父 DIR 7 补授、§五序列纠偏）；V1__init_core.sql:58-82 角色种子；V36__phase38_role_matrix.sq

**反驳者·数据真实性与口径**（推翻）：

唯一带「科室」维度的工作量指标 WORKLOAD_DEPT 的「签发份数」一列，锚点根本不是签发事件：SQL 的时间窗是 {wc}=s.collected_at（登记时刻），issued 只是 count(*) filter (where s.report_issued_at is not null)，即「本期**登记**的标本里、**截至查询那一刻**已签发的条数」。而同一份 zh() 表把 issued 译成「签发份数」——这个键在 REPORT_ROUTINE/REPORT_FROZEN/REPORT_DOUBLE_SIGN 里锚的是 {wi}=report_issued_at，那里的「签发份数」才真是本期签发量。于是同一块看板、同一个中文表头「签发份数」，在两组指标里是两个口径：一个是流量（本期签发），一个是存量（本期登记者现在的状态）。后果可实测两条：①同一个**已结束**的历史区间，今天导一次 CSV、下周再导一次，科室的「签发份数」会变大、「在办数」会变小——上月登记、本月才签发的标本会被追加进上月那一行，一张已经发给科室做工作量考核的表因此不可复现；②想拿「本期签发了多少」按科室分，平台拿不出来：WORKLOAD_REPORT 只 group by 日、没有科室维度，穿透的 dept 过滤又被 deptFilterFor 显式限定为「只对 WORKLOAD_DEPT 生效」。caveat 正文确实写了「截至查询时刻」「要看本期签发了多少请走 WORKLOAD_REPORT」，但被指路的那条指标根本没有科室维度，而顶着「签发份数」表头的数字仍然照出——参数摘要要的是「报告签发量等多维度的业务数据汇总」，本平台给出的「签发量 × 科室」这一格锚错了事件且会回溯变动。

证据：CSV 原文（docs/验收/技术偏离表-v3.csv，序号 2576，★，参数摘要逐字）：「支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。 +------...」，现结论「部分响应」。

主证据（全部为我自己读到的当前代码，绝对路径）：
1) C:\Users\drxuj\医院信息化\modules\medtech\src\main\java\cn\hip\medtech\web\PathQcController.java:841-853 WORKLOAD_DEPT 按日行 SQL：`where {wc}`，而 :236 `W_COLLECTED = "s.collected_at >= ?::date and s.collected_at < ?::date + 1"`；:845 `count(*) filter (where s.report_issued_at is not null) as issued`。区间汇总同形，:1099。
2) 同文件 :1692 `case "issued" -> "签发份数";`——zh() 是一张**扁平**映射表，前端 frontend/shell/src/views/medtech/pathology/PathQcView.vue:98-101（表头 `:label="zh(c)"`）与 CSV 导出 PathQcController.java:1604（`csv(zh(c))`）都走它。
3) 同一个 `issued` 键在 :750（REPORT_DOUBLE_SIGN，`select s.report_issued_at::date as issue_day, count(*) as issued`）、:1058/:1162（reportSummary，`where {wi}`，:237 `W_ISSUED = "s.report_issued_at >= ...")` 里语义是**本期签发份数**。两组指标同表头、两个口径。
4) 「本期签发量」无科室维度：:884-899 WORKLOAD_REPORT 只 `from generate_series(...) as d(day)` 按日出行，无任何 dept 分组；:1239-1243 `deptFilterFor` 明写 `if (!"WORKLOAD_DEPT".equals(code) ...) return null;`。:406-411 的 caveat 自己把用户指向 WORKLOAD_REPORT，而那条指标拿不出科室。
5) 回溯变动可复现：issued/in_progress 是「登记窗内 + 现时状态」的交叉，唯一写 report_issued_at 的地方是 PathologyReportController.java:560-562 `update path_specimen set report_issued_at = now()`（now()，非入参），所以跨期签发一定会把数字写回**过去**那个登记窗的行。

次要但同类的第二处（同样是「历史区间的数会变」）：:413-417 + :233-235 WORKLOAD_BLOCK 名为「蜡块产出数」，锚点却是 `coalesce(b.embedded_at, b.created_at)`——取材当日建的蜡块若隔日才包埋，就不落在取材日，且会**从**先前查过的那一天**迁走**；窗内取材、窗外包埋的蜡块则整个从该窗消失。caveat 只在括号里写了「未录包埋时刻的回落建档时刻」，没说这条双锚点会让同一历史日的产出数随后续包埋而改变。WORKLOAD_SLIDE :419-421 同形。

需要说明的是，我没有找到「回填/猜值/凑数」：V144__pathology_base.sql 开篇「零回填纪律」属实（全文件 0 条 update；`grep -c "insert into path_" 迁移目录` = 0，确认零种子）；report_issued_at 唯一写入点即上述 :560，diagnose 端点 PathologyController.java:130-145 `where barcode=?

核过的点：从 CSV 用 python csv + utf-8-sig 自读序号 2576 行，逐字取参数摘要（未采信审计者转述）；PathQcController.java:396-447 七条 WORKLOAD_* def 的 name/anchorField/anchor/caveat 原文；PathQcController.java:236-241 W_COLLECTED / W_ISSUED / W_SLIDE / W_BLOCK 四个时间窗常量与 q() 占位符展开(:250-262)；PathQcController.java:821-925 七条工作量按日行 SQL 逐条；PathQcController.java:1087-1150 七条工作量区间汇总 SQL（确认为全窗聚合，非日均值再平均；blocks_per_specimen 为 sum/sum）；PathQcController.java:1327-1405 穿透明细 SQL（确认与汇总同锚点同窗，非换列）；PathQcController.java:1239-1243 deptFilterFor（dept 过滤只对 WORKLOAD_DEPT 生效）；PathQcController.java:1640-1800 zh() 全量中文表头映射，逐键核对是否存在跨指标同键异义；PathQcController.java:1537-1615 两个 CSV 端点与 toCsv 页脚（确认 caveat 随文件走、错误不导空表）；PathQcController.java:174-222 DEPT_NAME / DEPT_JOINS / SPEC_SELECT（确认全 left join on id，无扇出重复计数）；PathQcController.java:340-370 三条 unavailable 指标的 reason/missingFields（确认不给近似值）；V144__pathology_base.sql 全文（零回填纪律、path_block/path_slide/path_process/path_tech_order/path_report 表定义、菜单 168/169、sys_conf

## 下一轮清单（主控从未推翻/推翻原话里提炼；修前逐条实测）

**2563/data 那条已在本轮修掉**（提交 91fcd5d + b694eea）：反驳者指出「SQL 与 Java 单测断言三源并集、而 CI 里的 E2E 四处仍严格钉『只等于首块』，二者不可能同时绿」——属实，第一次修补漏了 E2E。
已同步四处断言与两处文案，对真实服务复跑 e2e-v60-audit 四段全绿。**教训：改派生列口径时，SQL / 单测 / E2E 三处必须同改**，这是「同一事实两处口径」的又一例。

余下清单（下一轮修前须逐条实测）：

- **2530（2/3 推翻）** ①补取材追加后，「大体所见文本」是累积全文而「结构化字段」只有最后一次追加那一版，系统却把这种状态标成「与当前文本同版」、把仍在文本里的上一版字段标成「已被取代」——**口径与事实相反**，且无处可调阅出与当前全文对应的完整结构化记录；②前端没有自定义字段名入口（`grossFields` 只渲染模板字段 ∪ 已有键，唯一的「增加」按钮是加蜡块），模板清单只能由运维直改 `sys_config`——评委问「模板里没有这一项怎么办」，唯一答案是写进自由描述，即回落成非结构化文本。后端本就支持任意 label（20 项/32 字），是「只做了后端」。
- **2563（3/3 推翻）** ①「派生」标的判定条件 `!row.block_code && row.blocks_derived` 在补取材场景恒为 false——因为 v60 会把首块回写进 `block_id`；值改成并集了，判定「是否派生」的前端条件没跟着改，页面与 `TechOrderPanel` 自己的页首说明不符，`DiagnosisPanel` 同一列连标签都没有；②流转节点备注里同时有内部主键与裸英文枚举（`#12 免疫组化(IHC) CK7`、SECTION 备注更是纯裸码），而 ⑤/④ 两个清单**都没有医嘱 id 列**，`#12` 在清单上无从对应。
- **2576（2/3 推翻）** ①`WORKLOAD_DEPT` 的「签发份数」锚的是**登记时刻**窗口内「截至查询那一刻已签发」的存量，而同名表头在 `REPORT_*` 系列锚的是签发时刻的流量——同一块看板、同一个中文表头两个口径，后果是**同一历史区间今天导与下周导数字不同**（上月登记本月签发的会被追加进上月那一行），而想按科室看「本期签发量」平台拿不出来；②「按科室导出」只有 E2E 调得到：后端 `detailCsv` 有 `dept` 形参与页脚过滤说明，但前端 `exportCsv()` 不传 dept、穿透抽屉里没有导出按钮——评委看完某科室明细再点导出，拿到的是全科室最多 200 条且页脚不写过滤，与屏幕上刚看的不是同一份，恰好撞上抽屉顶部「对不上账即为缺陷」那句话。

**方法上这轮多学到的**：审计者 3/3 判「可上调」而反驳者 8/9 推翻，与前两轮同形。这轮新出现的一类是**「改了值没改判定」**（2563 的派生标）与**「同名表头两个口径」**（2576 的签发份数）——都不是功能缺失，是同一事实在系统内部有两份不一致的表达。前者可做成机械断言：凡是「派生列」与「判定该列是否派生的条件」必须同源。

