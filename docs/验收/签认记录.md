# ★ 条目签认记录（按桶，追加式；本文件不由工具重新生成）

> 与 `docs/验收/签认分桶清单.md` 的分工：清单由 `tools/signoff-triage.py` 每次重生成、只反映**当前**分桶；
> 本文件是**决策留痕**，逐次追加、不改写历史。

---

## 2026-09-19 · v67 L1 第一次签认（A/B/C/F 四桶）

**签认范围**：项目负责人按桶签认 A、B、C、F 四桶的处置动作。
**执行结论**：**四桶不能整桶照签**——逐条回查代码后，14 条 A 桶里只有 7 条该改判，
F 桶 71 条里有 53 条不具备签认条件。下文逐桶记录实际处置与例外理由。

### 前置动作：E 桶 11 条已全部重写（签认的前提）

E 桶的「响应说明」原为机器泛化占位或内部词泄漏，**无法作为签认依据**，故先清。
11 条均经源码逐条查证后重写（查证要点见下），结论一律维持「部分响应」：

| 序号 | 原说明的问题 | 查证后的真实状态（要点） |
|---|---|---|
| 229 | 「精麻毒放标识为 md_drug 字段扩展」——内部列名上屏 | 高警示药品属性维护与发药核对逐行强制确认**已闭环**（含双人核对、看似听似）；抗菌药分级开方硬拦截；但精/麻/毒/放四类属性标志确实不存在 |
| 707 | 「集成适配器+路由雏形已有，调度编排属 ESB 级配套」——内部口吻 | 定时任务与多实例互斥选主为真（5 个作业）；执行器注册/新增/编辑/调度界面全链路缺失 |
| 719 | 「对应模块查询统计能力已实现，界面细节逐条比对」——占位 | 数据元/术语的新增与列表查询为真；编辑、启停、编码规则、版本信息、分页检索均无；值域与备注两列界面不开放填写 |
| 1004 | 同上占位 | 仅支持药品名称汉字检索；拼音/五笔/编码/商品名均无；通用名列已有维护入口但不作检索项 |
| 1005 | 同上占位 | 三级处方模板与协定处方为真（携带剂量/频次/途径/天数）；药品级默认用法不存在（**开单表单初值为全局固定值，与药品无关，易被误认为已实现**） |
| 1072 | 同上占位 | 参数写入接口与取值域校验为真；分页表格、按编码/名称检索、界面改值全无 |
| 1076 | 同上占位 | 病历为纯文本书写＋四级模板；字符/段落/版式/页眉页脚/特殊符号/图文/矢量图/表格**七项全无** |
| 1402 | 同上占位 | 消毒供应包四步流转与追溯为真；器械包规格、包内器械明细、模板套用均无；手术器械清点无 |
| 2467 | 同上占位 | 门诊结构化元素录入六型为真；医学表达式与特殊符号全无；结构化元素定义无维护界面、住院侧未接入 |
| 2535 | 同上占位 | **染色**质量多选批量评价为真；**包埋**无质量属性、逐块登记、不支持批量 |
| 3120 | 同上占位 | 三管监测闭环与千日感染率、现患率为真；**按病区**维度查询全无（病区数据已存在，查询层未使用） |

### A 桶（原 14 条）→ 实际改判 7 条

**改判为「配套产品响应」7 条**：180、181、219、242、243、246、1093
（知识包版本管理与更新提醒、药品不良反应信息通报、150+ 判断条件与规则编辑器、
40+ 药理属性、中药颗粒目录、医学知识库词条——内容确属商用知识库范围。）

**不改判 7 条，理由逐条**：

| 序号 | 不改判理由 |
|---|---|
| 163 | 混合条款：药物手册属知识库内容，医药学公式属平台功能，整条改判会错误撇清后者 |
| 167 | 说明列原为占位文案，且条款性质介于内容供给与平台功能之间，须人工裁决后再定 |
| 168 | 「用户自定义维护文献」是**院方自己维护**的资料库，属平台功能欠账，不是厂商内容 |
| 207 | 乌头碱成分累计判断依赖药品成分属性，平台**连承载列都没有**，不宜说成由厂商供给 |
| 209 | 平台已具备药理类别字典与药品归类维护（承载框架为真），现说明低估了平台，应改写说明而非改判 |
| 228 | 同 207，作用机制属性无承载列 |
| 993 | 诊断库与病历模板库主体已具备，缺的是标准编码全量导入，属 D1 快赢，不是厂商内容 |

### B 桶（原 4 条）→ 实际改判 2 条

**改判为「配套产品响应」2 条**：4293（审计网专线线路租赁）、4295（网络安全设备硬件规格）——均属非软件条款。

**不改判 2 条**：
- **131 软件兼容国产操作系统**：平台技术栈确可部署于国产操作系统，缺的是**兼容性测试报告/适配证书**，
  属证明材料而非能力缺失。走承诺函路径；待材料出具后按正常复核流程申请上调，**不在本轮擅自上调**。
- **629 ClickHouse 分层数仓**：属真实功能缺口（现统计直接基于业务库），不是配套产品能掩盖的，维持部分响应。

### C 桶（2 条）→ 0 条改判，仅改写说明

- **54（删除用户 / 数据权限）**：已改写说明，把「停用+审计留痕替代物理删除」表述为**合规设计选择**并说明可另行约定；
  但该条同时缺「数据权限管理（按科室/病区/本人）」，属真实缺口，**故仍维持部分响应，不可上调**。
- **1003（缺药阻断）**：已改写说明，讲清开单提醒、发药硬拦截的两段式处置与业务理由，并申明可配置为阻断；
  参数字面要求「不准许继续开具」，维持部分响应。

### F 桶（71 条）→ **不予整桶签认**，仅 18 条具备签认条件

F 桶原判「确认现结论口径无误即可」。抽验后发现**口径并非无误**：

1. **6 条「外部条件」的理由与条目无关**，写的是「需医保局/省平台等外部环境开通后联调」，
   而条目实为：病历打印预览/续打/合并/区域打印（1088、2464）、住院病人费用明细查询（2032）、
   病人区域来源分布（1035）、手术室与家属等候区电子看板屏（1417、1419）——**与医保局无任何关系**。
   实测：平台打印能力覆盖 12 类单据、预览已具备，续打/合并打印/区域打印为零；
   住院每日费用清单（含单价、数量、金额与按日期检索）**平台其实已有**。
   → **这 6 条应退回重判**，其中 2032 有上调空间，1088/2464 应为部分响应并枚举缺口。
2. **47 条「配套产品响应」共用同一句样板**：「属设备配套软件/商用知识库/CA 等产品参数，平台已留标准接入点」。
   该样板全表用了 202 次（其中 ★ 47 条）。问题有二：一是把设备/知识库/CA 三类混为一谈，未说明本条属哪类；
   二是「平台已留标准接入点」是对 47 条不同能力的**统一断言**，而实测各接入点差异极大
   （玻片打码设备、药房自动化设备的专用接入点在仓内明确标注为尚未提供）。
   → **这 47 条须逐条改写说明**，不得以一句样板统一承诺。
3. 其余 18 条说明为逐条撰写、内容具体，**可签认**。

---

### 本次实际清账结果

| 项 | 数 |
|---|---:|
| E 桶重写（解除签认阻塞） | 11 |
| 改判为配套产品响应 | 9（A 桶 7 + B 桶 2） |
| 改写说明、结论维持 | 13（E 桶 11 + C 桶 2） |
| F 桶可直接签认 | 18 |
| **本轮真正清掉** | **31 条**（9 改判 + 18 确认 + 4 待确认口径已改写） |
| 退回重判 | 6（F 桶误判为外部条件） |
| 须逐条改写说明 | 47（F 桶样板文案） |

> **对 v67 立项节的修正**：立项时称「A/B/C/E/F/Y 共 109 条是口径问题，签认即可清账」。
> 逐条核过后这个数字**站不住**：真正签认即清的是 31 条，另有 53 条（6 退回 + 47 改写）
> 需要返工才能进入签认。分桶给出的是**处置方向**，不是**可直接执行的清账量**——
> 这一点须写进下一轮的口径，避免再用桶大小估算工作量。

**签认人**：____________  **日期**：____________

---

## 2026-09-20 · v67 L1 第二批（F 桶样板改写 + A 桶补正）

承上批「F 桶不予整桶签认」的结论执行返工。

### F 桶 47 条样板文案 → 按 12 组逐条改写（本批完成 45 条）

原文案 47 条共用一句「属设备配套软件/商用知识库/CA 等产品参数，平台已留标准接入点」，
问题是把三类配套产品混为一谈，且对 47 种不同能力作**统一的接入点承诺**。
改写后每条都点名**属哪一类配套产品**，并如实写平台侧到底提供什么承载。分组：

| 组 | 条数 | 内容 | 平台侧承载的如实口径 |
|---|---:|---|---|
| G1 | 8 | 药品说明书库与药学工具书 | 仅药品主数据维护与批量导入；**平台自身不提供说明书内容库** |
| G2 | 8 | 成套规则集内容与更新机制 | 规则结构化承载＋在线新增启停；开单时相互作用禁用级、年龄禁忌、抗菌越权**实时拦截**；内置规则仅示例数据，成套规则须按知识库接口另行对接 |
| G3 | 7 | 剂量审查（体重/体表面积/累计/成分） | 仅疗程上限提醒与年龄拦截；**数值剂量计算未提供**，患者无体重身高、药品无成分属性 |
| G4 | 4 | 注射剂配伍、溶媒、钾离子浓度、滴速 | 途径词表与溶媒配伍规则维护**已具备但内容未预置**，且**尚未接入开单主流程**，当前仅供独立核对 |
| G5 | 5 | 中药饮片（十八反十九畏/毒性剂量/孕期/外用/肝肾） | 相互作用规则库可承载配伍禁忌；特殊人群状态登记与规则承载已有但须人工申报；饮片专属属性未设置 |
| G6 | 1 | 麻精缓控释制剂重复使用 | 同药重复硬拦＋通用名/药理类别重复承载；**管制药品属性标志未设置** |
| G7 | 6 | 肠外营养 TPN 审查 | 仅药品主数据与审查承载；制剂分类与特征参数计算未提供 |
| G8 | 2 | 自定义规则与管理规则 | 规则可新增启停、即时生效；**已有规则不可在线修改删除**，无多条件规则编辑器 |
| G9 | 2 | 处方审查实时返回与点评推送 | 药师审核（待审队列/通过打回留痕）＋开单实时审查已有；点评推送、免密跳转、申述未提供 |
| G10 | 1 | 草药倍数药校验 | 校验承载机制已有（可拦截并阻止保存）；草药成品类型与倍数规格属性未设置 |
| G12 | 1 | 冠脉影像后处理与辅助诊断 | 属影像工作站/影像辅助诊断产品；平台提供检查申请、报告书写审核、分享与影像系统地址配置项 |

**G11（序 664、671「指标自动采集需依托源头业务系统」）2 条本批未改**——
参数列举的源头系统里有相当一部分**正是本平台自有模块**，须先逐个核准归属再写，避免把自家能力
说成需要外部系统。查证在途。

### 改写中自查到的一处过度承诺（已当场修正）

G1 初稿写「平台侧提供药品主数据维护（…与**抗菌药物分级**等基础属性）」，
而抗菌药物分级**没有任何在线写入路径**（仅迁移期种子，实施期须直连数据库改），
把它列为可维护属性即为假应答。已改为「药品编码、名称、规格、剂型、单位、价格、费用类别与自费标志」
并补「按目录批量导入」。
> 同一批里 G2 写的「开单环节抗菌药物越权开具实时拦截」是**真的**（硬拦截），保留。
> 这两句只差一个词，一句假一句真——**说明列必须逐句对着代码写，不能凭该域「大致做了」下笔**。

### A 桶两条按实际能力补正（结论仍维持部分响应）

- **209 重复治疗审查**：原说明称「药品主数据当前无成分与**治疗分类**属性」——**治疗分类已经有了**。
  平台现有重复用药三层次（同药硬拦／同通用名／同药理类别）＋药理类别字典与药品归类在线维护。
  改写后如实写明三层次与维护能力，并声明**平台不预置任何类别与通用名内容，未维护前第②③层不产生结果**。
  仍缺成分维度与给药途径维度，故维持部分响应。
- **993 诊断库与病历模板库**：原说明低估。模板库实为四级使用范围（全局/全院/科室/个人）＋模板授权＋
  科室默认模板＋按既往病历新建＋存为模板；诊断支持名称、编码、拼音检索与常用诊断收藏。
  缺的仅是标准编码全量导入与字典在线维护。**此条具备上调条件，但上调须走三方复核零推翻，本轮不擅自上调。**

### 本批清账

47 条落表（45 F 桶改写 + 209/993 补正），结论未变，全部为说明列改写。
★ 条目里仍用样板文案的从 47 条降至 2 条（即待查的 664、671）。
四维校验本批 47 条零问题；v3 的 BOM、CRLF 与 4062 行数不变。

---

## 2026-09-20 · v67 L1 第三批（F 桶退回重判 8 条，样板文案清零）

### 664、671：原判「配套产品响应」是把自家能力送人

两条参数说「自动采集指标需依托对应源头业务系统提供数据支撑」，并列举二十余个系统名。
原文案按样板答「属设备配套软件/商用知识库/CA 等产品参数」——**三类都不沾边**。
逐个核建表证据后的实情是：**这些「源头系统」大部分正是平台自有模块**，数据在平台内生产生。

| 序 664 所列 | 平台自有 | 尚不覆盖 |
|---|---|---|
| 18 个系统 | 医院信息系统、重症监护记录、单病种、检验、手术麻醉、院感、处方审查、急诊、病理、不良事件、护理管理、DRG、科研、输血（14） | 药学监护、血液透析、手卫生依从、处方点评（4） |

| 序 671 所列 | 平台自有 | 尚不覆盖 |
|---|---|---|
| 8 个系统 | 医院信息系统、手术麻醉、检验、DRG、处方审查、设备管理（6）；财务侧有收费日结与交款核对 | 双向转诊（1） |

> 核查中剔除一处误判：「双向转诊」的关键词命中出自病历复印申请的用途下拉选项，不是转诊模块。

改判为**部分响应**并如实写明覆盖与未覆盖清单，同时补上一条平台自身的短板：
**指标快照当前为手动触发，按周期自动采集与自动计算尚未提供**。

### 6 条原判「外部条件」——理由与条目无关，全部退回重判

原理由一律写「需医保局/省平台等外部环境开通后联调」，而条目与医保局毫无关系。逐条实测后改判部分响应：

| 序号 | 实测已有 | 实测缺口 |
|---|---|---|
| 1088、2464 病历打印 | 出院小结、体温单、护理记录单等文书的打印版式与打印前预览，含医院名、单据名、患者页眉、分页与签名栏，并有打印留痕 | 续打、合并打印、区域打印三项全无 |
| 2032 住院费用明细 | 项目名称、规格、**数量、单价、金额**、费用类别与执行时间逐条列出并给合计，可打印交患者，患者端亦可查 | 只能按指定日期逐日查、无时间区间；无按管床医生过滤的「我的病人」视角 |
| 1035 患者区域来源 | 患者档案登记联系地址 | 地址为整段文本，无省市区县结构化字段，无区域来源分布统计 |
| 1417 手术室看板屏 | 手术排台与台账；**已采集手术间与入室、开台、结束、出室四个进程节点**，以及手术级别、麻醉分级、切口等级、类别与取消原因；平台已有门诊分诊叫号大屏可作看板技术基础 | 无手术室看板屏页面；无多院区多住院楼同步展示（平台当前单院区部署） |
| 1419 家属等候区看板屏 | 同上进程节点可支撑病房等待、手术中、返回病房三态判定 | 无家属等候区看板屏页面 |

### 本批与 L1 全程结果

8 条落表；★ 条目中使用样板文案的**从 47 条降至 0 条**。
这 8 条随之从 F 桶移入 D1／D2——即它们现在被正确识别为**平台自己的小缺口**，
而不再挂在「配套产品／外部条件」名下。四维校验本批零问题。

**★ 结论分布变化（L1 全程）**：部分响应 423 → 422、配套产品响应 64 → 71、外部条件 7 → 1、
正偏离 23（未动，上调一律须走三方复核零推翻）。
**分桶变化**：E 11 → 0、A 14 → 7、B 4 → 2、F 71 → 72（净值含 9 条改判流入与 8 条退回流出）、
D1 133 → 143、D2 252 → 261。

> **L1 三批合计改写 77 条**（E 桶 11 + A/B/C 13 + F 桶样板 45 + A 桶补正 2 + F 桶退回 8，
> 去重后为 77 个不同序号），全部经源码逐条查证，四维校验零问题，v3 的 BOM、CRLF 与 4062 行数始终不变。

---

## 2026-09-22 · v68 三线并行（Y 桶裁决 / 993 复核 / L2 盘点）

三个方向同时开工，结果有两条是**对我方自己前两批产出的纠错**，记在最前面。

### 甲、两处「我自己写进去的过度声称」，已当场修正

| 序号 | 我在哪一批写错 | 错在哪 | 已修正为 |
|---|---|---|---|
| **993** | 第二批（09-20）A 桶补正 | 我写「模板按全局、全院、科室、个人**四级使用范围管理**」。复核实测：医生套用模板走的是不认使用范围的旧取数路径，门诊医生站取模板时连科室都不传，**返回的是全院全部启用模板，含所有人的个人模板**；而模板维护页屏上明写「管理员也看不到别人的个人模板」——两者直接冲突 | 不再把四级范围列为已交付能力；改为「可按科室与个人设置使用范围并逐项授权」，并把「模板使用范围在医生套用列表中的过滤」列入尚未提供 |
| **1088 / 2464** | 第三批（09-20）F 桶退回重判 | 我写「并提供打印留痕记录」。实测打印留痕白名单只有收费票据与挂号凭条，**病历文书打印不留痕**，且该处代码明确拒绝把临床单据写进财务打印日志 | 删除该半句，并把「病历文书的打印次数留痕」列入尚未提供 |

> **教训（与本轮 G1「抗菌药物分级」同型，一周内第二次）**：我两次都是**从相邻能力推断本条能力**——
> 「平台有打印留痕」推出「病历打印有留痕」，「模板表有四级范围列」推出「四级范围在书写时生效」。
> 相邻能力存在 ≠ 本条链路打通。**写对外说明时，每一句都要问「这条链路本身有没有被走通」，
> 而不是「这个域有没有这个东西」。**

### 乙、993 三方复核：**三个镜头全部推翻，维持部分响应，不可上调**

复核按本项目既定判据（零推翻才上调）执行。三个镜头各自找到硬缺口：

| 镜头 | 结论 | 硬缺口 |
|---|---|---|
| 子要求拆解 | 推翻 | 「病历存为模板」「按既往病历新建病历」**后端端点齐备、前端零调用方**；医生套用走不认使用范围的旧路径 |
| 演示可行性 | 推翻 | 零种子库下模板库**一行种子都没有**，门诊医生站的模板套用栏整条不渲染，评委按默认路径走会认为该功能不存在 |
| 数据真实性与口径 | 推翻 | 模板维护页「管理员也看不到别人的个人模板」在医生站下拉这条库态下**为假**；「诊断库」实为常见病目录二十条，且无独立浏览菜单 |

另两项缺口：科室默认模板**只能设、无人取**（医生站不读该标记）；诊断字典只覆盖门诊与入院登记，
**出院诊断与住院其他诊断是裸文本录入**。

> 复核者同时列出七个「试过但没能推翻」的攻击点（四级范围定义齐全、模板授权有界面、
> 停用确实挡在下拉外、默认模板唯一性是数据库约束、存为模板有越权校验、菜单已授权、拼音检索为真），
> 证明其确实攻过而非未找。

### 丙、Y 桶 7 条裁决：**全部维持部分响应**，且查出挂起建议本身的系统性问题

7 条的「响应说明」原是内部待办文案（【二级证据评审建议，待项目组签字】），不能对外，已全部改写。
裁决过程中发现**挂起建议里的「已备」有三条是跨域搬运**——拿错业务线的证据当依据：

| 序号 | 挂起建议的依据 | 实情 |
|---|---|---|
| 1168 / 1169 | 引门诊收费的「日结报表＋交款核对」 | 条目上下文是**药品库房**月结，两条业务线；同域邻证应是药品盘点单与库存流水 |
| 1406 | 引 CSSD「回收→清洗→灭菌→发放」 | **环节名写错**，实为打包→灭菌→发放→使用；且 CSSD 与**手术室器械清点**是两条线 |
| 1411 | 称「麻醉记录单已备」 | 实为术中记录的**录入与查询接口，没有记录单生成与打印**；对外若写"记录单已备"，评委点打印会落空 |

> 上一轮预埋的 CSSD 疑点**坐实**：那条建议是凭印象写的。
> 另：1067/1071/2445 三条**未采纳「改判配套产品响应」**——源码里既无元素库/数据源实体，
> 也无任何第三方报表平台的对接痕迹，「属报表平台产品」在本仓内找不到可复核依据；
> 要改判须由项目组提供所投产品的功能证明与集成方案，**在那之前不改**。

### 丁、L2 护理与病历文书 37 条盘点：找出 3 条零成本上调候选

**最值钱的产出不是开发清单，是三条「说明写着未提供、其实整条已经做完」的条目**：

| 序号 | 参数 | 实情 |
|---|---|---|
| 992 | 临床数据资料引用 | 四类资料（基本信息/检验/检查/历史病历）的引用抽屉、医生站「引用资料」入口、点选插入正文，**全链已通** |
| 994 | 病历历次更新痕迹 | 签名前书写阶段的版本留痕有表、有端点、有比对页面、有医生站直达入口，**全链已通** |
| 1098 | 结构化元素检索 | 按元素编码检索的端点与可配置结构化元素模型均在，后端类注释直接点名兑现本条 |

这三条**具备上调条件，但上调须走三方复核零推翻**，已排入复核队列，本轮不擅自上调。

另有五条属**部分低估**，其「尚未提供」清单里各有一项其实已做，本轮已逐条改写：
991（签名入口与切换患者签署引导已有）、1092（从既往病历带入正文已有）、1095（个人模板已有）、
2463（修改方式记录已有）、2011（主治医生数据后端已返回）。

**S 档可归并的四个包**（供 L2 立项排期，本轮不实施）：
- **包 A（性价比最高，可不动后端）**：四个已有端点前端零调用——按既往病历新建病历、病历存为模板、
  临床数据全文检索、标准文档导出。一次「医生站＋患者全景加入口」改动同时兑现 6 条子项。
- **包 B**：门诊医生站工作队列加日期区间与「我的患者」过滤（住院侧已有现成写法），一个端点兑现 3 条。
- **包 C**：把已有的病历完整性检查结果显示到书写页；在院一览表加主治医生与年龄两列。
- **包 D**：指标快照加周期调度（现有生成逻辑已幂等），单文件改动兑现 1 条。

**L 档退回真欠账**：移动端病历质控（三条应合并独立立项）、临床数据中心纳入面扩容、
质控解锁审批流、病历存为疾病组、区域打印、全文索引引擎、门诊病历质控规则集。

### 本轮落表

15 条改写（修正 3 + Y 桶 7 + 低估项 5），结论全部维持，均为说明列改写。
**E 桶与 Y 桶双双归零**；四维校验本批零问题；v3 的 BOM、CRLF 与 4062 行数不变。

---

## 2026-09-23 · v74 L1 收尾（F 桶 18 条签认）

承 09-19 第一批「F 桶仅 18 条具备签认条件」的结论执行签认。
**18 条在记录里只有个数、没有序号**，先用 git 把它们定出来：
拿第一批之前的表作基线，F 桶 72 条中**说明与结论至今从未改动**的恰好 18 条
（72 = 18 未动 + 54 已改写），与当初的计数独立对上。

> 序号：29 164 187 198 199 202 213 247 594 622 998 1105 2024 2040 2523 2524 2541 4298

### 一、当初判它们「可签认」的依据不成立

09-19 的原话是「其余 18 条说明为**逐条撰写、内容具体**」——这是**格式判据，不是真伪判据**。
那 6 条误判与 47 条样板就是这么漏过去的。逐条核过之后，**18 条里有 8 条不该签**：

| 不予签认 | 问题 |
|---|---|
| 164、198、199、202、213、2040、2523 | 对外的「响应说明」列里**嵌着内部评审标记**「二级证据评审建议，**待项目组签字**」。签一句「待项目组签字」本身就自相矛盾，且客户会直接读到我方内部流程语 |
| 198、199、202、213 | 上述之外还**逐字共用同一句样板**（"商用用药知识库深度审查；平台 CDSS 四规则表留有同构接入点"）——**与当初否掉那 47 条是同一个毛病**，只是换了一句样板，原规则只认"设备/知识库/CA"那句，认不出这句 |
| 2524 | 全文 22 字「外设采集属设备配套（图像以档案名登记制留痕）」，是内部速记片段，不是对外应答 |

**→ 这 8 条退回重写，不签。**

### 二、其余 10 条逐条核实通过，予以签认

不只看文字，**每条的声称都回代码核过**（正面声称要找得到，否定声称要确实没有）：

| 序 | 结论 | 核实要点 |
|---|---|---|
| 29 | 外部条件 | 医保接入适配层在（适配层 + Mock 实现）；DRG 所需数据源在门急诊/住院/病案模块内；**对接确实待医保部门给规范与联调环境**，未声称已对接 |
| 187 | 配套产品响应 | 过敏史交叉禁忌、年龄禁忌、药物相互作用三类规则表与执行服务均在，门诊开单实时审查已接；**"内置规则仅为少量示例数据"属实**（种子合计个位数） |
| 247 | 配套产品响应 | 否定声称属实——病人特征字典与基于其的自定义规则，全仓零命中 |
| 594 | 配套产品响应 | 签名/验签接入接口在；"不涉及证书生命周期管理"属实 |
| 622 | 配套产品响应 | 消息进出流水记录在；**"集成监控页面"也真有前端页面**（不是只有后端） |
| 998 | 配套产品响应 | DDI／疗程上限／年龄限制三类审查在开单环节实到；医保统一接入适配层在 |
| 1105 | 配套产品响应 | 面向检验检查的 MLLP/HL7 接入通道在；药房自动化设备专用接入点确实没有——**代码注释里把这条硬边界写得和应答一样** |
| 2024 | 配套产品响应 | PACS 地址配置项在；检查报告分享短链**前端真在调用**，不是孤立端点 |
| 2541 | 配套产品响应 | 同 1105 的通道；玻片打码设备对接确实没有，代码注释同样明确 |
| 4298 | 配套产品响应 | 资产模块确为医院固定资产台账，与本条所指网络资产测绘**确非同一范畴** |

**10 条结论均维持不变，本次为确认签认，不改表一个字。**

### 三、顺带查出一个系统性问题：对外表里嵌着内部标记，而质检**结构上看不见**

那句「二级证据评审建议，待项目组签字」**全表 4062 条里有 120 条**带着它
（★ 7 条，即上面退回的那 7 条；其余 113 条非 ★）。
**若 v3 就这么对外，客户会在 120 处读到我方"待项目组签字"。**

#### 为什么四维质检一次都没报过它——两层原因，第二层更要命

**第一层**：内部词正则只认代码符号（文件名/类名/迁移号/表名）与判定词，**认不出流程语**。
已补入 `二级证据 / 待项目组签字 / 评审建议` 三词。

**第二层（查下去才发现的）**：质检**只扫 `rewrite_wave*.json` 里被改写过的行，从不扫最终表**。
那 120 条恰恰是**从未进过任何改写波次**的行——改写侧的正则补得再全也拦不住它们，
因为它们压根没被改写过。**第一层的补丁对这 120 条无效。**

> 更正：本节初稿只写了第一层，说"安全网已补上"。**那句话不成立**，据实改写如上。

**还有一处静默失效**：质检脚本把审计与改写证据的路径**写死在一个旧会话的临时目录**
（`AppData\Local\Temp` 下）。那目录碰巧还在，所以它报"已改写 3581 条"；
**一旦被清理，统计会悄悄掉到只剩仓内证据的条数，而且照样打印结果**——
这正是「一次就绿是可疑信号」的同型。

#### 处置

1. **证据落仓**：把 `audit_r1`、`audit_r2_wave1–5`、`rewrite_wave1/2/3/9` 共 10 份
   从临时目录复制进 `docs/验收/`。**这本来就是项目自己写过的纪律**
   （"scratchpad 随会话消失，判定证据必须落仓"），wave10 起做到了，wave1–9 漏了。
2. **质检加 `--final` 模式**：直接逐行扫最终表，不依赖改写波次证据。**对外发布前以此模式为准。**

实测 `--final` 首次运行结果：

| 维度 | 条数 | ★ |
|---|---:|---:|
| A 内部词 | **123** | 7 |
| E 说明过短 | 5 | 0 |

123 = 120（内部标记）+ 3（库表名 `hrp_material_txn` 漏进对外列，序 3793/3794/3861，均非 ★）。
新增三词**零误伤**——按「误报比漏报更伤」的既有纪律，补正则必须实测校准过才算数。

### 四、本次清账

| 项 | 数 |
|---|---:|
| F 桶实际签认 | **10** |
| F 桶退回重写 | **8**（7 条内部标记 + 1 条速记片段；其中 4 条另属样板共用） |
| 表内容改动 | **0 行** |
| 工具修补 | 质检内部词正则补 3 词 |

> **对 09-19 那条结论的修正**：「F 桶 18 条可签认」应更正为 **10 条**。
> 原判据「逐条撰写、内容具体」是看格式，**F 桶前后三次翻车都栽在同一件事上——
> 用文本形态推断内容真伪**。此后凡进入签认的条目，一律要求附"声称→代码"的逐条核实。

**待处理（不在本次范围）**：`--final` 报出的 128 条（内部词 123 + 说明过短 5）
须批量重写后才能对外，其中 ★ 7 条。这不是签认能解决的，须走重出流水线。
**另建议**：把 `--final` 挂进对外发布前的必过检查——它现在是全表唯一的兜底。

**签认人**：____________  **日期**：____________

