> **后续**：2026-09-08 v55/v56 交付后对本文「被反驳推翻的 10 条」做了二次复核，**2553 已上调**（v3 首次上调），其余见 `v57核账-v55交付后.md`。

# v48–v53 交付后核账 —— 结论：**129 条 ★ 全部维持部分响应，v3 不动一个字**

核账日期 2026-09-07（v54 已交付）。159 个 agent，零失败。

## 为什么要重跑这一轮

上一次陈旧面复核在 v47 之后，结论 0 条可上调——但那时 **v48 病理、v50 药房、
v51 CDSS、v53 病历留痕都还没建**。这几版建出来正是为了让偏离表的应答变成真的，
所以要回来核账。候选是这四个域里仍标「部分响应」的 **★ 129 条**。

## 结论

| 档位 | 条数 |
|---|---|
| 过审可上调 | **0** |
| 复核者判可上调、**被反驳者推翻** | 10 |
| 复核者自己就判维持 | 113 |
| 属外部边界（补开发救不了） | 6 |

**v3 无需任何改动。** 建了功能不等于应答可以上调。

## 方法可信度（三重校验）

1. **主控不喂条款原文**，只传序号，agent 自己读 CSV 并回填 `param_verbatim`。
   （立此规矩是因为主控上一轮转述时把「护理监测集成化记录」写成「病理取材登记」，
   而**全链路不会有任何一环报错**。）
2. **主控逐条核对回填原文：129/129 与表内一致**，判定确实建立在真实条款上。
3. **每条判可上调的过三个独立反驳者**（子要求拆解 / 演示可行性 / 数据真实性），
   立场默认推翻、不确定即推翻，三人全跑完且零推翻才算过审。本轮 10 条主张上调的**无一幸存**。

## 反驳者挖出的真缺陷——**「后端做了，前端够不着」**

这是本轮最有价值的产出，也是 v54 `ReachabilityTest` **覆盖不到的一层**：
它保证了「控制器有前端引用」，但**控制器有前端引用 ≠ 用户走得到**。

### 217（3/3 推翻）
**条款原文**：在获取病人既往过敏原或过敏类信息的基础上，提示病人用药处方中是否存在与病人既往过敏药品、食物相关的、可能导致类似过敏反应的药品；

- 本条的实际生产判定路径仍是 DoctorStationService.java:725 的自由文本关键词闸——它在 :742 结构化引擎之前执行、:869/:874 直接抛 4012 且完全不受 cdss.gate.allergy 管辖，而本仓自己的 V51CdssTest.java:706-708 把它登记为「既漏又误」、:660 判词「两个方向都错的闸不叫闸」，其中假阴性（原文「阿莫西林过敏」开阿莫西林胶囊 → code=0 放行，实测代码在 :670-676 仍在 HEAD）至今一个字未修。与此同时，被拿来主张上调的结构化引擎受 V51CdssTest.java:580-587/:634-637 零回填纪律约束、V152:320-328 五表全空，出厂覆盖 0 名患者，AllergyRuleService.java:434 对每个有原文的患者只能逐字输出「本次审查未覆盖其中任何内容」
- DoctorStationService.java:725 的旧关键词过敏拦截（方法体 :862-875，关键词表 :827-831，抛 4012 于 :869/:874）排在结构化引擎 :742 之前并抛异常回滚整笔开单——对「过敏史原文含青霉素/头孢/磺胺/阿司匹林」的患者，v51 结构化过敏引擎根本不执行：无横幅提示、无 gate 台账、错误码与文案都是 v3 时代的旧路径，且不受 cdss.gate.allergy 任何档位管辖、无开关可关。本仓自己的端到端用例 V51CdssTest.java:398/408/450 靠传 allergyHistory=null 绕开这条路，等于承认结构化链路只在「原文为空」的患者上被验证过——而那正是本条款目标人群的反面。

### 994（3/3 推翻）
**条款原文**：病历内容历次更新痕迹保留，拥有病历留痕功能。

- 病历版本留痕页要手工输入 outp_emr.id，而全系统没有任何界面显示这个 id、也没有任何跳转入口：EmrVersionView.vue:11/:236 是空输入框且不读 route.query；`grep -rn "emrId" frontend/shell/src` 除该目录外命中 0，`grep -rn "emr-version" frontend/shell/src` 除该目录外命中 0；医生站按 registrationId 寻址（DoctorStationView.vue:917），DoctorStationController.java:123 返回的 OutpEmr 带 id 却被前端 :930-934 丢弃，工作台 :798-823 也不取 emr.id。又因 V157__emr_version.sql:36-43 零回填、存量病历查出来必空，演示只能现场造数据——
- 门诊诊断（outp_diagnosis）在签名前的历次修改全仓零留痕，且被去重主动判为「无改动」。DoctorStationService.java:148 每次 saveEmr 都 deleteByRegistrationId 硬删全部诊断再 :149-155 重插（OutpDiagnosisRepository.java:15 为派生删除，无软删除、无历史表——全部迁移中不存在 outp_diagnosis 历史表，CdrSyncService.java:129 亦只读当前值）；而 EmrVersionService.java:163-164 的 OUTP_FIELDS 只含主诉/现病史/既往史/体格检查/处理意见五段正文，诊断不在快照内。叠加 EmrVersionService.java:404-416 的 dedup（V157__emr_version.sql:170-175 默认

### 2522（3/3 推翻）
**条款原文**：支持拒收标本的专项查询与记录管理，方便追踪标本流转异常情况。

- 「方便追踪标本流转异常」不可达：全仓唯一渲染 path_process 轨迹的是 DiagnosisPanel.vue:200-217，其入口列表 SQL（modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:174 — `where s.diagnosed_at is null and s.rejected_at is null and s.received_at is not null`）把已拒收标本硬排除，已拒收标本永远进不了该列表；唯一替代入口 DiagnosisPanel.vue:26「按标本ID直接打开」需要 path_specimen.id，而 RegistryPanel.vue:127-172 的结果表与 :249-261 的登记结果弹窗都不显示该 id（全仓病理前端 
- modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:174 — 诊断工作列表 `where s.diagnosed_at is null and s.rejected_at is null and s.received_at is not null` 把已拒收标本从两个 scope 里一并排除；而 frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:200 是全仓唯一渲染「流转节点」的界面，其唯一替代入口 DiagnosisPanel.vue:26「按标本ID直接打开」需要数字主键，RegistryPanel.vue:130-172 的检索表与三个弹窗全都不显示该 id。结果：参数摘要第三条子要求「方便追踪标本流

### 2530（3/3 推翻）
**条款原文**：支持文字化记录完整的取材过程信息，确保描述内容的结构化存储与调阅。

- **全仓唯一的「大体所见」只读展示被「已写诊断」挡住，刚取材未诊断的标本根本看不到——主张的演示路径最后一步当场走不通。**

证据链（四步，逐条 file:line）：
1. frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:161-162 —— `<template v-if="hasPrimary">` 内才是 `<div class="sec"><b>大体所见</b><pre>{{ fmt(primary.grossFinding) }}</pre></div>`。
2. DiagnosisPanel.vue:176 —— v-else 分支是 `<el-empty description="尚未书写首次病理诊断（未写诊断不能签名、不能签发、不能出补充报告）">`，整块大体所见不渲染。
3. Diagnosi
- DiagnosisPanel.vue:161 的 `v-if="hasPrimary"` 把「大体所见」(:162) 挡死，而 hasPrimary 由 PathologyReportController.java:510 按 path_specimen.diagnosis 判定——刚取材完（diagnosis 为空）时，声称的演示路径「④诊断 → 阅片/报告 → 报告全景显示大体所见全文」渲染出的是 el-empty 空态；解锁需先提交病理诊断，而提交即触发 PathologyReportController.java:174 `where s.diagnosed_at is null` 把标本踢出两个档位的工作列表，只剩 DiagnosisPanel.vue:458-465 只认数据库内部主键的「按标本ID直接打开」。取材/诊断两个列表投影（PathologyProcessContro

### 2537（3/3 推翻）
**条款原文**：提供切片确认的批量处理机制，支持一次性对多组标本进行制片状态的核销与记录。

- 批量核销写的是 STAIN（染色）节点，制片本身的 SECTION 节点只能由 POST /slides 单蜡块写入（PathologyProcessController.java:698、:742），且 path_slide 无任何状态列、只有一个 stained_at（V144__pathology_base.sql:94-109，:101）——「制片状态」在库里不存在，控制器自己在 :91-93 与 :934-936 两处披露「没有独立的「制片完成」列，本版不加列」。故「一次性对多组标本进行制片状态的核销」只在染色一环成立，制片一环既无批量也无跨标本。
- 子要求②「一次性对多组标本」零执行证据 + 记录会算错：`batch-complete` 在全仓只有 3 处字符串命中（控制器 :853 映射、:762 javadoc、ProcessingPanel.vue:538 调用），无任何测试/e2e/CI 触达；server/src/test 下 pathology 相关零命中且无 V48*Test.java（V46/V49/V50/V51/V53 各有 3–5 个）；作者自己的 tools/e2e-v48-pathology.py 在 :177 调单张版 stain 却从不调 batch-complete，且 :159-174 切片全钉在单个 sid 上、:85 登记的第二条标本从无切片——K>1 的 specimenCount 在本仓历史上从未产生过。叠加具体缺陷：:878-882 无行锁读 → :901-907 带 `stained_at

### 2553（3/3 推翻）
**条款原文**：支持病理补充报告管理，允许在已签发的原始报告基础上，在线追加补充诊断或后续检测结论。

- PathologyReportController.java:174 —— 阅片工作列表基础 SQL 硬过滤 `s.diagnosed_at is null`（stained/all 两档共用、关键词过滤叠加其上），已写首次诊断/已签发的标本永久不在列表且搜不到；而全仓病理页面无一处渲染 path_specimen.id（工作列表、ProcessingPanel 表格、既往病理页签、SpecialtyView 均只显示病理号/条码），故 DiagnosisPanel.vue:458-465「按标本ID直接打开」所需的主键在 UI 上无从获得。结果：「在已签发的原始报告基础上在线追加」只能在签发后不关抽屉的同一次会话里演示，数日后免疫组化回报再出补充报告的真实场景在工作站上走不通。
- 已签发标本在诊断工位无任何可达入口：worklist 硬条件 `where s.diagnosed_at is null`（modules/medtech/src/main/java/cn/hip/medtech/web/PathologyReportController.java:174）把写过诊断的标本永久排除，列表关键词检索（同文件:195）作用于同一被过滤集合；唯一补救的"按标本ID直接打开"（frontend/shell/src/views/medtech/pathology/DiagnosisPanel.vue:26-27、:458-465）要的是内部主键 path_specimen.id，而该 id 在全部病理页面中从未展示——RegistryPanel.vue:126-171 的标本检索是唯一能按病理号/条码/患者查到已诊断标本处，却既无 id 列也无"打开报告"操作，Di

### 2558（3/3 推翻）
**条款原文**：支持调取并追溯患者既往的病理受检记录，辅助医师对比历史诊断资料。

- 「追溯」无前端入口：/reports（首次报告+补充报告+path_process 时间线）只被 DiagnosisPanel 的报告抽屉调用，而抽屉入口一是阅片工作列表（PathologyReportController.java:174 固定 `s.diagnosed_at is null`，既往标本按定义永不入列），二是手输 path_specimen.id 的「按标本ID直接打开」（DiagnosisPanel.vue:26）——而该 id 在既往病理页签（DiagnosisPanel.vue:266-293）、登记侧既往病理抽屉（RegistryPanel.vue:334-348）、标本检索表（RegistryPanel.vue:130-172）三张表里一次都不显示，也无操作列/行点击。叠加 /prior 只回首次诊断 + supplement_count（PathologyRe
- 「追溯」在界面上是断头路：既往病理表（DiagnosisPanel.vue:269-293、RegistryPanel.vue:330-350）无操作列、无 id 列、无 row-click，而打开既往报告（首次报告全文＋补充报告内容＋path_process 流转时间线）的唯一入口是 DiagnosisPanel.vue:26/458-464 那个只接受纯数字 id 的输入框——诊断工作列表因 `s.diagnosed_at is null`（PathologyReportController.java:174）已把所有已出结果的既往标本排除，登记检索表的操作列（RegistryPanel.vue:162-171）也没有「查看报告」。全仓病理界面没有一处显示 specimen.id（/prior 的 SQL 选了 s.id，前端未渲染），病理号与条码输进去都是 NaN 被挡。验收会上要打

### 2563（3/3 推翻）
**条款原文**：支持特检医嘱的集中化管理与状态追踪，可实时调阅涵盖技术分类、具体检测项目、关联蜡块编号及当前执行进度在内的完整明细。

- 「集中化管理」子要求在 UI 上零实现：后端 GET /tech-orders 的全院清单分支（PathologyReportController.java:657-658）前端零调用，唯一调用点 DiagnosisPanel.vue:699-703 恒传 specimenId；而顶替它的 pending_tech_count 列（:164-165）挂在硬过滤 `s.diagnosed_at is null` 的 /worklist 上（:174），同时下达特检医嘱不禁止在已诊断标本（:561 只查 rejected_at），且 DiagnosisPanel.vue:324/332 明写"免疫组化回报后出补充报告"是主流程——导致这类待执行特检医嘱在全平台任何界面都列不出来，只能靠事先知道数字标本 ID 手打进 :26-27 的输入框才能查到。
- 「集中化管理」只有后端端点、零前端入口，且执行角色被挡在唯一的跨标本视图之外。PathologyReportController.java:643 把「不传 specimenId」定义为「技师侧的全院待执行清单」，但前端三个调用点（DiagnosisPanel.vue:700-702、:724、:744/:755）全部强制传 specimenId，该分支在整个前端零消费者；技师工位 ProcessingPanel.vue 对 tech 零命中；唯一跨标本表格 PathQc WORKLOAD_TECH 穿透（PathQcController.java:1295-1313）的菜单 169 在 V156__v52_menus.sql:31-33 只授 ADMIN/QUALITY 且注释写明「刻意不给 TECHNICIAN」，又按 ordered_at 落窗过滤、只读。演示会上评委问「全院还有哪些

### 2564（3/3 推翻）
**条款原文**：支持基于业务分类与流转进度对医嘱进行过滤，可快速筛选特定病理类型或执行状态下的医嘱清单。

- 全仓没有任何一个医嘱清单能按病理类型（业务分类）过滤，而且这是数据模型层面的结构性缺失、不是漏写参数。病理五档类型只存在于 path_specimen.specimen_type（V144__pathology_base.sql:42-44），它是**登记时才赋值**的——医嘱在成为标本之前根本没有这个属性。inp_order 全表无 specimen_type 列（迁移里只有 V124 加的 order_nature/stop_at/stop_doctor_id）；outp_order.specimen_type 虽存在（V137__outp_order_fields.sql:45），但注释写明是「v44 标本类型（1016），如 血液/尿液/痰」（:53），是**检验标本值域，与病理五档 ROUTINE/FROZEN/CYTOLOGY/CONSULT/MOLECULAR 完全不是一套码
- 住院病理医嘱的执行状态永远不回写：全仓 grep `inp_order set status` 零命中，唯一回写语句 modules/medtech/src/main/java/cn/hip/medtech/web/PathologyController.java:94-96 是 `update outp_order set status='EXECUTED' where id=(select order_id from path_specimen where barcode=?)`，而住院标本 order_id 恒为 NULL（V144__pathology_base.sql:33 chk_path_specimen_source），子查询返回 NULL 命中 0 行。结果是住院病理标本双签签发完毕后，PathologyRegistryController.java:173 回带的 io

### 2576（2/3 推翻）
**条款原文**：支持基础工作量自动化统计，涵盖病理登记总量、蜡块产出数及报告签发量等多维度的业务数据汇总。

- 全仓零病理种子数据（160+ 个 migration 中 insert into path_specimen/path_block/path_slide/path_report 零命中），质控页纯只读；唯一造数入口是菜单 168 病理工作台，而 V156__v52_menus.sql:24-26 只授 ADMIN/TECHNICIAN/DOCTOR_OUTP，主张自陈可用的 QUALITY 账号被排除在外——QUALITY 评委打开 /pathology/qc 只能看到登记总量/蜡块产出数/报告签发量三张「该统计区间内无数据」空表，且系统内无任何路径可以把它们填出来；即便换 ADMIN 现场手工走完六工位，PathologyReportController.java:294 的 5263（复诊人不得与初诊人同一，与 gate 无关永远生效）也决定了单账号签发必然带「未完成双签即签发」告警，

## 下一步

- **v3 不改**。
- 上面这批「够不着」应排进后续版本：它们**不是没做，是做了但用户到不了**——
  验收会上同样演示不出来，与「没做」在评委眼里没有区别。
- 建议给 `ReachabilityTest` 加一层更严的判据：**从菜单出发能否走到该功能**
  （而不只是「某个前端文件引用了这个 API」）。
