# 技术债交接单（供新会话短上下文执行）

> **✅ 四项已全部清偿（2026-08-06，新会话执行）**：48 测试 + 18 套 E2E 全绿，行为零变化。
> 执行结果摘要见各节尾注与文末待议区。
>
> 背景：主开发会话上下文极长，按纪律（memory：大面积重构勿在长上下文做）将四项技术债移交新会话。
> 执行前提：读 `CLAUDE 记忆 hip-project`、本单、`docs/下一步开发规划.md` v14 节即可，无需回溯历史会话。
> 验收标准统一：48 个测试 + `tools/` 下 15 套 E2E 全绿（CI 12+ 套），行为零变化。

## 债一：server 模块聚合控制器裁决（优先级最高）

`server/src/main/java/cn/hip/server/web/` 现有控制器逐一裁决：

| 控制器 | 预判 | 理由 |
|---|---|---|
| StatsController | 留 server | 真跨模块驾驶舱聚合 |
| MedRecordStatsController / DrugAnalysisController | 留 server | 跨模块统计 |
| DrgController | 可下沉 qualitycare | 仅依赖 inp_* + drg_*，与病案/质控同域 |
| InsuranceController | 可下沉新模块 modules/insurance | 医保域独立（对账跨 outp/inp 但表都是 yb_*） |
| PriceFinanceController | 拆：价格→masterdata，交款核对留 server | 价格属主数据域 |
| OpsController + SlowApiFilter + AuditLogFilter | 留 server | 平台横切关注点 |
| QueueCenterController | 可下沉 outpatient | 三队列数据源均门诊侧 |
| PrintReportController | 拆：审计查询→core，打印/日结留 server | 审计属平台 |
| InsuranceSplitTest 等测试 | 随迁移同步改 import | 见 memory 教训：git mv + sed 包名 |

方法：git mv + 包名 sed + pom 依赖核对（参考十八期下沉四模块的做法，提交 b97c811）。每迁一个跑一次 `mvn -q test`。

## 债二：索引审计

38 个迁移未系统审过索引。重点表与高频查询：
- `outp_order`（registration_id+status、charge_id、item_id）——分割/发药/队列都在扫
- `int_message_log`（ref_no、channel+created_at 已有 channel 单列）——对账逐单 msgExists 是 N 次点查
- `inp_vital_sign`（admission_id+measured_at）、`sys_audit_log`（username+id、path 模糊查询无解可接受）
- `outp_charge`（pay_method+created_at::date——考虑函数索引或改 range 查询）、`pay_order`（registration_id+status）
方法：WSL psql `EXPLAIN ANALYZE` 实测 E2E 常用查询 → 新迁移 V39__indexes.sql 补缺（只加不删）。

## 债三：前端瘦身

- element-plus 全量引入 → unplugin-vue-components 按需（当前独立 chunk 1.07MB gzip 339KB）
- 验证：npm run build 后首屏 chunk 明细 + 全页面点开无缺组件（重点：ElMessageBox/ElMessage 需手动引 style）

## 债四：E2E 公共库

15 个 `tools/e2e-*.py` 重复定义 call/ok/登录/找空床/建患者。抽 `tools/e2elib.py`（call/ok/login/find_free_bed/new_patient/discharge_cleanup），脚本逐个改造，每改一个跑一个。CI 无需改（同目录 import）。

## 不要做的事

- 不动业务逻辑与错误码；不改 Flyway 已有版本文件（新增 V39+）
- 不引入新框架；不做「顺手优化」——本单四项之外的发现记回本单末尾待议区

## 执行结果（2026-08-06）

- 债一：五项裁决全部落地，每迁一步 48 测试全绿。DrgController→qualitycare、InsuranceController→新模块 modules/insurance、QueueCenterController→outpatient、PriceFinanceController 拆（PriceController→masterdata + FinanceController 留 server）、PrintReportController 拆（AuditQueryController→core + 打印/日结留 server）
- 债二：V39__indexes.sql 补 8 索引（EXPLAIN 实测验证可用），只加不删
- 债三：unplugin-vue-components 按需（49/91 组件、CSS 251KB）。**根因**：manualChunks 把 element-plus 归并单 chunk 会因 es/index.mjs 的 export * 跨 chunk 重导出令摇树失效，须交 rollup 自动分 chunk
- 债四：e2elib.py 落地，18 脚本 −438 行；4 个变体脚本（emr-stats 关键字参数/phase3 状态码元组/phase38 HTTPError 断言/phase1821 import 位置）适配后 18/18 全绿

## 待议区（执行中发现的新债记这里）

- ~~`outp_charge`/`pay_order` 的 `created_at::date` 过滤~~ **已清偿（1.1.3 B-4）**：
  全仓 27 处 `::date` 谓词改半开区间（`>= ?::date and < ?::date + 1`），
  **1.1.5 EXPLAIN 实证**新写法走 `idx_outp_charge_created` 的 Bitmap Index Scan（旧写法为 Seq Scan），
  1.2.6 性能基线 v2 在 10 万级数据上复测日结窗口 11ms。当前源码残留 5 处命中：4 处是解释性注释，1 处是 `/stats/daily` 的 **group by 分组键**
  （非过滤谓词，按日分组的合法用法；其过滤条件 `created_at >= ...` 可 sarg，1.1.7 已补日期下界）
- ~~echarts 全量引入（1.1MB 懒加载 chunk）~~ **已清偿（1.0.5）**：echarts/core 按需注册
  （Line/Bar + Grid/Title/Tooltip + Canvas，仅驾驶舱一处用图；体温单为自绘 SVG 不涉及），
  chunk 1126KB→517KB（gzip 378KB→177KB），浏览器实测三图含标题正常渲染
- `@element-plus/icons-vue` 全量注册**必须保留**：菜单图标存于 sys_menu 的字符串经 `<component :is>` 动态解析，裁剪需先对 DB 图标做白名单——勿当作顺手优化
- E2E 依赖：phase1821 需 ai-service 起在 8100（`python -m uvicorn main:app --port 8100`），否则 AI 建议断言 fallback 失败


## 清账（2026-08-20，v25 方向 B）

交接单是给接手者看的，过时条目会让人做无用功。本次逐条核对待议区现状：
`created_at::date` 一条已由 1.1.3 全量修复并两轮实证（EXPLAIN + 10 万级基线），已勾销。
其余条目（element-plus 图标必须全量注册、E2E 需 ai-service、echarts 已清偿）复核后仍准确。

**当前无待偿技术债**——五轮审阅 121+ 项、v22–v25 四轮工艺验证的产物均已落到代码或文档。
新债请继续记入本区，并注明发现场景与判据。

- **（2026-09-11，v59 复核）** 三条偏离表维持项的残余缺口，已立 v60（规划文档顶节）：2530 标本描述六处不展示 + 历史版字段不可调阅；2563 补取材医嘱蜡块/进度不派生自事实、挂接染色列露英文；2576 多部位申请第一部位签发即整单 EXECUTED、口径 alert 原样输出 Markdown、缺科室维度。判据：`docs/验收/v59复核-偏离表四条.md` 反驳者原话。
- **（2026-09-10，v59 车道 C 观察）** 病理取材模板无管理界面（`GrossingPanel` 自陈「字段清单可配，模板管理未做」，只能改 sys_config `path.grossing.template.*`）；住院来源 `inp_order` 的执行状态在签发时不动（v59 明示，口径未定）。两者均待立项。
- **（2026-09-08，v58 车道 C 观察）** `SPECIMEN_RECEIVE` 间隔可能为负：`collected_at` 是 PG 端 now()、`received_at` 是 Java `Instant.now()`，跨机时钟偏差 ~0.8 s；CI 同机不出现。根治须让核收也走库端 now()（`PathologyRegistryController`）。
  - **（2026-09-14，v62 定版实测追记）** 这条债的影响**不止「间隔为负」，还会让流转轨迹整条乱序**：
    `path_process.occurred_at` 在 `logProcess` 走库端 `now()`，而 RECEIVE 节点在 `PathologyRegistryController`
    直接写 `Timestamp.from(received)`（Java 时钟）。trail 按 `occurred_at asc, id asc` 排，于是**后插入的行可能排到前面**。
    v62 定版时在本地（Windows JVM + WSL PostgreSQL）连撞两次：第一次 `e2e-v58-audit.py` 的
    `lastNode == TECH_CANCEL` 判成 `RECEIVE`（RECEIVE 的 occurred_at 比其后全部节点晚 ~0.96 s）；
    第二次换成 `e2e-v60-audit.py` 的 `gn[-1]` 取到首次取材而非补取材。**两次是不同套次**——
    代码缺陷会稳定红在同一处，这是环境时钟在漂（实测主机↔WSL 偏移几分钟内从 +0.96 s 摆到 −0.06 s）。
    第三次跑（偏移 −0.057 s）46/46 全绿，CI 同机也一直绿。
    **对生产的意义**：应用服务器与数据库分机部署（医院的常规形态）时，这就是一条会真实发生的乱序，
    而流转轨迹是给评委和质控看的时间线。根治口径不变：核收也走库端 `now()`。
    **顺带**：两套 E2E 的这两条断言本身依赖「插入序=时间序」，根治时应一并改为按 id 判序。

## 平台自身混用两个时钟源写同一条时间线（v63 记）

**现象**：`path_process.occurred_at` 由**库端 `now()`** 落（`PathologyProcessController.logProcess`），
而 RECEIVE 节点那一行由**应用侧 `Instant.now()`** 落
（`PathologyRegistryController` 接收核对：`jdbc.update(... , Timestamp.from(received), ...)`，
同一个值也写进 `path_specimen.received_at`）。两者是**两个时钟**。

**怎么暴露的**：v62 定版跑全新库全量 E2E 时 `e2e-v58-audit.py` 假红一次——
`lastNode` 期望 `TECH_CANCEL`、实得 `RECEIVE`。查库发现 RECEIVE 的 `occurred_at` 比其后**所有**流转节点晚约 1 秒。
实测本机 WSL 与 Windows 的时钟在漂（同一天内从 +0.96s 摆到 -0.06s 又到 -0.56s），
而 `/trail` 的 `lastNode` 取 `order by pr.occurred_at asc, pr.id asc` 的最后一行，于是 RECEIVE 排到了最后。

**为什么 CI 不会暴露**：CI 里应用与 PostgreSQL 在同一台机器、同一个时钟，两个来源恒等。

**为什么这是真债而不只是本机环境问题**：多医院部署操作指南给的就是**应用与库分机部署**的形态。
一旦两台机器有秒级时钟差（NTP 未配或漂移），就会出现：
流转轨迹里 RECEIVE 排在取材之后、`hours_from_receive` 为负、`hours_since_prev` 错位、
按「最近一次活动」判停滞的 `/anomalies` 误报。表现是数据错乱而不是报错，很难从现场反查。

**处置方向**（未做，留给后续轮次）：
- 要么把 RECEIVE 这一行也改成库端 `now()`（但「接收时刻」允许补录历史值，不能一律用 now()——
  需区分「用户补录的业务时刻」与「系统记录的发生时刻」，可能要拆成两列）；
- 要么在写入前做一次时钟偏移校正（应用侧读一次库端 `now()` 求差）。
- 无论哪种，`/trail` 的排序与 `hours_*` 计算应当明确它依赖的是哪一个时钟。

**现有线索**：`PathologyRegistryController` 已有 `CLOCK_SKEW_SECONDS` 常量（用于「接收时刻不能晚于当前时刻」的容差），
说明这个问题此前被局部意识到过，但只用在校验上，没用在排序与时长计算上。

## HRP 资产编号靠 nanoTime 取模生成，撞号即 4091（v64 记）

**现状**：`modules/hrp/src/main/java/cn/hip/hrp/web/AssetController.java` 建资产时
`assetNo = "ZC" + 当日日期 + "-" + System.nanoTime()`。v64 之前是 `System.nanoTime() % 100000`。

**那个取模为什么会撞**：Windows 上 `System.nanoTime()` 走 QueryPerformanceCounter，
常见 10 MHz 频率使返回值恒为 100 的整数倍，于是 `% 100000` 的可能取值只有 **1000 个**（不是十万）。
一次 E2E 建十来个资产，生日碰撞概率约 4–5%——2026-09-17 的全新库全量 E2E 就是这么红的：
`hrp_asset_asset_no_key` 唯一约束冲突 → 全局兜底 4091「数据不符合约束要求」，
而那句话既不说是哪个字段、也不说是撞号，运维只能一个个删着试。

**v64 做了什么**：只去掉截断（`nanoTime()` 在同一 JVM 内单调递增，不截断就不会撞），
长度 30 < `varchar(32)`。这止住了血，但号变成了 19 位数字，人读不了。

**正解（未做）**：按日序列号，`ZC20260917-0001` 这种。需要：
- 一张按日计数的序列表或 Postgres 序列（**要新迁移**，v64 声明了无新迁移，故没做）；
- 并发下取号要么走序列、要么 `insert ... on conflict` 重试，不能用 `count(*)+1`（并发必撞）。

**为什么值得做**：资产编号是要印在资产标签上、盘点时人工核对的号。19 位随机数字等于没有编号规则，
一线盘点没法用；而这个号一旦发出去贴到设备上就改不了了，越早规范越好。

**顺带**：4091 是 `GlobalExceptionHandler` 对 `DataIntegrityViolationException` 的兜底，
它不说哪个约束。这类「唯一约束撞号」应当在业务侧捕获并给出可操作的话
（「资产编号重复，请重试」），否则每次撞号都是一次无法自助排查的报障。

## 「开发者视角内容上屏」是全平台问题，v64 只清了病理三个工位（v64 记）

**v64 做了什么**：把库表列名 / 内部 JSON 键 / 迁移号 / 平台自述缺陷史 / 英文枚举码 / gate 档位值
从病理的取材、特检、质控三个工位的屏上文案里清掉，档位统一走 `gateTierName`（拦截/提示/不判）。

**没清的（已实测确认仍在，均为旧账，不是 v64 造的）**：
- `RegistryPanel.vue:185-186` 活的静态 title，① 登记屏（评委必经的第一站）上逐字打出
  `path_specimen 无 patient_id 列、chk_path_specimen_source 要求门诊/住院恰有其一`——**库列名加约束名**；
- `ProcessingPanel.vue:194` 的 `slideNote`（来自 `PathologyProcessController`）打出
  `dateField=CREATED 按切片产出时间、STAINED 按染色完成时间`——参数名加枚举码；
- 质控页顶层「统计口径」段逐条声明归集锚点（`path_specimen.collected_at`、
  `coalesce(embedded_at, created_at)` 等）。这一条**未必是缺陷**：它是平台自有的「诚实注释」设计，
  告诉质控员每个指标锚的是哪个时间戳列，对口径争议有用。但同一段里出现 `sys_config` 与
  `path.report.routine_hours` 这类配置键，那是纯泄漏。要改就得整段重排，属页面级改造；
- `WORKLOAD_REGISTER` / `WORKLOAD_REPORT` 等**兄弟指标的 caveat 正文**仍把区间合计说成「当日」
  （`registered 是当日登记的标本条数` 等）。v64 新加的 `summaryTitle`（对所有指标生效，
  逐字说「整个统计区间一个数，不是某一天」）把危害压得很低，但那句话本身仍不准；
- 其它域同型（护理记录、时效、收费、双签、病历引用等面板把 gate 键名与 5xxx 错误码写进提示文案），
  由 v64 车道 B 在 blocked_notes 里列出，**本轮一处未动**。

**建议**：单独立一轮做全平台扫盲，而不是每轮顺手清几处。可复用的判据已经有了——
`V62TechRemarkTest.DEV_FACING`（配置键 / 错误码 / 英文档位名）与
`V62NodeRemarkCodeTest.RAW_ENUM / RAW_COLUMN`（库枚举 / 库列名）。
做法应当是**先建一条扫全部 `.vue` 模板段与全部上屏后端文案的守卫**（带活对照组），
让它先红着，再逐屏清——而不是反过来。
