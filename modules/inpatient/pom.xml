<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>cn.hip</groupId>
        <artifactId>hip-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>hip-inpatient</artifactId>
    <name>住院业务（入出转/住院医嘱/出院结算）</name>

    <dependencies>
        <dependency>
            <groupId>cn.hip</groupId>
            <artifactId>hip-platform-core</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.hip</groupId>
            <artifactId>hip-platform-empi</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.hip</groupId>
            <artifactId>hip-platform-masterdata</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.hip</groupId>
            <artifactId>hip-platform-integration</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.hip</groupId>
            <artifactId>hip-insurance</artifactId>
        </dependency>
        <!-- v73：住院病历版本留痕。EmrVersionService 住在门诊模块但本就支持住院侧
             （INP 常量 / INP_FIELDS / 独占锁命名空间 / 按值传参入口俱全，类注释明写
             「住院接缝由主控在 InpEmrController 侧调用」），此前缺的只是这条依赖与调用。
             方向已查：hip-outpatient 不依赖 hip-inpatient，不成环。 -->
        <dependency>
            <groupId>cn.hip</groupId>
            <artifactId>hip-outpatient</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>
