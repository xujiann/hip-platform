# 医院通用信息化平台 — 总体规划

> 参考《采购需求》（107 模块）与《平台建设规划》（8 大建设域），规划一个**通用的、产品化的**医院信息化平台：不绑定单一医院，通过配置适配不同规模医院（县级/市级二甲、三乙）。
> 编制日期：2026-08-04 ｜ 状态：v1.0 草案

---

## 一、定位与设计原则

**定位**：面向中小型公立医院的一体化信息平台（HIS + EMR + 医技 + 管理 + 互联网），以「平台底座 + 可插拔业务模块」方式交付，单医院可按需启用模块。

**设计原则**：

1. **底座先行**：主数据、字典、组织机构、权限、集成引擎是所有业务模块的地基，最先建且保持稳定。
2. **模块化单体起步，微服务留口**：初期以模块化单体（Modular Monolith）交付，降低部署与运维成本（县级医院无专职 DevOps）；模块间只通过接口与事件通信，日后可按需拆分为微服务。
3. **配置优于定制**：字典、表单、流程、报表、收费规则全部配置化，避免每家医院改代码。
4. **标准内建**：数据模型对齐国家卫健委互联互通标准（WS/T 500 系列 CDA、HL7 FHIR R4），从第一天起按电子病历分级评价 4 级、互联互通四级甲等的要求建模。
5. **安全合规内建**：等保三级要求（审计日志、数据加密、权限最小化）、CA 电子签名预留接口。

## 二、总体架构（五层）

```
┌─────────────────────────────────────────────────────┐
│ 接入层  Web(医护/管理) · 移动端(医护) · 患者端(公众号/APP) · 自助机 │
├─────────────────────────────────────────────────────┤
│ 应用层  门急诊 · 住院 · 医技 · 药事 · 管理质控 · HRP · 互联网+   │
├─────────────────────────────────────────────────────┤
│ 平台层  集成引擎 · 消息总线 · 规则引擎 · 表单引擎 · 报表引擎 ·    │
│         工作流 · CDSS 服务 · 统一认证(SSO) · CA 签名适配        │
├─────────────────────────────────────────────────────┤
│ 数据层  业务库(OLTP) · CDR 临床数据中心 · ODR 运营数据中心 ·     │
│         EMPI 患者主索引 · 主数据/术语字典 · 全文检索             │
├─────────────────────────────────────────────────────┤
│ 基础设施  容器化部署(Docker Compose→K8s) · 对象存储 · 备份容灾    │
└─────────────────────────────────────────────────────┘
```

**关键架构决策（ADR 摘要）**：

| # | 决策 | 理由 |
|---|---|---|
| 1 | 模块化单体 + 事件驱动，非微服务 | 中小医院单机房 2–4 台服务器即可跑；避免分布式复杂度 |
| 2 | 单一 PostgreSQL 主库 + 按模块 schema 隔离 | 事务一致性（收费/药房扣库存）优先；schema 隔离保留拆库能力 |
| 3 | CDR 与业务库分离，通过 CDC/事件同步 | 查询分析不拖垮 OLTP；对齐互联互通文档注册要求 |
| 4 | 集成引擎自研轻量版（HL7 V2/FHIR/WebService 适配器） | 对接 LIS/PACS 设备、医保、卫健平台是刚需，商用引擎成本高 |
| 5 | 前端单一工作台外壳 + 微前端模块加载 | 医生站/护士站/收费台共享登录、消息、患者上下文 |

## 三、技术选型（推荐）

| 层 | 选型 | 备选与说明 |
|---|---|---|
| 后端 | **Java 17 + Spring Boot 3**（Spring Modulith 组织模块） | 国内 HIS 生态主流，人才好招；备选 .NET 8 |
| 前端 | **Vue 3 + TypeScript + Element Plus** + Vite | 医疗行业事实标准；工作台外壳用 qiankun 微前端 |
| 数据库 | **PostgreSQL 16**（业务库 + CDR），Redis（缓存/队列） | 备选国产信创：达梦/人大金仓（PG 兼容，迁移成本低） |
| 消息 | 起步用 PostgreSQL LISTEN/NOTIFY + 事务发件箱，量大后上 **RabbitMQ** | 避免过早引入 Kafka |
| 全文检索 | **Elasticsearch / OpenSearch**（病历检索、EMPI 模糊匹配） | 电子病历全文检索模块依赖 |
| 报表 | **积木报表(JimuReport) 或 ECharts + 自研指标引擎** | 统计报表、驾驶舱 |
| 集成标准 | **HL7 FHIR R4（HAPI FHIR）+ HL7 V2（设备）+ CDA(WS/T 500)** | 医保接口按国家医保局 SDK 规范封装 |
| 部署 | **Docker Compose**（单院）→ K8s（区域化时） | CI：GitLab CI 或 Gitea Actions |
| 移动端 | 医护端 PWA/uni-app；患者端微信公众号 H5 优先，APP 后置 | 公众号 H5 覆盖 90% 患者场景 |

## 四、工程结构（Monorepo）

```
医院信息化/
├── docs/                    # 规划、需求、ADR、接口文档
│   ├── 采购需求分析/
│   ├── adr/                 # 架构决策记录
│   └── standards/           # 互联互通/CDA/医保接口规范摘录
├── platform/                # 平台底座（最先开发）
│   ├── core/                # 组织机构、用户、RBAC 权限、SSO
│   ├── masterdata/          # 主数据：科室、人员、字典、ICD、药品/收费目录
│   ├── empi/                # 患者主索引
│   ├── integration/         # 集成引擎：适配器、消息路由、事务发件箱
│   ├── forms/               # 表单引擎（结构化病历/护理单基础）
│   ├── rules/               # 规则引擎（收费规则、合理用药、审方）
│   └── workflow/            # 审批流（OA、数据填报）
├── modules/                 # 业务模块（按 8 大域组织，可插拔）
│   ├── outpatient/          # 挂号、收费、门诊医生站/护士站、门诊药房
│   ├── inpatient/           # 入出转、住院结算、住院医患站、住院药房
│   ├── emr/                 # 电子病历（编辑器、模板、质控、全文检索）
│   ├── lis/  ├── ris-pacs/  ├── pharmacy/  ├── nursing/
│   ├── quality/             # DRG、医保审核、单病种、院感、不良事件
│   ├── hrp/                 # 财务、人事、物资、设备、耗材、固资
│   └── internet/            # 互联网医院、公众号、随访、满意度
├── datacenter/              # CDR/ODR、指标引擎、驾驶舱
├── frontend/                # 前端 monorepo（pnpm workspace）
│   ├── shell/               # 工作台外壳（登录、菜单、患者上下文总线）
│   ├── apps/*               # 各业务模块前端
│   └── shared/              # 组件库、患者卡片、医学控件（体温单等）
├── deploy/                  # docker-compose、初始化 SQL、演示数据
└── tools/                   # 代码生成、字典导入、造数工具
```

## 五、数据架构要点

1. **患者主索引（EMPI）**：身份证/医保卡/就诊卡多标识合一，相似度匹配合并，全模块以 `patient_id` 为唯一主线。
2. **主数据五大目录**：科室/人员/床位、诊疗项目与收费目录、药品目录（对齐国家医保贯标码）、ICD-10/ICD-9-CM3/中医病证、检验检查项目字典。
3. **CDR 文档模型**：按 WS/T 500 的 53 类 CDA 文档组织（门急诊病历、入院记录、手术记录、检验/检查报告…），业务库经事件同步入 CDR，支撑调阅、质控、上报、科研。
4. **收费一致性**：挂号-开单-执行-计费-结算全链路以「医嘱/申请单」为源，费用不落两处账。
5. **审计与溯源**：所有医疗行为记录操作人+时间+IP+前值后值，病历签名后版本冻结（CA 预留）。

## 六、开发路线图（四期，MVP 递进）

> 节奏：2 周一个迭代；每期末产出可演示、可部署的版本。人力假设：3–5 名开发（或 AI 辅助的小团队）。

### 第一期「底座 + 门诊主线」（第 1–4 月）★ MVP
- platform/core、masterdata、EMPI、表单引擎最小版
- 门诊主线闭环：**建卡 → 挂号 → 门诊医生站（开单/处方）→ 收费 → 门诊药房发药**
- 门诊电子病历（模板化）、门诊叫号
- 里程碑：单医院可用门诊系统 demo，全流程 15 分钟演示跑通

### 第二期「住院 + 药事 + 护理」（第 4–8 月）
- 入出转、住院医嘱闭环（开立→审核→执行→计费）、住院结算
- 住院电子病历、护理病历/体温单、护理白板、危急值
- 药库/药房进销存、合理用药干预（基础规则）、智能审方
- 里程碑：门诊+住院全业务线闭环，可承接小型医院真实试点

### 第三期「医技 + 集成 + 数据中心」（第 8–12 月）
- LIS（申请-标本-设备接入-报告-危急值）、RIS/PACS 对接（DICOM 存储可先用 Orthanc）、检查检验预约与叫号
- 集成引擎完整版：HL7 V2 设备适配、医保接口封装、卫健上报
- CDR/ODR 建设，统计报表、公立医院绩效/评审指标首批上线
- 里程碑：满足电子病历分级评价 4 级的功能基线

### 第四期「管理质控 + 互联网+」（第 12–16 月）
- DRG 分组与管理、医保智能审核、处方点评、单病种、院感、不良事件
- HRP：财务对账、物资/设备/耗材、固定资产、OA 审批
- 互联网医院：公众号预约挂号/缴费/报告查询、在线问诊、随访、满意度
- 手麻、ICU、体检等专科系统按试点医院需求排优先级
- 里程碑：对标采购需求 8 大域全覆盖的 1.0 产品版本

## 七、里程碑与工期总览

| 时间 | 里程碑 | 对应采购需求域 |
|---|---|---|
| M4 | 门诊 MVP 可演示 | 核心收费主线（部分） |
| M8 | 门诊+住院闭环，可试点 | 核心主线 + 临床 + 药事（部分）+ 护理 |
| M12 | 医技集成 + 数据中心，EMR 4 级基线 | 医技 + 基础支撑 + 数据中台 |
| M16 | 1.0 产品版：管理质控 + 互联网+ | 全部 8 域基本覆盖 |

**结论：通用平台 1.0 约需 16 个月**；若目标只是单医院最小可用（门诊+住院），8 个月可交付试点。此后进入按医院实施交付周期（单院实施 3–6 个月，含数据迁移与切换，参照《平台建设规划》）。

## 八、风险与对策（工程侧）

| 风险 | 对策 |
|---|---|
| 范围失控（107 模块全铺开） | 严格按路线图收敛，每期只做里程碑内模块；专科系统（手麻/ICU/病理）后置或集成第三方 |
| 收费/医嘱模型返工 | 第一期投入最强人力做领域建模，医嘱-费用-库存三张主线模型先行评审 |
| 医保/设备接口无真实环境 | 集成引擎全部走适配器 + Mock，进试点医院后再联调 |
| 电子病历编辑器难度高 | 起步用结构化表单 + 富文本混合方案，成熟后再引入专业病历编辑器内核 |
| 单人/小团队产能 | 代码生成器（CRUD/字典/报表）+ AI 辅助开发；公共组件先行沉淀 |

## 九、下一步行动（第 0 迭代）

1. 初始化 monorepo 工程骨架（后端 Spring Boot 3 多模块 + 前端 pnpm workspace + docker-compose 一键起）
2. 领域建模评审：患者/就诊/医嘱/费用/库存 五个核心聚合的 ER 草图
3. 落地 platform/core：登录、组织机构、RBAC、菜单
4. 主数据字典表 + 导入工具（ICD-10、医保贯标药品目录样例数据）
